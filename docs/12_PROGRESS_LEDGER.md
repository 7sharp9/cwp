# Progress and Evidence Ledger

This ledger records what was actually done, not intended progress. It is
append-only except for correcting factual errors.

It is split in two. This file is the **index**: a pinned-facts snapshot and one
row per entry. The full text of each entry (its owner, source revision,
environment, changes, verification commands, evidence, deviations, documents
updated, and review) lives in its own **detail file** under `docs/ledger/`,
named `YYYY-MM-DD-<ID>-<slug>.md` and keeping its original heading as the title.

A completed task appends a row to the index table below and adds a matching
detail file under `docs/ledger/`; if any pinned value changed, the same change
refreshes the "Pinned facts" block. A past row or a past detail file is never
rewritten except to correct a factual error.

## Pinned facts

A faithful snapshot for quick reference, not a source of new claims. Provenance
and detail are in the linked entries.

| Fact | Value | Confirmed by |
|---|---|---|
| .NET SDK | `10.0.303` (`global.json`, `rollForward: latestPatch`) | TASK-001 |
| Target framework | `net10.0` (all projects) | TASK-001 |
| FSharp.Core | `10.1.303` (implicit, SDK-pinned) | TASK-001 |
| `Canonical.FormatVersion` | `8` (1 -> 2 TASK-018: `AgentState.Progress`; 2 -> 3 TASK-026: `WorldState.TacticalKnowledge` section; 3 -> 4 TASK-028: `AgentState.Order` + `AgentState.Disposition` sections; 4 -> 5 TASK-032: `AgentState.Suppression`; 5 -> 6 TASK-033: `AgentState.SuppressionBand` + `AgentState.Stress`; 6 -> 7 TASK-034: `WorldState.HostileTacticalKnowledge` section; 7 -> 8 TASK-037: `PlayerIntent.Suppress` + `DecisionReason.TargetNotKnown` inside the existing `Order`/`Disposition` sections — every bump a full re-pin; behaviour-neutral except TASK-028 (moves by one `OrderAppraised` per order; `blocked-goal` now `Unable(NoKnownRoute)` at appraisal, same tick/event count), TASK-034 (`open-engagement` / `perception-contact` / `exposed-approach` legitimately gain a genuine new non-zero `HostileTacticalKnowledge` entry, same tick/event counts everywhere), and TASK-037 (only the new `suppress-relieves-exposure` corpus entry carries genuine new `Suppress`-order/`Suppression` state; every other entry, the fixture, and `envelope-full` re-pin behaviour-neutrally)) | TASK-003; bumped TASK-018, TASK-026, TASK-028, TASK-032, TASK-033, TASK-034, TASK-037 |
| `Replay.FormatVersion` | `1` | TASK-003 |
| `CommandLog.Version` | `1` | TASK-003 |
| `ReplaySerialisation.FormatVersion` (`.cwreplay` replay-command file) | `1` (production replay-command serialisation; independent of the four versions above and of `Canonical.FormatVersion`) | TASK-025 |
| `ScenarioContent.Version` | `2` (authored terrain layer added; independent of the three versions above; version 1 rejected, not migrated) | TASK-010 |
| PRNG | SplitMix64 v1 (seed initialises the 64-bit counter directly) | TASK-003 |
| State hash | FNV-1a-64 over `Canonical.encode` (not a cryptographic primitive) | TASK-003 |
| BenchmarkDotNet | `0.15.8` (pinned; `bench/CommandoWar.Benchmarks/` only, never in `CommandoWar.Sim` or the test project) | TASK-014 |
| FsCheck / FsCheck.Xunit | `3.3.4` (pinned; `tests/CommandoWar.Sim.Tests/` only, never in `CommandoWar.Sim` or `CommandoWar.Headless`) | TASK-019 |
| Green tests | `297` — `dotnet test CommandoWar.slnx -c Release` (`Passed: 297`; +4 from TASK-038 — three new `DiagnosticsTests` facts against the new `canonical-refusal-and-correction` corpus entry (tick 1's `Refused RouteTooExposed threat-agent-2` is a structured `DecisionReason`, never a raw score — docs/07 section 8 step 4; tick 4's automatic threat-suppression-change reappraisal to `Accepted` — steps 5-6, unchanged from TASK-037's mechanism; the tick-8 reissue's fresh `Accepted` + `CommitmentEstablished` for the identical target, `Accepted`-or-unappraised held through tick 13 — steps 7-8, plus the tick-8 golden ASCII/SVG pair) and one new `CorpusTests` theory case (`canonical-refusal-and-correction`); no `CommandoWar.Sim` change, no `Canonical.FormatVersion` bump — every mechanism already existed; +6 from TASK-037 — five new `SimulationTests` facts (a `Suppress` order naming an unknown contact is `Unable(TargetNotKnown)`; naming a known one is `Accepted` with no `Destination` and a `Suppressing` commitment; `Appraisal.routeExposure` zeroes a suppressed threat's contribution at the pure-function level; a threat's `SuppressionBand` flip reappraises a *different* agent's `Refused` order to `Accepted` via direct field mutation, no combat RNG; a `Suppressing` agent fires at its named target even when a nearer contact is also visible) plus one extension to the existing `Commitment.ofAgent` enumeration fact (a `Suppress` + `Accepted` case); `DeterminismPropertyTests`/`CorpusTests` gain defensive exhaustive-match arms only (no new fact); no `Canonical.FormatVersion` change from the test additions themselves — the bump to 8 is the task's own canonical-state addition, not test-only; +2 from TASK-034 — one `CanonicalHashTests` fact (the hostile-tactical-knowledge canonical section / `firstDifferingSection` label, the `TacticalKnowledge` fact's precedent) and one `SimulationTests` fact (a Hostile agent with a stale `HostileTacticalKnowledge` entry for a friendly it can no longer see never appears as a shooter in that tick's `ShotFired` events, proving `docs/09` section 8's observed-only-targeting criterion); `DeterminismPropertyTests` property 6 extended in place to also ground `WorldState.HostileTacticalKnowledge` in a Hostile agent's own observation; the perception-contact `DiagnosticsTests` fact and the exposed-approach `AppraisalDemo` unhandled-overlay `DiagnosticsTests` fact (`6 -> 8`) both extended in place for the new symmetric `HostileKnownContact` overlay; no `Canonical.FormatVersion` change from the test additions themselves — the bump to 7 is the task's own canonical-state addition, not test-only; +11 from TASK-033 — three `Stress.gain`/`.raise`/`.decay` pure-function `SimulationTests` facts (the `Suppression.fs` precedent), three `Appraisal.resolveThreshold` facts (suppressed drops by exactly `SuppressionBandPenalty`; full stress drops by exactly `MaxStress / StressDivisor`; floored at 0 at minimum discipline / cautious risk / full stress / suppressed), two phase-level Stress facts (an agent with a visible contact gains net `GainPerTick - DecayPerTick`, one without stays at 0; Stress accumulates over continuous contact and decays once it is lost), one knowledge-change-trigger fact (a `Refused` order becomes `Accepted` once its only known threat's contact expires after `PerceptionConfig.ExpireAfter` ticks unseen, no real hostile agent needed since `Appraisal.appraise` reads `WorldState.TacticalKnowledge` directly), one suppression-band-trigger fact (an `Accepted` order is reappraised — a fresh `OrderAppraised`, outcome unchanged on a safe route — on either latch flip direction and not on a quiet tick), one `DeterminismPropertyTests` property 12 (`MaxTest = 200`, reusing `appraisalCaseGen` unchanged: every agent's post-tick `Stress` and `SuppressionBand` reproducible from a fresh recompute); the existing combat-determinism `SimulationTests` fact extended in place with `Stress`/`SuppressionBand` array comparisons (no new fact); the disposable `AppraisalDemo` `unhandled`-overlay `DiagnosticsTests` fact's expected count corrected `3 -> 6` (three agents now also carry a sparse `AgentStress` overlay this demo doesn't render — genuine new behaviour); no `Canonical.FormatVersion` change from these additions themselves — the bump to 6 is the task's own canonical-state addition, not test-only; +9 from TASK-032 — eight `SimulationTests` facts (`Suppression.gain` gives a hit strictly more than a miss; `Suppression.gain` strictly decreases as cover level increases, floored at 0; `Suppression.raise` accumulates two shots and clamps at `MaxSuppression`; `Suppression.decay` drops by `DecayPerTick`, floored at 0; a qualifying shot raises the target's `Suppression` net of the same-tick decay, data-driven off the actual `ShotFired.hit` value; two shooters engaging the same target the same tick compose their gains; an already-suppressed agent decays by exactly `DecayPerTick` on a tick with no qualifying target; an agent never shot at stays at `Suppression = 0` indefinitely), one `DeterminismPropertyTests` property 11 (`MaxTest = 200`, reusing `appraisalCaseGen` unchanged: every agent's post-tick `Suppression` reproducible from a fresh recompute — pre-tick value folded with `Suppression.gain`/`.raise` over this tick's targeting `ShotFired` events, then one `Suppression.decay`); the existing combat-determinism `SimulationTests` fact extended in place to also compare both runs' `Suppression` arrays (no new fact); the `open-engagement` `FireLine` `DiagnosticsTests` fact extended in place with `AgentSuppression` overlay assertions (no new fact); no `Canonical.FormatVersion` change from these additions themselves — the bump to 5 is the task's own canonical-state addition, not test-only; +11 from TASK-031 — one `CorpusTests` theory case (`open-engagement`), eight `SimulationTests` facts (both sides fire when in range/LOS; out-of-range excluded; `Combat.chooseTarget` excludes an LOS-blocked candidate; `Combat.hitChance` monotonic in range and in cover, clamped at the extremes; `Combat.chooseTarget` nearest + ascending-id tie-break; two-run determinism with an exact draw-count match), one `DeterminismPropertyTests` property 10 (`MaxTest = 200`, every `ShotFired` reproducible from a fresh recompute + redraw, exact draw count, reusing `appraisalCaseGen` unchanged), one `DiagnosticsTests` fact (the `open-engagement` golden, byte-equal ASCII/SVG, both `FireLine` overlays and their event kinds checked); no `Canonical.FormatVersion` change — `WorldState.Random` was already canonical, only the values it draws are new; +10 from TASK-030 — eight `SimulationTests` commitment facts (a fresh `Accepted` order emits `CommitmentEstablished` the same tick as `OrderAppraised`, an agent that arrives emits `CommitmentCompleted` with the identical Order/Disposition clear as before TASK-030, a second order delivered mid-route emits a fresh `CommitmentEstablished` with no event for the first, a `Refused`/`Unable` order never emits `CommitmentEstablished`, an unchanged already-appraised order emits neither event on an idle tick, determinism + zero draws, and a `Commitment.ofAgent` unit fact covering every reachable `(Order, Disposition, Destination)` combination), one `DeterminismPropertyTests` property 9 (`MaxTest = 200`, reusing property 8's generator: every agent's derived `Commitment` matches its `Order`/`Disposition`/`Destination`, no PRNG draw), one `DiagnosticsTests` fact (the new `reissued-order` corpus entry's tick-3 golden, proving commitment supersession); no `Canonical.FormatVersion` change and no hash re-pin anywhere — `Commitment` is a derived value, not canonical state; +2 from TASK-029 — two `DiagnosticsTests` facts for the read-only Godot appraisal-demo helper `src/CommandoWar.Headless/AppraisalDemo.fs` (`dispositionText` matches the committed golden vocabulary; `loadExposedApproachFrames` reproduces the `exposed-approach` tick-1 hash + the two divergent dispositions + the flat C#-facing view model); no `CommandoWar.Sim` change; +14 from TASK-028 — eight `SimulationTests` order-appraisal facts (clear-route `Accepted` + `Destination` same tick, no-path `Unable(NoKnownRoute)`, never `Accepted` after a hard feasibility failure, exposed route `Refused(RouteTooExposed)` for a low-`Discipline` agent, the same order `Accepted` for a high-`Discipline` agent — the G3 divergence, directional cover drops exposure below threshold, not re-appraised on an idle tick, deterministic + zero draws), three `DiagnosticsTests` facts (a hand-built `OrderAppraisal` overlay render, the `exposed-approach` golden showing the divergence, the `blocked-goal` golden showing `Unable` at appraisal), one `DeterminismPropertyTests` property 8 (`MaxTest = 200`, every `Disposition` consistent with a fresh recompute, no PRNG draw), one new `SimulationTests` fact pinning that the Navigation phase still emits `MovementBlocked` for a `Destination` set outside the order pipeline (the two former direct-`MovementBlocked` order tests became `Unable(NoKnownRoute)` appraisal tests), one new `CorpusTests` `[<Theory>]` case (`exposed-approach`); +12 from TASK-027 — five `SimulationTests` order-delivery facts (reachable delivered, unreachable `OrderUndelivered`, multi-recipient partial delivery, undelivered order does not cancel an in-progress one, determinism), one `DeterminismPropertyTests` property 7 (`MaxTest = 200`, comms-blacked-out agents inert and `OrderUndelivered` well-formed), one `CanonicalHashTests` fact (`CommunicationAvailable` outside the canonical image), two hand-built `DiagnosticsTests` facts (`UndeliveredOrder` overlay, `AgentMarker.CommunicationAvailable`) + one `lost-comms` golden fact, one `ScenarioTests` round-trip fact, one new `CorpusTests` theory case (`lost-comms`); +10 from TASK-026 — five `SimulationTests` perception facts, one `DeterminismPropertyTests` property (`MaxTest = 200`, squad contacts grounded in observation), one `CanonicalHashTests` fact (tactical-knowledge section / `firstDifferingSection`), one hand-built `KnownContact` overlay + one `perception-contact` golden fact in `DiagnosticsTests`, one new `CorpusTests` theory case (`perception-contact`); +7 over TASK-019's 171 from TASK-021 — five `ScenarioTests` move-cost-range facts, one `PathfindingTests` near-ceiling long-path fact, a fourth `DeterminismPropertyTests` FsCheck optimality property; +6 from TASK-020 — multi-recipient accept, ascending-id ordering, hostile co-recipient rejection, empty recipients, repeated recipient, duplicate-command-id reject-all + batch-order independence; +10 from TASK-022 — five `SimulationTests` cell-occupancy facts, a fifth `DeterminismPropertyTests` property (distinct live agents hold distinct cells), a hand-built `Obstructed` overlay `DiagnosticsTests` fact + the `swap-standoff` golden fact, and two new `CorpusTests` theory cases (`follow-chain`, `swap-standoff`); the benchmark project contributes no tests; +5 from TASK-024 — four `SimulationTests` issue-tick-eligibility facts and one `ReplayTests` `DuplicateCommandIdInLog` fact; +7 from TASK-025 — a `ReplaySerialisation` round-trip property (`MaxTest = 200`), a full-field-matrix round-trip fact, unknown-version / canonical-mismatch / out-of-order rejection facts, a hand-written full-envelope parse fact, and the committed `envelope-full.cwreplay` fixture cross-check). CI-verified on `windows-latest` by `.github/workflows/ci.yml` on every push and PR (TASK-023); the count itself is not pinned by CI. | TASK-019; +7 TASK-021; +6 TASK-020; +10 TASK-022; +5 TASK-024; +7 TASK-025; +10 TASK-026; +12 TASK-027; +14 TASK-028; +2 TASK-029; +10 TASK-030; +11 TASK-031; +9 TASK-032; +11 TASK-033; +2 TASK-034; +6 TASK-037; +4 TASK-038; CI TASK-023 |
| Accepted ADRs | ADR-0001 (Godot, accepted 2026-09-03; 2026-09-06 note: Mibo packaging blocker may be resolved upstream, decision stands); ADR-0002 (framework-independent sim, project baseline); ADR-0003 (Mibo: spike complete, **not adopted**; 2026-09-06 amendment records Mibo 5.x classic MVU as a live reconsideration option after the upstream package re-separation); ADR-0004 (low-impedance C#/F# boundary, accepted 2026-09-03) | TASK-007 acceptance; ADR amendments 2026-09-06 |
| Current gate | `G4_vertical_slice_feature_complete` (pending) | G3 gate passed, 2026-09-16 |
| Current phase | `P4_playable_integration` | G3 gate passed, 2026-09-16 |

### Shared fixture

Defined in `src/CommandoWar.Headless/Fixture.fs`; pinned by
`tests/CommandoWar.Sim.Tests/FixtureTests.fs`; per-tick hash table in
`content/fixtures/SPIKE-FIXTURE.md`; canonical command log
`content/fixtures/spike-fixture.cwlog`.

| Parameter | Value |
|---|---|
| Builder | `Setup.sixAgentWorld` |
| Grid | 32 x 32 |
| Seed | `20260902` (`0x0000000001352826`) |
| Agents at tick 0 | 6 friendly, column `x = 0`, rows `y = 0..5` |
| Command | tick 1: agent 3 `MoveTo (20, 14)`, `CommandId 1` |
| Tick count | 40 (agent 3 reaches `(20, 14)` at tick 31; ticks 32..40 are rest) |
| Initial hash (tick 0) | `0x55F43D66C7AECB7F` (format 4; TASK-028 re-pin, was `0x50BFA007EDFC42FE` at format 3, `0xE13D7540912C7E25` at format 2, `0xF2F3DF0D820AD9AC` at format 1) |
| Final hash (tick 40) | `0x7737282578E821C6` (format 4; TASK-028 re-pin, was `0xD9D6EC3DDC1D602F` at format 3, `0xAFA35198CC6BD8D4` at format 2, `0x838D3AE7DBFB735D` at format 1) |
| Domain events | 34 (1 `CommandAccepted` + 1 `OrderAppraised` + 31 `MovementStepped` + 1 `MovementCompleted`); 0 random draws — the fixture is enemy-free, so agent 3's order is `Accepted` at appraisal (TASK-028) with an empty route exposure. The 33 -> 34 move is the one `OrderAppraised` event; `AgentState.Progress` / `WorldState.TacticalKnowledge` are still empty at every tick, and agent 3 still reaches `(20, 14)` at tick 31 (tick 40 unchanged) |
| Environment (TASK-023) | Hashes pinned on Windows x64 / .NET SDK `10.0.303`; re-verified there by `.github/workflows/ci.yml` on every push and PR. Other environments are outside the per-environment determinism contract (`docs/09` section 3). |

## Detail entry template

Each `docs/ledger/*.md` file holds one entry in this shape:

```markdown
## YYYY-MM-DD - TASK-NNN - concise outcome

**Owner:**
**Source revision:**
**Environment:**
**Status change:** `old -> new`

### Changes

- ...

### Verification

- Command: `...`
  - Result: ...
- Manual check: ...
  - Result: ...

### Evidence

- Paths, logs, screenshots, benchmark files, replay IDs, or hashes.

### Deviations and unresolved issues

- None, or explicit details.

### Documents updated

- ...

### Review

- Reviewer: Dave
- Accepted: yes/no/pending
- Notes: ...
```

## Index

Chronological. One row per detail file.

| Date | ID | Outcome | Status change | Accepted | Detail |
|---|---|---|---|---|---|
| 2026-09-02 | PLAN-001 | Initial project control pack assembled (charter, gates, ADRs, risks, backlog, bounded tasks) | `unstructured concept -> P0 governance and feasibility` | pending (G0 passed at TASK-001 acceptance) | [detail](ledger/2026-09-02-PLAN-001-initial-control-pack.md) |
| 2026-09-02 | TASK-001 | Repository and build baseline: `global.json`, `CommandoWar.slnx`, F# sim + xUnit projects, boundary tests | `active -> review` | yes (2026-09-02) | [detail](ledger/2026-09-02-TASK-001-repository-baseline.md) |
| 2026-09-02 | TASK-002 | Framework-neutral simulation skeleton: domain types, 11-phase `Simulation.step`, placeholder movement | `active -> review` | yes (2026-09-02) | [detail](ledger/2026-09-02-TASK-002-simulation-skeleton.md) |
| 2026-09-02 | TASK-003 | Deterministic SplitMix64 PRNG, canonical encoding, FNV-1a-64 hash, replay + divergence harness | `active -> review` | yes (2026-09-02) | [detail](ledger/2026-09-02-TASK-003-determinism-harness.md) |
| 2026-09-02 | TASK-004 | Disposable Godot .NET framework spike; shared `cwheadless` reference and pinned fixture added | `active -> review` | yes (2026-09-02) | [detail](ledger/2026-09-02-TASK-004-godot-spike.md) |
| 2026-09-02 | TASK-005 | Environment check: stable Mibo hard-depends on prohibited `Mibo.Adaptive`; spike rescoped and pinned to 4.1.0 | none (TASK-005 stays `ready after dependency`) | yes (2026-09-02) | [detail](ledger/2026-09-02-TASK-005-mibo-environment-check.md) |
| 2026-09-02 | TASK-005 | Disposable Mibo + raylib framework spike (Mibo 4.1.0); shared fixture 41-hash sequence reproduced | `active -> review` | yes (2026-09-03) | [detail](ledger/2026-09-02-TASK-005-mibo-spike.md) |
| 2026-09-03 | TASK-004 interactive addendum | Godot editor GUI still not drivable in-session; project imports into the 4.7.2 editor with no error | none (TASK-004 stays `done`) | n/a (addendum) | [detail](ledger/2026-09-03-TASK-004-interactive-addendum.md) |
| 2026-09-03 | TASK-006 | Spikes evaluated; ADR-0001 scored table completed (Godot 4.13 / Mibo 3.62); Godot recommendation put to Dave | TASK-005 `review -> done`; TASK-006 `proposed -> active` | yes (2026-09-03) | [detail](ledger/2026-09-03-TASK-006-spikes-evaluated.md) |
| 2026-09-03 | TASK-006 finalisation | ADR-0001 accepted (Godot); G1 passed; Mibo route retired | ADR-0001 `proposed -> accepted`; `G1 -> passed`; gate `G1 -> G2`; phase `P1 -> P2`; TASK-006 `active -> done` | yes (2026-09-03) | [detail](ledger/2026-09-03-TASK-006-finalisation.md) |
| 2026-09-03 | TASK-007 | Godot C#/F# client boundary designed; ADR-0004 proposed; disposable proof built (questions 1-5) | TASK-007 `-> active -> review`; ADR-0004 created (`proposed`) | yes (2026-09-03) | [detail](ledger/2026-09-03-TASK-007-godot-fsharp-boundary.md) |
| 2026-09-03 | TASK-007 acceptance | ADR-0004 accepted; boundary design signed off | ADR-0004 `proposed -> accepted`; TASK-007 `review -> done` | yes (2026-09-03) | [detail](ledger/2026-09-03-TASK-007-acceptance.md) |
| 2026-09-03 | TASK-008 | Authored `Scenario` model, `ScenarioContent.Version`, one-pass `Scenario.validate`, `World.ofScenario` | `active -> review` | yes (2026-09-03) | [detail](ledger/2026-09-03-TASK-008-scenario-dto.md) |
| 2026-09-03 | TASK-009 | Progress ledger restructured into this index plus per-entry detail files under `docs/ledger/` | `active -> review` | yes (2026-09-03) | [detail](ledger/2026-09-03-TASK-009-ledger-restructure.md) |
| 2026-09-03 | TASK-010 | Authoritative terrain grid: `Terrain` module (elevation, passability, cost, opacity, directional cover), authored layer + validation, `ScenarioContent.Version` 2 | `active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-03-TASK-010-terrain-grid.md) |
| 2026-09-03 | TASK-011 | Framework-neutral `DiagnosticFrame` observer in `CommandoWar.Sim` + deterministic ASCII/SVG/HTML renderers, `cwheadless render` verb, terrain-demo scenario, golden renders, and the "visually inspectable" standing rule | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-03-TASK-011-diagnostic-visualisation.md) |
| 2026-09-04 | TASK-012 | Deterministic point-to-point line of sight (`Sight` module: integer supercover walk, symmetric corner rule, ridge-occlusion elevation rule) + `SightRay` diagnostic overlay + `--los` render option + LOS demo goldens | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-04-TASK-012-line-of-sight.md) |
| 2026-09-04 | TASK-013 | Deterministic grid pathfinding (`Pathfinding` module: 4-connected A*, integer cost + Manhattan heuristic, total-order frontier key, N/E/S/W neighbour order, explicit expansion budget) + `PlannedPath` diagnostic overlay + `--path` render option + pathfinding demo goldens | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-04-TASK-013-pathfinding.md) |
| 2026-09-04 | TASK-014 | Headless performance and allocation benchmark harness (`bench/CommandoWar.Benchmarks/`: empty tick, six- and ~50-agent placeholder movement, line-of-sight batch, pathfinding open/blocked/choke, canonical encode + state hash, replay run) + committed `content/benchmarks/BASELINE.md` | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-04-TASK-014-benchmark-harness.md) |
| 2026-09-04 | TASK-015 | Navigation and movement phase: `Pathfinding`-driven single-agent executor replacing `PlaceholderMovement` (`docs/04` section 8 steps 2/4/5/6) + `AgentState.Route` non-canonical derived cache (`FormatVersion` stays 1, fixture hashes unmoved) + `MovementBlocked` event + `Diagnostics.frameOf` `PlannedPath` overlay with `fixture-mid-route.*` goldens; cell reservation / formation slots / sub-cell progress split to B-011b | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-04-TASK-015-movement-executor.md) |
| 2026-09-04 | TASK-016 | Divergence diagnostics and replay corpus infrastructure: committed multi-entry corpus `content/replays/` (spike fixture, wall detour, `MovementBlocked` no-path, two-agent converging routes) over the existing `.cwlog` v1 format, `Corpus.fs` name -> `WorldState` registry, `cwheadless corpus [--regenerate]` verb, in-suite `CorpusTests.fs` `[<Theory>]`; generative property tests and component subhashes split to B-012b | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-04-TASK-016-replay-corpus.md) |
| 2026-09-04 | TASK-017 | Same-tick cell reservation and deadlock avoidance (backlog B-011b, narrowed): `Simulation.navigationAndMovement` resolves contested-cell moves by fewest remaining route steps (ties broken by agent id), provably terminating for a shared-target-cell contest; new `MovementYielded` event and `Reserved` diagnostic overlay; `content/replays/converging-routes` re-pinned as TASK-016 anticipated; `Canonical.FormatVersion` stays 1; formation slots and sub-cell movement progress split to B-011c | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-04-TASK-017-cell-reservation.md) |
| 2026-09-04 | TASK-018 | Sub-cell movement progress within an edge (backlog B-011c, narrowed): `AgentState.Progress` (threshold `Terrain.moveCost`, increment `Terrain.BaseMoveCost`), `Canonical.FormatVersion` bumped 1 -> 2 (every pinned hash re-pinned, confirmed behaviour-neutral for every pre-existing scenario); reservation generalised to "would complete this tick" claimants with no new state; new `slow-terrain` corpus entry and diagnostics golden; a route-persistence bug found and fixed during implementation; formation slots split to B-011d | `proposed -> active -> review -> done` | yes (2026-09-05) | [detail](ledger/2026-09-04-TASK-018-subcell-movement-progress.md) |
| 2026-09-05 | TASK-019 | Generative determinism property tests (backlog B-012b, narrowed): `FsCheck`/`FsCheck.Xunit` 3.3.4 added to the test project only; three FsCheck properties over generated small worlds and command sequences (determinism under random commands via the `Replay`/`Divergence` self-double-run pattern; no agent on an invalid cell after movement; pathfinding endpoints/cost on random terrain); component-level subhashes stay proposed under B-012b with a narrowed justification | `proposed -> active -> review -> done` | yes (2026-09-05) | [detail](ledger/2026-09-05-TASK-019-determinism-property-tests.md) |
| 2026-09-05 | G2 gate decision | G2 declared passed on the `docs/08` section 5 evidence bullets alone (all six now have landed evidence: TASK-003, TASK-014, TASK-016, TASK-019); "tactical knowledge" removed from section 5's Required work as a P3 forward-reference, not a G2 blocker; gate/phase advanced to G3/P3 | `gates.G2_deterministic_core_proven` `pending -> passed`; `current_gate` `G2 -> G3_command_loop_proven_headless`; `current_phase` `P2_deterministic_core -> P3_command_loop_headless` | yes (2026-09-05) | [detail](ledger/2026-09-05-G2-gate-decision.md) |
| 2026-09-05 | TASK-020 drafted | Command validation and recipient selection (backlog B-014, first P3 task): task file drafted, not started — `PlayerCommand.Recipients: AgentId list` generalised with a back-compatible `Command.moveTo`; `Urgency`/`RiskTolerance` envelope fields, inert until B-017; new `CommandRejection` cases `EmptyRecipients`, `UnauthorisedRecipient`, batch-scoped `DuplicateCommandId`; no `Canonical.FormatVersion` or `CommandLogFile.Version` change | `proposed -> ready` (task file created); B-014 `proposed -> ready` | pending | [detail](ledger/2026-09-05-TASK-020-drafted.md) |
| 2026-09-06 | ADR-0003 amendment | Mibo 5.0.0 (2026-09-04) re-separates classic MVU (`Mibo.Mvu`) from the adaptive runtime (`Mibo.Adaptive.Mibo`); `Mibo.Core` no longer depends on `Mibo.Adaptive`. The 2026-09-02 packaging blocker is lifted on the 5.x line; Mibo 5.x classic MVU recorded as a live reconsideration option. Framework decision unchanged (Godot stands). Control files only, no source. | ADR-0003 amended (2026-09-06); ADR-0001 dated note added; R-003 `closed -> watch`; backlog B-043 added `proposed`, B-024 now depends on B-043 | pending | [detail](ledger/2026-09-06-mibo-5-repackaging-recorded.md) |
| 2026-09-06 | Control-plane reconciliation | Root `README.md` and `PROJECT_STATE.yaml` brought into line with the actual state: framework decided (Godot, ADR-0001), G0/G1/G2 passed, phase P3, TASK-020 drafted-and-ready-not-selected. Removed the stale `README` "framework decision pending / next task TASK-001 / no framework selected" claims and the obsolete `PROJECT_STATE` note about the unresolved G2 question. No source, task, or gate change. | `README.md` header + "Immediate sequence" + "Architectural position" + baseline section updated; `PROJECT_STATE.yaml` `active_work.note` rewritten, `updated -> 2026-09-06` | pending | [detail](ledger/2026-09-06-control-plane-reconciliation.md) |
| 2026-09-06 | TASK-020 revised | `tasks/TASK-020-*.md` corrected before selection, per the standing review: objective reworded as a **partial** section 13 envelope (drops the "in full" claim); duplicate recipients rejected `DuplicateRecipient` not silently normalised; a duplicated `CommandId` now rejects **every** command in the group (batch-order-independent) not "first wins"; `commandIntake` copies the agent array once per batch, not per accepted command (`BASELINE.md` O(n^2) hot spot); issue-tick semantics and the replay-command serialisation each get a mandatory-before-G3 follow-up (B-044, B-045), added to the G3 Required-work list. Task-file / backlog / roadmap / ledger only, no source. | `tasks/TASK-020-*.md` objective + central decisions + scope + acceptance criteria revised; `docs/11_BACKLOG.md` TASK-020 row updated, B-044 + B-045 added `proposed`; `docs/08` section 6 P3 Required work gains the B-044/B-045 bullet | pending | [detail](ledger/2026-09-06-TASK-020-revised.md) |
| 2026-09-06 | TASK-021 drafted | Terrain/pathfinding correctness task drafted (backlog B-046, from the standing review's defect #1): passable `MoveCost` to be bounded `[Terrain.BaseMoveCost, Terrain.MaxMoveCost]` — restores the A* heuristic's admissibility/consistency precondition, keeps a passable cell's `moveCost` below the `BlockedCost` sentinel, keeps cost accumulation inside `int32`; adds a Dijkstra-reference optimality property (the TASK-019 property only recomputes the returned path's own cost). No A* algorithm change, no `Canonical.FormatVersion` bump, no hash re-pin. Task file + backlog + ledger only, not started, not selected. | `tasks/TASK-021-MOVEMENT-COST-BOUNDS.md` created (`ready`); `docs/11_BACKLOG.md` new TASK-021 row + B-046 `ready` | pending | [detail](ledger/2026-09-06-TASK-021-drafted.md) |
| 2026-09-06 | TASK-022 drafted | Movement-phase cell-occupancy correctness task drafted (backlog B-047, from the standing review's defect #2): `Simulation.navigationAndMovement` assigns `Position = next` for an unrivalled mover without checking a stationary occupant of `next` — TASK-017 reservation only arbitrates rival claimants for the same cell, and its ledger flagged the stationary-occupant and rotation-deadlock gaps for a later task that never took them. Task defines the pre-combat occupancy policy (occupied cell blocks entry; swaps and rotation cycles blocked; follow chains resolve same-tick to a free cell) via a deterministic vacation-chain stage in Pass 2, a new `MovementObstructed` event + `Obstructed` overlay, and a unique-live-agent-cell-occupancy property. No cooperative pathfinder, no `Canonical.FormatVersion` bump, no new `AgentState` field, no hash re-pin. Task file + backlog + ledger only, not started, not selected. | `tasks/TASK-022-CELL-OCCUPANCY.md` created (`ready`); `docs/11_BACKLOG.md` new TASK-022 row + B-047 `ready` | pending | [detail](ledger/2026-09-06-TASK-022-drafted.md) |
| 2026-09-06 | TASK-023 drafted | Simulation-CI task drafted (backlog B-048, from the standing review's recommendation #6 and the control-plane-reconciliation "no B-item yet" deviation): a `.github/workflows/ci.yml` on `windows-latest` (matching the environment the committed hashes were pinned in — `docs/09` section 3), on push and PR, that restores/builds/tests `CommandoWar.slnx` with the `global.json` SDK pin then runs `cwheadless corpus` (fails the job on any divergence from the committed tables) and `cwheadless fixture` (smoke). Decisions recorded: client-compile / content-validation / package-smoke deferred to the first P4 client task; lockfiles deferred (source change, existing pins suffice); `bench/` builds with the slnx but benchmarks are not run; `windows-latest` not `ubuntu-latest` (cross-platform determinism is ADR-gated, not a CI side effect); sized `S` — one workflow file plus a one-line environment annotation to the corpus/fixture docs. No source / `.fsproj` / test / hash / `Canonical.FormatVersion` change; no ADR; no `docs/08` gate change. Task file + backlog + ledger only, not started, not selected. | `tasks/TASK-023-SIMULATION-CI.md` created (`ready`); `docs/11_BACKLOG.md` new TASK-023 row + B-048 `ready` | pending | [detail](ledger/2026-09-06-TASK-023-drafted.md) |
| 2026-09-06 | Tooling / debug-display design note | Exploratory analysis of scenario-authoring efficiency and the developer debug display (standing review). Findings: (1) a text scenario format is premature and would collide with B-045; a shared F# fixture builder generalised from `Corpus.fs` is worth it but only after B-045 and TASK-022 — proposed as B-049. (2) The `cwheadless render --format html` multi-tick scrubber already is the headless "explain any state transition" tool; do not pull B-029 forward or build a live viewer before P4; the real gap is rendering the first divergent tick — proposed as B-050. HTML overlay toggles / cell inspection left as opportunistic work inside P3 tasks, no row. No task files, no source. | `docs/notes/2026-09-06-tooling-and-debug-display.md` created; `docs/11_BACKLOG.md` section 3 new B-049 + B-050 `proposed` | pending | [detail](ledger/2026-09-06-tooling-and-debug-display-note.md) |
| 2026-09-06 | TASK-020 | Command validation and recipient selection (backlog B-014), a **partial** section 13 envelope: `PlayerCommand.Recipients: AgentId list` replaces the single `Agent` field (back-compatible `Command.moveTo` + a `.Agent` head accessor kept so pre-existing `CorpusTests` compile unchanged; new `Command.moveToMany`); inert `Urgency` / `RiskTolerance` envelope fields (B-017 appraisal inputs nothing reads yet); four new `CommandRejection` cases (`EmptyRecipients`, `DuplicateRecipient`, `UnauthorisedRecipient` for a `Hostile`-side recipient, batch-order-independent `DuplicateCommandId` that rejects every command in a duplicated-id group); `Simulation.commandIntake` copies the agent array once per phase invocation (was per accepted command — `BASELINE.md` O(n^2) hot spot) and emits `CommandAccepted` before writing the destination; `Diagnostics.eventMarker` gains one arm per new case. Issuer identity and issue-tick eligibility stay out with mandatory-before-G3 follow-ups B-044 / B-045 (already in the backlog and `docs/08` section 6). `178 -> 184` green; `dotnet build` `0/0`; `-- corpus` / `-- fixture` byte-identical with no `--regenerate`; no `Canonical.FormatVersion` or `CommandLogFile.Version` change, no hash re-pin. | `ready -> review -> done`; `docs/11_BACKLOG.md` TASK-020 row + B-014 `ready -> done`; `PROJECT_STATE.yaml` `active_work.selected_task` `TASK-021 -> TASK-020` | yes (2026-09-06) | [detail](ledger/2026-09-06-TASK-020-command-validation-and-recipients.md) |
| 2026-09-06 | TASK-021 | Minimum passable movement cost and overflow-safe pathfinding cost (backlog B-046): new `Terrain.MaxMoveCost = 1000` literal; new `ScenarioError.MoveCostOutOfRange` reported for a passable authored cell outside `[Terrain.BaseMoveCost, Terrain.MaxMoveCost]` (`NegativeMoveCost` kept for `< 0`; an impassable cell's cost still ignored). Restores the A* Manhattan heuristic's admissibility/consistency precondition (every passable step now costs `>= BaseMoveCost`) and keeps a passable cell's `moveCost` below the `BlockedCost` sentinel with cost accumulation inside `int32` — stated as the bound `Width * Height * MaxMoveCost < Int32.MaxValue` in the `Pathfinding` "## Bounded work" comment. No `Pathfinding` algorithm change, no `Canonical.FormatVersion` bump, no hash re-pin; every committed scenario/corpus authors passable costs of 1/3/5 only (grep-verified), so `-- corpus` / `-- fixture` reproduce their tables byte-identically. Tests: +5 `ScenarioTests` (below/above/exact-endpoints/impassable-accepted/zero-cost-corridor), +1 `PathfindingTests` (near-ceiling long path), +1 `DeterminismPropertyTests` property (optimality vs an independent relaxation shortest-path search, 200 cases); `171 -> 178` green. | `proposed -> active -> review -> done`; `docs/11_BACKLOG.md` TASK-021 row + B-046 `ready -> done`; `PROJECT_STATE.yaml` `active_work.selected_task none -> TASK-021` | yes (2026-09-06) | [detail](ledger/2026-09-06-TASK-021-movement-cost-bounds.md) |
| 2026-09-06 | TASK-022 | Runtime cell-occupancy correctness in the Navigation and movement phase (backlog B-047, a post-gate correctness fix to TASK-015 / TASK-017). `Simulation.navigationAndMovement` gains a stage-2b vacation-chain resolution between rival arbitration (2a, unchanged) and Pass 3: an additive fixpoint over the completing non-yielded movers computes `movers` and `obstructedBy: Map<int, AgentId>`; a new Pass 3 arm freezes an obstructed `Advancing` agent like a rival-contest loser and emits the new `MovementObstructed of agent * at * blocked * occupant` event. Swaps and n-agent rotation cycles fall out as all-obstructed; a 3-agent pure cycle is geometrically impossible on the bipartite 4-connected grid (the rotation test uses four agents around a 2x2). `WorldError.AgentsShareCell` guard added to `World.create` / `World.ofScenario` (the vacation-chain algorithm relies on pre-tick one-agent-per-cell uniqueness). `Overlay.Obstructed of cell * occupant` derived in `frameOf`, rendered in ASCII/SVG. Two corpus entries (`follow-chain`, `swap-standoff`), one golden pair (`swap-standoff-tick-001.*`). `Canonical.FormatVersion` stays 2, no `AgentState`/`WorldState` field, no existing hash re-pin (`-- corpus` / `-- fixture` byte-identical before and after, with and without `--regenerate`). Tests `184 -> 194` (+5 `SimulationTests`, +1 `DeterminismPropertyTests` property at 200 cases, +2 `DiagnosticsTests`, +2 `CorpusTests` theory cases). One pre-existing `SimulationTests` fact (`an agent routes around an impassable wall`) had its wall repositioned — its detour ran the moving agent through two parked friendly agents, the exact defect fixed. | `ready -> review -> done`; `docs/11_BACKLOG.md` TASK-022 row + B-047 `ready -> done`; `PROJECT_STATE.yaml` `active_work.selected_task TASK-020 -> TASK-022`; `docs/04` sections 8 / 12.7 / 20, `docs/09` sections 2.2 / 2.3 / 2.4; "Green tests" pinned `184 -> 194` | yes (2026-09-06) | [detail](ledger/2026-09-06-TASK-022-cell-occupancy.md) |
| 2026-09-06 | TASK-023 | Continuous integration for the framework-neutral solution (backlog B-048). New `.github/workflows/ci.yml`: one job on `windows-latest`, triggered on `push` (any branch) and `pull_request` targeting `main`, `concurrency` group per ref with `cancel-in-progress`, `permissions: contents: read`. Steps in order: `actions/checkout` (SHA-pinned `3d3c42e5` / v7.0.1), `actions/setup-dotnet` (SHA-pinned `a98b5685` / v6.0.0, `global-json-file: global.json`), `dotnet restore CommandoWar.slnx` (cold cache — the pinned-restore check; no NuGet cache added), `dotnet build -c Release --no-restore` (0/0, enforced by the per-project `TreatWarningsAsErrors`; no global `-warnaserror`), `dotnet test -c Release --no-build`, `cwheadless -- corpus` (no `--regenerate`; exit 3 fails the job on any divergence from the committed `content/replays/*.md`), `cwheadless -- fixture` (CLI smoke — prints, does not self-check), and a `git status --porcelain` clean-tree check. `bench/` builds with the solution; benchmarks are not run. Client-compile / content-validation / package-smoke deferred to the first P4 client task; `packages.lock.json` / `--locked-mode` considered and deferred (source change). One-line Windows-x64 / .NET `10.0.303` environment annotation added to `content/replays/CORPUS.md`, `content/fixtures/SPIKE-FIXTURE.md`, and the "Shared fixture" block above. `docs/09` section 6 marks the framework-neutral matrix items realised by TASK-023, client rows still P4. No source / `.fsproj` / test / hash / `Canonical.FormatVersion` change; no ADR; no `docs/08` gate change. Verified locally from the repo root: `restore` 0, `build -c Release --no-restore` 0 (0 warn / 0 err), `test -c Release --no-build` 0 (`Passed: 194`), `-- corpus` 0 (7× PASS), `-- fixture` 0, `git status --porcelain` clean after the verbs. Negative test: perturbing one `content/replays/swap-standoff.md` tick row made `dotnet test` fail (`Failed: 1, Passed: 193`, the `swap-standoff` `CorpusTests` theory case, `Mismatch { FirstBadTick = 2L }`) and `-- corpus` exit `3` naming the first bad tick and the expected/actual hash; reverted clean. The green-run and red-run acceptance criteria on a real Actions runner could not be executed in this environment (no push) and are left for Dave to confirm by pushing branch `task-023-simulation-ci`. | `ready -> review -> done`; `docs/11_BACKLOG.md` TASK-023 row + B-048 `ready -> done`; `PROJECT_STATE.yaml` `active_work.selected_task TASK-022 -> TASK-023`, note, `updated`; `docs/09` section 6; `content/replays/CORPUS.md`, `content/fixtures/SPIKE-FIXTURE.md`; "Shared fixture" + "Green tests" pinned-facts notes; merged to `main` via PR #1 | yes (2026-09-06) | [detail](ledger/2026-09-06-TASK-023-simulation-ci.md) |
| 2026-09-06 | Reconcile 021/022/023 acceptances | TASK-021 (B-046), TASK-022 (B-047), TASK-023 (B-048) recorded as accepted by Dave: task `Status` lines, ledger `Review` blocks, `docs/11` rows, and the `docs/12` `Accepted` cells. TASK-023's `.github/workflows/ci.yml` was merged to `main` via PR #1 and its first `windows-latest` run was green. `PROJECT_STATE.yaml` `active_work` reset to nothing-selected with a clean current-state note. Confirmed no `docs/08` change needed (section 6 P3 Required work already lists B-044 / B-045; CI already lives in `docs/09` section 6). No source, no gate change. | TASK-021 / TASK-022 / TASK-023 status-change cells `-> done`; `docs/11` B-046 / B-047 / B-048 `-> done`; `PROJECT_STATE.yaml` `active_work.selected_task -> none`, `task_file -> null`, note + both `updated` lines refreshed | n/a (recording acceptances) | [detail](ledger/2026-09-06-reconcile-021-022-023-acceptances.md) |
| 2026-09-06 | TASK-024 drafted | Issue-tick / submission-tick semantics task drafted (backlog B-044, mandatory before G3). A decision-plus-minimal-implementation task in the TASK-020 shape: settle what `PlayerCommand.IssueTick` means (recommended `IssuedAtTick` — issue-time provenance and a future appraisal input — distinct from a `SubmitAtTick` delivery tick), reject future-dated commands (`IssuedInFuture`), do **not** add a staleness horizon at intake (appraisal's concern), keep replay tick and issue tick independent, and keep command identity a batch-local dedup key plus a replay-log invariant (`DuplicateCommandIdInLog`) — **not** `WorldState`, so `Canonical.FormatVersion` stays 2 and no hash re-pins. The legacy `.cwlog` collapses both ticks to its one field. No ADR. Task file + backlog + ledger only, not started, not selected. | `tasks/TASK-024-ISSUE-TICK-SEMANTICS.md` created (`ready`); `docs/11_BACKLOG.md` new TASK-024 row + B-044 `proposed -> ready` | pending | [detail](ledger/2026-09-06-TASK-024-drafted.md) |
| 2026-09-06 | TASK-025 drafted | Production replay-command serialisation task drafted (backlog B-045, mandatory before G3, depends on TASK-024 / B-044). Recommendation: a **separate** new versioned, lossless, hand-rolled replay-command format in `CommandoWar.Sim` (deterministic line-based text, no new package), leaving `.cwlog` frozen at v1 as legacy fixture-script. Adds a `parse`/`serialise` round-trip property, one committed new-format replay fixture carrying a full envelope (`.cwlog` cannot), and a `cwheadless replay` verb. The seven existing corpus entries and the spike fixture are untouched; corpus migration + the shared F# fixture builder are B-049. No `Canonical.FormatVersion` bump, no existing-hash re-pin, no ADR. B-045 kept `proposed` (not `ready`) while B-044 is unresolved, per section 1. Task file + backlog + ledger only. | `tasks/TASK-025-REPLAY-COMMAND-SERIALISATION.md` created (`proposed`, complete); `docs/11_BACKLOG.md` new TASK-025 row + B-045 dependency `B-014 -> B-014, B-044`, size `S -> M` | pending | [detail](ledger/2026-09-06-TASK-025-drafted.md) |
| 2026-09-06 | TASK-026 drafted | Observations and shared squad tactical knowledge task drafted (backlog B-015). First phase consumer of the `Sight` module: Perception (12.3) -> per-agent `AgentState.VisibleContacts` **derived cache** (excluded from `Canonical.encode`, the `Route` precedent) from `Sight.visible` within a `SightRange` cap, both sides, `ContactObserved` on a new sighting; Tactical knowledge (12.4) -> new **canonical** `WorldState.TacticalKnowledge` (contact id, last-known cell, `LastSeenTick`, decaying confidence, instant single-squad sharing, `ContactExpired` on decay-out). `Canonical.FormatVersion` **2 -> 3** with a full **behaviour-neutral** hash re-pin — every committed scenario is enemy-free so the store is empty at every checkpoint (the TASK-018 `Progress` precedent); tick/event counts unchanged for all seven entries. New `KnownContact` diagnostic overlay + committed golden, first enemy-bearing corpus entry, one tuning structure, a tactical-knowledge property. Symmetric raw visibility, friendly-squad-only shared knowledge. B-016 / B-017 / B-019 / B-022 and private beliefs explicitly out. Independent of TASK-024 / TASK-025. No ADR (ADR-0002 amendment governs the bump). Task file + backlog + ledger only, not started, not selected. | `tasks/TASK-026-PERCEPTION-AND-TACTICAL-KNOWLEDGE.md` created (`ready`); `docs/11_BACKLOG.md` new TASK-026 row + B-015 `proposed -> ready` | pending | [detail](ledger/2026-09-06-TASK-026-drafted.md) |
| 2026-09-07 | TASK-024 | Issue-tick / submission-tick semantics (backlog B-044, mandatory before G3). Implemented on the recommended resolutions: `PlayerCommand.IssueTick -> IssuedAtTick` (only that field renamed; `RecordedCommand.Tick` kept, doc comment expanded to pin it as the caller-owned delivery tick, independent of `IssuedAtTick` — renaming it too would have been ~15 sites of churn on plumbing TASK-025 reworks, and `.cwlog` already collapses the two). New `CommandRejection.IssueTickOutOfRange of issuedAtTick * tick` — `commandIntake` rejects `IssuedAtTick` outside `[0, s.Tick]` as a whole-command check ordered first; a stale (earlier-issued, later-delivered) command is accepted (following an outdated order is B-017 appraisal's call, no give-up horizon at intake). Command identity is not authoritative state: new `ReplayError.DuplicateCommandIdInLog of id * firstIndex * secondIndex` checked by `Replay.validate` (via `Array.groupBy` on `Command.Id`), `WorldState` gains no field, `Canonical.FormatVersion` stays 2. `CommandLogFile` doc updated (its one tick field maps to both `RecordedCommand.Tick` and `IssuedAtTick`); grammar and `CommandLogFile.Version` unchanged. `Diagnostics.eventMarker` + `Program.describeReplayError` gain one arm each. `194 -> 199` green (+4 `SimulationTests`, +1 `ReplayTests`); `dotnet build` `0/0`; `-- corpus` (7x PASS) / `-- fixture` (`0xAFA35198CC6BD8D4`, 33 events, format 2) byte-identical, no `--regenerate`; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR. `CorpusTests.fs` one-token field-rename; no other pre-existing fact modified. | `ready -> review -> done`; `docs/11_BACKLOG.md` TASK-024 row + B-044 `ready -> done`, B-045 `proposed -> ready`; `PROJECT_STATE.yaml` `active_work.selected_task none -> TASK-024 -> none`, note, both `updated` lines; `docs/04` sections 2 / 12.1 / 13 / 16, `docs/09` section 2.1; "Green tests" pinned `194 -> 199` | yes (2026-09-07) | [detail](ledger/2026-09-07-TASK-024-issue-tick-semantics.md) |
| 2026-09-07 | TASK-025 | Production replay-command serialisation (backlog B-045, mandatory before G3; depends on TASK-024). Both selection decisions confirmed with Dave: **deterministic line-based text** (not binary), and **header + `RecordedCommand[]`** (the initial `WorldState` stays a named scenario / builder reference, not serialised). New `src/CommandoWar.Sim/ReplaySerialisation.fs` — replay-command file format **v1**, hand-rolled `parse` / `serialise` over a `ReplayCommandFile` (version, seed, ticks, canonical, `ReplayMeta`, optional `initial-hash`, optional `checkpoint` lines, ordered `command` lines), a typed `ParseError` DU + `describeError`, `ofRecord` / `toReplayRecord` bridges to `ReplayRecord` + `Replay.run`. `FSharp.Core` only; integer parse/format forced culture-invariant. Unknown file-format version, canonical-format mismatch, and out-of-order command / checkpoint blocks are typed errors naming the line; no migration guessed. New `cwheadless replay-file <path>` verb (distinct from the legacy `replay` .cwlog verb): parse -> resolve scenario against `Corpus.all` -> `Replay.run` -> print per-tick hash table + ordered accepted commands; exit 2 on parse/validate failure, 3 on checkpoint divergence. New committed fixture `content/replays/envelope-full.{cwreplay,md}` — a 3-recipient `MoveTo` (agents 3/4/5), `Urgency = Immediate`, `RiskTolerance = Aggressive`, issued tick 1 / delivered tick 2, on the spike-fixture initial state, 24 ticks; `ReplayTests` cross-checks the file's `checkpoint` lines, the `.md` table, a pinned hash array, and a fresh `Replay.run`. `199 -> 206` green (+7 `ReplayTests`: round-trip property at `MaxTest = 200`, full-field-matrix fact, unknown-version / canonical-mismatch / out-of-order rejection, hand-written-envelope parse, committed-fixture cross-check). `dotnet build` `0/0`; `-- corpus` (7x PASS) / `-- fixture` (`0xAFA35198CC6BD8D4`, 33 events, format 2) byte-identical with and without `--regenerate`; `Canonical.FormatVersion` (2) / `CommandLogFile.Version` (1) / `Replay.FormatVersion` (1) / `CommandLog.Version` (1) all unchanged, no existing hash re-pinned; `src/CommandoWar.Sim` packages `FSharp.Core` only. `CommandLogFile.fs` doc-comment pointer only. No ADR. | `ready -> done`; `docs/11_BACKLOG.md` TASK-025 row + B-045 `ready -> done`, B-049 precondition note; `PROJECT_STATE.yaml` `active_work` + `updated`; `docs/04` sections 13 / 16, `docs/09` section 2.4; "Green tests" pinned `199 -> 206`; new `ReplaySerialisation.FormatVersion` pinned fact; merged to `main` | yes (2026-09-07) | [detail](ledger/2026-09-07-TASK-025-replay-command-serialisation.md) |
| 2026-09-07 | TASK-026 | Observations and shared squad tactical knowledge (backlog B-015; first phase consumer of the `Sight` module). Three central decisions confirmed with Dave before implementation: **full retained decaying store** (not the narrowed cut); **`PerceptionConfig` module literals** `SightRange = 10` (Chebyshev), `ConfidenceFull = 1000`, `ConfidenceBandDrop = 250`, `StaleAfter = 20`, `ExpireAfter = 60`; **symmetric visibility, friendly-only knowledge**. New leaf `src/CommandoWar.Sim/Perception.fs` (`PerceptionConfig`, `visibleContactsFor` / `sweep` / `mergeKnowledge`, pure/total/integer-only). Real `Perception` (12.3) and `TacticalKnowledge` (12.4) phases in `Simulation.fs`. `AgentState.VisibleContacts: AgentId[]` — **non-canonical derived cache** (the `Route` precedent, excluded from `Canonical.encode`). `WorldState.TacticalKnowledge: Contact[]` — **genuine canonical state** (`Contact` carries `LastSeenTick` + decaying `Confidence`); `Canonical.FormatVersion` **2 -> 3**, tactical-knowledge section in `encode` / `topLevelSections`. New `ContactObserved` / `ContactExpired` events; new `Overlay.KnownContact` derived in both `Diagnostics.frame` and `frameOf`, rendered in ASCII (text) + SVG (purple dashed `?<id>` ring). **Behaviour-neutral re-pin**: fixture + all seven `.cwlog` entries + `envelope-full.{cwreplay,md}` re-pinned (every committed scenario enemy-free; tick counts and event counts byte-identical — table in the detail file); `DemoScenario` (the one committed scenario with a hostile) legitimately gains a `KnownContact` overlay + `contact-observed` markers in `demo.html`. New enemy-bearing corpus entry `perception-contact` (14 ticks, 12 events) + golden `perception-contact-tick-005.*`. `206 -> 216` green (+5 `SimulationTests`, +1 `DeterminismPropertyTests` property at 200 cases, +1 `CanonicalHashTests`, +2 `DiagnosticsTests`, +1 `CorpusTests` theory case). `dotnet build` `0/0`; `-- corpus` 8/8 PASS, `-- fixture` format 3, **33 events unchanged**; `-- replay-file envelope-full` checkpoints OK; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR (ADR-0002 amendment governs the bump). | `ready -> review -> done`; `docs/11_BACKLOG.md` TASK-026 row + B-015 `ready -> done`, B-016 / B-017 / B-019 / B-022 unblocked note; `PROJECT_STATE.yaml` `active_work` + `updated`; `docs/04` sections 10 / 11 / 12.3 / 12.4 / 14 / 17 / 20, `docs/05` section 3, `docs/09` sections 2.2 / 2.3 / 2.4; "Pinned facts" `Canonical.FormatVersion` `2 -> 3`, shared-fixture hashes, "Green tests" `206 -> 216`; accepted by Dave and merged to `main` (`--no-ff`, branch deleted; not pushed) | yes (2026-09-07) | [detail](ledger/2026-09-07-TASK-026-perception-and-tactical-knowledge.md) |
| 2026-09-07 | TASK-027 | Communication constraints and order delivery (realises the communication-failure half of backlog B-016; range/delay/jamming/aging split to a new B-016b). Five central decisions confirmed with Dave before the phase bodies: **A** static per-agent `CommunicationAvailable` reachability check, no PRNG, flag excluded from `Canonical.encode` like `Terrain`; **B** the pending order is transient (a `StepState`-local list, not canonical) — `Canonical.FormatVersion` stays `3`, zero hash re-pin; **C** report aging → B-016b; **D** `OrderUndelivered` only, no `OrderDelivered` success event (with zero delay it duplicates `CommandAccepted`); **E** no ADR. `Simulation.commandIntake` reworked to record an accepted `(command, recipient, target)` as a pending order (no `Destination` write, no agent-array copy); new `Simulation.communication` phase in slot 2 delivers each pending order (writes `Destination`) to a recipient with `CommunicationAvailable = true` or emits `OrderUndelivered(command, recipient, UnableToCommunicate)` and drops it. New `AgentState.CommunicationAvailable: bool` (default `true`; `Agent.create` default, `World.ofScenario` override) — static, **not** in `writeAgent` / `Canonical.encode`. New `RawDeployment` / `Deployment.CommunicationAvailable` + `toDeployments` carry-through, no new `ScenarioError`. New `EventBody.OrderUndelivered` + `DeliveryFailure` DU; `DomainEvent` ordering doc comment updated (Communication events after command-intake, before `ContactObserved`, ascending `(recipient, command)`). New `Overlay.UndeliveredOrder` (derived in `frameOf` only) + `AgentMarker.CommunicationAvailable` (rendered only when `false` — the `Progress`-omitted-at-0 precedent); ASCII + SVG branches, FS0025 sites fixed with intended branches (listed in the detail file). New `lost-comms` corpus entry (8×8, comms-blacked-out friendly ordered to move, 4 ticks, 2 events: `CommandAccepted` + `OrderUndelivered`; the "Lost communication" scenario) + golden `lost-comms-tick-001.*`. **Zero re-pin**: `-- corpus` 9/9 PASS and `-- fixture` (format 3, `0x50BFA007EDFC42FE` / `0xD9D6EC3DDC1D602F`, 33 events) byte-identical before and after with and without `--regenerate`; `git diff content/replays` shows only the new entry + the `CORPUS.md` row; no `content/diagnostics/` golden moved; `-- replay-file envelope-full` checkpoints OK at `canonical 3`. `216 → 228` green. `dotnet build` `0/0`; `src/CommandoWar.Sim` packages `FSharp.Core` only; source scan clean. No ADR. | `ready -> review -> done`; `docs/11_BACKLOG.md` TASK-027 row + B-016 `proposed -> done`, new B-016b row, B-017 unblocked note; `PROJECT_STATE.yaml` `active_work` + `updated`; `docs/03` section 7, `docs/04` sections 11 / 12.1 / 12.2 / 14 / 17 / 20, `docs/05` section 5, `docs/09` sections 2.1 / 2.2 / 2.3; "Green tests" `216 -> 228` (no "Pinned facts" `Canonical.FormatVersion` change — stays `3`); accepted by Dave and merged to `main` (`--no-ff`, branch deleted; not pushed) 2026-09-08 | yes (2026-09-08) | [detail](ledger/2026-09-07-TASK-027-communication-and-order-delivery.md) |
| 2026-09-08 | TASK-027 accepted | Recorded acceptance of TASK-027 (backlog B-016) and merged to `main`. `Accept TASK-027; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-027 task file + ledger Review block) then `Merge TASK-027 ... (--no-ff)`; branch `task-027-communication-and-order-delivery` deleted; not pushed. `main` build 0/0, 228 green, `-- corpus` 9/9, `-- fixture` 33 events before branching TASK-028. | `tasks/TASK-027-*.md` Status `review -> done`; `docs/ledger/2026-09-07-TASK-027-*.md` Review `Accepted: yes (2026-09-08)`; `docs/11` TASK-027 + B-016 rows; `docs/12` TASK-027 row + this row | yes (2026-09-08) | [detail](ledger/2026-09-07-TASK-027-communication-and-order-delivery.md) |
| 2026-09-08 | TASK-028 | Staged order appraisal and typed reasons (backlog B-017; the G3 keystone). Eight central decisions confirmed with Dave before the phase bodies (A: Communication writes a new canonical `AgentState.Order` + resets `AgentState.Disposition` instead of `Destination`; the new Appraisal phase over a new `Appraisal` leaf reads `Order` + `TacticalKnowledge` + terrain + a static `AgentState.Discipline`, runs stages 1-4, produces an `OrderDisposition`, emits `OrderAppraised`, and on `Accepted` writes `Destination`; `Canonical.FormatVersion` 3 -> 4. B: `Discipline` static, EXCLUDED from `Canonical.encode` (the `CommunicationAvailable` precedent). C: stage 1 upstream; stage 2 `Pathfinding` -> `Unable(NoKnownRoute)`; stage 3 route exposure to known threats only; stage 4 exposure vs a `Discipline`/`RiskTolerance`/`Urgency` threshold -> `Accepted`/`Refused(RouteTooExposed)`. `OrderDisposition` / `DecisionReason` subset; `Adapted`/`Delayed` no case. D: `AppraisalConfig` integer literals, no PRNG, hysteresis deferred. E: "new order received" trigger only. F: `OrderAppraised of agent * command * disposition`. G: no ADR. H: new `exposed-approach` corpus entry — two friendlies of `Discipline` 1 and 6 ordered along the same exposed approach past a known hostile, one `Refused` one `Accepted` (the `docs/07` section 9 criterion 2 G3 evidence); `blocked-goal` repurposed as the appraisal-`Unable(NoKnownRoute)` case). New `AgentState.Order` / `.Disposition` / `.Discipline`; new `Appraisal.fs` leaf; `PlayerIntent` / `Urgency` / `RiskTolerance` moved `Commands.fs -> Domain.fs`; new `OrderAppraised` event; new `Overlay.OrderAppraisal` (derived in `frame` and `frameOf`); new `RawDeployment` / `Deployment.Discipline` + `ScenarioError.NegativeDiscipline`. `Canonical.FormatVersion` **3 -> 4**: full re-pin of the fixture + all ten `content/replays/*.md` + `envelope-full.{cwreplay,md}` + every `content/diagnostics` golden — **tick counts unchanged on all ten entries** (`git diff` shows no `Tick count` line moved), event counts move by one `OrderAppraised` per order (`blocked-goal` / `lost-comms` unchanged at 2; old/new hash + event table in the detail file); `demo.html` legitimately gains appraisal markers. New `exposed-approach` corpus entry (12 ticks, 19 events) + golden; new `blocked-goal-tick-001` golden. `228 -> 242` green (+9 `SimulationTests`, +3 `DiagnosticsTests` (hand-built `OrderAppraisal` + `exposed-approach` and `blocked-goal` goldens), +1 `DeterminismPropertyTests` property 8 at 200 cases, +1 `CorpusTests` `[<Theory>]` case for `exposed-approach`). `dotnet build` 0/0; `-- corpus` 10/10; `-- fixture` format 4, 34 events; `-- replay-file envelope-full` OK at canonical 4; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR (ADR-0002 amendment governs the bump). | `ready -> review -> done`; `docs/11` new TASK-028 row + B-017 `-> done`, B-018 / B-021 / B-023 unblocked notes; `PROJECT_STATE.yaml` `active_work`; `docs/03` section 7, `docs/04` sections 2 / 11 / 12.2 / 12.5 / 12.6 / 13 / 14 / 17 / 20, `docs/05` sections 5 / 6 / 7 / 8 / 13 / 14 / 15 / 16, `docs/07` section 8, `docs/09` sections 2.1 / 2.2 / 2.3; "Pinned facts" `Canonical.FormatVersion` `3 -> 4`, shared-fixture hashes, "Green tests" `228 -> 242` | yes (2026-09-08) | [detail](ledger/2026-09-08-TASK-028-order-appraisal-and-typed-reasons.md) |
| 2026-09-08 | TASK-028 accepted | Recorded acceptance of TASK-028 (backlog B-017) and merged to `main`. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 242`, `-- corpus` 10/10, `-- fixture` format 4 / 34 events, `-- replay-file envelope-full` OK at canonical 4, `corpus --regenerate` a byte-identical zero diff, `git status` clean, `src/CommandoWar.Sim` packages `FSharp.Core` only, source scan clean. `Accept TASK-028; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-028 task file + ledger Review block) then `Merge TASK-028 ... (--no-ff)`; branch `task-028-order-appraisal` deleted; not pushed. | `tasks/TASK-028-*.md` Status `review -> done`; `docs/ledger/2026-09-08-TASK-028-*.md` Review `Accepted: yes (2026-09-08)`; `docs/11` TASK-028 + B-017 rows; `docs/12` TASK-028 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-08) | [detail](ledger/2026-09-08-TASK-028-order-appraisal-and-typed-reasons.md) |
| 2026-09-08 | TASK-029 | Godot appraisal-divergence demo (a read-only, corpus-scoped slice of backlog B-029 pulled forward as P3 decision-support; B-029 proper stays P4). Central decisions A–F confirmed with Dave 2026-09-08 before the phase bodies (A: build on the existing `src/CommandoWar.Client.Godot` spike skeleton, all non-trivial logic in a new F# helper; B: corpus reuse — `Corpus.all` → `exposed-approach`, `Corpus.loadLog` against a `res://`-relative `content/replays`, fail loud if absent; C: 13-frame `HSlider` + `Left`/`Right`, default index 1; D: mirror the `DiagnosticRender.Svg` overlay vocabulary / colours / glyphs; E: pure-helper xUnit + headless-Godot-smoke test boundary; F: no ADR). New framework-neutral F# helper `src/CommandoWar.Headless/AppraisalDemo.fs` (`loadExposedApproachFrames`, `dispositionText` / `dispositionTone` matching the committed golden vocabulary, `frameView` / `loadFrameViews` — a flat `[<CLIMutable>]` per-tick view model, no F# `option` / DU / tuple crossing to C#). New Godot scene `scenes/AppraisalDemo.tscn` + `src/AppraisalDemoScene.cs` (a `Node2D` renderer: grid, agents by side + dashed destination lines, the `?2` `KnownContact` ring, the accepted agent's `PlannedPath` polyline + green start disc + goal rect, the `OrderAppraisal` exposed-cell tint + `A`/`R`/`U` glyph, a per-agent `DecisionReason` panel, a tick/hash/format/draws HUD, `--selfcheck` / `--screenshot` modes); `project.godot` `run/main_scene` repointed; the greybox spike files untouched. `src/CommandoWar.Client.Godot.csproj` + `.slnx` gain one `CommandoWar.Headless` reference (still a `Client -> Sim` edge; the simulation references neither); no new NuGet / Godot dependency. **Scoped deviation** from ADR-0004's "F# client-core / no logic in C#" rule for this disposable P3 demo — recorded here + the task file, **not** a precedent for the P4 client tasks (B-026 / B-027 / B-029). No `CommandoWar.Sim` change; no new event / `Overlay` case / `Canonical.FormatVersion` bump / golden re-pin. `242 -> 244` green (+2 `DiagnosticsTests`: `AppraisalDemo.dispositionText` matches the golden vocabulary; `loadExposedApproachFrames` reproduces the `exposed-approach` tick-1 hash `0xB03F8419E55F3592` + the two tick-1 dispositions + the flat view model). `dotnet build CommandoWar.slnx -c Release` 0/0; `dotnet build src/CommandoWar.Client.Godot/...slnx -c Debug` 0/0; `-- corpus` 10/10 and `-- fixture` (format 4, 34 events) byte-identical, `git diff content/` empty; headless Godot `--selfcheck` prints `agent 0: refused route-too-exposed threat-agent-2` / `agent 1: accepted` / `hash 0xB03F8419E55F3592`, `MATCH`, exit 0; committed screenshot `docs/evidence/task-029-appraisal-demo.png` (windowed capture). No pinned-fact change (`Canonical.FormatVersion` stays 4; "Green tests" `242 -> 244`). No ADR. | `draft -> review -> done`; `docs/11` new TASK-029 row + B-029 annotation; `PROJECT_STATE.yaml` `active_work`; `docs/06` section 11 realisation note; `src/CommandoWar.Client.Godot/README.md` launch note | yes (2026-09-12) | [detail](ledger/2026-09-08-TASK-029-godot-appraisal-demo.md) |
| 2026-09-12 | TASK-029 accepted | Recorded acceptance of TASK-029 (a read-only, corpus-scoped slice of backlog B-029) and merged to `main`. Dave launched the scene interactively and confirmed the divergence reads right. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 244`, `-- corpus` 10/10, `-- fixture` format 4 / 34 events, headless Godot `--selfcheck` MATCH `0xB03F8419E55F3592`, `git status` clean. `Accept TASK-029; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-029 task file + ledger Review block) then `Merge TASK-029 ... (--no-ff)`; branch `task-029-godot-appraisal-demo` deleted; not pushed. | `tasks/TASK-029-*.md` Status `review -> done`; `docs/ledger/2026-09-08-TASK-029-*.md` Review `Accepted: yes (2026-09-12)`; `docs/11` TASK-029 row; `docs/12` TASK-029 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-12) | [detail](ledger/2026-09-08-TASK-029-godot-appraisal-demo.md) |
| 2026-09-13 | TASK-030 | Commitment and finite move/hold executor (backlog B-018). Five central decisions confirmed with Dave before the phase bodies (A: `Commitment` DU subset `Holding \| Moving of MoveCommitment` only — no `PlayerIntent` or world state exists yet for `Suppressing`/`Assaulting`/`Withdrawing`. B: `Commitment` is a pure derived value over the already-canonical `Order`/`Disposition`/`Destination` — the `AgentState.Route` precedent — **not** new canonical state, so **no `Canonical.FormatVersion` bump and no hash re-pin anywhere**; this revised a heavier framing discussed the day before. C: stage-5 safer adaptation deferred — needs a separate exposure-aware route-search algorithm. D: the interrupt table realises exactly one of `docs/05` section 11's seven priorities (a new order supersedes the current commitment); no separate `Interrupt` type or `Superseded` outcome case needed, since the old commitment simply stops being derived. E: realised as its own `commitmentAndLocalAction` phase in the existing `Phases.order` slot, relocating one housekeeping branch out of `appraisal`. No ADR). New leaf `src/CommandoWar.Sim/Commitment.fs` (`MoveCommitment`, `Commitment`, `Commitment.ofAgent` — pure, total). New `commitmentAndLocalAction` phase (12.6) in `Simulation.fs`; `appraisal`'s fulfilled-order housekeeping branch relocated there unchanged. New events `CommitmentEstablished of agent * command * target` / `CommitmentCompleted of agent * command * at`. New `Overlay.AgentCommitment` (renamed from the task file's `Overlay.Commitment` during implementation to avoid a case/type name collision), derived in both `frame` and `frameOf`, rendered in `Ascii` + `Svg`. New corpus entry `reissued-order` (one friendly, order reissued mid-route, proving supersession end-to-end) + golden. Every existing corpus entry, the fixture, and `envelope-full` gained `CommitmentEstablished`/`CommitmentCompleted` events in their trace (honest surface of realising the phase) with **zero state-hash change anywhere** — verified by diffing every corpus `.md`, `envelope-full.md`, and every diagnostics golden for hash-line changes (none found, only "Domain events" counts and new overlay/event text); full before/after event-count table in the ledger detail file. Existing-test ripple (all behaviour-neutral): three golden-pinned "shared fixture" facts' hardcoded event-count literal `34 -> 36`; two `PathfindingTests`/`SightTests` "overlay-absent" facts narrowed from `Assert.DoesNotContain "overlays:"` to checking the specific ray/path text is absent (the section itself is no longer ever empty); their `losFrameWithRays`/`pathFrameWithRoutes` test helpers changed from replacing `frame.Overlays` to appending, matching the real `cwheadless render --los`/`--path` CLI behaviour; the TASK-029 `AppraisalDemo` demo (predates TASK-030, does not render commitments) gained an `unhandled`-bucket arm and its test's expected count corrected to 3 (2 friendlies + 1 hostile, not 2). `244 -> 254` green (+8 `SimulationTests`, +1 `DeterminismPropertyTests` property 9 at 200 cases reusing property 8's generator, +1 `DiagnosticsTests` golden fact). `dotnet build` 0/0; `-- corpus` 11/11 (`--regenerate` idempotent); `-- fixture` format 4, hashes unchanged, 34 -> 36 events; `-- replay-file envelope-full` OK at canonical 4, hashes unchanged, 75 -> 78 events; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR (Decision G). | `draft -> ready -> review -> done`; `docs/11_BACKLOG.md` new TASK-030 row + B-018 `proposed -> done`; `PROJECT_STATE.yaml` `active_work`; `docs/04` sections 11 / 12.5 / 12.6 / 14 / 17, `docs/05` sections 9 / 10 / 11, `docs/07` section 8; "Green tests" pinned `244 -> 254` (no `Canonical.FormatVersion` line change — stays 4) | yes (2026-09-13) | [detail](ledger/2026-09-13-TASK-030-commitment-and-finite-executor.md) |
| 2026-09-13 | TASK-030 accepted | Recorded acceptance of TASK-030 (backlog B-018) and merged to `main`. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 254`, `-- corpus` 11/11, `-- fixture` format 4 / hashes unchanged / 36 events, `git status` clean. `Accept TASK-030; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-030 task file + ledger Review block) then `Merge TASK-030 ... (--no-ff)`; branch `task-030-commitment-and-finite-executor` deleted; not pushed. | `tasks/TASK-030-*.md` Status `review -> done`; `docs/ledger/2026-09-13-TASK-030-*.md` Review `Accepted: yes (2026-09-13)`; `docs/11` TASK-030 + B-018 rows; `docs/12` TASK-030 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-13) | [detail](ledger/2026-09-13-TASK-030-commitment-and-finite-executor.md) |
| 2026-09-13 | TASK-031 | Basic hitscan combat and directional cover effects (backlog B-019). Nine central decisions confirmed with Dave before the phase bodies (A: automatic symmetric engagement, candidates from a shooter's own `VisibleContacts` only (risk R-023), line of fire re-verified fresh at combat time, nearest qualifying target wins ties by ascending id. B: no ammunition/weapon readiness — fires every valid tick; future direction (not built) is a cooldown-or-reload model with resupply TBD. C: no wound/death consequence — a hit only emits an event; future direction (not built) is an XCOM-style critical/bleed-out timer. D: a deterministic `0..1000`-scale hit chance (range + directional `Terrain.cover`), one `RandomStream.next` draw per engaging agent — **the simulation's first real gameplay PRNG consumer**; `WorldState.Random` was already canonical, so no `Canonical.FormatVersion` bump. E: reuse `Appraisal.attackDirection` (made public) rather than duplicate it. F: one event `ShotFired of shooter * target * hit`. G: new leaf `Combat.fs`. H: one new corpus entry (`open-engagement`) proves the phase wiring; cover/range monotonicity and target selection are proved directly on the pure `Combat.hitChance`/`Combat.chooseTarget` functions. I: no ADR). **Correction found during implementation (Decision H):** the original claim that only `open-engagement` would be newly pinned was wrong — `perception-contact` and `exposed-approach` (TASK-026/028's enemy-bearing entries) each legitimately bring an agent into weapon range and line of sight of the opposing side partway through their run, so real combat now fires there too (genuine, correct new behaviour, not a bug); **tick counts unchanged on both**, `exposed-approach` tick 1 (the TASK-029 Godot demo's pinned hash `0xB03F8419E55F3592`) unaffected; `demo.html` similarly gains combat from tick 8. Three existing tests whose "no PRNG draw" assumption predated Combat were corrected (two `DeterminismPropertyTests` properties 8/9 lost that assertion and property 8 was renamed; one `SimulationTests` fact's hand-built world sits exactly at `WeaponRange`, renamed and its assertion loosened to "both runs draw the same amount" rather than "draws zero"). `254 -> 265` green (+1 `CorpusTests` theory case, +8 `SimulationTests`, +1 `DeterminismPropertyTests` property 10 at 200 cases, +1 `DiagnosticsTests` golden fact). `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` idempotent); `-- fixture` byte-identical; `-- replay-file envelope-full` checkpoints unchanged; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR. | `draft -> ready -> review -> done`; `docs/11_BACKLOG.md` new TASK-031 row + B-019 `proposed -> done`; `PROJECT_STATE.yaml` `active_work`; `docs/04` sections 5 / 12.8, `docs/05` section 12, `docs/07` section 5, `docs/08` section 6 (added the missing "implement basic hitscan combat" bullet); "Green tests" pinned `254 -> 265` (no `Canonical.FormatVersion` line change — stays 4) | yes (2026-09-13) | [detail](ledger/2026-09-13-TASK-031-hitscan-combat.md) |
| 2026-09-13 | TASK-031 accepted | Recorded acceptance of TASK-031 (backlog B-019) and merged to `main`. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 265`, `-- corpus` 12/12, `-- fixture` byte-identical, `-- replay-file envelope-full` checkpoints unchanged, `git status` clean, `dotnet list src/CommandoWar.Sim package --include-transitive` `FSharp.Core` only. `Accept TASK-031; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-031 task file + ledger Review block) then `Merge TASK-031 ... (--no-ff)`; branch `task-031-hitscan-combat` deleted; not pushed. | `tasks/TASK-031-*.md` Status `review -> done`; `docs/ledger/2026-09-13-TASK-031-*.md` Review `Accepted: yes (2026-09-13)`; `docs/11` TASK-031 + B-019 rows; `docs/12` TASK-031 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-13) | [detail](ledger/2026-09-13-TASK-031-hitscan-combat.md) |
| 2026-09-14 | TASK-032 | Suppression and exposure model (backlog B-020). Eight central decisions confirmed with Dave before the phase bodies (A: mechanic only — no wiring into Appraisal's resolve threshold, no reappraisal trigger, no movement/executor effect, all deliberately deferred to B-021; confirmed directly via a scope question before drafting. B: suppression created in Combat, independent of a hit, mitigated by the same directional `Terrain.cover` geometry as hit chance — "the exposure model". C: decayed every tick in the previously no-op State consequences phase, unconditionally, floored at 0; Combat runs first, so a same-tick hit's gain and that tick's decay both apply. D: new leaf `Suppression.fs` (`SuppressionConfig`, `Suppression.gain`/`.raise`/`.decay`, pure); `Combat.fs` itself unchanged. E: `AgentState.Suppression` is genuine canonical state — `Canonical.FormatVersion` 4 -> 5, the `Order`/`Disposition` precedent, not `Discipline`'s; defaults to 0, no scenario-authored override. F: no new event (a rise derives from the already-emitted `ShotFired`, decay is silent); new sparse `Overlay.AgentSuppression`, one per agent with `Suppression > 0`. G: no new corpus entry — `open-engagement`'s agents are already in range/LOS from tick 1, so it naturally shows a real value; decay/clamping proved on the pure functions. H: no ADR). **Correction found during implementation (Decisions E/G wording):** the task file's acceptance criteria originally said a hit raises `Suppression` by exactly `Suppression.gain` — that omitted the same tick's unconditional decay (Decision C), so the actually observable value is `Suppression.gain(...) |> Suppression.raise 0 |> Suppression.decay`; every test was written against the corrected behaviour from the start, only the prose needed fixing. `Canonical.FormatVersion` **4 -> 5**: full re-pin of the fixture + all twelve `content/replays/*.md` + `envelope-full.{cwreplay,md}` + every `content/diagnostics` golden — **tick counts and event counts unchanged everywhere** (no new event type); `open-engagement`, `perception-contact`, and `exposed-approach` additionally carry genuine new non-zero `Suppression` values from the tick combat first fires (TASK-031's pinned ticks); the TASK-029 Godot demo's `--selfcheck` pinned `exposed-approach` tick-1 hash moved too (`0xB03F8419E55F3592` -> `0x2FA6E43B32599EE5`) and was updated in `AppraisalDemoScene.cs` / `README.md` (not originally listed). `265 -> 274` green (+8 `SimulationTests`, +1 `DeterminismPropertyTests` property 11 at 200 cases reusing `appraisalCaseGen` unchanged; the existing combat-determinism `SimulationTests` fact and the `open-engagement` `DiagnosticsTests` fact both extended in place, not new). `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` idempotent, confirmed via `md5sum`); `-- fixture` format 5, 36 events unchanged; `-- replay-file envelope-full` OK at canonical 5, 78 events unchanged; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR (Decision H). | `draft -> ready -> review -> done`; `docs/11_BACKLOG.md` new TASK-032 row + B-020 `proposed -> done`; `PROJECT_STATE.yaml` `active_work`; `docs/04` sections 11 / 12.8 / 12.9 / 17, `docs/05` section 8, `docs/07` section 5 / section 8; "Green tests" pinned `265 -> 274` (`Canonical.FormatVersion` line `4 -> 5`) | yes (2026-09-14) | [detail](ledger/2026-09-14-TASK-032-suppression-and-exposure.md) |
| 2026-09-14 | TASK-032 accepted | Recorded acceptance of TASK-032 (backlog B-020) and merged to `main`. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 274`, `-- corpus` 12/12, `-- fixture` format 5 / 36 events unchanged, `-- replay-file envelope-full` OK at canonical 5 / 78 events unchanged, `git status` clean, `dotnet list src/CommandoWar.Sim package --include-transitive` `FSharp.Core` only. `Accept TASK-032; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-032 task file + ledger Review block) then `Merge TASK-032 ... (--no-ff)`; branch `task-032-suppression-and-exposure` deleted; not pushed. | `tasks/TASK-032-*.md` Status `review -> done`; `docs/ledger/2026-09-14-TASK-032-*.md` Review `Accepted: yes (2026-09-14)`; `docs/11` TASK-032 + B-020 rows; `docs/12` TASK-032 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-14) | [detail](ledger/2026-09-14-TASK-032-suppression-and-exposure.md) |
| 2026-09-14 | TASK-033 | Stress and bounded reappraisal (backlog B-021, partial). Nine central decisions confirmed with Dave before the phase bodies (A: scope cut to Stress + suppression-band + knowledge-change only, confirmed directly via a scope question before drafting — exposure-band / wounded / support / leadership triggers and dynamic trust deferred, needing systems that don't exist (B-030/B-031) or a materially larger cut. B: Stress sourced from `VisibleContacts` only (the one `docs/05` section 8 source with a system behind it), raised and decayed together in State consequences. C: `Discipline` stays static and non-canonical, correcting B-021's literal backlog wording — it never mutates in this cut. D: `resolveThreshold` gains a continuous Stress drag (`StressDivisor = 25`) and a discrete SuppressionBand penalty (`SuppressionBandPenalty = 30`), the whole threshold floored at 0 so a fully unexposed route is never refused for stress/suppression alone. E: new `AgentState.SuppressionBand` hysteresis latch (`Enter = 500` / `Exit = 300`), computed inside the Appraisal phase (no new `Phases.order` slot). F: knowledge-change trigger fires globally on any `ContactObserved`/`ContactExpired`, not filtered per-route (the R-023 precedent). G: a fulfilled order excluded from both triggers. H: no new event type, no new corpus entry — proof via `SimulationTests` phase-level facts plus pure-function facts. I: no ADR). **Correction found only once the phase body was written (Decision G):** resetting a fulfilled order's `Disposition` would re-run `Appraisal.appraise`'s `fromCell = target` short-circuit, re-write `Destination`, and silently defeat `commitmentAndLocalAction`'s completion clear — both triggers now explicitly exclude it. `Canonical.FormatVersion` **5 -> 6**: full re-pin of the fixture + all twelve `content/replays/*.md` + `envelope-full.{cwreplay,md}` + every `content/diagnostics` golden — **tick counts and event counts unchanged everywhere** (no new event type, and both triggers only ever flip an outcome); `exposed-approach`, `open-engagement`, and `perception-contact` additionally carry genuine new non-zero `Stress` values from the tick each pair first sees each other; the TASK-029 Godot demo's `--selfcheck` pinned `exposed-approach` tick-1 hash moved a third time (`0x2FA6E43B32599EE5` -> `0x2066BC1FAF990E4A`) and was updated in `AppraisalDemoScene.cs` / `README.md`; the disposable `AppraisalDemo` `unhandled`-overlay `DiagnosticsTests` fact's expected count moved `3 -> 6` (genuine new behaviour). `274 -> 285` green (+10 `SimulationTests`, +1 `DeterminismPropertyTests` property 12 at 200 cases reusing `appraisalCaseGen` unchanged; the combat-determinism fact extended in place). `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` idempotent, confirmed via `md5sum`); `-- fixture` format 6, 36 events unchanged; `-- replay-file envelope-full` OK at canonical 6, 78 events unchanged; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR (Decision I). | `draft -> ready -> review -> done`; `docs/11_BACKLOG.md` new TASK-033 row + B-021 `proposed -> review` (stays `review`, deliberately partial — see Decision A); `PROJECT_STATE.yaml` `active_work`; `docs/04` sections 11 / 12.5 / 12.9, `docs/05` sections 5 / 8 / 14 / 15 / 16, `docs/07` sections 5 / 8; "Green tests" pinned `274 -> 285` (`Canonical.FormatVersion` line `5 -> 6`) | yes (2026-09-15) | [detail](ledger/2026-09-14-TASK-033-stress-and-bounded-reappraisal.md) |
| 2026-09-15 | TASK-033 accepted | Recorded acceptance of TASK-033 (backlog B-021, partial) and merged to `main`. The two commits (originally on `main` directly, unpushed) were first moved onto branch `task-033-stress-and-bounded-reappraisal` for consistency with the established per-task branch workflow. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 285`, `-- corpus` 12/12, `-- fixture` format 6 / 36 events unchanged, `-- replay-file envelope-full` OK at canonical 6 / 78 events unchanged, `git status` clean, `dotnet list src/CommandoWar.Sim/CommandoWar.Sim.fsproj package --include-transitive` `FSharp.Core` only. `Accept TASK-033; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-033 task file + ledger Review block) then `Merge TASK-033 ... (--no-ff)`; branch `task-033-stress-and-bounded-reappraisal` deleted; not pushed. B-021 stays `review`, not `done` — exposure-band, wounded, support, and leadership reappraisal triggers, and dynamic trust, remain open, needing systems this task deliberately did not build (B-030/B-031, or a materially larger cut). | `tasks/TASK-033-*.md` Status `review -> done`; `docs/ledger/2026-09-14-TASK-033-*.md` Review `Accepted: yes (2026-09-15)`; `docs/11` TASK-033 + B-021 rows; `docs/12` TASK-033 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-15) | [detail](ledger/2026-09-14-TASK-033-stress-and-bounded-reappraisal.md) |
| 2026-09-15 | TASK-034 drafted | Hostile tactical picture and observed-only targeting proof (backlog B-022, partial; both dependencies B-015/TASK-026 and B-019/TASK-031 done, so fully unblocked). Of `docs/05` section 12's six enemy-doctrine bullets, two are already real without new work ("hold assigned area" — true by omission for an unordered agent; "engage visible targets" — `Simulation.combat` is symmetric, TASK-031) and three need systems that do not exist (`Suppress` order / B-030; a reactive seek-cover movement decision with no design yet; scripted fall-back + `Withdraw` semantics). The top-level scope cut was confirmed with Dave directly (`AskUserQuestion`, three options) before drafting: build only the remaining bullet ("observe and report" — a new `WorldState.HostileTacticalKnowledge: Contact[]`, symmetric to the friendly store, built by calling the already side-agnostic `Perception.mergeKnowledge` a second time filtered to `Side = Hostile`) plus a proof, not a build, that `docs/09` section 8's "enemy does not target an unobserved player position" already holds (`Simulation.combat` draws shooter candidates only from same-tick `VisibleContacts`, TASK-031 Decision A — strictly tighter than any tactical-knowledge store). New `Overlay.HostileKnownContact` diagnostic case (the `AgentStress`/`AgentSuppression` precedent, not a reuse of `KnownContact`). `Canonical.FormatVersion` **6 -> 7** proposed, full re-pin — **not** behaviour-neutral: `open-engagement`, `perception-contact`, and `exposed-approach` will legitimately gain new non-zero `HostileTacticalKnowledge` values. No new corpus entry, no new event type, no ADR proposed. B-022 stays `review` after this task, not `done` — the three deferred bullets remain open (the B-021/TASK-033 precedent). Decisions B–F are proposed in the task file, not yet confirmed line-by-line the way TASK-032/033's were — that confirmation, and selecting the task, are separate deliberate steps before implementation begins. | `tasks/TASK-034-*.md` created (`proposed -> ready`); `docs/11_BACKLOG.md` new TASK-034 row + B-022 `proposed -> ready`; `PROJECT_STATE.yaml active_work` stays `none` (drafted, unselected) | pending | [detail](ledger/2026-09-15-TASK-034-drafted.md) |
| 2026-09-15 | TASK-034 | Hostile tactical picture and observed-only targeting proof (backlog B-022, partial). New `WorldState.HostileTacticalKnowledge: Contact[]`, symmetric to the friendly `TacticalKnowledge`, built by the `tacticalKnowledge` phase calling the side-agnostic `Perception.mergeKnowledge` a second time filtered to `Side = Hostile` — no change to `Perception.fs`. New `Overlay.HostileKnownContact` diagnostic case (a distinct case, not a reuse of `KnownContact`), derived in both `frame` and `frameOf`. Proves, rather than builds, `docs/09` section 8's "enemy does not target an unobserved player position": `Simulation.combat` already draws candidates from a shooter's own same-tick `VisibleContacts` only, strictly tighter than any tactical-knowledge store, so no `Combat.fs` change — one new `SimulationTests` fact pins it and `docs/09` section 8's wording is corrected from "partially realised" to fully realised for the targeting half. `Canonical.FormatVersion` `6 -> 7`, full re-pin, **not** behaviour-neutral: `open-engagement`, `perception-contact`, and `exposed-approach` each gain a genuine new non-zero `HostileTacticalKnowledge` entry from the tick a hostile first sees a friendly (tick counts and event counts unchanged everywhere). No new corpus entry, no new event type, no ADR. Deliberately narrower than B-022's full text: suppress-likely-routes, seek-adjacent-cover-under-pressure, and scripted fall-back all stay open (need B-030's order vocabulary or a new autonomous-movement design), so B-022 stays `review`, not `done` — the B-021/TASK-033 precedent. `285 -> 287` green; `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` twice byte-identical); `-- fixture` format 7, 36 events unchanged; `-- replay-file envelope-full` OK at canonical 7, 78 events unchanged; `dotnet list` FSharp.Core only; source scan clean. | `ready -> review` | pending | [detail](ledger/2026-09-15-TASK-034-hostile-tactical-picture.md) |
| 2026-09-15 | TASK-034 accepted | Recorded acceptance of TASK-034 (backlog B-022, partial) and merged to `main`. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 287`, `-- corpus` 12/12 (`--regenerate` twice byte-identical), `-- fixture` format 7 / 36 events unchanged, `-- replay-file envelope-full` OK at canonical 7 / 78 events unchanged, `git status` clean, `dotnet list src/CommandoWar.Sim/CommandoWar.Sim.fsproj package --include-transitive` `FSharp.Core` only. `Implement TASK-034: ...` then `Accept TASK-034; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-034 task file + ledger Review block) then `Merge TASK-034 ... (--no-ff)`; branch `task-034-hostile-tactical-picture` deleted; not pushed. B-022 stays `review`, not `done` — suppress-likely-routes, seek-adjacent-cover-under-pressure, and scripted fall-back remain open, needing B-030's order vocabulary or a new autonomous-movement design not yet built. | `tasks/TASK-034-*.md` Status `review -> done`; `docs/ledger/2026-09-15-TASK-034-*.md` Review `Accepted: yes (2026-09-15)`; `docs/11` TASK-034 + B-022 rows; `docs/12` TASK-034 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-15) | [detail](ledger/2026-09-15-TASK-034-hostile-tactical-picture.md) |
| 2026-09-15 | DIAG-001 | `content/diagnostics/demo.html` enriched with a per-tick annotation panel: a readable narration of that tick's `DiagnosticFrame.Events`, a per-agent table joining `AgentMarker` with its `OrderAppraisal` (TASK-028) / `AgentCommitment` (TASK-030) / `AgentSuppression` (TASK-032) / `AgentStress` (TASK-033) overlays, and the two shared tactical pictures (`KnownContact` / `HostileKnownContact`, TASK-026/034). Scoped lightweight by Dave's explicit request — no task file, done directly on `main`. Presentation only: no new `Overlay` case, no `Canonical.FormatVersion` change, `Ascii` / `Svg` output byte-unchanged (only `DiagnosticRender.Html` gained the new panel, plus a `commitmentText` helper extracted for reuse — same "holding" / "moving to (x,y)" text `Ascii` already printed). `dotnet build` 0/0; `dotnet test` `Passed: 287` (the `demo.html` byte-equal golden fact and the HTML-parses-as-XML fact both pass against the regenerated file); manually inspected ticks 1 and 8 of the regenerated file for readability and correctness against the scenario. | none (no phase/gate/decision/active-task change) | n/a (no task file to accept) | [detail](ledger/2026-09-15-DIAG-001-demo-html-annotations.md) |
| 2026-09-15 | TASK-035 | Event agent attribution for diagnostic narration — closes the gap DIAG-001 flagged rather than silently patched. `Diagnostics.EventMarker` gains `Agents: AgentId[]` (the `Cells: Cell[]` shape precedent), populated in every `eventMarker` arm from fields the `DomainEvent` body already carries (`CommandAccepted`'s recipient, `ShotFired`'s shooter/target, etc. — nothing new computed); two entries for a two-party event, primary first (shooter, yielding/obstructed agent, observer). `DiagnosticRender.eventNarration` now names the agent(s) in every sentence, e.g. `"Shot fired: hit."` -> `"Agent 0 fired at agent 5: hit."`. Decision B (in the task file): `Ascii`'s terse `kind@cell,cell` events line is deliberately left unchanged — redundant by design with the fuller `overlays:` section one line above it in the same render (e.g. `open-engagement-tick-001.ascii.txt` already spells out `fire (2,2) -> (7,2): agent 0 -> agent 1  miss`), and extending it would re-pin seven ASCII goldens for no new information — flagged explicitly for Dave's review since the task description framed it as an open choice, not a settled one. `content/diagnostics/demo.html` is the only golden that moves (311107 -> 311787 bytes, narration text only). No `Canonical.FormatVersion` change (`Canonical.fs` never references `Diagnostics`, confirmed by grep before starting) — `-- corpus` 12/12, `-- fixture` format 7 / 36 events unchanged. Two existing `DiagnosticsTests.fs` facts extended in place (no new `[<Fact>]`, the TASK-032/033 precedent): the demo tick-1 command-accepted fact now also asserts `Agents`, and the `open-engagement` shot-fired fact now also asserts each event's `Agents` matches its `FireLine` overlay's shooter/target. `287 -> 287` green (same count, both additions extended in place). `dotnet build` 0/0. No ADR, no `docs/11_BACKLOG.md` row (no matching backlog item — presentation-only tooling follow-up, the TASK-009/014/019/023-without-B-item precedent). No separate pre-implementation Dave confirmation round for the two central decisions — Dave's own task instructions asked the implementer to decide and document them directly, given the small single-session scope; both are reversible on review. | `tasks/TASK-035-*.md` created, `proposed -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work.note` only (`selected_task` stays `none`, the DIAG-001 precedent) | pending | [detail](ledger/2026-09-15-TASK-035-event-agent-attribution.md) |
| 2026-09-15 | TASK-035 accepted | Recorded acceptance of TASK-035 (event agent attribution for diagnostic narration, presentation-only, no backlog item). Implemented and self-verified directly on `main`, no branch (the DIAG-001 precedent), so acceptance is a re-verify and status update, not a branch merge. Re-verified before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 287`, `-- corpus` 12/12, `-- fixture` format 7 / final hash `0x56395A49904D017D` / 36 events unchanged, `git status --porcelain` matched the expected scope (this task's files, plus DIAG-001's still-uncommitted changes and the pre-existing unrelated `src/CommandoWar.Client.Godot/project.godot` edit, all untouched). Dave accepted Decision B (leaving `Ascii`'s events line terse) as-is. | `tasks/TASK-035-*.md` Status `review -> done`; `docs/ledger/2026-09-15-TASK-035-*.md` Review `Accepted: yes (2026-09-15)`; `docs/12` this row; `PROJECT_STATE.yaml active_work.note` updated to reflect accepted status (`selected_task` stays `none`, never occupied the slot) | yes (2026-09-15) | [detail](ledger/2026-09-15-TASK-035-event-agent-attribution.md) |
| 2026-09-15 | TASK-036 drafted | Shared F# fixture builder for the test corpus (backlog B-049; unblocked by B-045/TASK-025, precondition already met). Of the backlog items ready per `docs/11` §7 (all dependencies `done`) after TASK-035 — B-016b, B-049, B-050, B-011d — none sit on G3's critical path (that is blocked on a slice of B-030 for B-023 step 5, a separate larger discussion flagged to Dave, not resolved here); B-049 was picked as the one paying down compounding risk, the `Corpus.fs` / `.cwlog` / `SimulationTests.fs` duplication and drift growing with every corpus-touching task since TASK-016. One builder in `Corpus.fs` will author scenario geometry and a per-agent command schedule as a single value, replacing the eleven hand-written `…World ()` functions and their hand-authored `.cwlog` files. Two central decisions confirmed with Dave via `AskUserQuestion` before drafting: commit `.cwreplay` (`ReplaySerialisation`, B-045's production format) instead of the frozen `.cwlog` v1 grammar, which cannot express multi-recipient/`Urgency`/`RiskTolerance`; and migrate all eleven existing entries, not just future ones. Two further decisions made directly per existing convention: the builder's authored value becomes the sole runtime source of truth (`.cwreplay` generated and diff-checked, not read back — the `<name>.md` precedent, eliminating drift categorically); no retrofit of `SimulationTests.fs`'s ~280 existing facts (unrelated-refactor scope; that file does not use `Corpus`/`rawScenario` today). `spike-fixture` (backed by `Fixture.fs`) stays out of scope. No `CommandoWar.Sim` change, no `Canonical.FormatVersion` change, no ADR — a pure tooling refactor, proof of correctness is every migrated entry's `.md` hash table staying byte-identical. Not yet implemented. | `tasks/TASK-036-*.md` created (`proposed -> ready`); `docs/11_BACKLOG.md` new TASK-036 row + B-049 `proposed -> ready`; `PROJECT_STATE.yaml active_work.selected_task -> TASK-036` | pending | [detail](ledger/2026-09-15-TASK-036-drafted.md) |
| 2026-09-16 | TASK-036 | Shared F# fixture builder for the test corpus (realises B-049). One `ScenarioSpec` value (`src/CommandoWar.Headless/Corpus.fs`) authors geometry and a per-agent command schedule together; `worldOfSpec`/`commandsOfSpec` are its two projections. All eleven non-fixture corpus entries rewritten onto it, replacing `rawScenario`/the eleven `…World ()` functions. `Entry` gains `Commands: RecordedCommand[] option` — `Some` (the sole runtime source of truth) for a migrated entry, `None` for `spike-fixture` (unchanged, still `.cwlog`-backed). New `Corpus.commandsOf` replaces direct `loadLog` calls in `run`/`checkEntry`/`regenerateEntry`, plus in every test/demo call site that read a migrated entry's `.cwlog` directly — a deviation found during implementation, beyond the drafted "Allowed scope": `tests/CommandoWar.Sim.Tests/DiagnosticsTests.fs` (nine call sites) and `src/CommandoWar.Headless/AppraisalDemo.fs` (the TASK-029 Godot demo helper), both mechanical, no behaviour change. `regenerateEntry` now also writes each migrated entry's `content/replays/<name>.cwreplay` (`ReplaySerialisation.serialise`, empty `InitialHash`/`Checkpoints` — `<name>.md` already owns that role); `CommandId`/`Sequence` numbering deliberately mirrors `CommandLogFile.parse`'s order-of-appearance convention exactly, since `CommandId` is part of `Canonical.encode` and a mismatch would have silently re-pinned every migrated hash. Eleven `.cwlog` files deleted. Every migrated entry's committed `.md` is byte-identical **except** its "Command log" row, corrected from the deleted `.cwlog` name to the new `.cwreplay` name (`Corpus.parseTable` never reads that row — a deliberate correction, not a regression). `content/replays/CORPUS.md`, `docs/04_SIMULATION_SPEC.md`, `docs/09_TEST_STRATEGY.md` updated for the format change (targeted corrections only, not a full refresh of either doc's pre-existing staleness). No `CommandoWar.Sim` change, no `Canonical.FormatVersion` change, no ADR. `287 -> 287` green (no new fact — the loading-API swap reused every existing assertion). `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` twice byte-identical); `-- fixture` format 7, 36 events unchanged; `-- replay-file envelope-full` OK, 78 events unchanged. Implemented directly on `main`, no branch — the DIAG-001/TASK-035 precedent, justified by this task's low-risk, fully-verified refactor profile; flagged to Dave before committing (a departure from the TASK-030–034 M-sized-task branch precedent). | `tasks/TASK-036-*.md` `ready -> review`; `docs/11_BACKLOG.md` B-049 `ready -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work.note` updated | pending | [detail](ledger/2026-09-16-TASK-036-shared-fixture-builder.md) |
| 2026-09-16 | TASK-036 accepted | Recorded acceptance of TASK-036 (backlog B-049) — implemented directly on `main`, no branch, so acceptance is a re-verify and status update, not a branch merge. Re-verified before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 287`, `-- corpus` 12/12, `-- fixture` format 7 / 36 events unchanged, `git status --porcelain` matched the expected scope (only the pre-existing unrelated `src/CommandoWar.Client.Godot/project.godot` edit left untouched). Both deviations found during implementation (`AppraisalDemo.fs`, `DiagnosticsTests.fs` needing the same `loadLog` -> `commandsOf` swap) and the "Command log" row correction in every migrated `.md` accepted as-is. | `tasks/TASK-036-*.md` Status `review -> done`; `docs/ledger/2026-09-16-TASK-036-*.md` Review `Accepted: yes (2026-09-16)`; `docs/11_BACKLOG.md` TASK-036 + B-049 rows `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-16) | [detail](ledger/2026-09-16-TASK-036-shared-fixture-builder.md) |
| 2026-09-16 | TASK-037 | Minimal Suppress order and B-021/B-022 dependency closeout (a thin B-030 slice, pulled forward as P3 decision-support after Dave chose "do whichever unblocks the most progress; merge tasks if more than one unblocks more" via `AskUserQuestion`, over three narrower options). New `PlayerIntent.Suppress of target: AgentId` and `DecisionReason.TargetNotKnown` (the pre-planned `docs/05` vocabulary case); `Appraisal.appraise`'s `Suppress` branch is `Accepted` if the named contact is known, else `Unable(TargetNotKnown)`, never reaching stage 3/4. New `Commitment.Suppressing of SuppressCommitment` (`docs/05` section 9's own pre-named shape): holds position, pins the named contact as Combat's sole candidate. `Appraisal.routeExposure`/`cellPressure` gain a `suppressedThreats: AgentId[]` parameter and zero a threat's pressure while its `SuppressionBand` is latched (reusing the TASK-033/034 hysteresis latch, not new bookkeeping). A third reappraisal trigger — any agent's `SuppressionBand` flip, global scope — resets a *different* agent's non-fulfilled order, realising `docs/07` section 8 step 6. New `suppress-relieves-exposure` corpus entry proves it end to end, deterministically regardless of hit/miss RNG (worst case: 5 ticks of misses alone cross `SuppressionBandEnter`). `Canonical.FormatVersion` **7 -> 8** (inside the existing `Order`/`Disposition` sections, not a new `topLevelSections` entry); full re-pin, behaviour-neutral except the new entry. `ReplaySerialisation` gains a `suppress <targetAgentId>` command keyword with no version bump (its grammar had already left room for exactly this). B-021 and B-022 explicitly descoped to `done` per their own rows' "until those land or are explicitly descoped" wording — neither row's remaining items are touched by this task's player-issued order (B-021's need B-031/a materially larger cut; B-022's are Hostile-side doctrine, not player-issued). B-023's dependency list is now fully `done`. `287 -> 293` green (+6: five new `SimulationTests` facts plus one `Commitment.ofAgent` extension); `dotnet build` 0/0; `-- corpus` 13/13 (`--regenerate` twice byte-identical); `-- fixture` format 8, 36 events unchanged; `-- replay-file envelope-full` OK at canonical 8, 78 events unchanged; `dotnet list` `FSharp.Core` only; source scan clean. Godot's `--selfcheck` pinned hash updated for the moved `exposed-approach` tick-1 hash but not independently re-run through Godot in this session (no Godot install here) — flagged for Dave to confirm before accepting. Eleven central decisions (A-K) confirmed with Dave via `AskUserQuestion` before the phase bodies. | `tasks/TASK-037-*.md` created and implemented (`proposed -> in progress`); `docs/11_BACKLOG.md` new TASK-037 row, B-021/B-022 rows `review -> done`, B-023 dependency note, B-030 pulled-forward-slice note; `docs/12` this row + summary counters; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-16-TASK-037-suppress-order-and-dependency-closeout.md) |
| 2026-09-16 | TASK-037 accepted | Recorded acceptance of TASK-037 (a thin B-030 slice; B-021/B-022 dependency closeout) and merged to `main`. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 293`, `-- corpus` 13/13 (`--regenerate` twice byte-identical), `-- fixture` format 8 / 36 events unchanged, `-- replay-file envelope-full` OK at canonical 8 / 78 events unchanged, `git status` clean except the pre-existing unrelated `project.godot` edit, `dotnet list src/CommandoWar.Sim/CommandoWar.Sim.fsproj package --include-transitive` `FSharp.Core` only. Godot's `--selfcheck` pinned hash (`0x5D5A30C0DF64AC93`) still needs independent confirmation on Dave's machine — flagged, not a blocker. | `tasks/TASK-037-*.md` Status `review -> done`; `docs/ledger/2026-09-16-TASK-037-*.md` Review `Accepted: yes (2026-09-16)`; `docs/11_BACKLOG.md` TASK-037 row `in progress -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-16) | [detail](ledger/2026-09-16-TASK-037-suppress-order-and-dependency-closeout.md) |
| 2026-09-16 | TASK-038 | Canonical refusal-and-correction scenario end to end (realises B-023 — G3's headline evidence item). Central decision confirmed with Dave via `AskUserQuestion` before drafting: `docs/07` section 8 step 8 says "accepts or adapts it consistently," but no `Adapted` `OrderDisposition` case exists anywhere (stage-5 adaptation deferred by B-018) — closed on the accepts-only proof, the `Adapted` half recorded as a new, not-yet-filed follow-up rather than silently dropped. New `canonical-refusal-and-correction` corpus entry (`src/CommandoWar.Headless/Corpus.fs`): the `suppress-relieves-exposure` geometry exactly, plus one more order reissuing agent 0's identical `(11,3)` target on tick 8. Traced against the real seeded run before choosing that tick: agent 0 is `Refused RouteTooExposed threat-agent-2` ticks 1-3, the automatic threat-suppression-change reappraisal (TASK-037) `Accepts` it at tick 4 with a fresh `CommitmentEstablished`, the tick-8 reissue produces its own fresh `Accepted` + `CommitmentEstablished` for the same target (the TASK-030 "second order mid-route" mechanism), disposition stays `Accepted` (or unappraised, on arrival) through one extra reappraisal blip at tick 12 and arrival at tick 13 — never flipping back to `Refused`. Step 4 needed no new mechanism: `OrderAppraised` only ever carries a structured `OrderDisposition`/`DecisionReason`, never a raw score; this task adds a test assertion turning that standing type-system guarantee into checked evidence. Three new `DiagnosticsTests` facts plus one golden ASCII/SVG pair at tick 8 (the reissue moment); no `CommandoWar.Sim` change, no `Canonical.FormatVersion` bump — every mechanism already existed (TASK-028/030/033/037). Deviation from the drafted scope: no dedicated golden diagnostic pair was originally guaranteed by `AGENTS.md`'s diagnostics rule (this task adds no new `Overlay` case, the identical situation TASK-037 was in), but one was added anyway for a G3 headline-evidence entry, matching the `exposed-approach`/`reissued-order` precedent. `293 -> 297` green (+4: three `DiagnosticsTests` facts, one `CorpusTests` theory case); `dotnet build` 0/0; `-- corpus` 14/14 (`--regenerate` twice byte-identical, only the new entry's files untracked); `-- fixture` format 8, 36 events unchanged; `dotnet list` `FSharp.Core` only; source scan clean. Not yet accepted. | `tasks/TASK-038-*.md` created and implemented (`drafted -> review`); `docs/07_VERTICAL_SLICE.md` section 8 records the full sequence realised, accepts-only scope of step 8 noted; `docs/11_BACKLOG.md` B-023 row `proposed -> done`; `content/diagnostics/README.md` new golden pair documented; `docs/12` this row + Pinned facts green-test count; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-16-TASK-038-canonical-refusal-and-correction.md) |
| 2026-09-16 | TASK-038 accepted | Recorded acceptance of TASK-038 (realises B-023) — implemented directly on `main`, no branch (the TASK-035/036 low-risk precedent: no `CommandoWar.Sim` change, purely additive). Re-verified before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 297`, `-- corpus` 14/14, `-- fixture` format 8 / 36 events unchanged, `git status --porcelain` matched the expected scope (only the pre-existing unrelated `src/CommandoWar.Client.Godot/project.godot` edit left untouched). | `tasks/TASK-038-*.md` Status `review -> done`; `docs/ledger/2026-09-16-TASK-038-*.md` Review `Accepted: yes (2026-09-16)`; `docs/11_BACKLOG.md` B-023 row already `done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-16) | [detail](ledger/2026-09-16-TASK-038-canonical-refusal-and-correction.md) |
| 2026-09-16 | G3 gate decision | G3 declared passed following B-023's completion. `docs/08` section 6's "five vertical-slice intents" required-work line was only partly built (`PlayerIntent` has `MoveTo`/`Suppress`; `Hold`/`Assault`/`Withdraw` are B-030 proper, explicitly re-scoped to P4/G4 by TASK-037's own backlog row). Put to Dave via `AskUserQuestion` rather than resolved by inference (the G2 "tactical knowledge" precedent): Dave chose to declare G3 passed now — no G3 evidence bullet names or depends on the missing three intents, and the canonical refusal test needed only `MoveTo`/`Suppress`. `docs/08` section 6's required-work list corrected with a dated resolution note, the same move G2's "tactical knowledge" line received. | `gates.G3_command_loop_proven_headless` `pending -> passed`; `current_gate` `G3 -> G4_vertical_slice_feature_complete`; `current_phase` `P3_command_loop_headless -> P4_playable_integration`; `docs/08_ROADMAP_AND_GATES.md` section 6 corrected; `docs/12` Pinned facts (Current gate/phase) + this row; `PROJECT_STATE.yaml` updated | yes (2026-09-16) | [detail](ledger/2026-09-16-G3-gate-decision.md) |
| 2026-09-16 | TASK-039 | Snapshot rendering and isometric depth ordering (the first P4 client task, realises B-027). Of the P4 items now selectable (B-026, B-027, B-030 proper, B-031), Dave chose B-027 via `AskUserQuestion`: something to see before building input on top of it, and the real Godot editor-authoring work ADR-0001 review trigger 1 needs. Central decision confirmed via `AskUserQuestion`: render `DemoScenario` (the terrain-demo scenario — elevation ridge, impassable block, movement-cost patch, opaque wall, cover), not a corpus entry or the `Greybox.tscn` content-import path, since the real Bridgehead map is still blocked on B-043/B-024. New `src/CommandoWar.Client.Godot/Core/CommandoWar.Client.Godot.Core.fsproj` stands up ADR-0004's F# client-core architecture for real for the first time: `CwClientCore.IClientScene`/`DrawItem`/`TickHash` (`[<CLIMutable>]` records only — an F# tuple draft failed the C# build with `CS0029`, corrected) and `DemoRenderScene` (fixed-step accumulator, `Simulation.step`/`RenderSnapshot`-driven, a depth-sorted `DrawItem[]` each frame — terrain interleaved with agents by `(Cx + Cy)` ascending, not "all terrain then all agents", the disposable TASK-004 spike's actual gap). New generic `src/FSharpSceneHost.cs` (ADR-0004 form 1) resolves the named `IClientScene` by reflection; `scenes/SnapshotDemo.tscn` is the first production scene built on it (`AppraisalDemo.tscn` stays `run/main_scene`, unchanged). No `CommandoWar.Sim`/`CommandoWar.Headless` change. Verified for real through the Godot 4.7.2 editor available in this environment (no "flagged for Dave" deferral, unlike every prior Godot-touching task): `--editor --headless --quit` import, `dotnet build` both `.slnx` files, `--selfcheck` (`MATCH` the pinned tick-20 hash `0x11B06E6EDE0C52E3`, byte-identical to an independent `dotnet test` computation outside Godot — a cross-runtime determinism cross-check — exit 0), and a windowed `--screenshot` (`docs/evidence/task-039-snapshot-rendering.png`, committed). Sized `L` in practice (`docs/11`'s `M` undercounts the one-time client-core scaffold cost); every later P4 client task reuses it. ADR-0004's own review trigger 1 (interactive F# debugging) is explicitly **not** discharged by this task — needs Dave's own editor session, flagged rather than silently claimed. `dotnet build CommandoWar.slnx` 0/0; `dotnet test` 297/297 (both unaffected). | `tasks/TASK-039-*.md` created and implemented (`drafted -> review`); `src/CommandoWar.Client.Godot/README.md` new section + layout table; `decisions/ADR-0004-GODOT-FSHARP-BOUNDARY.md` dated note; `docs/11_BACKLOG.md` B-027 row `proposed -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-16-TASK-039-snapshot-rendering-and-depth-ordering.md) |
| 2026-09-16 | TASK-039 accepted | Recorded acceptance of TASK-039 (realises B-027, the first P4 client task) — implemented directly on `main`, no branch. Re-verified before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 297`, `git status --porcelain` matched the expected scope (only the pre-existing unrelated `src/CommandoWar.Client.Godot/project.godot` edit left untouched). ADR-0004's own review trigger 1 (an F# breakpoint hit from an interactive editor/F5-launched run) explicitly deferred by Dave to a later session — not a blocker for this task's own acceptance criteria, which the automated `--selfcheck`/`--screenshot` evidence already satisfies. | `tasks/TASK-039-*.md` Status `review -> done`; `docs/ledger/2026-09-16-TASK-039-*.md` Review `Accepted: yes (2026-09-16)`; `docs/11_BACKLOG.md` B-027 row already `done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-16) | [detail](ledger/2026-09-16-TASK-039-snapshot-rendering-and-depth-ordering.md) |
| 2026-09-17 | TASK-040 | Selection, input mapping, tactical pause, and command preview (realises B-026). Dave chose B-026 via `AskUserQuestion` over B-028/B-030 proper/B-031, building on TASK-039's scaffold. Four central decisions confirmed via `AskUserQuestion`: a new `CommandDemo.tscn` scene rather than adding input to `SnapshotDemo.tscn`; a real `Pathfinding.find` route preview, not a straight line; single-unit selection only; orders issuable while tactically paused, delivered on resume. `IClientScene` gains `OnClick`/`OnHover`/`OnTogglePause` (primitives only); new `Core/RenderShared.fs` (terrain items + depth key, extracted from `DemoRenderScene.fs` so `CommandDemoScene` doesn't duplicate them — `SnapshotDemo.tscn`'s own `--selfcheck` hash confirmed unchanged after the extraction); new `Core/CommandDemoScene.fs` (live selection over a friendly agent, a player-built `Command.moveTo` queued for next-tick delivery through the real `Simulation.step`, a `Pathfinding.find` hover preview, a selection halo and destination marker — all reusing existing `DrawItem` `Kind` values, no host-side render change) and `CommandDemoDrive.runScriptedSelfCheck` (drives the real `OnClick`/`OnHover` methods, not a canned log, the `DemoDrive.runFullSequence` precedent). `FSharpSceneHost.cs` gains `ScreenToCell` (inverse of the existing projection) and `_UnhandledInput` forwarding; `--selfcheck` dispatch and `--screenshot` priming both extended per-`SceneType` (the existing dispatch precedent). New `scenes/CommandDemo.tscn`, not `run/main_scene`. No `CommandoWar.Sim`/`CommandoWar.Headless` change. Verified through the real Godot 4.7.2 editor: `SnapshotDemo.tscn --selfcheck` unchanged (`MATCH 0x11B06E6EDE0C52E3`); a temporary scratch probe pinned `CommandDemo.tscn`'s scripted-input hash (`0x649FA4D08E2931CA`), then `CommandDemo.tscn --selfcheck` through Godot reproduced it byte-identically; a windowed `--screenshot` shows the selection halo, route-preview dots, and the HUD naming the selected agent. `dotnet build CommandoWar.slnx` 0/0; `dotnet test` 297/297 (both unaffected). | `tasks/TASK-040-*.md` created and implemented (`proposed -> review`); `src/CommandoWar.Client.Godot/README.md` new section + layout table; `docs/11_BACKLOG.md` B-026 row `proposed -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-040-selection-input-and-tactical-pause.md) |
| 2026-09-17 | TASK-040 review round 1 | Dave tried `CommandDemo.tscn` and reported selection only registering near the base of the agent sprite, and no visual difference between a hover preview and an actually-set order (especially while paused). Both were real: a click-projection bug (`ScreenToCell` inverted the diamond-centre formula, but an agent's circle draws `TileH/2` above that) fixed via a new `TryHitAgentCircle` hit-test against each agent's real rendered position; the route preview gained three distinct colours (hover/pending/committed) instead of one, so a paused, undelivered order stays visibly orange rather than looking identical to a live hover. A defensive replace-not-stack fix was added to `OnClick` (found while fixing the above — two clicks before delivery could otherwise hand `Simulation.step` two commands for one agent in the same tick). Dave separately raised multi-waypoint stacking + cancellation, explicitly hedged as undecided; put via `AskUserQuestion` rather than guessed — Dave chose to keep TASK-040 single-order-only and park the stacking design fork (client-side sequencing vs. an authoritative sim-side order queue) as new backlog row **B-051**. Both scenes' `--selfcheck` hashes unchanged (`0x11B06E6EDE0C52E3` / `0x649FA4D08E2931CA`); screenshot re-captured showing `PAUSED` + the orange pending route; `dotnet build` 0/0, `dotnet test` 297/297. | `tasks/TASK-040-*.md` review-round section added; `docs/11_BACKLOG.md` new row B-051; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-040-selection-input-and-tactical-pause.md) |
| 2026-09-17 | TASK-040 accepted | Recorded acceptance of TASK-040 (realises B-026) after round 1 review feedback was fixed: the click hit-testing bug (`TryHitAgentCircle`) and the hover/pending/committed route-colour distinction. Re-verified before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 297`, `dotnet build` the Godot client `.slnx` 0/0, `git status --porcelain` matched the expected scope (only the pre-existing unrelated `project.godot` edit left untouched). Multi-waypoint stacking and cancellation explicitly not part of this acceptance — deferred to backlog row B-051. | `tasks/TASK-040-*.md` Status `review -> done`; `docs/ledger/2026-09-17-TASK-040-*.md` Review `Accepted: yes (2026-09-17)`; `docs/11_BACKLOG.md` B-026 row `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-17) | [detail](ledger/2026-09-17-TASK-040-selection-input-and-tactical-pause.md) |
| 2026-09-17 | TASK-041 | Minimal coherent placeholder art set (realises B-034), selected via `AskUserQuestion` (run now, before B-028, since already drafted and unblocked). Live-checked kenney.nl per the task's own central decision (not assumed from memory): downloaded and inspected both `isometric-blocks` and `isometric-miniature-prototype`; chose **Isometric Miniature Prototype** (v2.3, CC0) alone, since its `Isometric/` (terrain) and `Characters/Human/` (agents) subfolders already ship together as one coherent family. New `src/CommandoWar.Client.Godot/art/` (`terrain_floor.png`, `terrain_block.png`, `terrain_crate.png`, `agent_human.png`, `LICENSE-THIRD-PARTY.md`). `DrawItem` (`Core/IClientScene.fs`) gains one new primitive field, `TextureId`, selecting a terrain texture by the same passable/opaque branches `RenderShared.buildTerrainItems` already computed (0 = floor, elevation-tinted; 1 = block, impassable; 2 = crate, passable-but-opaque) — texture shape, not colour alone, now carries that distinction. `FSharpSceneHost.cs` loads all four textures once (static fields); `_Draw`'s terrain branch (`DrawTerrainTile`) bottom-anchors the Kenney canvas at the same point `DrawDiamond` used; the agent branch (`DrawAgentFigure`) only replaces a real, full-opacity agent (`Kind = 1`, `A >= 0.99`, `TryHitAgentCircle`'s own existing "is this a real agent" test) with a cropped, tinted human figure — a translucent halo/route-preview marker still draws the original plain circle. Both existing scenes' `--selfcheck` hashes confirmed unchanged (`0x11B06E6EDE0C52E3`, `0x649FA4D08E2931CA`) — genuinely render-only. Deviation found during implementation: the first screenshot attempt used a stale `Debug`-config build (Godot's windowed run defaults to `Debug`, not the `Release` build already run for `dotnet test`) and still showed the old flat shapes; caught by inspecting the screenshot output, fixed by also building `-c Debug` before capturing. `dotnet build` both `.slnx` files (`Debug` and `Release`) 0/0; `dotnet test` 297/297 (unaffected, no `CommandoWar.Sim`/`CommandoWar.Headless` change). | `tasks/TASK-041-*.md` created and implemented (`ready -> review`); `src/CommandoWar.Client.Godot/README.md` new section + layout table; `docs/11_BACKLOG.md` B-034 row `ready -> review`, B-051 row updated with the resolved sim-side-queue design decision; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-041-placeholder-art-set.md) |
| 2026-09-17 | TASK-041 accepted | Recorded acceptance of TASK-041 (realises B-034, "seems to work"). Two follow-ups raised, neither blocking (both outside this task's scope, which explicitly excluded directional facings and never touched selection UI): agents don't always visually face the direction they last moved (new backlog row **B-052**, not designed); selection still feels fiddly, Dave suggested — hedged, "possibly" — a hover highlight on a selectable agent before clicking (new backlog row **B-053**, not designed). Recorded per Dave's explicit instruction, not implemented, before moving to the next task. | `tasks/TASK-041-*.md` Status `review -> done`; `docs/ledger/2026-09-17-TASK-041-*.md` Review `Accepted: yes (2026-09-17)`; `docs/11_BACKLOG.md` B-034 row `review -> done`, two new rows B-052/B-053; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-17) | [detail](ledger/2026-09-17-TASK-041-placeholder-art-set.md) |
| 2026-09-17 | TASK-042 | Player-facing order acknowledgement, disposition, and reason UI (realises B-028), selected via `AskUserQuestion` over B-030 proper/B-031. Central decision confirmed via `AskUserQuestion`: extend the existing corner `HudText()` label with the selected agent's disposition/reason, not a new floating label tracking the agent's on-screen position — smaller change, reuses the established `selected=agent N` pattern. `AgentSnapshot` (`src/CommandoWar.Sim/Snapshot.fs`) gains a values-only `Disposition: OrderDisposition option` field, a copy of the already-canonical `AgentState.Disposition` (the `Destination`/`Progress` precedent, docs/03 section 12); `Simulation.fs`'s `output` phase and both Godot scenes' `Ready()`-time initial-snapshot construction updated to match. New `RenderShared.dispositionText`, a player-facing structured-value-to-text mapping deliberately separate from `DiagnosticRender`'s private developer-facing one — docs/06 section 11 draws that distinction explicitly (Player-facing "order acknowledgement and disposition" vs. Developer-facing "last appraisal factors and selected reason"). `CommandDemoScene.HudText()` appends `order=<disposition text>` for the selected agent only, omitted when nothing is selected. No `FSharpSceneHost.cs`/`_Draw` change — text only, no new draw primitive. Both scenes' `--selfcheck` hashes confirmed unchanged (`RenderSnapshot` is not part of `Canonical.encode`) — genuinely values-only. A temporary `dotnet fsi` scratch probe (removed after use, the TASK-038/039 precedent) drove the real `CommandDemoScene`/`IClientScene` headlessly and printed `HudText()` at each tick, cross-checked against the exact pinned hash sequence: `"order=no order"` before issuing, `"order=accepted"` once delivered and appraised (ticks 1-3), `"order=no order"` again once fulfilled and cleared (tick 4, TASK-030's existing behaviour); the `Refused`/`Unable`/`None` text branches were exercised directly with constructed `DecisionReason` values. `dotnet build` both `.slnx` (Debug and Release) 0/0; `dotnet test` 297/297 (unaffected — no test constructs `AgentSnapshot` directly, only field-by-field comparison). | `tasks/TASK-042-*.md` created and implemented (`proposed -> review`); `src/CommandoWar.Client.Godot/README.md` new section; `docs/11_BACKLOG.md` B-028 row `proposed -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-042-reason-and-disposition-ui.md) |
| 2026-09-17 | TASK-042 review round 1 | Dave tried `CommandDemo.tscn` and reported the HUD's `order=` text "flashes up" too fast to read, and separately noted the game loop runs faster than expected — explicitly not a regression, just the first time he needed to read specific transient text rather than glance at a persistent value. Real: at the default 20 Hz sim rate, `DemoScenario`'s short 3-cell route completes in ~3 ticks (~150ms wall-clock), so `order=accepted` appeared and reverted to `order=no order` before it could be read. Fixed with a client-side, presentation-only hold timer (`orderTextHoldSeconds = 1.5`) in `CommandDemoScene`, tracked in `Update(deltaSeconds)` (the only place the real per-frame wall-clock delta is available — `HudText()` has none): a newly meaningful message always overrides immediately, only the fall-back to `"no order"` is delayed; reset immediately on any selection change so a newly selected agent never shows a stale message. No `Simulation.step`/`Disposition`/hash change (`AGENTS.md`: "rendering ... are non-authoritative"). Verified with a new headless scratch probe (removed after use) driving `Update(dt)` directly with small (`1/60s`) per-frame deltas — the actual windowed code path, unlike `StepTicksHeadless`/`--selfcheck`, which never call `Update` and so never exercised the hold timer at all: `order=accepted` now stays visible from delivery (~0.1s) through ~1.7s before reverting, over a second of readable time instead of ~150ms. Both scenes' `--selfcheck` hashes reconfirmed unchanged; `dotnet build` both `.slnx` (Debug and Release) 0/0; `dotnet test` 297/297. | `tasks/TASK-042-*.md` Review round 1 section added; `docs/11_BACKLOG.md` B-028 row updated; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-042-reason-and-disposition-ui.md) |
| 2026-09-17 | TASK-042 accepted | Recorded acceptance of TASK-042 (realises B-028, "thats readable now") after the round 1 hold-timer fix. Re-verified: both scenes' `--selfcheck` hashes unchanged, `dotnet build` both `.slnx` (Debug and Release) 0/0, `dotnet test` 297/297. Acceptance confirms the round 1 fix specifically; no further issues raised against the underlying disposition/reason feature. | `tasks/TASK-042-*.md` Status `review -> done`; `docs/ledger/2026-09-17-TASK-042-*.md` Review `Accepted: yes (2026-09-17)`; `docs/11_BACKLOG.md` B-028 row `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-17) | [detail](ledger/2026-09-17-TASK-042-reason-and-disposition-ui.md) |
| 2026-09-17 | TASK-043 | Developer overlay rendering over live input (realises B-029 proper), selected via `AskUserQuestion` over B-030/B-031/B-051/B-052/B-053. A second `AskUserQuestion` confirmed the scope fork: docs/06 section 11's developer-facing list has two items (line-of-sight rays/occluders, grid coordinates) needing interop primitives `DrawItem` doesn't have; Dave chose the full set now over a narrower cut, accepting L sizing over the backlog's M. Realised by making `CommandDemoScene` a fourth renderer of `Diagnostics.DiagnosticFrame` — the same `frame`/`frameOf` and `Overlay[]` every `DiagnosticRender` renderer already consumes, built from this scene's own live `Simulation.step` output rather than a replayed corpus entry (TASK-029's read-only `AppraisalDemoScene`). No `CommandoWar.Sim`/`CommandoWar.Headless` change of any kind. `DrawItem` (`Core/IClientScene.fs`) gained `Cx2`/`Cy2` (`Kind = 2`, a line segment — LOS rays, fire lines) and `Text` (`Kind = 3`, a label — grid coordinates); `IClientScene` gained `OnToggleDevOverlay` (`DemoRenderScene`: no-op, the `OnTogglePause` precedent). `F1` toggles the overlay in `CommandDemoScene`, independent of pause: every terrain cell gets a coordinate label; this-tick `Reserved`/`Obstructed` cells are marked (cyan/red); each `TacticalKnowledge` contact gets a pale-yellow ghost marker at its last-known cell, distinct from the real always-rendered agent marker (no fog-of-war in this demo, so the two visibly diverge once stale); the selected agent's `OrderAppraisal.exposedCells` (orange) and a new `RenderShared.devAgentText` `[dev]` HUD line (commitment/suppression/stress/reason, mirroring `DiagnosticRender`'s existing private developer vocabulary, not TASK-042's player-facing text) appear; hovering with an agent selected traces a `Sight.trace` line-of-sight ray, green if visible or red terminating at the traced `Blocker` cell (the occluder) if not; the HUD's first line gained an always-visible `draws=<random draw counter>` field. Discipline and trust excluded from the "suppression/stress/discipline/trust" bullet (trust has no backing state anywhere; discipline exists but has no `Diagnostics.Overlay` case, and adding one would ripple into `CommandoWar.Sim`/`Headless` and a golden regen, out of proportion here); "last appraisal factors" realised as `exposedCells` + reason text, not raw pressure numbers (not retained state anywhere). A new opt-in `--dev-overlay` CLI flag (kept separate from the existing `--screenshot` priming) captured the evidence screenshot without changing TASK-042's existing default capture. Both scenes' `--selfcheck` hashes confirmed unchanged (`0x11B06E6EDE0C52E3`, `0x649FA4D08E2931CA`); `dotnet build` both `.slnx` (Debug and Release) 0/0; `dotnet test` 297/297 (unaffected). A headless `dotnet fsi` scratch probe (removed after use) drove the real `CommandDemoScene` directly: with the overlay off, output matches TASK-042's existing shape plus `draws=`; toggling on adds exactly 96 coordinate labels for the 12x8 grid and a line that correctly reroutes to the traced blocker cell when occluded (checked against both a clear and a blocked hover target); the `[dev]` line appears only with the overlay on and an agent selected, and clears immediately on deselection/reselection. | `tasks/TASK-043-*.md` created and implemented (`proposed -> review`); `src/CommandoWar.Client.Godot/README.md` new section; `docs/11_BACKLOG.md` B-029 row `proposed -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-043-developer-overlay-rendering.md) |
| 2026-09-17 | TASK-043 review round 1 | Dave tried `CommandDemo.tscn` windowed and reported the LOS ray (red line) drew "underneath the other layers" and pointed toward the mouse even off the map, trailing into empty space. Two real bugs, both rendering-only: (1) z-order — `devItems` were folded into the same `Array.sortBy RenderShared.depthKey` pass as terrain/agents, and `depthKey` derives a depth from one origin cell, meaningless for a two-cell line, so it could sort as "near" and paint before terrain it visually crossed; fixed by appending `devItems` *after* the depth sort in `CommandDemoScene.DrawList()`, so the developer overlay always draws on top. (2) `OnHover` traced `Sight.trace` with no bounds check; off the grid, `Sight.trace` returns not-visible with no `Blocker`, and the existing fallback-to-`Blocker`-or-target logic drew all the way to that far, meaningless cell; fixed by setting `losRay` to `None` whenever the hovered cell fails `GridBounds.contains` (the `OnClick` precedent). Verified with a new headless scratch probe (removed after use): an in-bounds hover produces exactly one `Kind = 2` line positioned last in `DrawList()` (confirmed on top); hovering `(500,500)`/`(-3,-3)` produces zero lines; returning in-bounds restores the ray. Both scenes' `--selfcheck` hashes reconfirmed unchanged; `dotnet build` both `.slnx` (Debug/Release) 0/0; `dotnet test` 297/297. Evidence screenshot re-captured. Not investigated: a separately-reported "green flashing line from another agent" toward a hostile is most likely the same hover-driven ray behaving as designed (green = visible) while hovering near a hostile with a different agent selected, toggling as the mouse crossed a partial-occlusion cell boundary — flagged for Dave to re-check live now the two concrete bugs are fixed. | `tasks/TASK-043-*.md` Review round 1 section added; `docs/12` this row; evidence screenshot re-captured | pending | [detail](ledger/2026-09-17-TASK-043-developer-overlay-rendering.md) |
| 2026-09-17 | TASK-043 review round 2 | Dave's live re-check surfaced three more points. The round 1 "flashing green line" theory was wrong: it is `FireLine`, not the LOS ray — `Diagnostics.frameOf` emits one `FireLine` per shot fired *that tick only*, green on hit, grey on miss, and `devFrame` is replaced every tick, so during combat the line flickers in and out with each shot, independent of the mouse — working as designed, no code change. Real gap: no legend for overlay colours, and two hues are each reused for a different meaning by draw shape (LOS-visible green vs. `FireLine`-hit green; `Obstructed` red vs. LOS-blocked red); fixed with a new `RenderShared.devLegendText`, a third `HudText()` line grouping colours by draw shape, shown whenever the overlay is on. Agent facing (doesn't turn to face movement) and sim speed (`simHz = 20`) are confirmed out of scope — B-052 and a pre-existing TASK-040 constant respectively, neither a TASK-043 regression. `dotnet build` both `.slnx` (Debug/Release) 0/0; `dotnet test` 297/297. Both scenes' `--selfcheck` hashes were **not** independently re-run this round: every headless Godot launch hung indefinitely (engine banner printed, then near-zero CPU forever) on both this session's sandbox and Dave's own terminal, for both scenes, surviving a build-server kill and a full `.godot` cache clear; a normal windowed launch worked, but windowed `--selfcheck` itself did not trigger the flag either. Flagged as a known gap, not blocking: the round 2 diff is one new string-literal HUD line, unreachable from the self-check driver, and the build/test run above already covers the compiled assembly. | `tasks/TASK-043-*.md` Review round 2 section added; `docs/12` this row | pending | [detail](ledger/2026-09-17-TASK-043-developer-overlay-rendering.md) |
| 2026-09-17 | TASK-043 accepted | Recorded acceptance of TASK-043 (realises B-029 proper, "i confirm the features work") after the round 2 legend addition and the corrected flashing-line diagnosis. Dave confirmed live in a normal windowed run: the `[legend]` HUD line appears with F1 on; the round 1 z-order/off-grid LOS fixes hold; the flashing green/grey line reads as combat shots, not the mouse. The `--selfcheck` hash re-check remains an open, non-blocking gap (see the round 2 row) — an environment-level headless-launch hang unrelated to this change. | `tasks/TASK-043-*.md` Status `review -> done`; `docs/ledger/2026-09-17-TASK-043-*.md` Review `Accepted: yes (2026-09-17)`; `docs/11_BACKLOG.md` B-029 row `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-17) | [detail](ledger/2026-09-17-TASK-043-developer-overlay-rendering.md) |
