# Progress and Evidence Ledger

This ledger records what was actually done, not intended progress. It is
append-only except for correcting factual errors.

It is split in two. This file is the **index**: a pinned-facts snapshot and one
row per entry. The full text of each entry (its owner, source revision,
environment, changes, verification commands, evidence, deviations, documents
updated, and review) lives in its own **detail file** under `docs/ledger/`,
named `YYYY-MM-DD-<ID>-<slug>.md` and keeping its original heading as the title.

A completed task appends a row to the index table below and adds a matching
detail file under `docs/ledger/`; if any pinned value changed, the same change
refreshes the "Pinned facts" block. A past row or a past detail file is never
rewritten except to correct a factual error.

## Pinned facts

A faithful snapshot for quick reference, not a source of new claims. Provenance
and detail are in the linked entries.

| Fact | Value | Confirmed by |
|---|---|---|
| .NET SDK | `10.0.303` (`global.json`, `rollForward: latestPatch`) | TASK-001 |
| Target framework | `net10.0` (all projects) | TASK-001 |
| FSharp.Core | `10.1.303` (implicit, SDK-pinned) | TASK-001 |
| `Canonical.FormatVersion` | `15` (1 -> 2 TASK-018: `AgentState.Progress`; 2 -> 3 TASK-026: `WorldState.TacticalKnowledge` section; 3 -> 4 TASK-028: `AgentState.Order` + `AgentState.Disposition` sections; 4 -> 5 TASK-032: `AgentState.Suppression`; 5 -> 6 TASK-033: `AgentState.SuppressionBand` + `AgentState.Stress`; 6 -> 7 TASK-034: `WorldState.HostileTacticalKnowledge` section; 7 -> 8 TASK-037: `PlayerIntent.Suppress` + `DecisionReason.TargetNotKnown` inside the existing `Order`/`Disposition` sections; 8 -> 9 TASK-044: `AgentState.OrderQueue` section (backlog B-051, list order not sorted); 9 -> 10 TASK-045: `AgentState.Vitals` + `AgentState.RecentlyWounded` (backlog B-031); 10 -> 11 TASK-047: `AgentState.Ammo` section plus three new `PlayerIntent` cases inside `Order`/`Disposition` (backlog B-030 proper; not individually narrated in this cell's older prose, see the TASK-047 ledger detail); 11 -> 12 TASK-058: `AgentState.RadioDestroyed` + `AgentState.PendingDelivery` (backlog B-016b); 12 -> 13 TASK-062: `AgentState.Extracted` + `WorldState.MissionOutcome`/`.CompletedObjectives`/`.ObjectiveProgress` (backlog B-032) — every bump a full re-pin; behaviour-neutral except TASK-028 (moves by one `OrderAppraised` per order; `blocked-goal` now `Unable(NoKnownRoute)` at appraisal, same tick/event count), TASK-034 (`open-engagement` / `perception-contact` / `exposed-approach` legitimately gain a genuine new non-zero `HostileTacticalKnowledge` entry, same tick/event counts everywhere), TASK-037 (only the new `suppress-relieves-exposure` corpus entry carries genuine new `Suppress`-order/`Suppression` state; every other entry, the fixture, and `envelope-full` re-pin behaviour-neutrally), TASK-044 (only the new `order-queue-stacking-and-cancellation` corpus entry carries genuine new `OrderQueue` state; every other entry, the fixture, and `envelope-full` re-pin behaviour-neutrally), TASK-045 (only the new `casualties-succession-and-squad-failure` corpus entry carries genuine new wound/incapacitation/death/leadership state; `perception-contact`, `exposed-approach`, `suppress-relieves-exposure`, and `canonical-refusal-and-correction` legitimately gain or lose events too — a leadership/squad-failure detection bug fixed mid-task changed their exact counts, see the TASK-045 ledger detail — but every tick count is unchanged everywhere and every other entry, the fixture, and `envelope-full` re-pin byte-layout-only), TASK-058 (every one of the 16 existing corpus entries, the fixture, and `envelope-full` re-pin byte-layout-only — the whole range/delay/jamming/radio-destroyed feature is gated behind a scenario authoring a `Headquarters`, which none of them do; no new corpus entry, see the TASK-058 ledger detail), and TASK-062 (every pre-existing corpus entry, the fixture, and `envelope-full` re-pin byte-layout-only -- none of them authors `Objectives` beyond the shared `ScenarioSpec` builder's single always-non-blocking `reach` objective; `casualties-succession-and-squad-failure` legitimately gains a genuine `mission-failed` event once its scripted combat wipes out the friendly side, wiring up its own already-authored `fail-on-friendly-eliminated true`; a genuinely new `demolition-success` corpus entry proves `MissionOutcome -> Succeeded` end to end, see the TASK-062 ledger detail), and 13 -> 14 (TASK-065, backlog B-065: `AgentState.StalledTicks` added -- a consecutive-freeze counter for `docs/10_RISK_REGISTER.md` R-010's stall-abandon fix; every pre-existing corpus/fixture/diagnostics entry and `envelope-full` re-pin byte-layout-only, since none of them sustains a same-tick-reservation-loss or stationary-occupant freeze anywhere near `Simulation.StallAbandonTicks` (40) -- only the new `stalled-order-abandoned` corpus entry carries genuine new non-zero `StalledTicks` state and a real `MovementAbandoned` event, see the TASK-065 ledger detail), and 14 -> 15 (TASK-067, backlog B-067: `ReceivedOrder.AsGroup` added -- formation redirect now keys off the originating command's own recipient count instead of the recipient's static `FormationOffset` alone. Genuinely behaviour-changing, not byte-layout-only, for the one corpus entry exercising formation redirect (`formation-slots`, re-authored to issue a real `Command.moveToMany` so it keeps demonstrating real slot resolution, its own tick/event counts unchanged) and for the `CommandDemo.tscn` Godot self-check (its scripted sequence's six target cells were updated to the pre-TASK-067 resolved cells directly, reproducing the identical "no friendly casualties, machine gun neutralised" outcome, confirmed via a temporary probe matching the real selfcheck hash exactly); every other corpus/fixture/diagnostics entry and `envelope-full` re-pin byte-layout-only, since none of them authors a formation, see the TASK-067 ledger detail)) | TASK-003; bumped TASK-018, TASK-026, TASK-028, TASK-032, TASK-033, TASK-034, TASK-037, TASK-044, TASK-045, TASK-047, TASK-058, TASK-062, TASK-065, TASK-067 |
| `Replay.FormatVersion` | `1` | TASK-003 |
| `CommandLog.Version` | `1` | TASK-003 |
| `ReplaySerialisation.FormatVersion` (`.cwreplay` replay-command file) | `1` (production replay-command serialisation; independent of the four versions above and of `Canonical.FormatVersion`) | TASK-025 |
| `ScenarioContent.Version` | `7` (1 -> 2 TASK-010: authored terrain layer; 2 -> 3 TASK-047: `ResupplyAreas`; 3 -> 4 TASK-049: `UnitTypes` table + `RawDeployment.UnitType`; 4 -> 5 TASK-058: `Headquarters` + `Jammers` (backlog B-016b, both optional -- an absent `Headquarters` opts a scenario out of the whole range/delay/jamming/radio-destroyed feature set); 5 -> 6 TASK-059: `Formations` table + optional `RawDeployment.FormationId`/`.SlotIndex` (backlog B-011d, a blank `FormationId` opts a deployment out); 6 -> 7 TASK-062: a validation-rule change, not a new authored field (backlog B-032) -- a `"destroy"` objective now requires `HoldTicks > 0`, reusing the existing column rather than adding one; independent of the three versions above; an earlier version is rejected, not migrated) | TASK-010; bumped TASK-047, TASK-049, TASK-058, TASK-059, TASK-062 |
| PRNG | SplitMix64 v1 (seed initialises the 64-bit counter directly) | TASK-003 |
| State hash | FNV-1a-64 over `Canonical.encode` (not a cryptographic primitive) | TASK-003 |
| BenchmarkDotNet | `0.15.8` (pinned; `bench/CommandoWar.Benchmarks/` only, never in `CommandoWar.Sim` or the test project) | TASK-014 |
| FsCheck / FsCheck.Xunit | `3.3.4` (pinned; `tests/CommandoWar.Sim.Tests/` only, never in `CommandoWar.Sim` or `CommandoWar.Headless`) | TASK-019 |
| Green tests | `422` — `dotnet test CommandoWar.slnx -c Release` (`Passed: 422`; +7 from TASK-070 (backlog B-069) — three `TerrainTests` facts for the new `Terrain.withImpassable` helper (forces the given cells `Impassable`, leaves every other field untouched, does not mutate the source value; ignores an out-of-bounds cell; an empty cell set is behaviourally a no-op), two `SimulationTests` facts (an agent obstructed by a parked agent with a real detour available reroutes around it instead of stalling, naming the avoided blocker and reaching its original destination; two jointly-ordered formationed agents through a shared chokepoint both reach their own resolved slots — the TASK-068 repro, reproduced directly and now resolving instead of one agent abandoning one cell short), one `DiagnosticsTests` fact (the new `chokepoint-detour` corpus entry's tick-2 `Rerouted` overlay, byte-equal ASCII/SVG goldens), and one new `CorpusTests` theory row (`chokepoint-detour`, picked up automatically); three pre-existing `SimulationTests` facts were re-examined and updated, not smoothed over — two ("a mover whose only route runs through a permanently idle agent..."; "an order permanently obstructed by a stationary agent is abandoned...") moved onto a new `corridorWorld` fixture (row `y=1` walled off) since their whole purpose, proving a genuine no-possible-detour give-up, no longer held on `occWorld`'s open terrain once a detour became possible; a third ("two agents converging on a cell held by a stationary third...") still passed unmodified but for a materially different reason (each agent now reroutes around the parked third agent in turn, then the two rerouted agents obstruct each other instead — a known, documented limitation of independent, uncoordinated per-agent detours, not resolved further), so its doc comment and assertions were updated to assert the real mechanism directly rather than leave a stale description; no `Canonical.FormatVersion` bump (`AgentState.Route` remains the pre-existing non-canonical derived cache, the TASK-066 behaviour-only precedent); every corpus/diagnostics golden that changed was re-pinned and the diff inspected — `stalled-order-abandoned` re-authored onto the same walled corridor, hashes byte-identical (`Terrain` is excluded from `Canonical.encode`), only its description/`InitialStateNote` text and its tick-41 ascii/svg goldens' walled row changed; a genuinely new `chokepoint-detour` corpus entry (open terrain, no wall) proves the detour end to end; all three Godot `--selfcheck` hashes reconfirmed `MATCH` **unchanged**, since `CommandDemoDrive.runScriptedSelfCheck` is single-agent/no-shift (TASK-068's own finding) and never exercises the joint-order contention this task fixes; +1 from TASK-067 (backlog B-067) — one new `SimulationTests` fact (a solo `MoveTo` order for a formationed agent resolves to the literal clicked cell, not its slot offset); the pre-existing TASK-059 fact already covered a genuine group order (`Command.moveToMany`) still redirecting, unchanged; every `CanonicalHashTests`/`FixtureTests`/`PathfindingTests`/`ScenarioTests`/`SightTests`/`TerrainTests`/`DiagnosticsTests`/`CorpusTests`/`ReplayTests` fact pinning a literal hash or `Canonical.FormatVersion = 14` re-pinned to the new format-15 values; +2 from TASK-066 (backlog B-066) — two `SimulationTests` facts (a live agent walks straight through a `Dead` agent's cell instead of freezing; the identical shape for an `Incapacitated` occupant); no corpus/fixture/diagnostics golden moves, since no existing entry combines a death with a subsequent move through that exact cell; +4 from TASK-065 (backlog B-065) — two `SimulationTests` facts (a same-tick yield that clears within a few ticks never aborts the order; an order permanently obstructed by a stationary agent aborts after `Simulation.StallAbandonTicks` (40) consecutive ticks, emitting `MovementAbandoned`, `Destination`/`Route` cleared, `AgentState.StalledTicks` reset to 0), one new `DiagnosticsTests` fact (the new `stalled-order-abandoned` corpus entry's tick-41 `Abandoned` overlay golden, byte-equal ASCII/SVG) and one new `CorpusTests` theory row (`stalled-order-abandoned`, picked up automatically); every `CanonicalHashTests`/`FixtureTests`/`PathfindingTests`/`ScenarioTests`/`SightTests`/`TerrainTests`/`DiagnosticsTests`/`CorpusTests` fact pinning a literal hash or `Canonical.FormatVersion = 13` re-pinned to the new format-14 values (byte-layout only — none of the 18 pre-existing corpus/fixture/diagnostics entries sustains a freeze anywhere near the threshold); +13 from TASK-062 (backlog B-032) — eight `SimulationTests` Mission-phase facts (an `Alive` friendly agent occupying a `DestroyTarget`'s cell for its authored ticks completes it; vacating the cell before completion resets progress to 0; a completed objective stays completed after the agent leaves, sticky; `ExtractAgents` excludes a non-`Alive` agent from its requirement; `ExtractAgents` stays satisfied once a required agent has visited the extraction cell, sticky; an empty `Objectives` array never reaches `Succeeded`, the vacuous-`Array.forall` regression this task found and fixed during implementation; every non-`Optional` objective completing reaches `Succeeded` exactly once, one-way; every Friendly agent going non-`Alive` with `FailOnFriendlyForceEliminated` reaches `Failed`, one-way), two `ScenarioTests` facts (`DestroyTarget` carries its authored `HoldTicks` as the plant duration; a non-positive `HoldTicks` on a `"destroy"` objective is `NonPositivePlantTicks`, no silent default), two `DiagnosticsTests` facts for the new `demolition-success` corpus entry (an in-progress `MissionStatus` overlay mid-plant, tick 3; a `Succeeded` `MissionStatus` overlay once both objectives complete, tick 14, both byte-equal to new goldens), and one new `CorpusTests` theory case (`demolition-success`, picked up automatically); every `CanonicalHashTests`/`FixtureTests`/`PathfindingTests`/`ScenarioTests`/`SightTests`/`TerrainTests`/`ReplayTests`/`DiagnosticsTests`/`CorpusTests` fact pinning a literal hash, `Canonical.FormatVersion = 12`, or `ScenarioContent.Version = 6` re-pinned to the new format-13/version-7 values; the `fixtureScenario` `ScenarioTests` pin (a `Scenario`-validated expression of the shared six-agent fixture, authoring one real `reach observation` objective at agent 3's own `MoveTo` target) now genuinely reaches `MissionOutcome = Succeeded` at tick 31 -- a real behaviour change on top of the byte-layout move, not test-only, found by inspection and confirmed by rerunning it; +18 from TASK-060 (backlog B-024) — a new `ScenarioFileTests.fs` covering the new `.cwscenario` content format (`ScenarioFile.fs`): one FsCheck round-trip property (`MaxTest = 200`, a constrained "representable" `RawScenario` generator — single-token id/reference fields, no reserved `-` sentinel, the `ReplaySerialisation.fs` issuer-token precedent), 17 explicit facts (a known-good scenario round-trips; an all-empty/no-terrain/no-headquarters scenario round-trips; a well-formed file with content `Scenario.validate` would reject still parses; `EmptyFile` on an empty or comment-only file; `ExpectedVersionFirst`; `UnsupportedFormatVersion`; `MissingDirective`; `MalformedDirective` on a bad `map` line; `NonIntegerField`; `UnknownBoolValue`; `OrphanedTerrainDirective`; `UnknownDirective`; `DuplicateDirective`; comment/blank-line tolerance; `serialise` rejects a reserved-token or whitespace-bearing id token); no `Canonical.FormatVersion`/`ScenarioContent.Version` change (a reader, not a new field); +15 from TASK-059 (backlog B-011d) — six `SimulationTests` facts (`Appraisal.resolveFormationTarget` pure-function facts: no offset reproduces the anchor unchanged, a free offset applies directly, an occupied or impassable exact offset redirects to the nearest free cell in radius, every cell in radius blocked falls back to the literal anchor; one full-pipeline fact, two formationed agents ordered to the identical nominal cell via `Command.moveToMany` resolve to two distinct `Destination`s matching their own slot offsets), seven `ScenarioTests` facts (a blank `FormationId` validates to `None`, the pre-TASK-059 default; a non-blank `FormationId`/`SlotIndex` resolves to `Deployment.FormationOffset`; a blank formation id, a duplicate formation id, a formation with no slot offsets, an unknown-formation reference, and an out-of-range slot index are each reported), one `DiagnosticsTests` fact (the new `formation-slots` corpus entry's tick-1 golden: one `AgentFormationSlot` overlay per formationed agent, byte-equal ASCII/SVG), one new `CorpusTests` theory row (`formation-slots`, picked up automatically); four pre-existing `ScenarioTests` facts pinning a literal `ScenarioContent.Version`/`UnsupportedContentVersion` value mechanically updated to the new values (no behaviour change, no new fact); no `Canonical.FormatVersion` change (the offset is static, excluded); +18 from TASK-058 (backlog B-016b) — 10 `SimulationTests` communication-range/delay/jamming/radio-destroyed facts and 8 `ScenarioTests` `Headquarters`/`Jammers` validation facts, plus one `DiagnosticsTests` fact proving the two new sparse overlays render (folded into the running total without its own `+N` line here); the exact per-task deltas from TASK-046 through TASK-057 are not individually narrated in this cell (see each task's own `docs/ledger/` detail file) — this cell's older history resumes below, current only through TASK-045; +13 from TASK-045 — ten `SimulationTests` casualty facts (`Casualty.wound`/`.tickBleedOut`/`.currentLeader` pure-function facts, the `Suppression`/`Stress` precedent; an `Incapacitated`/`Dead` agent never fires and is never targeted; an `Incapacitated` agent with a live `Destination` never moves and it is left inert; an order to a `Dead`/`Incapacitated` agent appraises `Unable(CriticallyWounded)`; a wound taken this tick triggers a fresh appraisal the very next tick and spends the flag; leadership transfers the same tick Combat incapacitates the leader — the `StepState.InitialLeader` bug's own regression proof; `SquadFailure` fires exactly once and does not halt stepping; plus the pre-existing `resolveThreshold` wound-penalty fact drafted earlier in the same task), one `DiagnosticsTests` fact (the `casualties-succession-and-squad-failure` corpus entry traced end to end: wound accumulation, incapacitation, leadership succession, squad failure, both tick-5/tick-65 goldens), one `DeterminismPropertyTests` property 13 (`MaxTest = 200`, reusing `appraisalCaseGen` unchanged: every agent's post-tick `Vitals` reproducible from a fresh recompute, folding one `Casualty.wound` per qualifying same-tick `ShotFired` hit in shooter-id order then one `Casualty.tickBleedOut` — the property 11 `Suppression` precedent), one `CorpusTests` theory row (`casualties-succession-and-squad-failure`, picked up automatically by the existing `Corpus.all`-driven `[<MemberData>]`, no new case needed); every `CanonicalHashTests`/`FixtureTests`/`PathfindingTests`/`ScenarioTests`/`SightTests`/`TerrainTests`/`ReplayTests`/`DiagnosticsTests` fact pinning a literal hash or `Canonical.FormatVersion = 9` re-pinned to the new format-10 values (byte-layout only, no test-count change from these); the disposable `AppraisalDemo` `unhandled`-overlay `DiagnosticsTests` fact's expected count corrected `8 -> 12` (the new unconditional `AgentVitals`/`SquadLeadership` overlays this demo doesn't render) and its pinned `exposed-approach` tick-1 hash re-pinned; the "at rest" `frameOf` fact's expected-overlay-kinds set extended for the same two new unconditional overlays; no forbidden dependency, `Simulation.step` signature unchanged; +9 from TASK-044 — five `SimulationTests` order-queue facts (an appended order queues behind the active one, activates on fulfilment, and is judged the following tick, not the same one; a `Replace` order clears both the active order and anything already queued; cancelling the active order promotes the queue head, judged the *same* tick, unlike the fulfilment-driven promotion; cancelling a queued order removes only that entry; cancelling an unknown `CommandId` is rejected explicitly), one `Destination`-clearing fact for a Cancel with an empty queue (the deviation found during implementation), two `DiagnosticsTests` facts for the new `order-queue-stacking-and-cancellation` corpus entry (the tick-1 `AgentOrderQueue` overlay + golden ASCII/SVG; both cancel outcomes and the one-tick promotion gap traced end to end); every `CanonicalHashTests`/`ScenarioTests`/`PathfindingTests`/`SightTests`/`FixtureTests`/`CorpusTests`/`DiagnosticsTests`/`TerrainTests` fact pinning a literal hash or `Canonical.FormatVersion = 8` re-pinned to the new values (byte-layout only, no test-count change from these); no forbidden dependency, `Simulation.step` signature unchanged; +4 from TASK-038 — three new `DiagnosticsTests` facts against the new `canonical-refusal-and-correction` corpus entry (tick 1's `Refused RouteTooExposed threat-agent-2` is a structured `DecisionReason`, never a raw score — docs/07 section 8 step 4; tick 4's automatic threat-suppression-change reappraisal to `Accepted` — steps 5-6, unchanged from TASK-037's mechanism; the tick-8 reissue's fresh `Accepted` + `CommitmentEstablished` for the identical target, `Accepted`-or-unappraised held through tick 13 — steps 7-8, plus the tick-8 golden ASCII/SVG pair) and one new `CorpusTests` theory case (`canonical-refusal-and-correction`); no `CommandoWar.Sim` change, no `Canonical.FormatVersion` bump — every mechanism already existed; +6 from TASK-037 — five new `SimulationTests` facts (a `Suppress` order naming an unknown contact is `Unable(TargetNotKnown)`; naming a known one is `Accepted` with no `Destination` and a `Suppressing` commitment; `Appraisal.routeExposure` zeroes a suppressed threat's contribution at the pure-function level; a threat's `SuppressionBand` flip reappraises a *different* agent's `Refused` order to `Accepted` via direct field mutation, no combat RNG; a `Suppressing` agent fires at its named target even when a nearer contact is also visible) plus one extension to the existing `Commitment.ofAgent` enumeration fact (a `Suppress` + `Accepted` case); `DeterminismPropertyTests`/`CorpusTests` gain defensive exhaustive-match arms only (no new fact); no `Canonical.FormatVersion` change from the test additions themselves — the bump to 8 is the task's own canonical-state addition, not test-only; +2 from TASK-034 — one `CanonicalHashTests` fact (the hostile-tactical-knowledge canonical section / `firstDifferingSection` label, the `TacticalKnowledge` fact's precedent) and one `SimulationTests` fact (a Hostile agent with a stale `HostileTacticalKnowledge` entry for a friendly it can no longer see never appears as a shooter in that tick's `ShotFired` events, proving `docs/09` section 8's observed-only-targeting criterion); `DeterminismPropertyTests` property 6 extended in place to also ground `WorldState.HostileTacticalKnowledge` in a Hostile agent's own observation; the perception-contact `DiagnosticsTests` fact and the exposed-approach `AppraisalDemo` unhandled-overlay `DiagnosticsTests` fact (`6 -> 8`) both extended in place for the new symmetric `HostileKnownContact` overlay; no `Canonical.FormatVersion` change from the test additions themselves — the bump to 7 is the task's own canonical-state addition, not test-only; +11 from TASK-033 — three `Stress.gain`/`.raise`/`.decay` pure-function `SimulationTests` facts (the `Suppression.fs` precedent), three `Appraisal.resolveThreshold` facts (suppressed drops by exactly `SuppressionBandPenalty`; full stress drops by exactly `MaxStress / StressDivisor`; floored at 0 at minimum discipline / cautious risk / full stress / suppressed), two phase-level Stress facts (an agent with a visible contact gains net `GainPerTick - DecayPerTick`, one without stays at 0; Stress accumulates over continuous contact and decays once it is lost), one knowledge-change-trigger fact (a `Refused` order becomes `Accepted` once its only known threat's contact expires after `PerceptionConfig.ExpireAfter` ticks unseen, no real hostile agent needed since `Appraisal.appraise` reads `WorldState.TacticalKnowledge` directly), one suppression-band-trigger fact (an `Accepted` order is reappraised — a fresh `OrderAppraised`, outcome unchanged on a safe route — on either latch flip direction and not on a quiet tick), one `DeterminismPropertyTests` property 12 (`MaxTest = 200`, reusing `appraisalCaseGen` unchanged: every agent's post-tick `Stress` and `SuppressionBand` reproducible from a fresh recompute); the existing combat-determinism `SimulationTests` fact extended in place with `Stress`/`SuppressionBand` array comparisons (no new fact); the disposable `AppraisalDemo` `unhandled`-overlay `DiagnosticsTests` fact's expected count corrected `3 -> 6` (three agents now also carry a sparse `AgentStress` overlay this demo doesn't render — genuine new behaviour); no `Canonical.FormatVersion` change from these additions themselves — the bump to 6 is the task's own canonical-state addition, not test-only; +9 from TASK-032 — eight `SimulationTests` facts (`Suppression.gain` gives a hit strictly more than a miss; `Suppression.gain` strictly decreases as cover level increases, floored at 0; `Suppression.raise` accumulates two shots and clamps at `MaxSuppression`; `Suppression.decay` drops by `DecayPerTick`, floored at 0; a qualifying shot raises the target's `Suppression` net of the same-tick decay, data-driven off the actual `ShotFired.hit` value; two shooters engaging the same target the same tick compose their gains; an already-suppressed agent decays by exactly `DecayPerTick` on a tick with no qualifying target; an agent never shot at stays at `Suppression = 0` indefinitely), one `DeterminismPropertyTests` property 11 (`MaxTest = 200`, reusing `appraisalCaseGen` unchanged: every agent's post-tick `Suppression` reproducible from a fresh recompute — pre-tick value folded with `Suppression.gain`/`.raise` over this tick's targeting `ShotFired` events, then one `Suppression.decay`); the existing combat-determinism `SimulationTests` fact extended in place to also compare both runs' `Suppression` arrays (no new fact); the `open-engagement` `FireLine` `DiagnosticsTests` fact extended in place with `AgentSuppression` overlay assertions (no new fact); no `Canonical.FormatVersion` change from these additions themselves — the bump to 5 is the task's own canonical-state addition, not test-only; +11 from TASK-031 — one `CorpusTests` theory case (`open-engagement`), eight `SimulationTests` facts (both sides fire when in range/LOS; out-of-range excluded; `Combat.chooseTarget` excludes an LOS-blocked candidate; `Combat.hitChance` monotonic in range and in cover, clamped at the extremes; `Combat.chooseTarget` nearest + ascending-id tie-break; two-run determinism with an exact draw-count match), one `DeterminismPropertyTests` property 10 (`MaxTest = 200`, every `ShotFired` reproducible from a fresh recompute + redraw, exact draw count, reusing `appraisalCaseGen` unchanged), one `DiagnosticsTests` fact (the `open-engagement` golden, byte-equal ASCII/SVG, both `FireLine` overlays and their event kinds checked); no `Canonical.FormatVersion` change — `WorldState.Random` was already canonical, only the values it draws are new; +10 from TASK-030 — eight `SimulationTests` commitment facts (a fresh `Accepted` order emits `CommitmentEstablished` the same tick as `OrderAppraised`, an agent that arrives emits `CommitmentCompleted` with the identical Order/Disposition clear as before TASK-030, a second order delivered mid-route emits a fresh `CommitmentEstablished` with no event for the first, a `Refused`/`Unable` order never emits `CommitmentEstablished`, an unchanged already-appraised order emits neither event on an idle tick, determinism + zero draws, and a `Commitment.ofAgent` unit fact covering every reachable `(Order, Disposition, Destination)` combination), one `DeterminismPropertyTests` property 9 (`MaxTest = 200`, reusing property 8's generator: every agent's derived `Commitment` matches its `Order`/`Disposition`/`Destination`, no PRNG draw), one `DiagnosticsTests` fact (the new `reissued-order` corpus entry's tick-3 golden, proving commitment supersession); no `Canonical.FormatVersion` change and no hash re-pin anywhere — `Commitment` is a derived value, not canonical state; +2 from TASK-029 — two `DiagnosticsTests` facts for the read-only Godot appraisal-demo helper `src/CommandoWar.Headless/AppraisalDemo.fs` (`dispositionText` matches the committed golden vocabulary; `loadExposedApproachFrames` reproduces the `exposed-approach` tick-1 hash + the two divergent dispositions + the flat C#-facing view model); no `CommandoWar.Sim` change; +14 from TASK-028 — eight `SimulationTests` order-appraisal facts (clear-route `Accepted` + `Destination` same tick, no-path `Unable(NoKnownRoute)`, never `Accepted` after a hard feasibility failure, exposed route `Refused(RouteTooExposed)` for a low-`Discipline` agent, the same order `Accepted` for a high-`Discipline` agent — the G3 divergence, directional cover drops exposure below threshold, not re-appraised on an idle tick, deterministic + zero draws), three `DiagnosticsTests` facts (a hand-built `OrderAppraisal` overlay render, the `exposed-approach` golden showing the divergence, the `blocked-goal` golden showing `Unable` at appraisal), one `DeterminismPropertyTests` property 8 (`MaxTest = 200`, every `Disposition` consistent with a fresh recompute, no PRNG draw), one new `SimulationTests` fact pinning that the Navigation phase still emits `MovementBlocked` for a `Destination` set outside the order pipeline (the two former direct-`MovementBlocked` order tests became `Unable(NoKnownRoute)` appraisal tests), one new `CorpusTests` `[<Theory>]` case (`exposed-approach`); +12 from TASK-027 — five `SimulationTests` order-delivery facts (reachable delivered, unreachable `OrderUndelivered`, multi-recipient partial delivery, undelivered order does not cancel an in-progress one, determinism), one `DeterminismPropertyTests` property 7 (`MaxTest = 200`, comms-blacked-out agents inert and `OrderUndelivered` well-formed), one `CanonicalHashTests` fact (`CommunicationAvailable` outside the canonical image), two hand-built `DiagnosticsTests` facts (`UndeliveredOrder` overlay, `AgentMarker.CommunicationAvailable`) + one `lost-comms` golden fact, one `ScenarioTests` round-trip fact, one new `CorpusTests` theory case (`lost-comms`); +10 from TASK-026 — five `SimulationTests` perception facts, one `DeterminismPropertyTests` property (`MaxTest = 200`, squad contacts grounded in observation), one `CanonicalHashTests` fact (tactical-knowledge section / `firstDifferingSection`), one hand-built `KnownContact` overlay + one `perception-contact` golden fact in `DiagnosticsTests`, one new `CorpusTests` theory case (`perception-contact`); +7 over TASK-019's 171 from TASK-021 — five `ScenarioTests` move-cost-range facts, one `PathfindingTests` near-ceiling long-path fact, a fourth `DeterminismPropertyTests` FsCheck optimality property; +6 from TASK-020 — multi-recipient accept, ascending-id ordering, hostile co-recipient rejection, empty recipients, repeated recipient, duplicate-command-id reject-all + batch-order independence; +10 from TASK-022 — five `SimulationTests` cell-occupancy facts, a fifth `DeterminismPropertyTests` property (distinct live agents hold distinct cells), a hand-built `Obstructed` overlay `DiagnosticsTests` fact + the `swap-standoff` golden fact, and two new `CorpusTests` theory cases (`follow-chain`, `swap-standoff`); the benchmark project contributes no tests; +5 from TASK-024 — four `SimulationTests` issue-tick-eligibility facts and one `ReplayTests` `DuplicateCommandIdInLog` fact; +7 from TASK-025 — a `ReplaySerialisation` round-trip property (`MaxTest = 200`), a full-field-matrix round-trip fact, unknown-version / canonical-mismatch / out-of-order rejection facts, a hand-written full-envelope parse fact, and the committed `envelope-full.cwreplay` fixture cross-check). CI-verified on `windows-latest` by `.github/workflows/ci.yml` on every push and PR (TASK-023); the count itself is not pinned by CI. | TASK-019; +7 TASK-021; +6 TASK-020; +10 TASK-022; +5 TASK-024; +7 TASK-025; +10 TASK-026; +12 TASK-027; +14 TASK-028; +2 TASK-029; +10 TASK-030; +11 TASK-031; +9 TASK-032; +11 TASK-033; +2 TASK-034; +6 TASK-037; +4 TASK-038; +9 TASK-044; +13 TASK-045; +18 TASK-058; +15 TASK-059; +18 TASK-060; +13 TASK-062; +4 TASK-065; +2 TASK-066; +7 TASK-070; CI TASK-023 |
| Accepted ADRs | ADR-0001 (Godot, accepted 2026-09-03; 2026-09-06 note: Mibo packaging blocker may be resolved upstream, decision stands); ADR-0002 (framework-independent sim, project baseline); ADR-0003 (Mibo: spike complete, **not adopted**; 2026-09-06 amendment records Mibo 5.x classic MVU as a live reconsideration option after the upstream package re-separation); ADR-0004 (low-impedance C#/F# boundary, accepted 2026-09-03) | TASK-007 acceptance; ADR amendments 2026-09-06 |
| Current gate | `G4_vertical_slice_feature_complete` (pending) | G3 gate passed, 2026-09-16 |
| Current phase | `P4_playable_integration` | G3 gate passed, 2026-09-16 |

### Shared fixture

Defined in `src/CommandoWar.Headless/Fixture.fs`; pinned by
`tests/CommandoWar.Sim.Tests/FixtureTests.fs`; per-tick hash table in
`content/fixtures/SPIKE-FIXTURE.md`; canonical command log
`content/fixtures/spike-fixture.cwlog`.

| Parameter | Value |
|---|---|
| Builder | `Setup.sixAgentWorld` |
| Grid | 32 x 32 |
| Seed | `20260902` (`0x0000000001352826`) |
| Agents at tick 0 | 6 friendly, column `x = 0`, rows `y = 0..5` |
| Command | tick 1: agent 3 `MoveTo (20, 14)`, `CommandId 1` |
| Tick count | 40 (agent 3 reaches `(20, 14)` at tick 31; ticks 32..40 are rest) |
| Initial hash (tick 0) | `0x5049F6F0E9FCA1E2` (format 15; TASK-067 re-pin, byte-layout only — this fixture authors no formation, so `ReceivedOrder.AsGroup` stays `false` throughout. A prior TASK-065 note here claimed this row and `content/fixtures/SPIKE-FIXTURE.md` had already been corrected to format 14; that claim did not match the committed file (`content/fixtures/SPIKE-FIXTURE.md` was still format 4, confirmed by `git log`) — corrected for real this time, straight to format 15, flagged for Dave) |
| Final hash (tick 40) | `0xD2A6A1AE46AD76A5` (format 15; TASK-067 re-pin, same note as above) |
| Domain events | 36 (1 `CommandAccepted` + 1 `OrderAppraised` + 1 `CommitmentEstablished` + 31 `MovementStepped` + 1 `MovementCompleted` + 1 `CommitmentCompleted`, TASK-030); 0 random draws — the fixture is enemy-free, so agent 3's order is `Accepted` at appraisal (TASK-028) with an empty route exposure; `AgentState.Progress`/`WorldState.TacticalKnowledge` are still empty at every tick, and agent 3 still reaches `(20, 14)` at tick 31 (tick 40 unchanged). This count was also stale at 34 (its TASK-028 value, predating TASK-030's two commitment events) — corrected alongside the hashes above |
| Environment (TASK-023) | Hashes pinned on Windows x64 / .NET SDK `10.0.303`; re-verified there by `.github/workflows/ci.yml` on every push and PR. Other environments are outside the per-environment determinism contract (`docs/09` section 3). |

## Detail entry template

Each `docs/ledger/*.md` file holds one entry in this shape:

```markdown
## YYYY-MM-DD - TASK-NNN - concise outcome

**Owner:**
**Source revision:**
**Environment:**
**Status change:** `old -> new`

### Changes

- ...

### Verification

- Command: `...`
  - Result: ...
- Manual check: ...
  - Result: ...

### Evidence

- Paths, logs, screenshots, benchmark files, replay IDs, or hashes.

### Deviations and unresolved issues

- None, or explicit details.

### Documents updated

- ...

### Review

- Reviewer: Dave
- Accepted: yes/no/pending
- Notes: ...
```

## Index

Chronological. One row per detail file.

| Date | ID | Outcome | Status change | Accepted | Detail |
|---|---|---|---|---|---|
| 2026-09-02 | PLAN-001 | Initial project control pack assembled (charter, gates, ADRs, risks, backlog, bounded tasks) | `unstructured concept -> P0 governance and feasibility` | pending (G0 passed at TASK-001 acceptance) | [detail](ledger/2026-09-02-PLAN-001-initial-control-pack.md) |
| 2026-09-02 | TASK-001 | Repository and build baseline: `global.json`, `CommandoWar.slnx`, F# sim + xUnit projects, boundary tests | `active -> review` | yes (2026-09-02) | [detail](ledger/2026-09-02-TASK-001-repository-baseline.md) |
| 2026-09-02 | TASK-002 | Framework-neutral simulation skeleton: domain types, 11-phase `Simulation.step`, placeholder movement | `active -> review` | yes (2026-09-02) | [detail](ledger/2026-09-02-TASK-002-simulation-skeleton.md) |
| 2026-09-02 | TASK-003 | Deterministic SplitMix64 PRNG, canonical encoding, FNV-1a-64 hash, replay + divergence harness | `active -> review` | yes (2026-09-02) | [detail](ledger/2026-09-02-TASK-003-determinism-harness.md) |
| 2026-09-02 | TASK-004 | Disposable Godot .NET framework spike; shared `cwheadless` reference and pinned fixture added | `active -> review` | yes (2026-09-02) | [detail](ledger/2026-09-02-TASK-004-godot-spike.md) |
| 2026-09-02 | TASK-005 | Environment check: stable Mibo hard-depends on prohibited `Mibo.Adaptive`; spike rescoped and pinned to 4.1.0 | none (TASK-005 stays `ready after dependency`) | yes (2026-09-02) | [detail](ledger/2026-09-02-TASK-005-mibo-environment-check.md) |
| 2026-09-02 | TASK-005 | Disposable Mibo + raylib framework spike (Mibo 4.1.0); shared fixture 41-hash sequence reproduced | `active -> review` | yes (2026-09-03) | [detail](ledger/2026-09-02-TASK-005-mibo-spike.md) |
| 2026-09-03 | TASK-004 interactive addendum | Godot editor GUI still not drivable in-session; project imports into the 4.7.2 editor with no error | none (TASK-004 stays `done`) | n/a (addendum) | [detail](ledger/2026-09-03-TASK-004-interactive-addendum.md) |
| 2026-09-03 | TASK-006 | Spikes evaluated; ADR-0001 scored table completed (Godot 4.13 / Mibo 3.62); Godot recommendation put to Dave | TASK-005 `review -> done`; TASK-006 `proposed -> active` | yes (2026-09-03) | [detail](ledger/2026-09-03-TASK-006-spikes-evaluated.md) |
| 2026-09-03 | TASK-006 finalisation | ADR-0001 accepted (Godot); G1 passed; Mibo route retired | ADR-0001 `proposed -> accepted`; `G1 -> passed`; gate `G1 -> G2`; phase `P1 -> P2`; TASK-006 `active -> done` | yes (2026-09-03) | [detail](ledger/2026-09-03-TASK-006-finalisation.md) |
| 2026-09-03 | TASK-007 | Godot C#/F# client boundary designed; ADR-0004 proposed; disposable proof built (questions 1-5) | TASK-007 `-> active -> review`; ADR-0004 created (`proposed`) | yes (2026-09-03) | [detail](ledger/2026-09-03-TASK-007-godot-fsharp-boundary.md) |
| 2026-09-03 | TASK-007 acceptance | ADR-0004 accepted; boundary design signed off | ADR-0004 `proposed -> accepted`; TASK-007 `review -> done` | yes (2026-09-03) | [detail](ledger/2026-09-03-TASK-007-acceptance.md) |
| 2026-09-03 | TASK-008 | Authored `Scenario` model, `ScenarioContent.Version`, one-pass `Scenario.validate`, `World.ofScenario` | `active -> review` | yes (2026-09-03) | [detail](ledger/2026-09-03-TASK-008-scenario-dto.md) |
| 2026-09-03 | TASK-009 | Progress ledger restructured into this index plus per-entry detail files under `docs/ledger/` | `active -> review` | yes (2026-09-03) | [detail](ledger/2026-09-03-TASK-009-ledger-restructure.md) |
| 2026-09-03 | TASK-010 | Authoritative terrain grid: `Terrain` module (elevation, passability, cost, opacity, directional cover), authored layer + validation, `ScenarioContent.Version` 2 | `active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-03-TASK-010-terrain-grid.md) |
| 2026-09-03 | TASK-011 | Framework-neutral `DiagnosticFrame` observer in `CommandoWar.Sim` + deterministic ASCII/SVG/HTML renderers, `cwheadless render` verb, terrain-demo scenario, golden renders, and the "visually inspectable" standing rule | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-03-TASK-011-diagnostic-visualisation.md) |
| 2026-09-04 | TASK-012 | Deterministic point-to-point line of sight (`Sight` module: integer supercover walk, symmetric corner rule, ridge-occlusion elevation rule) + `SightRay` diagnostic overlay + `--los` render option + LOS demo goldens | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-04-TASK-012-line-of-sight.md) |
| 2026-09-04 | TASK-013 | Deterministic grid pathfinding (`Pathfinding` module: 4-connected A*, integer cost + Manhattan heuristic, total-order frontier key, N/E/S/W neighbour order, explicit expansion budget) + `PlannedPath` diagnostic overlay + `--path` render option + pathfinding demo goldens | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-04-TASK-013-pathfinding.md) |
| 2026-09-04 | TASK-014 | Headless performance and allocation benchmark harness (`bench/CommandoWar.Benchmarks/`: empty tick, six- and ~50-agent placeholder movement, line-of-sight batch, pathfinding open/blocked/choke, canonical encode + state hash, replay run) + committed `content/benchmarks/BASELINE.md` | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-04-TASK-014-benchmark-harness.md) |
| 2026-09-04 | TASK-015 | Navigation and movement phase: `Pathfinding`-driven single-agent executor replacing `PlaceholderMovement` (`docs/04` section 8 steps 2/4/5/6) + `AgentState.Route` non-canonical derived cache (`FormatVersion` stays 1, fixture hashes unmoved) + `MovementBlocked` event + `Diagnostics.frameOf` `PlannedPath` overlay with `fixture-mid-route.*` goldens; cell reservation / formation slots / sub-cell progress split to B-011b | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-04-TASK-015-movement-executor.md) |
| 2026-09-04 | TASK-016 | Divergence diagnostics and replay corpus infrastructure: committed multi-entry corpus `content/replays/` (spike fixture, wall detour, `MovementBlocked` no-path, two-agent converging routes) over the existing `.cwlog` v1 format, `Corpus.fs` name -> `WorldState` registry, `cwheadless corpus [--regenerate]` verb, in-suite `CorpusTests.fs` `[<Theory>]`; generative property tests and component subhashes split to B-012b | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-04-TASK-016-replay-corpus.md) |
| 2026-09-04 | TASK-017 | Same-tick cell reservation and deadlock avoidance (backlog B-011b, narrowed): `Simulation.navigationAndMovement` resolves contested-cell moves by fewest remaining route steps (ties broken by agent id), provably terminating for a shared-target-cell contest; new `MovementYielded` event and `Reserved` diagnostic overlay; `content/replays/converging-routes` re-pinned as TASK-016 anticipated; `Canonical.FormatVersion` stays 1; formation slots and sub-cell movement progress split to B-011c | `proposed -> active -> review -> done` | yes (2026-09-04) | [detail](ledger/2026-09-04-TASK-017-cell-reservation.md) |
| 2026-09-04 | TASK-018 | Sub-cell movement progress within an edge (backlog B-011c, narrowed): `AgentState.Progress` (threshold `Terrain.moveCost`, increment `Terrain.BaseMoveCost`), `Canonical.FormatVersion` bumped 1 -> 2 (every pinned hash re-pinned, confirmed behaviour-neutral for every pre-existing scenario); reservation generalised to "would complete this tick" claimants with no new state; new `slow-terrain` corpus entry and diagnostics golden; a route-persistence bug found and fixed during implementation; formation slots split to B-011d | `proposed -> active -> review -> done` | yes (2026-09-05) | [detail](ledger/2026-09-04-TASK-018-subcell-movement-progress.md) |
| 2026-09-05 | TASK-019 | Generative determinism property tests (backlog B-012b, narrowed): `FsCheck`/`FsCheck.Xunit` 3.3.4 added to the test project only; three FsCheck properties over generated small worlds and command sequences (determinism under random commands via the `Replay`/`Divergence` self-double-run pattern; no agent on an invalid cell after movement; pathfinding endpoints/cost on random terrain); component-level subhashes stay proposed under B-012b with a narrowed justification | `proposed -> active -> review -> done` | yes (2026-09-05) | [detail](ledger/2026-09-05-TASK-019-determinism-property-tests.md) |
| 2026-09-05 | G2 gate decision | G2 declared passed on the `docs/08` section 5 evidence bullets alone (all six now have landed evidence: TASK-003, TASK-014, TASK-016, TASK-019); "tactical knowledge" removed from section 5's Required work as a P3 forward-reference, not a G2 blocker; gate/phase advanced to G3/P3 | `gates.G2_deterministic_core_proven` `pending -> passed`; `current_gate` `G2 -> G3_command_loop_proven_headless`; `current_phase` `P2_deterministic_core -> P3_command_loop_headless` | yes (2026-09-05) | [detail](ledger/2026-09-05-G2-gate-decision.md) |
| 2026-09-05 | TASK-020 drafted | Command validation and recipient selection (backlog B-014, first P3 task): task file drafted, not started — `PlayerCommand.Recipients: AgentId list` generalised with a back-compatible `Command.moveTo`; `Urgency`/`RiskTolerance` envelope fields, inert until B-017; new `CommandRejection` cases `EmptyRecipients`, `UnauthorisedRecipient`, batch-scoped `DuplicateCommandId`; no `Canonical.FormatVersion` or `CommandLogFile.Version` change | `proposed -> ready` (task file created); B-014 `proposed -> ready` | pending | [detail](ledger/2026-09-05-TASK-020-drafted.md) |
| 2026-09-06 | ADR-0003 amendment | Mibo 5.0.0 (2026-09-04) re-separates classic MVU (`Mibo.Mvu`) from the adaptive runtime (`Mibo.Adaptive.Mibo`); `Mibo.Core` no longer depends on `Mibo.Adaptive`. The 2026-09-02 packaging blocker is lifted on the 5.x line; Mibo 5.x classic MVU recorded as a live reconsideration option. Framework decision unchanged (Godot stands). Control files only, no source. | ADR-0003 amended (2026-09-06); ADR-0001 dated note added; R-003 `closed -> watch`; backlog B-043 added `proposed`, B-024 now depends on B-043 | pending | [detail](ledger/2026-09-06-mibo-5-repackaging-recorded.md) |
| 2026-09-06 | Control-plane reconciliation | Root `README.md` and `PROJECT_STATE.yaml` brought into line with the actual state: framework decided (Godot, ADR-0001), G0/G1/G2 passed, phase P3, TASK-020 drafted-and-ready-not-selected. Removed the stale `README` "framework decision pending / next task TASK-001 / no framework selected" claims and the obsolete `PROJECT_STATE` note about the unresolved G2 question. No source, task, or gate change. | `README.md` header + "Immediate sequence" + "Architectural position" + baseline section updated; `PROJECT_STATE.yaml` `active_work.note` rewritten, `updated -> 2026-09-06` | pending | [detail](ledger/2026-09-06-control-plane-reconciliation.md) |
| 2026-09-06 | TASK-020 revised | `tasks/TASK-020-*.md` corrected before selection, per the standing review: objective reworded as a **partial** section 13 envelope (drops the "in full" claim); duplicate recipients rejected `DuplicateRecipient` not silently normalised; a duplicated `CommandId` now rejects **every** command in the group (batch-order-independent) not "first wins"; `commandIntake` copies the agent array once per batch, not per accepted command (`BASELINE.md` O(n^2) hot spot); issue-tick semantics and the replay-command serialisation each get a mandatory-before-G3 follow-up (B-044, B-045), added to the G3 Required-work list. Task-file / backlog / roadmap / ledger only, no source. | `tasks/TASK-020-*.md` objective + central decisions + scope + acceptance criteria revised; `docs/11_BACKLOG.md` TASK-020 row updated, B-044 + B-045 added `proposed`; `docs/08` section 6 P3 Required work gains the B-044/B-045 bullet | pending | [detail](ledger/2026-09-06-TASK-020-revised.md) |
| 2026-09-06 | TASK-021 drafted | Terrain/pathfinding correctness task drafted (backlog B-046, from the standing review's defect #1): passable `MoveCost` to be bounded `[Terrain.BaseMoveCost, Terrain.MaxMoveCost]` — restores the A* heuristic's admissibility/consistency precondition, keeps a passable cell's `moveCost` below the `BlockedCost` sentinel, keeps cost accumulation inside `int32`; adds a Dijkstra-reference optimality property (the TASK-019 property only recomputes the returned path's own cost). No A* algorithm change, no `Canonical.FormatVersion` bump, no hash re-pin. Task file + backlog + ledger only, not started, not selected. | `tasks/TASK-021-MOVEMENT-COST-BOUNDS.md` created (`ready`); `docs/11_BACKLOG.md` new TASK-021 row + B-046 `ready` | pending | [detail](ledger/2026-09-06-TASK-021-drafted.md) |
| 2026-09-06 | TASK-022 drafted | Movement-phase cell-occupancy correctness task drafted (backlog B-047, from the standing review's defect #2): `Simulation.navigationAndMovement` assigns `Position = next` for an unrivalled mover without checking a stationary occupant of `next` — TASK-017 reservation only arbitrates rival claimants for the same cell, and its ledger flagged the stationary-occupant and rotation-deadlock gaps for a later task that never took them. Task defines the pre-combat occupancy policy (occupied cell blocks entry; swaps and rotation cycles blocked; follow chains resolve same-tick to a free cell) via a deterministic vacation-chain stage in Pass 2, a new `MovementObstructed` event + `Obstructed` overlay, and a unique-live-agent-cell-occupancy property. No cooperative pathfinder, no `Canonical.FormatVersion` bump, no new `AgentState` field, no hash re-pin. Task file + backlog + ledger only, not started, not selected. | `tasks/TASK-022-CELL-OCCUPANCY.md` created (`ready`); `docs/11_BACKLOG.md` new TASK-022 row + B-047 `ready` | pending | [detail](ledger/2026-09-06-TASK-022-drafted.md) |
| 2026-09-06 | TASK-023 drafted | Simulation-CI task drafted (backlog B-048, from the standing review's recommendation #6 and the control-plane-reconciliation "no B-item yet" deviation): a `.github/workflows/ci.yml` on `windows-latest` (matching the environment the committed hashes were pinned in — `docs/09` section 3), on push and PR, that restores/builds/tests `CommandoWar.slnx` with the `global.json` SDK pin then runs `cwheadless corpus` (fails the job on any divergence from the committed tables) and `cwheadless fixture` (smoke). Decisions recorded: client-compile / content-validation / package-smoke deferred to the first P4 client task; lockfiles deferred (source change, existing pins suffice); `bench/` builds with the slnx but benchmarks are not run; `windows-latest` not `ubuntu-latest` (cross-platform determinism is ADR-gated, not a CI side effect); sized `S` — one workflow file plus a one-line environment annotation to the corpus/fixture docs. No source / `.fsproj` / test / hash / `Canonical.FormatVersion` change; no ADR; no `docs/08` gate change. Task file + backlog + ledger only, not started, not selected. | `tasks/TASK-023-SIMULATION-CI.md` created (`ready`); `docs/11_BACKLOG.md` new TASK-023 row + B-048 `ready` | pending | [detail](ledger/2026-09-06-TASK-023-drafted.md) |
| 2026-09-06 | Tooling / debug-display design note | Exploratory analysis of scenario-authoring efficiency and the developer debug display (standing review). Findings: (1) a text scenario format is premature and would collide with B-045; a shared F# fixture builder generalised from `Corpus.fs` is worth it but only after B-045 and TASK-022 — proposed as B-049. (2) The `cwheadless render --format html` multi-tick scrubber already is the headless "explain any state transition" tool; do not pull B-029 forward or build a live viewer before P4; the real gap is rendering the first divergent tick — proposed as B-050. HTML overlay toggles / cell inspection left as opportunistic work inside P3 tasks, no row. No task files, no source. | `docs/notes/2026-09-06-tooling-and-debug-display.md` created; `docs/11_BACKLOG.md` section 3 new B-049 + B-050 `proposed` | pending | [detail](ledger/2026-09-06-tooling-and-debug-display-note.md) |
| 2026-09-06 | TASK-020 | Command validation and recipient selection (backlog B-014), a **partial** section 13 envelope: `PlayerCommand.Recipients: AgentId list` replaces the single `Agent` field (back-compatible `Command.moveTo` + a `.Agent` head accessor kept so pre-existing `CorpusTests` compile unchanged; new `Command.moveToMany`); inert `Urgency` / `RiskTolerance` envelope fields (B-017 appraisal inputs nothing reads yet); four new `CommandRejection` cases (`EmptyRecipients`, `DuplicateRecipient`, `UnauthorisedRecipient` for a `Hostile`-side recipient, batch-order-independent `DuplicateCommandId` that rejects every command in a duplicated-id group); `Simulation.commandIntake` copies the agent array once per phase invocation (was per accepted command — `BASELINE.md` O(n^2) hot spot) and emits `CommandAccepted` before writing the destination; `Diagnostics.eventMarker` gains one arm per new case. Issuer identity and issue-tick eligibility stay out with mandatory-before-G3 follow-ups B-044 / B-045 (already in the backlog and `docs/08` section 6). `178 -> 184` green; `dotnet build` `0/0`; `-- corpus` / `-- fixture` byte-identical with no `--regenerate`; no `Canonical.FormatVersion` or `CommandLogFile.Version` change, no hash re-pin. | `ready -> review -> done`; `docs/11_BACKLOG.md` TASK-020 row + B-014 `ready -> done`; `PROJECT_STATE.yaml` `active_work.selected_task` `TASK-021 -> TASK-020` | yes (2026-09-06) | [detail](ledger/2026-09-06-TASK-020-command-validation-and-recipients.md) |
| 2026-09-06 | TASK-021 | Minimum passable movement cost and overflow-safe pathfinding cost (backlog B-046): new `Terrain.MaxMoveCost = 1000` literal; new `ScenarioError.MoveCostOutOfRange` reported for a passable authored cell outside `[Terrain.BaseMoveCost, Terrain.MaxMoveCost]` (`NegativeMoveCost` kept for `< 0`; an impassable cell's cost still ignored). Restores the A* Manhattan heuristic's admissibility/consistency precondition (every passable step now costs `>= BaseMoveCost`) and keeps a passable cell's `moveCost` below the `BlockedCost` sentinel with cost accumulation inside `int32` — stated as the bound `Width * Height * MaxMoveCost < Int32.MaxValue` in the `Pathfinding` "## Bounded work" comment. No `Pathfinding` algorithm change, no `Canonical.FormatVersion` bump, no hash re-pin; every committed scenario/corpus authors passable costs of 1/3/5 only (grep-verified), so `-- corpus` / `-- fixture` reproduce their tables byte-identically. Tests: +5 `ScenarioTests` (below/above/exact-endpoints/impassable-accepted/zero-cost-corridor), +1 `PathfindingTests` (near-ceiling long path), +1 `DeterminismPropertyTests` property (optimality vs an independent relaxation shortest-path search, 200 cases); `171 -> 178` green. | `proposed -> active -> review -> done`; `docs/11_BACKLOG.md` TASK-021 row + B-046 `ready -> done`; `PROJECT_STATE.yaml` `active_work.selected_task none -> TASK-021` | yes (2026-09-06) | [detail](ledger/2026-09-06-TASK-021-movement-cost-bounds.md) |
| 2026-09-06 | TASK-022 | Runtime cell-occupancy correctness in the Navigation and movement phase (backlog B-047, a post-gate correctness fix to TASK-015 / TASK-017). `Simulation.navigationAndMovement` gains a stage-2b vacation-chain resolution between rival arbitration (2a, unchanged) and Pass 3: an additive fixpoint over the completing non-yielded movers computes `movers` and `obstructedBy: Map<int, AgentId>`; a new Pass 3 arm freezes an obstructed `Advancing` agent like a rival-contest loser and emits the new `MovementObstructed of agent * at * blocked * occupant` event. Swaps and n-agent rotation cycles fall out as all-obstructed; a 3-agent pure cycle is geometrically impossible on the bipartite 4-connected grid (the rotation test uses four agents around a 2x2). `WorldError.AgentsShareCell` guard added to `World.create` / `World.ofScenario` (the vacation-chain algorithm relies on pre-tick one-agent-per-cell uniqueness). `Overlay.Obstructed of cell * occupant` derived in `frameOf`, rendered in ASCII/SVG. Two corpus entries (`follow-chain`, `swap-standoff`), one golden pair (`swap-standoff-tick-001.*`). `Canonical.FormatVersion` stays 2, no `AgentState`/`WorldState` field, no existing hash re-pin (`-- corpus` / `-- fixture` byte-identical before and after, with and without `--regenerate`). Tests `184 -> 194` (+5 `SimulationTests`, +1 `DeterminismPropertyTests` property at 200 cases, +2 `DiagnosticsTests`, +2 `CorpusTests` theory cases). One pre-existing `SimulationTests` fact (`an agent routes around an impassable wall`) had its wall repositioned — its detour ran the moving agent through two parked friendly agents, the exact defect fixed. | `ready -> review -> done`; `docs/11_BACKLOG.md` TASK-022 row + B-047 `ready -> done`; `PROJECT_STATE.yaml` `active_work.selected_task TASK-020 -> TASK-022`; `docs/04` sections 8 / 12.7 / 20, `docs/09` sections 2.2 / 2.3 / 2.4; "Green tests" pinned `184 -> 194` | yes (2026-09-06) | [detail](ledger/2026-09-06-TASK-022-cell-occupancy.md) |
| 2026-09-06 | TASK-023 | Continuous integration for the framework-neutral solution (backlog B-048). New `.github/workflows/ci.yml`: one job on `windows-latest`, triggered on `push` (any branch) and `pull_request` targeting `main`, `concurrency` group per ref with `cancel-in-progress`, `permissions: contents: read`. Steps in order: `actions/checkout` (SHA-pinned `3d3c42e5` / v7.0.1), `actions/setup-dotnet` (SHA-pinned `a98b5685` / v6.0.0, `global-json-file: global.json`), `dotnet restore CommandoWar.slnx` (cold cache — the pinned-restore check; no NuGet cache added), `dotnet build -c Release --no-restore` (0/0, enforced by the per-project `TreatWarningsAsErrors`; no global `-warnaserror`), `dotnet test -c Release --no-build`, `cwheadless -- corpus` (no `--regenerate`; exit 3 fails the job on any divergence from the committed `content/replays/*.md`), `cwheadless -- fixture` (CLI smoke — prints, does not self-check), and a `git status --porcelain` clean-tree check. `bench/` builds with the solution; benchmarks are not run. Client-compile / content-validation / package-smoke deferred to the first P4 client task; `packages.lock.json` / `--locked-mode` considered and deferred (source change). One-line Windows-x64 / .NET `10.0.303` environment annotation added to `content/replays/CORPUS.md`, `content/fixtures/SPIKE-FIXTURE.md`, and the "Shared fixture" block above. `docs/09` section 6 marks the framework-neutral matrix items realised by TASK-023, client rows still P4. No source / `.fsproj` / test / hash / `Canonical.FormatVersion` change; no ADR; no `docs/08` gate change. Verified locally from the repo root: `restore` 0, `build -c Release --no-restore` 0 (0 warn / 0 err), `test -c Release --no-build` 0 (`Passed: 194`), `-- corpus` 0 (7× PASS), `-- fixture` 0, `git status --porcelain` clean after the verbs. Negative test: perturbing one `content/replays/swap-standoff.md` tick row made `dotnet test` fail (`Failed: 1, Passed: 193`, the `swap-standoff` `CorpusTests` theory case, `Mismatch { FirstBadTick = 2L }`) and `-- corpus` exit `3` naming the first bad tick and the expected/actual hash; reverted clean. The green-run and red-run acceptance criteria on a real Actions runner could not be executed in this environment (no push) and are left for Dave to confirm by pushing branch `task-023-simulation-ci`. | `ready -> review -> done`; `docs/11_BACKLOG.md` TASK-023 row + B-048 `ready -> done`; `PROJECT_STATE.yaml` `active_work.selected_task TASK-022 -> TASK-023`, note, `updated`; `docs/09` section 6; `content/replays/CORPUS.md`, `content/fixtures/SPIKE-FIXTURE.md`; "Shared fixture" + "Green tests" pinned-facts notes; merged to `main` via PR #1 | yes (2026-09-06) | [detail](ledger/2026-09-06-TASK-023-simulation-ci.md) |
| 2026-09-06 | Reconcile 021/022/023 acceptances | TASK-021 (B-046), TASK-022 (B-047), TASK-023 (B-048) recorded as accepted by Dave: task `Status` lines, ledger `Review` blocks, `docs/11` rows, and the `docs/12` `Accepted` cells. TASK-023's `.github/workflows/ci.yml` was merged to `main` via PR #1 and its first `windows-latest` run was green. `PROJECT_STATE.yaml` `active_work` reset to nothing-selected with a clean current-state note. Confirmed no `docs/08` change needed (section 6 P3 Required work already lists B-044 / B-045; CI already lives in `docs/09` section 6). No source, no gate change. | TASK-021 / TASK-022 / TASK-023 status-change cells `-> done`; `docs/11` B-046 / B-047 / B-048 `-> done`; `PROJECT_STATE.yaml` `active_work.selected_task -> none`, `task_file -> null`, note + both `updated` lines refreshed | n/a (recording acceptances) | [detail](ledger/2026-09-06-reconcile-021-022-023-acceptances.md) |
| 2026-09-06 | TASK-024 drafted | Issue-tick / submission-tick semantics task drafted (backlog B-044, mandatory before G3). A decision-plus-minimal-implementation task in the TASK-020 shape: settle what `PlayerCommand.IssueTick` means (recommended `IssuedAtTick` — issue-time provenance and a future appraisal input — distinct from a `SubmitAtTick` delivery tick), reject future-dated commands (`IssuedInFuture`), do **not** add a staleness horizon at intake (appraisal's concern), keep replay tick and issue tick independent, and keep command identity a batch-local dedup key plus a replay-log invariant (`DuplicateCommandIdInLog`) — **not** `WorldState`, so `Canonical.FormatVersion` stays 2 and no hash re-pins. The legacy `.cwlog` collapses both ticks to its one field. No ADR. Task file + backlog + ledger only, not started, not selected. | `tasks/TASK-024-ISSUE-TICK-SEMANTICS.md` created (`ready`); `docs/11_BACKLOG.md` new TASK-024 row + B-044 `proposed -> ready` | pending | [detail](ledger/2026-09-06-TASK-024-drafted.md) |
| 2026-09-06 | TASK-025 drafted | Production replay-command serialisation task drafted (backlog B-045, mandatory before G3, depends on TASK-024 / B-044). Recommendation: a **separate** new versioned, lossless, hand-rolled replay-command format in `CommandoWar.Sim` (deterministic line-based text, no new package), leaving `.cwlog` frozen at v1 as legacy fixture-script. Adds a `parse`/`serialise` round-trip property, one committed new-format replay fixture carrying a full envelope (`.cwlog` cannot), and a `cwheadless replay` verb. The seven existing corpus entries and the spike fixture are untouched; corpus migration + the shared F# fixture builder are B-049. No `Canonical.FormatVersion` bump, no existing-hash re-pin, no ADR. B-045 kept `proposed` (not `ready`) while B-044 is unresolved, per section 1. Task file + backlog + ledger only. | `tasks/TASK-025-REPLAY-COMMAND-SERIALISATION.md` created (`proposed`, complete); `docs/11_BACKLOG.md` new TASK-025 row + B-045 dependency `B-014 -> B-014, B-044`, size `S -> M` | pending | [detail](ledger/2026-09-06-TASK-025-drafted.md) |
| 2026-09-06 | TASK-026 drafted | Observations and shared squad tactical knowledge task drafted (backlog B-015). First phase consumer of the `Sight` module: Perception (12.3) -> per-agent `AgentState.VisibleContacts` **derived cache** (excluded from `Canonical.encode`, the `Route` precedent) from `Sight.visible` within a `SightRange` cap, both sides, `ContactObserved` on a new sighting; Tactical knowledge (12.4) -> new **canonical** `WorldState.TacticalKnowledge` (contact id, last-known cell, `LastSeenTick`, decaying confidence, instant single-squad sharing, `ContactExpired` on decay-out). `Canonical.FormatVersion` **2 -> 3** with a full **behaviour-neutral** hash re-pin — every committed scenario is enemy-free so the store is empty at every checkpoint (the TASK-018 `Progress` precedent); tick/event counts unchanged for all seven entries. New `KnownContact` diagnostic overlay + committed golden, first enemy-bearing corpus entry, one tuning structure, a tactical-knowledge property. Symmetric raw visibility, friendly-squad-only shared knowledge. B-016 / B-017 / B-019 / B-022 and private beliefs explicitly out. Independent of TASK-024 / TASK-025. No ADR (ADR-0002 amendment governs the bump). Task file + backlog + ledger only, not started, not selected. | `tasks/TASK-026-PERCEPTION-AND-TACTICAL-KNOWLEDGE.md` created (`ready`); `docs/11_BACKLOG.md` new TASK-026 row + B-015 `proposed -> ready` | pending | [detail](ledger/2026-09-06-TASK-026-drafted.md) |
| 2026-09-07 | TASK-024 | Issue-tick / submission-tick semantics (backlog B-044, mandatory before G3). Implemented on the recommended resolutions: `PlayerCommand.IssueTick -> IssuedAtTick` (only that field renamed; `RecordedCommand.Tick` kept, doc comment expanded to pin it as the caller-owned delivery tick, independent of `IssuedAtTick` — renaming it too would have been ~15 sites of churn on plumbing TASK-025 reworks, and `.cwlog` already collapses the two). New `CommandRejection.IssueTickOutOfRange of issuedAtTick * tick` — `commandIntake` rejects `IssuedAtTick` outside `[0, s.Tick]` as a whole-command check ordered first; a stale (earlier-issued, later-delivered) command is accepted (following an outdated order is B-017 appraisal's call, no give-up horizon at intake). Command identity is not authoritative state: new `ReplayError.DuplicateCommandIdInLog of id * firstIndex * secondIndex` checked by `Replay.validate` (via `Array.groupBy` on `Command.Id`), `WorldState` gains no field, `Canonical.FormatVersion` stays 2. `CommandLogFile` doc updated (its one tick field maps to both `RecordedCommand.Tick` and `IssuedAtTick`); grammar and `CommandLogFile.Version` unchanged. `Diagnostics.eventMarker` + `Program.describeReplayError` gain one arm each. `194 -> 199` green (+4 `SimulationTests`, +1 `ReplayTests`); `dotnet build` `0/0`; `-- corpus` (7x PASS) / `-- fixture` (`0xAFA35198CC6BD8D4`, 33 events, format 2) byte-identical, no `--regenerate`; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR. `CorpusTests.fs` one-token field-rename; no other pre-existing fact modified. | `ready -> review -> done`; `docs/11_BACKLOG.md` TASK-024 row + B-044 `ready -> done`, B-045 `proposed -> ready`; `PROJECT_STATE.yaml` `active_work.selected_task none -> TASK-024 -> none`, note, both `updated` lines; `docs/04` sections 2 / 12.1 / 13 / 16, `docs/09` section 2.1; "Green tests" pinned `194 -> 199` | yes (2026-09-07) | [detail](ledger/2026-09-07-TASK-024-issue-tick-semantics.md) |
| 2026-09-07 | TASK-025 | Production replay-command serialisation (backlog B-045, mandatory before G3; depends on TASK-024). Both selection decisions confirmed with Dave: **deterministic line-based text** (not binary), and **header + `RecordedCommand[]`** (the initial `WorldState` stays a named scenario / builder reference, not serialised). New `src/CommandoWar.Sim/ReplaySerialisation.fs` — replay-command file format **v1**, hand-rolled `parse` / `serialise` over a `ReplayCommandFile` (version, seed, ticks, canonical, `ReplayMeta`, optional `initial-hash`, optional `checkpoint` lines, ordered `command` lines), a typed `ParseError` DU + `describeError`, `ofRecord` / `toReplayRecord` bridges to `ReplayRecord` + `Replay.run`. `FSharp.Core` only; integer parse/format forced culture-invariant. Unknown file-format version, canonical-format mismatch, and out-of-order command / checkpoint blocks are typed errors naming the line; no migration guessed. New `cwheadless replay-file <path>` verb (distinct from the legacy `replay` .cwlog verb): parse -> resolve scenario against `Corpus.all` -> `Replay.run` -> print per-tick hash table + ordered accepted commands; exit 2 on parse/validate failure, 3 on checkpoint divergence. New committed fixture `content/replays/envelope-full.{cwreplay,md}` — a 3-recipient `MoveTo` (agents 3/4/5), `Urgency = Immediate`, `RiskTolerance = Aggressive`, issued tick 1 / delivered tick 2, on the spike-fixture initial state, 24 ticks; `ReplayTests` cross-checks the file's `checkpoint` lines, the `.md` table, a pinned hash array, and a fresh `Replay.run`. `199 -> 206` green (+7 `ReplayTests`: round-trip property at `MaxTest = 200`, full-field-matrix fact, unknown-version / canonical-mismatch / out-of-order rejection, hand-written-envelope parse, committed-fixture cross-check). `dotnet build` `0/0`; `-- corpus` (7x PASS) / `-- fixture` (`0xAFA35198CC6BD8D4`, 33 events, format 2) byte-identical with and without `--regenerate`; `Canonical.FormatVersion` (2) / `CommandLogFile.Version` (1) / `Replay.FormatVersion` (1) / `CommandLog.Version` (1) all unchanged, no existing hash re-pinned; `src/CommandoWar.Sim` packages `FSharp.Core` only. `CommandLogFile.fs` doc-comment pointer only. No ADR. | `ready -> done`; `docs/11_BACKLOG.md` TASK-025 row + B-045 `ready -> done`, B-049 precondition note; `PROJECT_STATE.yaml` `active_work` + `updated`; `docs/04` sections 13 / 16, `docs/09` section 2.4; "Green tests" pinned `199 -> 206`; new `ReplaySerialisation.FormatVersion` pinned fact; merged to `main` | yes (2026-09-07) | [detail](ledger/2026-09-07-TASK-025-replay-command-serialisation.md) |
| 2026-09-07 | TASK-026 | Observations and shared squad tactical knowledge (backlog B-015; first phase consumer of the `Sight` module). Three central decisions confirmed with Dave before implementation: **full retained decaying store** (not the narrowed cut); **`PerceptionConfig` module literals** `SightRange = 10` (Chebyshev), `ConfidenceFull = 1000`, `ConfidenceBandDrop = 250`, `StaleAfter = 20`, `ExpireAfter = 60`; **symmetric visibility, friendly-only knowledge**. New leaf `src/CommandoWar.Sim/Perception.fs` (`PerceptionConfig`, `visibleContactsFor` / `sweep` / `mergeKnowledge`, pure/total/integer-only). Real `Perception` (12.3) and `TacticalKnowledge` (12.4) phases in `Simulation.fs`. `AgentState.VisibleContacts: AgentId[]` — **non-canonical derived cache** (the `Route` precedent, excluded from `Canonical.encode`). `WorldState.TacticalKnowledge: Contact[]` — **genuine canonical state** (`Contact` carries `LastSeenTick` + decaying `Confidence`); `Canonical.FormatVersion` **2 -> 3**, tactical-knowledge section in `encode` / `topLevelSections`. New `ContactObserved` / `ContactExpired` events; new `Overlay.KnownContact` derived in both `Diagnostics.frame` and `frameOf`, rendered in ASCII (text) + SVG (purple dashed `?<id>` ring). **Behaviour-neutral re-pin**: fixture + all seven `.cwlog` entries + `envelope-full.{cwreplay,md}` re-pinned (every committed scenario enemy-free; tick counts and event counts byte-identical — table in the detail file); `DemoScenario` (the one committed scenario with a hostile) legitimately gains a `KnownContact` overlay + `contact-observed` markers in `demo.html`. New enemy-bearing corpus entry `perception-contact` (14 ticks, 12 events) + golden `perception-contact-tick-005.*`. `206 -> 216` green (+5 `SimulationTests`, +1 `DeterminismPropertyTests` property at 200 cases, +1 `CanonicalHashTests`, +2 `DiagnosticsTests`, +1 `CorpusTests` theory case). `dotnet build` `0/0`; `-- corpus` 8/8 PASS, `-- fixture` format 3, **33 events unchanged**; `-- replay-file envelope-full` checkpoints OK; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR (ADR-0002 amendment governs the bump). | `ready -> review -> done`; `docs/11_BACKLOG.md` TASK-026 row + B-015 `ready -> done`, B-016 / B-017 / B-019 / B-022 unblocked note; `PROJECT_STATE.yaml` `active_work` + `updated`; `docs/04` sections 10 / 11 / 12.3 / 12.4 / 14 / 17 / 20, `docs/05` section 3, `docs/09` sections 2.2 / 2.3 / 2.4; "Pinned facts" `Canonical.FormatVersion` `2 -> 3`, shared-fixture hashes, "Green tests" `206 -> 216`; accepted by Dave and merged to `main` (`--no-ff`, branch deleted; not pushed) | yes (2026-09-07) | [detail](ledger/2026-09-07-TASK-026-perception-and-tactical-knowledge.md) |
| 2026-09-07 | TASK-027 | Communication constraints and order delivery (realises the communication-failure half of backlog B-016; range/delay/jamming/aging split to a new B-016b). Five central decisions confirmed with Dave before the phase bodies: **A** static per-agent `CommunicationAvailable` reachability check, no PRNG, flag excluded from `Canonical.encode` like `Terrain`; **B** the pending order is transient (a `StepState`-local list, not canonical) — `Canonical.FormatVersion` stays `3`, zero hash re-pin; **C** report aging → B-016b; **D** `OrderUndelivered` only, no `OrderDelivered` success event (with zero delay it duplicates `CommandAccepted`); **E** no ADR. `Simulation.commandIntake` reworked to record an accepted `(command, recipient, target)` as a pending order (no `Destination` write, no agent-array copy); new `Simulation.communication` phase in slot 2 delivers each pending order (writes `Destination`) to a recipient with `CommunicationAvailable = true` or emits `OrderUndelivered(command, recipient, UnableToCommunicate)` and drops it. New `AgentState.CommunicationAvailable: bool` (default `true`; `Agent.create` default, `World.ofScenario` override) — static, **not** in `writeAgent` / `Canonical.encode`. New `RawDeployment` / `Deployment.CommunicationAvailable` + `toDeployments` carry-through, no new `ScenarioError`. New `EventBody.OrderUndelivered` + `DeliveryFailure` DU; `DomainEvent` ordering doc comment updated (Communication events after command-intake, before `ContactObserved`, ascending `(recipient, command)`). New `Overlay.UndeliveredOrder` (derived in `frameOf` only) + `AgentMarker.CommunicationAvailable` (rendered only when `false` — the `Progress`-omitted-at-0 precedent); ASCII + SVG branches, FS0025 sites fixed with intended branches (listed in the detail file). New `lost-comms` corpus entry (8×8, comms-blacked-out friendly ordered to move, 4 ticks, 2 events: `CommandAccepted` + `OrderUndelivered`; the "Lost communication" scenario) + golden `lost-comms-tick-001.*`. **Zero re-pin**: `-- corpus` 9/9 PASS and `-- fixture` (format 3, `0x50BFA007EDFC42FE` / `0xD9D6EC3DDC1D602F`, 33 events) byte-identical before and after with and without `--regenerate`; `git diff content/replays` shows only the new entry + the `CORPUS.md` row; no `content/diagnostics/` golden moved; `-- replay-file envelope-full` checkpoints OK at `canonical 3`. `216 → 228` green. `dotnet build` `0/0`; `src/CommandoWar.Sim` packages `FSharp.Core` only; source scan clean. No ADR. | `ready -> review -> done`; `docs/11_BACKLOG.md` TASK-027 row + B-016 `proposed -> done`, new B-016b row, B-017 unblocked note; `PROJECT_STATE.yaml` `active_work` + `updated`; `docs/03` section 7, `docs/04` sections 11 / 12.1 / 12.2 / 14 / 17 / 20, `docs/05` section 5, `docs/09` sections 2.1 / 2.2 / 2.3; "Green tests" `216 -> 228` (no "Pinned facts" `Canonical.FormatVersion` change — stays `3`); accepted by Dave and merged to `main` (`--no-ff`, branch deleted; not pushed) 2026-09-08 | yes (2026-09-08) | [detail](ledger/2026-09-07-TASK-027-communication-and-order-delivery.md) |
| 2026-09-08 | TASK-027 accepted | Recorded acceptance of TASK-027 (backlog B-016) and merged to `main`. `Accept TASK-027; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-027 task file + ledger Review block) then `Merge TASK-027 ... (--no-ff)`; branch `task-027-communication-and-order-delivery` deleted; not pushed. `main` build 0/0, 228 green, `-- corpus` 9/9, `-- fixture` 33 events before branching TASK-028. | `tasks/TASK-027-*.md` Status `review -> done`; `docs/ledger/2026-09-07-TASK-027-*.md` Review `Accepted: yes (2026-09-08)`; `docs/11` TASK-027 + B-016 rows; `docs/12` TASK-027 row + this row | yes (2026-09-08) | [detail](ledger/2026-09-07-TASK-027-communication-and-order-delivery.md) |
| 2026-09-08 | TASK-028 | Staged order appraisal and typed reasons (backlog B-017; the G3 keystone). Eight central decisions confirmed with Dave before the phase bodies (A: Communication writes a new canonical `AgentState.Order` + resets `AgentState.Disposition` instead of `Destination`; the new Appraisal phase over a new `Appraisal` leaf reads `Order` + `TacticalKnowledge` + terrain + a static `AgentState.Discipline`, runs stages 1-4, produces an `OrderDisposition`, emits `OrderAppraised`, and on `Accepted` writes `Destination`; `Canonical.FormatVersion` 3 -> 4. B: `Discipline` static, EXCLUDED from `Canonical.encode` (the `CommunicationAvailable` precedent). C: stage 1 upstream; stage 2 `Pathfinding` -> `Unable(NoKnownRoute)`; stage 3 route exposure to known threats only; stage 4 exposure vs a `Discipline`/`RiskTolerance`/`Urgency` threshold -> `Accepted`/`Refused(RouteTooExposed)`. `OrderDisposition` / `DecisionReason` subset; `Adapted`/`Delayed` no case. D: `AppraisalConfig` integer literals, no PRNG, hysteresis deferred. E: "new order received" trigger only. F: `OrderAppraised of agent * command * disposition`. G: no ADR. H: new `exposed-approach` corpus entry — two friendlies of `Discipline` 1 and 6 ordered along the same exposed approach past a known hostile, one `Refused` one `Accepted` (the `docs/07` section 9 criterion 2 G3 evidence); `blocked-goal` repurposed as the appraisal-`Unable(NoKnownRoute)` case). New `AgentState.Order` / `.Disposition` / `.Discipline`; new `Appraisal.fs` leaf; `PlayerIntent` / `Urgency` / `RiskTolerance` moved `Commands.fs -> Domain.fs`; new `OrderAppraised` event; new `Overlay.OrderAppraisal` (derived in `frame` and `frameOf`); new `RawDeployment` / `Deployment.Discipline` + `ScenarioError.NegativeDiscipline`. `Canonical.FormatVersion` **3 -> 4**: full re-pin of the fixture + all ten `content/replays/*.md` + `envelope-full.{cwreplay,md}` + every `content/diagnostics` golden — **tick counts unchanged on all ten entries** (`git diff` shows no `Tick count` line moved), event counts move by one `OrderAppraised` per order (`blocked-goal` / `lost-comms` unchanged at 2; old/new hash + event table in the detail file); `demo.html` legitimately gains appraisal markers. New `exposed-approach` corpus entry (12 ticks, 19 events) + golden; new `blocked-goal-tick-001` golden. `228 -> 242` green (+9 `SimulationTests`, +3 `DiagnosticsTests` (hand-built `OrderAppraisal` + `exposed-approach` and `blocked-goal` goldens), +1 `DeterminismPropertyTests` property 8 at 200 cases, +1 `CorpusTests` `[<Theory>]` case for `exposed-approach`). `dotnet build` 0/0; `-- corpus` 10/10; `-- fixture` format 4, 34 events; `-- replay-file envelope-full` OK at canonical 4; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR (ADR-0002 amendment governs the bump). | `ready -> review -> done`; `docs/11` new TASK-028 row + B-017 `-> done`, B-018 / B-021 / B-023 unblocked notes; `PROJECT_STATE.yaml` `active_work`; `docs/03` section 7, `docs/04` sections 2 / 11 / 12.2 / 12.5 / 12.6 / 13 / 14 / 17 / 20, `docs/05` sections 5 / 6 / 7 / 8 / 13 / 14 / 15 / 16, `docs/07` section 8, `docs/09` sections 2.1 / 2.2 / 2.3; "Pinned facts" `Canonical.FormatVersion` `3 -> 4`, shared-fixture hashes, "Green tests" `228 -> 242` | yes (2026-09-08) | [detail](ledger/2026-09-08-TASK-028-order-appraisal-and-typed-reasons.md) |
| 2026-09-08 | TASK-028 accepted | Recorded acceptance of TASK-028 (backlog B-017) and merged to `main`. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 242`, `-- corpus` 10/10, `-- fixture` format 4 / 34 events, `-- replay-file envelope-full` OK at canonical 4, `corpus --regenerate` a byte-identical zero diff, `git status` clean, `src/CommandoWar.Sim` packages `FSharp.Core` only, source scan clean. `Accept TASK-028; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-028 task file + ledger Review block) then `Merge TASK-028 ... (--no-ff)`; branch `task-028-order-appraisal` deleted; not pushed. | `tasks/TASK-028-*.md` Status `review -> done`; `docs/ledger/2026-09-08-TASK-028-*.md` Review `Accepted: yes (2026-09-08)`; `docs/11` TASK-028 + B-017 rows; `docs/12` TASK-028 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-08) | [detail](ledger/2026-09-08-TASK-028-order-appraisal-and-typed-reasons.md) |
| 2026-09-08 | TASK-029 | Godot appraisal-divergence demo (a read-only, corpus-scoped slice of backlog B-029 pulled forward as P3 decision-support; B-029 proper stays P4). Central decisions A–F confirmed with Dave 2026-09-08 before the phase bodies (A: build on the existing `src/CommandoWar.Client.Godot` spike skeleton, all non-trivial logic in a new F# helper; B: corpus reuse — `Corpus.all` → `exposed-approach`, `Corpus.loadLog` against a `res://`-relative `content/replays`, fail loud if absent; C: 13-frame `HSlider` + `Left`/`Right`, default index 1; D: mirror the `DiagnosticRender.Svg` overlay vocabulary / colours / glyphs; E: pure-helper xUnit + headless-Godot-smoke test boundary; F: no ADR). New framework-neutral F# helper `src/CommandoWar.Headless/AppraisalDemo.fs` (`loadExposedApproachFrames`, `dispositionText` / `dispositionTone` matching the committed golden vocabulary, `frameView` / `loadFrameViews` — a flat `[<CLIMutable>]` per-tick view model, no F# `option` / DU / tuple crossing to C#). New Godot scene `scenes/AppraisalDemo.tscn` + `src/AppraisalDemoScene.cs` (a `Node2D` renderer: grid, agents by side + dashed destination lines, the `?2` `KnownContact` ring, the accepted agent's `PlannedPath` polyline + green start disc + goal rect, the `OrderAppraisal` exposed-cell tint + `A`/`R`/`U` glyph, a per-agent `DecisionReason` panel, a tick/hash/format/draws HUD, `--selfcheck` / `--screenshot` modes); `project.godot` `run/main_scene` repointed; the greybox spike files untouched. `src/CommandoWar.Client.Godot.csproj` + `.slnx` gain one `CommandoWar.Headless` reference (still a `Client -> Sim` edge; the simulation references neither); no new NuGet / Godot dependency. **Scoped deviation** from ADR-0004's "F# client-core / no logic in C#" rule for this disposable P3 demo — recorded here + the task file, **not** a precedent for the P4 client tasks (B-026 / B-027 / B-029). No `CommandoWar.Sim` change; no new event / `Overlay` case / `Canonical.FormatVersion` bump / golden re-pin. `242 -> 244` green (+2 `DiagnosticsTests`: `AppraisalDemo.dispositionText` matches the golden vocabulary; `loadExposedApproachFrames` reproduces the `exposed-approach` tick-1 hash `0xB03F8419E55F3592` + the two tick-1 dispositions + the flat view model). `dotnet build CommandoWar.slnx -c Release` 0/0; `dotnet build src/CommandoWar.Client.Godot/...slnx -c Debug` 0/0; `-- corpus` 10/10 and `-- fixture` (format 4, 34 events) byte-identical, `git diff content/` empty; headless Godot `--selfcheck` prints `agent 0: refused route-too-exposed threat-agent-2` / `agent 1: accepted` / `hash 0xB03F8419E55F3592`, `MATCH`, exit 0; committed screenshot `docs/evidence/task-029-appraisal-demo.png` (windowed capture). No pinned-fact change (`Canonical.FormatVersion` stays 4; "Green tests" `242 -> 244`). No ADR. | `draft -> review -> done`; `docs/11` new TASK-029 row + B-029 annotation; `PROJECT_STATE.yaml` `active_work`; `docs/06` section 11 realisation note; `src/CommandoWar.Client.Godot/README.md` launch note | yes (2026-09-12) | [detail](ledger/2026-09-08-TASK-029-godot-appraisal-demo.md) |
| 2026-09-12 | TASK-029 accepted | Recorded acceptance of TASK-029 (a read-only, corpus-scoped slice of backlog B-029) and merged to `main`. Dave launched the scene interactively and confirmed the divergence reads right. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 244`, `-- corpus` 10/10, `-- fixture` format 4 / 34 events, headless Godot `--selfcheck` MATCH `0xB03F8419E55F3592`, `git status` clean. `Accept TASK-029; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-029 task file + ledger Review block) then `Merge TASK-029 ... (--no-ff)`; branch `task-029-godot-appraisal-demo` deleted; not pushed. | `tasks/TASK-029-*.md` Status `review -> done`; `docs/ledger/2026-09-08-TASK-029-*.md` Review `Accepted: yes (2026-09-12)`; `docs/11` TASK-029 row; `docs/12` TASK-029 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-12) | [detail](ledger/2026-09-08-TASK-029-godot-appraisal-demo.md) |
| 2026-09-13 | TASK-030 | Commitment and finite move/hold executor (backlog B-018). Five central decisions confirmed with Dave before the phase bodies (A: `Commitment` DU subset `Holding \| Moving of MoveCommitment` only — no `PlayerIntent` or world state exists yet for `Suppressing`/`Assaulting`/`Withdrawing`. B: `Commitment` is a pure derived value over the already-canonical `Order`/`Disposition`/`Destination` — the `AgentState.Route` precedent — **not** new canonical state, so **no `Canonical.FormatVersion` bump and no hash re-pin anywhere**; this revised a heavier framing discussed the day before. C: stage-5 safer adaptation deferred — needs a separate exposure-aware route-search algorithm. D: the interrupt table realises exactly one of `docs/05` section 11's seven priorities (a new order supersedes the current commitment); no separate `Interrupt` type or `Superseded` outcome case needed, since the old commitment simply stops being derived. E: realised as its own `commitmentAndLocalAction` phase in the existing `Phases.order` slot, relocating one housekeeping branch out of `appraisal`. No ADR). New leaf `src/CommandoWar.Sim/Commitment.fs` (`MoveCommitment`, `Commitment`, `Commitment.ofAgent` — pure, total). New `commitmentAndLocalAction` phase (12.6) in `Simulation.fs`; `appraisal`'s fulfilled-order housekeeping branch relocated there unchanged. New events `CommitmentEstablished of agent * command * target` / `CommitmentCompleted of agent * command * at`. New `Overlay.AgentCommitment` (renamed from the task file's `Overlay.Commitment` during implementation to avoid a case/type name collision), derived in both `frame` and `frameOf`, rendered in `Ascii` + `Svg`. New corpus entry `reissued-order` (one friendly, order reissued mid-route, proving supersession end-to-end) + golden. Every existing corpus entry, the fixture, and `envelope-full` gained `CommitmentEstablished`/`CommitmentCompleted` events in their trace (honest surface of realising the phase) with **zero state-hash change anywhere** — verified by diffing every corpus `.md`, `envelope-full.md`, and every diagnostics golden for hash-line changes (none found, only "Domain events" counts and new overlay/event text); full before/after event-count table in the ledger detail file. Existing-test ripple (all behaviour-neutral): three golden-pinned "shared fixture" facts' hardcoded event-count literal `34 -> 36`; two `PathfindingTests`/`SightTests` "overlay-absent" facts narrowed from `Assert.DoesNotContain "overlays:"` to checking the specific ray/path text is absent (the section itself is no longer ever empty); their `losFrameWithRays`/`pathFrameWithRoutes` test helpers changed from replacing `frame.Overlays` to appending, matching the real `cwheadless render --los`/`--path` CLI behaviour; the TASK-029 `AppraisalDemo` demo (predates TASK-030, does not render commitments) gained an `unhandled`-bucket arm and its test's expected count corrected to 3 (2 friendlies + 1 hostile, not 2). `244 -> 254` green (+8 `SimulationTests`, +1 `DeterminismPropertyTests` property 9 at 200 cases reusing property 8's generator, +1 `DiagnosticsTests` golden fact). `dotnet build` 0/0; `-- corpus` 11/11 (`--regenerate` idempotent); `-- fixture` format 4, hashes unchanged, 34 -> 36 events; `-- replay-file envelope-full` OK at canonical 4, hashes unchanged, 75 -> 78 events; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR (Decision G). | `draft -> ready -> review -> done`; `docs/11_BACKLOG.md` new TASK-030 row + B-018 `proposed -> done`; `PROJECT_STATE.yaml` `active_work`; `docs/04` sections 11 / 12.5 / 12.6 / 14 / 17, `docs/05` sections 9 / 10 / 11, `docs/07` section 8; "Green tests" pinned `244 -> 254` (no `Canonical.FormatVersion` line change — stays 4) | yes (2026-09-13) | [detail](ledger/2026-09-13-TASK-030-commitment-and-finite-executor.md) |
| 2026-09-13 | TASK-030 accepted | Recorded acceptance of TASK-030 (backlog B-018) and merged to `main`. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 254`, `-- corpus` 11/11, `-- fixture` format 4 / hashes unchanged / 36 events, `git status` clean. `Accept TASK-030; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-030 task file + ledger Review block) then `Merge TASK-030 ... (--no-ff)`; branch `task-030-commitment-and-finite-executor` deleted; not pushed. | `tasks/TASK-030-*.md` Status `review -> done`; `docs/ledger/2026-09-13-TASK-030-*.md` Review `Accepted: yes (2026-09-13)`; `docs/11` TASK-030 + B-018 rows; `docs/12` TASK-030 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-13) | [detail](ledger/2026-09-13-TASK-030-commitment-and-finite-executor.md) |
| 2026-09-13 | TASK-031 | Basic hitscan combat and directional cover effects (backlog B-019). Nine central decisions confirmed with Dave before the phase bodies (A: automatic symmetric engagement, candidates from a shooter's own `VisibleContacts` only (risk R-023), line of fire re-verified fresh at combat time, nearest qualifying target wins ties by ascending id. B: no ammunition/weapon readiness — fires every valid tick; future direction (not built) is a cooldown-or-reload model with resupply TBD. C: no wound/death consequence — a hit only emits an event; future direction (not built) is an XCOM-style critical/bleed-out timer. D: a deterministic `0..1000`-scale hit chance (range + directional `Terrain.cover`), one `RandomStream.next` draw per engaging agent — **the simulation's first real gameplay PRNG consumer**; `WorldState.Random` was already canonical, so no `Canonical.FormatVersion` bump. E: reuse `Appraisal.attackDirection` (made public) rather than duplicate it. F: one event `ShotFired of shooter * target * hit`. G: new leaf `Combat.fs`. H: one new corpus entry (`open-engagement`) proves the phase wiring; cover/range monotonicity and target selection are proved directly on the pure `Combat.hitChance`/`Combat.chooseTarget` functions. I: no ADR). **Correction found during implementation (Decision H):** the original claim that only `open-engagement` would be newly pinned was wrong — `perception-contact` and `exposed-approach` (TASK-026/028's enemy-bearing entries) each legitimately bring an agent into weapon range and line of sight of the opposing side partway through their run, so real combat now fires there too (genuine, correct new behaviour, not a bug); **tick counts unchanged on both**, `exposed-approach` tick 1 (the TASK-029 Godot demo's pinned hash `0xB03F8419E55F3592`) unaffected; `demo.html` similarly gains combat from tick 8. Three existing tests whose "no PRNG draw" assumption predated Combat were corrected (two `DeterminismPropertyTests` properties 8/9 lost that assertion and property 8 was renamed; one `SimulationTests` fact's hand-built world sits exactly at `WeaponRange`, renamed and its assertion loosened to "both runs draw the same amount" rather than "draws zero"). `254 -> 265` green (+1 `CorpusTests` theory case, +8 `SimulationTests`, +1 `DeterminismPropertyTests` property 10 at 200 cases, +1 `DiagnosticsTests` golden fact). `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` idempotent); `-- fixture` byte-identical; `-- replay-file envelope-full` checkpoints unchanged; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR. | `draft -> ready -> review -> done`; `docs/11_BACKLOG.md` new TASK-031 row + B-019 `proposed -> done`; `PROJECT_STATE.yaml` `active_work`; `docs/04` sections 5 / 12.8, `docs/05` section 12, `docs/07` section 5, `docs/08` section 6 (added the missing "implement basic hitscan combat" bullet); "Green tests" pinned `254 -> 265` (no `Canonical.FormatVersion` line change — stays 4) | yes (2026-09-13) | [detail](ledger/2026-09-13-TASK-031-hitscan-combat.md) |
| 2026-09-13 | TASK-031 accepted | Recorded acceptance of TASK-031 (backlog B-019) and merged to `main`. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 265`, `-- corpus` 12/12, `-- fixture` byte-identical, `-- replay-file envelope-full` checkpoints unchanged, `git status` clean, `dotnet list src/CommandoWar.Sim package --include-transitive` `FSharp.Core` only. `Accept TASK-031; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-031 task file + ledger Review block) then `Merge TASK-031 ... (--no-ff)`; branch `task-031-hitscan-combat` deleted; not pushed. | `tasks/TASK-031-*.md` Status `review -> done`; `docs/ledger/2026-09-13-TASK-031-*.md` Review `Accepted: yes (2026-09-13)`; `docs/11` TASK-031 + B-019 rows; `docs/12` TASK-031 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-13) | [detail](ledger/2026-09-13-TASK-031-hitscan-combat.md) |
| 2026-09-14 | TASK-032 | Suppression and exposure model (backlog B-020). Eight central decisions confirmed with Dave before the phase bodies (A: mechanic only — no wiring into Appraisal's resolve threshold, no reappraisal trigger, no movement/executor effect, all deliberately deferred to B-021; confirmed directly via a scope question before drafting. B: suppression created in Combat, independent of a hit, mitigated by the same directional `Terrain.cover` geometry as hit chance — "the exposure model". C: decayed every tick in the previously no-op State consequences phase, unconditionally, floored at 0; Combat runs first, so a same-tick hit's gain and that tick's decay both apply. D: new leaf `Suppression.fs` (`SuppressionConfig`, `Suppression.gain`/`.raise`/`.decay`, pure); `Combat.fs` itself unchanged. E: `AgentState.Suppression` is genuine canonical state — `Canonical.FormatVersion` 4 -> 5, the `Order`/`Disposition` precedent, not `Discipline`'s; defaults to 0, no scenario-authored override. F: no new event (a rise derives from the already-emitted `ShotFired`, decay is silent); new sparse `Overlay.AgentSuppression`, one per agent with `Suppression > 0`. G: no new corpus entry — `open-engagement`'s agents are already in range/LOS from tick 1, so it naturally shows a real value; decay/clamping proved on the pure functions. H: no ADR). **Correction found during implementation (Decisions E/G wording):** the task file's acceptance criteria originally said a hit raises `Suppression` by exactly `Suppression.gain` — that omitted the same tick's unconditional decay (Decision C), so the actually observable value is `Suppression.gain(...) |> Suppression.raise 0 |> Suppression.decay`; every test was written against the corrected behaviour from the start, only the prose needed fixing. `Canonical.FormatVersion` **4 -> 5**: full re-pin of the fixture + all twelve `content/replays/*.md` + `envelope-full.{cwreplay,md}` + every `content/diagnostics` golden — **tick counts and event counts unchanged everywhere** (no new event type); `open-engagement`, `perception-contact`, and `exposed-approach` additionally carry genuine new non-zero `Suppression` values from the tick combat first fires (TASK-031's pinned ticks); the TASK-029 Godot demo's `--selfcheck` pinned `exposed-approach` tick-1 hash moved too (`0xB03F8419E55F3592` -> `0x2FA6E43B32599EE5`) and was updated in `AppraisalDemoScene.cs` / `README.md` (not originally listed). `265 -> 274` green (+8 `SimulationTests`, +1 `DeterminismPropertyTests` property 11 at 200 cases reusing `appraisalCaseGen` unchanged; the existing combat-determinism `SimulationTests` fact and the `open-engagement` `DiagnosticsTests` fact both extended in place, not new). `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` idempotent, confirmed via `md5sum`); `-- fixture` format 5, 36 events unchanged; `-- replay-file envelope-full` OK at canonical 5, 78 events unchanged; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR (Decision H). | `draft -> ready -> review -> done`; `docs/11_BACKLOG.md` new TASK-032 row + B-020 `proposed -> done`; `PROJECT_STATE.yaml` `active_work`; `docs/04` sections 11 / 12.8 / 12.9 / 17, `docs/05` section 8, `docs/07` section 5 / section 8; "Green tests" pinned `265 -> 274` (`Canonical.FormatVersion` line `4 -> 5`) | yes (2026-09-14) | [detail](ledger/2026-09-14-TASK-032-suppression-and-exposure.md) |
| 2026-09-14 | TASK-032 accepted | Recorded acceptance of TASK-032 (backlog B-020) and merged to `main`. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 274`, `-- corpus` 12/12, `-- fixture` format 5 / 36 events unchanged, `-- replay-file envelope-full` OK at canonical 5 / 78 events unchanged, `git status` clean, `dotnet list src/CommandoWar.Sim package --include-transitive` `FSharp.Core` only. `Accept TASK-032; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-032 task file + ledger Review block) then `Merge TASK-032 ... (--no-ff)`; branch `task-032-suppression-and-exposure` deleted; not pushed. | `tasks/TASK-032-*.md` Status `review -> done`; `docs/ledger/2026-09-14-TASK-032-*.md` Review `Accepted: yes (2026-09-14)`; `docs/11` TASK-032 + B-020 rows; `docs/12` TASK-032 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-14) | [detail](ledger/2026-09-14-TASK-032-suppression-and-exposure.md) |
| 2026-09-14 | TASK-033 | Stress and bounded reappraisal (backlog B-021, partial). Nine central decisions confirmed with Dave before the phase bodies (A: scope cut to Stress + suppression-band + knowledge-change only, confirmed directly via a scope question before drafting — exposure-band / wounded / support / leadership triggers and dynamic trust deferred, needing systems that don't exist (B-030/B-031) or a materially larger cut. B: Stress sourced from `VisibleContacts` only (the one `docs/05` section 8 source with a system behind it), raised and decayed together in State consequences. C: `Discipline` stays static and non-canonical, correcting B-021's literal backlog wording — it never mutates in this cut. D: `resolveThreshold` gains a continuous Stress drag (`StressDivisor = 25`) and a discrete SuppressionBand penalty (`SuppressionBandPenalty = 30`), the whole threshold floored at 0 so a fully unexposed route is never refused for stress/suppression alone. E: new `AgentState.SuppressionBand` hysteresis latch (`Enter = 500` / `Exit = 300`), computed inside the Appraisal phase (no new `Phases.order` slot). F: knowledge-change trigger fires globally on any `ContactObserved`/`ContactExpired`, not filtered per-route (the R-023 precedent). G: a fulfilled order excluded from both triggers. H: no new event type, no new corpus entry — proof via `SimulationTests` phase-level facts plus pure-function facts. I: no ADR). **Correction found only once the phase body was written (Decision G):** resetting a fulfilled order's `Disposition` would re-run `Appraisal.appraise`'s `fromCell = target` short-circuit, re-write `Destination`, and silently defeat `commitmentAndLocalAction`'s completion clear — both triggers now explicitly exclude it. `Canonical.FormatVersion` **5 -> 6**: full re-pin of the fixture + all twelve `content/replays/*.md` + `envelope-full.{cwreplay,md}` + every `content/diagnostics` golden — **tick counts and event counts unchanged everywhere** (no new event type, and both triggers only ever flip an outcome); `exposed-approach`, `open-engagement`, and `perception-contact` additionally carry genuine new non-zero `Stress` values from the tick each pair first sees each other; the TASK-029 Godot demo's `--selfcheck` pinned `exposed-approach` tick-1 hash moved a third time (`0x2FA6E43B32599EE5` -> `0x2066BC1FAF990E4A`) and was updated in `AppraisalDemoScene.cs` / `README.md`; the disposable `AppraisalDemo` `unhandled`-overlay `DiagnosticsTests` fact's expected count moved `3 -> 6` (genuine new behaviour). `274 -> 285` green (+10 `SimulationTests`, +1 `DeterminismPropertyTests` property 12 at 200 cases reusing `appraisalCaseGen` unchanged; the combat-determinism fact extended in place). `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` idempotent, confirmed via `md5sum`); `-- fixture` format 6, 36 events unchanged; `-- replay-file envelope-full` OK at canonical 6, 78 events unchanged; `src/CommandoWar.Sim` packages `FSharp.Core` only. No ADR (Decision I). | `draft -> ready -> review -> done`; `docs/11_BACKLOG.md` new TASK-033 row + B-021 `proposed -> review` (stays `review`, deliberately partial — see Decision A); `PROJECT_STATE.yaml` `active_work`; `docs/04` sections 11 / 12.5 / 12.9, `docs/05` sections 5 / 8 / 14 / 15 / 16, `docs/07` sections 5 / 8; "Green tests" pinned `274 -> 285` (`Canonical.FormatVersion` line `5 -> 6`) | yes (2026-09-15) | [detail](ledger/2026-09-14-TASK-033-stress-and-bounded-reappraisal.md) |
| 2026-09-15 | TASK-033 accepted | Recorded acceptance of TASK-033 (backlog B-021, partial) and merged to `main`. The two commits (originally on `main` directly, unpushed) were first moved onto branch `task-033-stress-and-bounded-reappraisal` for consistency with the established per-task branch workflow. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 285`, `-- corpus` 12/12, `-- fixture` format 6 / 36 events unchanged, `-- replay-file envelope-full` OK at canonical 6 / 78 events unchanged, `git status` clean, `dotnet list src/CommandoWar.Sim/CommandoWar.Sim.fsproj package --include-transitive` `FSharp.Core` only. `Accept TASK-033; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-033 task file + ledger Review block) then `Merge TASK-033 ... (--no-ff)`; branch `task-033-stress-and-bounded-reappraisal` deleted; not pushed. B-021 stays `review`, not `done` — exposure-band, wounded, support, and leadership reappraisal triggers, and dynamic trust, remain open, needing systems this task deliberately did not build (B-030/B-031, or a materially larger cut). | `tasks/TASK-033-*.md` Status `review -> done`; `docs/ledger/2026-09-14-TASK-033-*.md` Review `Accepted: yes (2026-09-15)`; `docs/11` TASK-033 + B-021 rows; `docs/12` TASK-033 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-15) | [detail](ledger/2026-09-14-TASK-033-stress-and-bounded-reappraisal.md) |
| 2026-09-15 | TASK-034 drafted | Hostile tactical picture and observed-only targeting proof (backlog B-022, partial; both dependencies B-015/TASK-026 and B-019/TASK-031 done, so fully unblocked). Of `docs/05` section 12's six enemy-doctrine bullets, two are already real without new work ("hold assigned area" — true by omission for an unordered agent; "engage visible targets" — `Simulation.combat` is symmetric, TASK-031) and three need systems that do not exist (`Suppress` order / B-030; a reactive seek-cover movement decision with no design yet; scripted fall-back + `Withdraw` semantics). The top-level scope cut was confirmed with Dave directly (`AskUserQuestion`, three options) before drafting: build only the remaining bullet ("observe and report" — a new `WorldState.HostileTacticalKnowledge: Contact[]`, symmetric to the friendly store, built by calling the already side-agnostic `Perception.mergeKnowledge` a second time filtered to `Side = Hostile`) plus a proof, not a build, that `docs/09` section 8's "enemy does not target an unobserved player position" already holds (`Simulation.combat` draws shooter candidates only from same-tick `VisibleContacts`, TASK-031 Decision A — strictly tighter than any tactical-knowledge store). New `Overlay.HostileKnownContact` diagnostic case (the `AgentStress`/`AgentSuppression` precedent, not a reuse of `KnownContact`). `Canonical.FormatVersion` **6 -> 7** proposed, full re-pin — **not** behaviour-neutral: `open-engagement`, `perception-contact`, and `exposed-approach` will legitimately gain new non-zero `HostileTacticalKnowledge` values. No new corpus entry, no new event type, no ADR proposed. B-022 stays `review` after this task, not `done` — the three deferred bullets remain open (the B-021/TASK-033 precedent). Decisions B–F are proposed in the task file, not yet confirmed line-by-line the way TASK-032/033's were — that confirmation, and selecting the task, are separate deliberate steps before implementation begins. | `tasks/TASK-034-*.md` created (`proposed -> ready`); `docs/11_BACKLOG.md` new TASK-034 row + B-022 `proposed -> ready`; `PROJECT_STATE.yaml active_work` stays `none` (drafted, unselected) | pending | [detail](ledger/2026-09-15-TASK-034-drafted.md) |
| 2026-09-15 | TASK-034 | Hostile tactical picture and observed-only targeting proof (backlog B-022, partial). New `WorldState.HostileTacticalKnowledge: Contact[]`, symmetric to the friendly `TacticalKnowledge`, built by the `tacticalKnowledge` phase calling the side-agnostic `Perception.mergeKnowledge` a second time filtered to `Side = Hostile` — no change to `Perception.fs`. New `Overlay.HostileKnownContact` diagnostic case (a distinct case, not a reuse of `KnownContact`), derived in both `frame` and `frameOf`. Proves, rather than builds, `docs/09` section 8's "enemy does not target an unobserved player position": `Simulation.combat` already draws candidates from a shooter's own same-tick `VisibleContacts` only, strictly tighter than any tactical-knowledge store, so no `Combat.fs` change — one new `SimulationTests` fact pins it and `docs/09` section 8's wording is corrected from "partially realised" to fully realised for the targeting half. `Canonical.FormatVersion` `6 -> 7`, full re-pin, **not** behaviour-neutral: `open-engagement`, `perception-contact`, and `exposed-approach` each gain a genuine new non-zero `HostileTacticalKnowledge` entry from the tick a hostile first sees a friendly (tick counts and event counts unchanged everywhere). No new corpus entry, no new event type, no ADR. Deliberately narrower than B-022's full text: suppress-likely-routes, seek-adjacent-cover-under-pressure, and scripted fall-back all stay open (need B-030's order vocabulary or a new autonomous-movement design), so B-022 stays `review`, not `done` — the B-021/TASK-033 precedent. `285 -> 287` green; `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` twice byte-identical); `-- fixture` format 7, 36 events unchanged; `-- replay-file envelope-full` OK at canonical 7, 78 events unchanged; `dotnet list` FSharp.Core only; source scan clean. | `ready -> review` | pending | [detail](ledger/2026-09-15-TASK-034-hostile-tactical-picture.md) |
| 2026-09-15 | TASK-034 accepted | Recorded acceptance of TASK-034 (backlog B-022, partial) and merged to `main`. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 287`, `-- corpus` 12/12 (`--regenerate` twice byte-identical), `-- fixture` format 7 / 36 events unchanged, `-- replay-file envelope-full` OK at canonical 7 / 78 events unchanged, `git status` clean, `dotnet list src/CommandoWar.Sim/CommandoWar.Sim.fsproj package --include-transitive` `FSharp.Core` only. `Implement TASK-034: ...` then `Accept TASK-034; record acceptance and merge` (PROJECT_STATE, `docs/11`, `docs/12`, the TASK-034 task file + ledger Review block) then `Merge TASK-034 ... (--no-ff)`; branch `task-034-hostile-tactical-picture` deleted; not pushed. B-022 stays `review`, not `done` — suppress-likely-routes, seek-adjacent-cover-under-pressure, and scripted fall-back remain open, needing B-030's order vocabulary or a new autonomous-movement design not yet built. | `tasks/TASK-034-*.md` Status `review -> done`; `docs/ledger/2026-09-15-TASK-034-*.md` Review `Accepted: yes (2026-09-15)`; `docs/11` TASK-034 + B-022 rows; `docs/12` TASK-034 row + this row; `PROJECT_STATE.yaml` `active_work.selected_task -> none` | yes (2026-09-15) | [detail](ledger/2026-09-15-TASK-034-hostile-tactical-picture.md) |
| 2026-09-15 | DIAG-001 | `content/diagnostics/demo.html` enriched with a per-tick annotation panel: a readable narration of that tick's `DiagnosticFrame.Events`, a per-agent table joining `AgentMarker` with its `OrderAppraisal` (TASK-028) / `AgentCommitment` (TASK-030) / `AgentSuppression` (TASK-032) / `AgentStress` (TASK-033) overlays, and the two shared tactical pictures (`KnownContact` / `HostileKnownContact`, TASK-026/034). Scoped lightweight by Dave's explicit request — no task file, done directly on `main`. Presentation only: no new `Overlay` case, no `Canonical.FormatVersion` change, `Ascii` / `Svg` output byte-unchanged (only `DiagnosticRender.Html` gained the new panel, plus a `commitmentText` helper extracted for reuse — same "holding" / "moving to (x,y)" text `Ascii` already printed). `dotnet build` 0/0; `dotnet test` `Passed: 287` (the `demo.html` byte-equal golden fact and the HTML-parses-as-XML fact both pass against the regenerated file); manually inspected ticks 1 and 8 of the regenerated file for readability and correctness against the scenario. | none (no phase/gate/decision/active-task change) | n/a (no task file to accept) | [detail](ledger/2026-09-15-DIAG-001-demo-html-annotations.md) |
| 2026-09-15 | TASK-035 | Event agent attribution for diagnostic narration — closes the gap DIAG-001 flagged rather than silently patched. `Diagnostics.EventMarker` gains `Agents: AgentId[]` (the `Cells: Cell[]` shape precedent), populated in every `eventMarker` arm from fields the `DomainEvent` body already carries (`CommandAccepted`'s recipient, `ShotFired`'s shooter/target, etc. — nothing new computed); two entries for a two-party event, primary first (shooter, yielding/obstructed agent, observer). `DiagnosticRender.eventNarration` now names the agent(s) in every sentence, e.g. `"Shot fired: hit."` -> `"Agent 0 fired at agent 5: hit."`. Decision B (in the task file): `Ascii`'s terse `kind@cell,cell` events line is deliberately left unchanged — redundant by design with the fuller `overlays:` section one line above it in the same render (e.g. `open-engagement-tick-001.ascii.txt` already spells out `fire (2,2) -> (7,2): agent 0 -> agent 1  miss`), and extending it would re-pin seven ASCII goldens for no new information — flagged explicitly for Dave's review since the task description framed it as an open choice, not a settled one. `content/diagnostics/demo.html` is the only golden that moves (311107 -> 311787 bytes, narration text only). No `Canonical.FormatVersion` change (`Canonical.fs` never references `Diagnostics`, confirmed by grep before starting) — `-- corpus` 12/12, `-- fixture` format 7 / 36 events unchanged. Two existing `DiagnosticsTests.fs` facts extended in place (no new `[<Fact>]`, the TASK-032/033 precedent): the demo tick-1 command-accepted fact now also asserts `Agents`, and the `open-engagement` shot-fired fact now also asserts each event's `Agents` matches its `FireLine` overlay's shooter/target. `287 -> 287` green (same count, both additions extended in place). `dotnet build` 0/0. No ADR, no `docs/11_BACKLOG.md` row (no matching backlog item — presentation-only tooling follow-up, the TASK-009/014/019/023-without-B-item precedent). No separate pre-implementation Dave confirmation round for the two central decisions — Dave's own task instructions asked the implementer to decide and document them directly, given the small single-session scope; both are reversible on review. | `tasks/TASK-035-*.md` created, `proposed -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work.note` only (`selected_task` stays `none`, the DIAG-001 precedent) | pending | [detail](ledger/2026-09-15-TASK-035-event-agent-attribution.md) |
| 2026-09-15 | TASK-035 accepted | Recorded acceptance of TASK-035 (event agent attribution for diagnostic narration, presentation-only, no backlog item). Implemented and self-verified directly on `main`, no branch (the DIAG-001 precedent), so acceptance is a re-verify and status update, not a branch merge. Re-verified before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 287`, `-- corpus` 12/12, `-- fixture` format 7 / final hash `0x56395A49904D017D` / 36 events unchanged, `git status --porcelain` matched the expected scope (this task's files, plus DIAG-001's still-uncommitted changes and the pre-existing unrelated `src/CommandoWar.Client.Godot/project.godot` edit, all untouched). Dave accepted Decision B (leaving `Ascii`'s events line terse) as-is. | `tasks/TASK-035-*.md` Status `review -> done`; `docs/ledger/2026-09-15-TASK-035-*.md` Review `Accepted: yes (2026-09-15)`; `docs/12` this row; `PROJECT_STATE.yaml active_work.note` updated to reflect accepted status (`selected_task` stays `none`, never occupied the slot) | yes (2026-09-15) | [detail](ledger/2026-09-15-TASK-035-event-agent-attribution.md) |
| 2026-09-15 | TASK-036 drafted | Shared F# fixture builder for the test corpus (backlog B-049; unblocked by B-045/TASK-025, precondition already met). Of the backlog items ready per `docs/11` §7 (all dependencies `done`) after TASK-035 — B-016b, B-049, B-050, B-011d — none sit on G3's critical path (that is blocked on a slice of B-030 for B-023 step 5, a separate larger discussion flagged to Dave, not resolved here); B-049 was picked as the one paying down compounding risk, the `Corpus.fs` / `.cwlog` / `SimulationTests.fs` duplication and drift growing with every corpus-touching task since TASK-016. One builder in `Corpus.fs` will author scenario geometry and a per-agent command schedule as a single value, replacing the eleven hand-written `…World ()` functions and their hand-authored `.cwlog` files. Two central decisions confirmed with Dave via `AskUserQuestion` before drafting: commit `.cwreplay` (`ReplaySerialisation`, B-045's production format) instead of the frozen `.cwlog` v1 grammar, which cannot express multi-recipient/`Urgency`/`RiskTolerance`; and migrate all eleven existing entries, not just future ones. Two further decisions made directly per existing convention: the builder's authored value becomes the sole runtime source of truth (`.cwreplay` generated and diff-checked, not read back — the `<name>.md` precedent, eliminating drift categorically); no retrofit of `SimulationTests.fs`'s ~280 existing facts (unrelated-refactor scope; that file does not use `Corpus`/`rawScenario` today). `spike-fixture` (backed by `Fixture.fs`) stays out of scope. No `CommandoWar.Sim` change, no `Canonical.FormatVersion` change, no ADR — a pure tooling refactor, proof of correctness is every migrated entry's `.md` hash table staying byte-identical. Not yet implemented. | `tasks/TASK-036-*.md` created (`proposed -> ready`); `docs/11_BACKLOG.md` new TASK-036 row + B-049 `proposed -> ready`; `PROJECT_STATE.yaml active_work.selected_task -> TASK-036` | pending | [detail](ledger/2026-09-15-TASK-036-drafted.md) |
| 2026-09-16 | TASK-036 | Shared F# fixture builder for the test corpus (realises B-049). One `ScenarioSpec` value (`src/CommandoWar.Headless/Corpus.fs`) authors geometry and a per-agent command schedule together; `worldOfSpec`/`commandsOfSpec` are its two projections. All eleven non-fixture corpus entries rewritten onto it, replacing `rawScenario`/the eleven `…World ()` functions. `Entry` gains `Commands: RecordedCommand[] option` — `Some` (the sole runtime source of truth) for a migrated entry, `None` for `spike-fixture` (unchanged, still `.cwlog`-backed). New `Corpus.commandsOf` replaces direct `loadLog` calls in `run`/`checkEntry`/`regenerateEntry`, plus in every test/demo call site that read a migrated entry's `.cwlog` directly — a deviation found during implementation, beyond the drafted "Allowed scope": `tests/CommandoWar.Sim.Tests/DiagnosticsTests.fs` (nine call sites) and `src/CommandoWar.Headless/AppraisalDemo.fs` (the TASK-029 Godot demo helper), both mechanical, no behaviour change. `regenerateEntry` now also writes each migrated entry's `content/replays/<name>.cwreplay` (`ReplaySerialisation.serialise`, empty `InitialHash`/`Checkpoints` — `<name>.md` already owns that role); `CommandId`/`Sequence` numbering deliberately mirrors `CommandLogFile.parse`'s order-of-appearance convention exactly, since `CommandId` is part of `Canonical.encode` and a mismatch would have silently re-pinned every migrated hash. Eleven `.cwlog` files deleted. Every migrated entry's committed `.md` is byte-identical **except** its "Command log" row, corrected from the deleted `.cwlog` name to the new `.cwreplay` name (`Corpus.parseTable` never reads that row — a deliberate correction, not a regression). `content/replays/CORPUS.md`, `docs/04_SIMULATION_SPEC.md`, `docs/09_TEST_STRATEGY.md` updated for the format change (targeted corrections only, not a full refresh of either doc's pre-existing staleness). No `CommandoWar.Sim` change, no `Canonical.FormatVersion` change, no ADR. `287 -> 287` green (no new fact — the loading-API swap reused every existing assertion). `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` twice byte-identical); `-- fixture` format 7, 36 events unchanged; `-- replay-file envelope-full` OK, 78 events unchanged. Implemented directly on `main`, no branch — the DIAG-001/TASK-035 precedent, justified by this task's low-risk, fully-verified refactor profile; flagged to Dave before committing (a departure from the TASK-030–034 M-sized-task branch precedent). | `tasks/TASK-036-*.md` `ready -> review`; `docs/11_BACKLOG.md` B-049 `ready -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work.note` updated | pending | [detail](ledger/2026-09-16-TASK-036-shared-fixture-builder.md) |
| 2026-09-16 | TASK-036 accepted | Recorded acceptance of TASK-036 (backlog B-049) — implemented directly on `main`, no branch, so acceptance is a re-verify and status update, not a branch merge. Re-verified before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 287`, `-- corpus` 12/12, `-- fixture` format 7 / 36 events unchanged, `git status --porcelain` matched the expected scope (only the pre-existing unrelated `src/CommandoWar.Client.Godot/project.godot` edit left untouched). Both deviations found during implementation (`AppraisalDemo.fs`, `DiagnosticsTests.fs` needing the same `loadLog` -> `commandsOf` swap) and the "Command log" row correction in every migrated `.md` accepted as-is. | `tasks/TASK-036-*.md` Status `review -> done`; `docs/ledger/2026-09-16-TASK-036-*.md` Review `Accepted: yes (2026-09-16)`; `docs/11_BACKLOG.md` TASK-036 + B-049 rows `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-16) | [detail](ledger/2026-09-16-TASK-036-shared-fixture-builder.md) |
| 2026-09-16 | TASK-037 | Minimal Suppress order and B-021/B-022 dependency closeout (a thin B-030 slice, pulled forward as P3 decision-support after Dave chose "do whichever unblocks the most progress; merge tasks if more than one unblocks more" via `AskUserQuestion`, over three narrower options). New `PlayerIntent.Suppress of target: AgentId` and `DecisionReason.TargetNotKnown` (the pre-planned `docs/05` vocabulary case); `Appraisal.appraise`'s `Suppress` branch is `Accepted` if the named contact is known, else `Unable(TargetNotKnown)`, never reaching stage 3/4. New `Commitment.Suppressing of SuppressCommitment` (`docs/05` section 9's own pre-named shape): holds position, pins the named contact as Combat's sole candidate. `Appraisal.routeExposure`/`cellPressure` gain a `suppressedThreats: AgentId[]` parameter and zero a threat's pressure while its `SuppressionBand` is latched (reusing the TASK-033/034 hysteresis latch, not new bookkeeping). A third reappraisal trigger — any agent's `SuppressionBand` flip, global scope — resets a *different* agent's non-fulfilled order, realising `docs/07` section 8 step 6. New `suppress-relieves-exposure` corpus entry proves it end to end, deterministically regardless of hit/miss RNG (worst case: 5 ticks of misses alone cross `SuppressionBandEnter`). `Canonical.FormatVersion` **7 -> 8** (inside the existing `Order`/`Disposition` sections, not a new `topLevelSections` entry); full re-pin, behaviour-neutral except the new entry. `ReplaySerialisation` gains a `suppress <targetAgentId>` command keyword with no version bump (its grammar had already left room for exactly this). B-021 and B-022 explicitly descoped to `done` per their own rows' "until those land or are explicitly descoped" wording — neither row's remaining items are touched by this task's player-issued order (B-021's need B-031/a materially larger cut; B-022's are Hostile-side doctrine, not player-issued). B-023's dependency list is now fully `done`. `287 -> 293` green (+6: five new `SimulationTests` facts plus one `Commitment.ofAgent` extension); `dotnet build` 0/0; `-- corpus` 13/13 (`--regenerate` twice byte-identical); `-- fixture` format 8, 36 events unchanged; `-- replay-file envelope-full` OK at canonical 8, 78 events unchanged; `dotnet list` `FSharp.Core` only; source scan clean. Godot's `--selfcheck` pinned hash updated for the moved `exposed-approach` tick-1 hash but not independently re-run through Godot in this session (no Godot install here) — flagged for Dave to confirm before accepting. Eleven central decisions (A-K) confirmed with Dave via `AskUserQuestion` before the phase bodies. | `tasks/TASK-037-*.md` created and implemented (`proposed -> in progress`); `docs/11_BACKLOG.md` new TASK-037 row, B-021/B-022 rows `review -> done`, B-023 dependency note, B-030 pulled-forward-slice note; `docs/12` this row + summary counters; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-16-TASK-037-suppress-order-and-dependency-closeout.md) |
| 2026-09-16 | TASK-037 accepted | Recorded acceptance of TASK-037 (a thin B-030 slice; B-021/B-022 dependency closeout) and merged to `main`. Re-verified on the branch before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 293`, `-- corpus` 13/13 (`--regenerate` twice byte-identical), `-- fixture` format 8 / 36 events unchanged, `-- replay-file envelope-full` OK at canonical 8 / 78 events unchanged, `git status` clean except the pre-existing unrelated `project.godot` edit, `dotnet list src/CommandoWar.Sim/CommandoWar.Sim.fsproj package --include-transitive` `FSharp.Core` only. Godot's `--selfcheck` pinned hash (`0x5D5A30C0DF64AC93`) still needs independent confirmation on Dave's machine — flagged, not a blocker. | `tasks/TASK-037-*.md` Status `review -> done`; `docs/ledger/2026-09-16-TASK-037-*.md` Review `Accepted: yes (2026-09-16)`; `docs/11_BACKLOG.md` TASK-037 row `in progress -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-16) | [detail](ledger/2026-09-16-TASK-037-suppress-order-and-dependency-closeout.md) |
| 2026-09-16 | TASK-038 | Canonical refusal-and-correction scenario end to end (realises B-023 — G3's headline evidence item). Central decision confirmed with Dave via `AskUserQuestion` before drafting: `docs/07` section 8 step 8 says "accepts or adapts it consistently," but no `Adapted` `OrderDisposition` case exists anywhere (stage-5 adaptation deferred by B-018) — closed on the accepts-only proof, the `Adapted` half recorded as a new, not-yet-filed follow-up rather than silently dropped. New `canonical-refusal-and-correction` corpus entry (`src/CommandoWar.Headless/Corpus.fs`): the `suppress-relieves-exposure` geometry exactly, plus one more order reissuing agent 0's identical `(11,3)` target on tick 8. Traced against the real seeded run before choosing that tick: agent 0 is `Refused RouteTooExposed threat-agent-2` ticks 1-3, the automatic threat-suppression-change reappraisal (TASK-037) `Accepts` it at tick 4 with a fresh `CommitmentEstablished`, the tick-8 reissue produces its own fresh `Accepted` + `CommitmentEstablished` for the same target (the TASK-030 "second order mid-route" mechanism), disposition stays `Accepted` (or unappraised, on arrival) through one extra reappraisal blip at tick 12 and arrival at tick 13 — never flipping back to `Refused`. Step 4 needed no new mechanism: `OrderAppraised` only ever carries a structured `OrderDisposition`/`DecisionReason`, never a raw score; this task adds a test assertion turning that standing type-system guarantee into checked evidence. Three new `DiagnosticsTests` facts plus one golden ASCII/SVG pair at tick 8 (the reissue moment); no `CommandoWar.Sim` change, no `Canonical.FormatVersion` bump — every mechanism already existed (TASK-028/030/033/037). Deviation from the drafted scope: no dedicated golden diagnostic pair was originally guaranteed by `AGENTS.md`'s diagnostics rule (this task adds no new `Overlay` case, the identical situation TASK-037 was in), but one was added anyway for a G3 headline-evidence entry, matching the `exposed-approach`/`reissued-order` precedent. `293 -> 297` green (+4: three `DiagnosticsTests` facts, one `CorpusTests` theory case); `dotnet build` 0/0; `-- corpus` 14/14 (`--regenerate` twice byte-identical, only the new entry's files untracked); `-- fixture` format 8, 36 events unchanged; `dotnet list` `FSharp.Core` only; source scan clean. Not yet accepted. | `tasks/TASK-038-*.md` created and implemented (`drafted -> review`); `docs/07_VERTICAL_SLICE.md` section 8 records the full sequence realised, accepts-only scope of step 8 noted; `docs/11_BACKLOG.md` B-023 row `proposed -> done`; `content/diagnostics/README.md` new golden pair documented; `docs/12` this row + Pinned facts green-test count; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-16-TASK-038-canonical-refusal-and-correction.md) |
| 2026-09-16 | TASK-038 accepted | Recorded acceptance of TASK-038 (realises B-023) — implemented directly on `main`, no branch (the TASK-035/036 low-risk precedent: no `CommandoWar.Sim` change, purely additive). Re-verified before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 297`, `-- corpus` 14/14, `-- fixture` format 8 / 36 events unchanged, `git status --porcelain` matched the expected scope (only the pre-existing unrelated `src/CommandoWar.Client.Godot/project.godot` edit left untouched). | `tasks/TASK-038-*.md` Status `review -> done`; `docs/ledger/2026-09-16-TASK-038-*.md` Review `Accepted: yes (2026-09-16)`; `docs/11_BACKLOG.md` B-023 row already `done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-16) | [detail](ledger/2026-09-16-TASK-038-canonical-refusal-and-correction.md) |
| 2026-09-16 | G3 gate decision | G3 declared passed following B-023's completion. `docs/08` section 6's "five vertical-slice intents" required-work line was only partly built (`PlayerIntent` has `MoveTo`/`Suppress`; `Hold`/`Assault`/`Withdraw` are B-030 proper, explicitly re-scoped to P4/G4 by TASK-037's own backlog row). Put to Dave via `AskUserQuestion` rather than resolved by inference (the G2 "tactical knowledge" precedent): Dave chose to declare G3 passed now — no G3 evidence bullet names or depends on the missing three intents, and the canonical refusal test needed only `MoveTo`/`Suppress`. `docs/08` section 6's required-work list corrected with a dated resolution note, the same move G2's "tactical knowledge" line received. | `gates.G3_command_loop_proven_headless` `pending -> passed`; `current_gate` `G3 -> G4_vertical_slice_feature_complete`; `current_phase` `P3_command_loop_headless -> P4_playable_integration`; `docs/08_ROADMAP_AND_GATES.md` section 6 corrected; `docs/12` Pinned facts (Current gate/phase) + this row; `PROJECT_STATE.yaml` updated | yes (2026-09-16) | [detail](ledger/2026-09-16-G3-gate-decision.md) |
| 2026-09-16 | TASK-039 | Snapshot rendering and isometric depth ordering (the first P4 client task, realises B-027). Of the P4 items now selectable (B-026, B-027, B-030 proper, B-031), Dave chose B-027 via `AskUserQuestion`: something to see before building input on top of it, and the real Godot editor-authoring work ADR-0001 review trigger 1 needs. Central decision confirmed via `AskUserQuestion`: render `DemoScenario` (the terrain-demo scenario — elevation ridge, impassable block, movement-cost patch, opaque wall, cover), not a corpus entry or the `Greybox.tscn` content-import path, since the real Bridgehead map is still blocked on B-043/B-024. New `src/CommandoWar.Client.Godot/Core/CommandoWar.Client.Godot.Core.fsproj` stands up ADR-0004's F# client-core architecture for real for the first time: `CwClientCore.IClientScene`/`DrawItem`/`TickHash` (`[<CLIMutable>]` records only — an F# tuple draft failed the C# build with `CS0029`, corrected) and `DemoRenderScene` (fixed-step accumulator, `Simulation.step`/`RenderSnapshot`-driven, a depth-sorted `DrawItem[]` each frame — terrain interleaved with agents by `(Cx + Cy)` ascending, not "all terrain then all agents", the disposable TASK-004 spike's actual gap). New generic `src/FSharpSceneHost.cs` (ADR-0004 form 1) resolves the named `IClientScene` by reflection; `scenes/SnapshotDemo.tscn` is the first production scene built on it (`AppraisalDemo.tscn` stays `run/main_scene`, unchanged). No `CommandoWar.Sim`/`CommandoWar.Headless` change. Verified for real through the Godot 4.7.2 editor available in this environment (no "flagged for Dave" deferral, unlike every prior Godot-touching task): `--editor --headless --quit` import, `dotnet build` both `.slnx` files, `--selfcheck` (`MATCH` the pinned tick-20 hash `0x11B06E6EDE0C52E3`, byte-identical to an independent `dotnet test` computation outside Godot — a cross-runtime determinism cross-check — exit 0), and a windowed `--screenshot` (`docs/evidence/task-039-snapshot-rendering.png`, committed). Sized `L` in practice (`docs/11`'s `M` undercounts the one-time client-core scaffold cost); every later P4 client task reuses it. ADR-0004's own review trigger 1 (interactive F# debugging) is explicitly **not** discharged by this task — needs Dave's own editor session, flagged rather than silently claimed. `dotnet build CommandoWar.slnx` 0/0; `dotnet test` 297/297 (both unaffected). | `tasks/TASK-039-*.md` created and implemented (`drafted -> review`); `src/CommandoWar.Client.Godot/README.md` new section + layout table; `decisions/ADR-0004-GODOT-FSHARP-BOUNDARY.md` dated note; `docs/11_BACKLOG.md` B-027 row `proposed -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-16-TASK-039-snapshot-rendering-and-depth-ordering.md) |
| 2026-09-16 | TASK-039 accepted | Recorded acceptance of TASK-039 (realises B-027, the first P4 client task) — implemented directly on `main`, no branch. Re-verified before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 297`, `git status --porcelain` matched the expected scope (only the pre-existing unrelated `src/CommandoWar.Client.Godot/project.godot` edit left untouched). ADR-0004's own review trigger 1 (an F# breakpoint hit from an interactive editor/F5-launched run) explicitly deferred by Dave to a later session — not a blocker for this task's own acceptance criteria, which the automated `--selfcheck`/`--screenshot` evidence already satisfies. | `tasks/TASK-039-*.md` Status `review -> done`; `docs/ledger/2026-09-16-TASK-039-*.md` Review `Accepted: yes (2026-09-16)`; `docs/11_BACKLOG.md` B-027 row already `done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-16) | [detail](ledger/2026-09-16-TASK-039-snapshot-rendering-and-depth-ordering.md) |
| 2026-09-17 | TASK-040 | Selection, input mapping, tactical pause, and command preview (realises B-026). Dave chose B-026 via `AskUserQuestion` over B-028/B-030 proper/B-031, building on TASK-039's scaffold. Four central decisions confirmed via `AskUserQuestion`: a new `CommandDemo.tscn` scene rather than adding input to `SnapshotDemo.tscn`; a real `Pathfinding.find` route preview, not a straight line; single-unit selection only; orders issuable while tactically paused, delivered on resume. `IClientScene` gains `OnClick`/`OnHover`/`OnTogglePause` (primitives only); new `Core/RenderShared.fs` (terrain items + depth key, extracted from `DemoRenderScene.fs` so `CommandDemoScene` doesn't duplicate them — `SnapshotDemo.tscn`'s own `--selfcheck` hash confirmed unchanged after the extraction); new `Core/CommandDemoScene.fs` (live selection over a friendly agent, a player-built `Command.moveTo` queued for next-tick delivery through the real `Simulation.step`, a `Pathfinding.find` hover preview, a selection halo and destination marker — all reusing existing `DrawItem` `Kind` values, no host-side render change) and `CommandDemoDrive.runScriptedSelfCheck` (drives the real `OnClick`/`OnHover` methods, not a canned log, the `DemoDrive.runFullSequence` precedent). `FSharpSceneHost.cs` gains `ScreenToCell` (inverse of the existing projection) and `_UnhandledInput` forwarding; `--selfcheck` dispatch and `--screenshot` priming both extended per-`SceneType` (the existing dispatch precedent). New `scenes/CommandDemo.tscn`, not `run/main_scene`. No `CommandoWar.Sim`/`CommandoWar.Headless` change. Verified through the real Godot 4.7.2 editor: `SnapshotDemo.tscn --selfcheck` unchanged (`MATCH 0x11B06E6EDE0C52E3`); a temporary scratch probe pinned `CommandDemo.tscn`'s scripted-input hash (`0x649FA4D08E2931CA`), then `CommandDemo.tscn --selfcheck` through Godot reproduced it byte-identically; a windowed `--screenshot` shows the selection halo, route-preview dots, and the HUD naming the selected agent. `dotnet build CommandoWar.slnx` 0/0; `dotnet test` 297/297 (both unaffected). | `tasks/TASK-040-*.md` created and implemented (`proposed -> review`); `src/CommandoWar.Client.Godot/README.md` new section + layout table; `docs/11_BACKLOG.md` B-026 row `proposed -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-040-selection-input-and-tactical-pause.md) |
| 2026-09-17 | TASK-040 review round 1 | Dave tried `CommandDemo.tscn` and reported selection only registering near the base of the agent sprite, and no visual difference between a hover preview and an actually-set order (especially while paused). Both were real: a click-projection bug (`ScreenToCell` inverted the diamond-centre formula, but an agent's circle draws `TileH/2` above that) fixed via a new `TryHitAgentCircle` hit-test against each agent's real rendered position; the route preview gained three distinct colours (hover/pending/committed) instead of one, so a paused, undelivered order stays visibly orange rather than looking identical to a live hover. A defensive replace-not-stack fix was added to `OnClick` (found while fixing the above — two clicks before delivery could otherwise hand `Simulation.step` two commands for one agent in the same tick). Dave separately raised multi-waypoint stacking + cancellation, explicitly hedged as undecided; put via `AskUserQuestion` rather than guessed — Dave chose to keep TASK-040 single-order-only and park the stacking design fork (client-side sequencing vs. an authoritative sim-side order queue) as new backlog row **B-051**. Both scenes' `--selfcheck` hashes unchanged (`0x11B06E6EDE0C52E3` / `0x649FA4D08E2931CA`); screenshot re-captured showing `PAUSED` + the orange pending route; `dotnet build` 0/0, `dotnet test` 297/297. | `tasks/TASK-040-*.md` review-round section added; `docs/11_BACKLOG.md` new row B-051; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-040-selection-input-and-tactical-pause.md) |
| 2026-09-17 | TASK-040 accepted | Recorded acceptance of TASK-040 (realises B-026) after round 1 review feedback was fixed: the click hit-testing bug (`TryHitAgentCircle`) and the hover/pending/committed route-colour distinction. Re-verified before accepting: `dotnet build CommandoWar.slnx -c Release` 0/0, `dotnet test` `Passed: 297`, `dotnet build` the Godot client `.slnx` 0/0, `git status --porcelain` matched the expected scope (only the pre-existing unrelated `project.godot` edit left untouched). Multi-waypoint stacking and cancellation explicitly not part of this acceptance — deferred to backlog row B-051. | `tasks/TASK-040-*.md` Status `review -> done`; `docs/ledger/2026-09-17-TASK-040-*.md` Review `Accepted: yes (2026-09-17)`; `docs/11_BACKLOG.md` B-026 row `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-17) | [detail](ledger/2026-09-17-TASK-040-selection-input-and-tactical-pause.md) |
| 2026-09-17 | TASK-041 | Minimal coherent placeholder art set (realises B-034), selected via `AskUserQuestion` (run now, before B-028, since already drafted and unblocked). Live-checked kenney.nl per the task's own central decision (not assumed from memory): downloaded and inspected both `isometric-blocks` and `isometric-miniature-prototype`; chose **Isometric Miniature Prototype** (v2.3, CC0) alone, since its `Isometric/` (terrain) and `Characters/Human/` (agents) subfolders already ship together as one coherent family. New `src/CommandoWar.Client.Godot/art/` (`terrain_floor.png`, `terrain_block.png`, `terrain_crate.png`, `agent_human.png`, `LICENSE-THIRD-PARTY.md`). `DrawItem` (`Core/IClientScene.fs`) gains one new primitive field, `TextureId`, selecting a terrain texture by the same passable/opaque branches `RenderShared.buildTerrainItems` already computed (0 = floor, elevation-tinted; 1 = block, impassable; 2 = crate, passable-but-opaque) — texture shape, not colour alone, now carries that distinction. `FSharpSceneHost.cs` loads all four textures once (static fields); `_Draw`'s terrain branch (`DrawTerrainTile`) bottom-anchors the Kenney canvas at the same point `DrawDiamond` used; the agent branch (`DrawAgentFigure`) only replaces a real, full-opacity agent (`Kind = 1`, `A >= 0.99`, `TryHitAgentCircle`'s own existing "is this a real agent" test) with a cropped, tinted human figure — a translucent halo/route-preview marker still draws the original plain circle. Both existing scenes' `--selfcheck` hashes confirmed unchanged (`0x11B06E6EDE0C52E3`, `0x649FA4D08E2931CA`) — genuinely render-only. Deviation found during implementation: the first screenshot attempt used a stale `Debug`-config build (Godot's windowed run defaults to `Debug`, not the `Release` build already run for `dotnet test`) and still showed the old flat shapes; caught by inspecting the screenshot output, fixed by also building `-c Debug` before capturing. `dotnet build` both `.slnx` files (`Debug` and `Release`) 0/0; `dotnet test` 297/297 (unaffected, no `CommandoWar.Sim`/`CommandoWar.Headless` change). | `tasks/TASK-041-*.md` created and implemented (`ready -> review`); `src/CommandoWar.Client.Godot/README.md` new section + layout table; `docs/11_BACKLOG.md` B-034 row `ready -> review`, B-051 row updated with the resolved sim-side-queue design decision; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-041-placeholder-art-set.md) |
| 2026-09-17 | TASK-041 accepted | Recorded acceptance of TASK-041 (realises B-034, "seems to work"). Two follow-ups raised, neither blocking (both outside this task's scope, which explicitly excluded directional facings and never touched selection UI): agents don't always visually face the direction they last moved (new backlog row **B-052**, not designed); selection still feels fiddly, Dave suggested — hedged, "possibly" — a hover highlight on a selectable agent before clicking (new backlog row **B-053**, not designed). Recorded per Dave's explicit instruction, not implemented, before moving to the next task. | `tasks/TASK-041-*.md` Status `review -> done`; `docs/ledger/2026-09-17-TASK-041-*.md` Review `Accepted: yes (2026-09-17)`; `docs/11_BACKLOG.md` B-034 row `review -> done`, two new rows B-052/B-053; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-17) | [detail](ledger/2026-09-17-TASK-041-placeholder-art-set.md) |
| 2026-09-17 | TASK-042 | Player-facing order acknowledgement, disposition, and reason UI (realises B-028), selected via `AskUserQuestion` over B-030 proper/B-031. Central decision confirmed via `AskUserQuestion`: extend the existing corner `HudText()` label with the selected agent's disposition/reason, not a new floating label tracking the agent's on-screen position — smaller change, reuses the established `selected=agent N` pattern. `AgentSnapshot` (`src/CommandoWar.Sim/Snapshot.fs`) gains a values-only `Disposition: OrderDisposition option` field, a copy of the already-canonical `AgentState.Disposition` (the `Destination`/`Progress` precedent, docs/03 section 12); `Simulation.fs`'s `output` phase and both Godot scenes' `Ready()`-time initial-snapshot construction updated to match. New `RenderShared.dispositionText`, a player-facing structured-value-to-text mapping deliberately separate from `DiagnosticRender`'s private developer-facing one — docs/06 section 11 draws that distinction explicitly (Player-facing "order acknowledgement and disposition" vs. Developer-facing "last appraisal factors and selected reason"). `CommandDemoScene.HudText()` appends `order=<disposition text>` for the selected agent only, omitted when nothing is selected. No `FSharpSceneHost.cs`/`_Draw` change — text only, no new draw primitive. Both scenes' `--selfcheck` hashes confirmed unchanged (`RenderSnapshot` is not part of `Canonical.encode`) — genuinely values-only. A temporary `dotnet fsi` scratch probe (removed after use, the TASK-038/039 precedent) drove the real `CommandDemoScene`/`IClientScene` headlessly and printed `HudText()` at each tick, cross-checked against the exact pinned hash sequence: `"order=no order"` before issuing, `"order=accepted"` once delivered and appraised (ticks 1-3), `"order=no order"` again once fulfilled and cleared (tick 4, TASK-030's existing behaviour); the `Refused`/`Unable`/`None` text branches were exercised directly with constructed `DecisionReason` values. `dotnet build` both `.slnx` (Debug and Release) 0/0; `dotnet test` 297/297 (unaffected — no test constructs `AgentSnapshot` directly, only field-by-field comparison). | `tasks/TASK-042-*.md` created and implemented (`proposed -> review`); `src/CommandoWar.Client.Godot/README.md` new section; `docs/11_BACKLOG.md` B-028 row `proposed -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-042-reason-and-disposition-ui.md) |
| 2026-09-17 | TASK-042 review round 1 | Dave tried `CommandDemo.tscn` and reported the HUD's `order=` text "flashes up" too fast to read, and separately noted the game loop runs faster than expected — explicitly not a regression, just the first time he needed to read specific transient text rather than glance at a persistent value. Real: at the default 20 Hz sim rate, `DemoScenario`'s short 3-cell route completes in ~3 ticks (~150ms wall-clock), so `order=accepted` appeared and reverted to `order=no order` before it could be read. Fixed with a client-side, presentation-only hold timer (`orderTextHoldSeconds = 1.5`) in `CommandDemoScene`, tracked in `Update(deltaSeconds)` (the only place the real per-frame wall-clock delta is available — `HudText()` has none): a newly meaningful message always overrides immediately, only the fall-back to `"no order"` is delayed; reset immediately on any selection change so a newly selected agent never shows a stale message. No `Simulation.step`/`Disposition`/hash change (`AGENTS.md`: "rendering ... are non-authoritative"). Verified with a new headless scratch probe (removed after use) driving `Update(dt)` directly with small (`1/60s`) per-frame deltas — the actual windowed code path, unlike `StepTicksHeadless`/`--selfcheck`, which never call `Update` and so never exercised the hold timer at all: `order=accepted` now stays visible from delivery (~0.1s) through ~1.7s before reverting, over a second of readable time instead of ~150ms. Both scenes' `--selfcheck` hashes reconfirmed unchanged; `dotnet build` both `.slnx` (Debug and Release) 0/0; `dotnet test` 297/297. | `tasks/TASK-042-*.md` Review round 1 section added; `docs/11_BACKLOG.md` B-028 row updated; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-042-reason-and-disposition-ui.md) |
| 2026-09-17 | TASK-042 accepted | Recorded acceptance of TASK-042 (realises B-028, "thats readable now") after the round 1 hold-timer fix. Re-verified: both scenes' `--selfcheck` hashes unchanged, `dotnet build` both `.slnx` (Debug and Release) 0/0, `dotnet test` 297/297. Acceptance confirms the round 1 fix specifically; no further issues raised against the underlying disposition/reason feature. | `tasks/TASK-042-*.md` Status `review -> done`; `docs/ledger/2026-09-17-TASK-042-*.md` Review `Accepted: yes (2026-09-17)`; `docs/11_BACKLOG.md` B-028 row `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-17) | [detail](ledger/2026-09-17-TASK-042-reason-and-disposition-ui.md) |
| 2026-09-17 | TASK-043 | Developer overlay rendering over live input (realises B-029 proper), selected via `AskUserQuestion` over B-030/B-031/B-051/B-052/B-053. A second `AskUserQuestion` confirmed the scope fork: docs/06 section 11's developer-facing list has two items (line-of-sight rays/occluders, grid coordinates) needing interop primitives `DrawItem` doesn't have; Dave chose the full set now over a narrower cut, accepting L sizing over the backlog's M. Realised by making `CommandDemoScene` a fourth renderer of `Diagnostics.DiagnosticFrame` — the same `frame`/`frameOf` and `Overlay[]` every `DiagnosticRender` renderer already consumes, built from this scene's own live `Simulation.step` output rather than a replayed corpus entry (TASK-029's read-only `AppraisalDemoScene`). No `CommandoWar.Sim`/`CommandoWar.Headless` change of any kind. `DrawItem` (`Core/IClientScene.fs`) gained `Cx2`/`Cy2` (`Kind = 2`, a line segment — LOS rays, fire lines) and `Text` (`Kind = 3`, a label — grid coordinates); `IClientScene` gained `OnToggleDevOverlay` (`DemoRenderScene`: no-op, the `OnTogglePause` precedent). `F1` toggles the overlay in `CommandDemoScene`, independent of pause: every terrain cell gets a coordinate label; this-tick `Reserved`/`Obstructed` cells are marked (cyan/red); each `TacticalKnowledge` contact gets a pale-yellow ghost marker at its last-known cell, distinct from the real always-rendered agent marker (no fog-of-war in this demo, so the two visibly diverge once stale); the selected agent's `OrderAppraisal.exposedCells` (orange) and a new `RenderShared.devAgentText` `[dev]` HUD line (commitment/suppression/stress/reason, mirroring `DiagnosticRender`'s existing private developer vocabulary, not TASK-042's player-facing text) appear; hovering with an agent selected traces a `Sight.trace` line-of-sight ray, green if visible or red terminating at the traced `Blocker` cell (the occluder) if not; the HUD's first line gained an always-visible `draws=<random draw counter>` field. Discipline and trust excluded from the "suppression/stress/discipline/trust" bullet (trust has no backing state anywhere; discipline exists but has no `Diagnostics.Overlay` case, and adding one would ripple into `CommandoWar.Sim`/`Headless` and a golden regen, out of proportion here); "last appraisal factors" realised as `exposedCells` + reason text, not raw pressure numbers (not retained state anywhere). A new opt-in `--dev-overlay` CLI flag (kept separate from the existing `--screenshot` priming) captured the evidence screenshot without changing TASK-042's existing default capture. Both scenes' `--selfcheck` hashes confirmed unchanged (`0x11B06E6EDE0C52E3`, `0x649FA4D08E2931CA`); `dotnet build` both `.slnx` (Debug and Release) 0/0; `dotnet test` 297/297 (unaffected). A headless `dotnet fsi` scratch probe (removed after use) drove the real `CommandDemoScene` directly: with the overlay off, output matches TASK-042's existing shape plus `draws=`; toggling on adds exactly 96 coordinate labels for the 12x8 grid and a line that correctly reroutes to the traced blocker cell when occluded (checked against both a clear and a blocked hover target); the `[dev]` line appears only with the overlay on and an agent selected, and clears immediately on deselection/reselection. | `tasks/TASK-043-*.md` created and implemented (`proposed -> review`); `src/CommandoWar.Client.Godot/README.md` new section; `docs/11_BACKLOG.md` B-029 row `proposed -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-043-developer-overlay-rendering.md) |
| 2026-09-17 | TASK-043 review round 1 | Dave tried `CommandDemo.tscn` windowed and reported the LOS ray (red line) drew "underneath the other layers" and pointed toward the mouse even off the map, trailing into empty space. Two real bugs, both rendering-only: (1) z-order — `devItems` were folded into the same `Array.sortBy RenderShared.depthKey` pass as terrain/agents, and `depthKey` derives a depth from one origin cell, meaningless for a two-cell line, so it could sort as "near" and paint before terrain it visually crossed; fixed by appending `devItems` *after* the depth sort in `CommandDemoScene.DrawList()`, so the developer overlay always draws on top. (2) `OnHover` traced `Sight.trace` with no bounds check; off the grid, `Sight.trace` returns not-visible with no `Blocker`, and the existing fallback-to-`Blocker`-or-target logic drew all the way to that far, meaningless cell; fixed by setting `losRay` to `None` whenever the hovered cell fails `GridBounds.contains` (the `OnClick` precedent). Verified with a new headless scratch probe (removed after use): an in-bounds hover produces exactly one `Kind = 2` line positioned last in `DrawList()` (confirmed on top); hovering `(500,500)`/`(-3,-3)` produces zero lines; returning in-bounds restores the ray. Both scenes' `--selfcheck` hashes reconfirmed unchanged; `dotnet build` both `.slnx` (Debug/Release) 0/0; `dotnet test` 297/297. Evidence screenshot re-captured. Not investigated: a separately-reported "green flashing line from another agent" toward a hostile is most likely the same hover-driven ray behaving as designed (green = visible) while hovering near a hostile with a different agent selected, toggling as the mouse crossed a partial-occlusion cell boundary — flagged for Dave to re-check live now the two concrete bugs are fixed. | `tasks/TASK-043-*.md` Review round 1 section added; `docs/12` this row; evidence screenshot re-captured | pending | [detail](ledger/2026-09-17-TASK-043-developer-overlay-rendering.md) |
| 2026-09-17 | TASK-043 review round 2 | Dave's live re-check surfaced three more points. The round 1 "flashing green line" theory was wrong: it is `FireLine`, not the LOS ray — `Diagnostics.frameOf` emits one `FireLine` per shot fired *that tick only*, green on hit, grey on miss, and `devFrame` is replaced every tick, so during combat the line flickers in and out with each shot, independent of the mouse — working as designed, no code change. Real gap: no legend for overlay colours, and two hues are each reused for a different meaning by draw shape (LOS-visible green vs. `FireLine`-hit green; `Obstructed` red vs. LOS-blocked red); fixed with a new `RenderShared.devLegendText`, a third `HudText()` line grouping colours by draw shape, shown whenever the overlay is on. Agent facing (doesn't turn to face movement) and sim speed (`simHz = 20`) are confirmed out of scope — B-052 and a pre-existing TASK-040 constant respectively, neither a TASK-043 regression. `dotnet build` both `.slnx` (Debug/Release) 0/0; `dotnet test` 297/297. Both scenes' `--selfcheck` hashes were **not** independently re-run this round: every headless Godot launch hung indefinitely (engine banner printed, then near-zero CPU forever) on both this session's sandbox and Dave's own terminal, for both scenes, surviving a build-server kill and a full `.godot` cache clear; a normal windowed launch worked, but windowed `--selfcheck` itself did not trigger the flag either. Flagged as a known gap, not blocking: the round 2 diff is one new string-literal HUD line, unreachable from the self-check driver, and the build/test run above already covers the compiled assembly. | `tasks/TASK-043-*.md` Review round 2 section added; `docs/12` this row | pending | [detail](ledger/2026-09-17-TASK-043-developer-overlay-rendering.md) |
| 2026-09-17 | TASK-043 accepted | Recorded acceptance of TASK-043 (realises B-029 proper, "i confirm the features work") after the round 2 legend addition and the corrected flashing-line diagnosis. Dave confirmed live in a normal windowed run: the `[legend]` HUD line appears with F1 on; the round 1 z-order/off-grid LOS fixes hold; the flashing green/grey line reads as combat shots, not the mouse. The `--selfcheck` hash re-check remains an open, non-blocking gap (see the round 2 row) — an environment-level headless-launch hang unrelated to this change. | `tasks/TASK-043-*.md` Status `review -> done`; `docs/ledger/2026-09-17-TASK-043-*.md` Review `Accepted: yes (2026-09-17)`; `docs/11_BACKLOG.md` B-029 row `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-17) | [detail](ledger/2026-09-17-TASK-043-developer-overlay-rendering.md) |
| 2026-09-17 | TASK-043 follow-up | Later session: re-checked the round 2 headless `--selfcheck` hang against the exact commands in TASK-043's own Required verification section. Did not reproduce — both scenes ran headless to completion in well under a minute, matching their then-pinned hashes (`0x11B06E6EDE0C52E3`, `0x649FA4D08E2931CA`). Retroactively closes round 2's known non-blocking gap; root cause never identified, so treated as not-currently-reproducing rather than fixed. `$GODOT` had to be resolved fresh (`C:\Users\Dave\Documents\GitHub\Godot_v4.7.2-stable_mono_win64\Godot_v4.7.2-stable_mono_win64_console.exe`) — no `GODOT` env var was set in this session. | none (informational; no task selected at the time) | n/a (not a task acceptance) | [detail](ledger/2026-09-17-TASK-043-developer-overlay-rendering.md) |
| 2026-09-17 | TASK-044 | Order queue: multi-waypoint stacking and cancellation (realises B-051), selected via `AskUserQuestion` over B-030 proper/B-031/B-053. B-051's own design fork (sim-side order queue vs. client-side sequencing) had already been resolved earlier in the session via `AskUserQuestion`; four further central decisions (queue scope — any order type; replace stays default, append opt-in; cancel by `CommandId`; legacy `.cwlog` left alone) confirmed the same way before drafting. `AgentState.OrderQueue: ReceivedOrder list`, new canonical field (insertion order, not sorted). `PlayerCommand.Intent` renamed `.Body: PlayerCommandBody` (`Order of intent * QueueMode \| Cancel of target`); `PlayerIntent`/`ReceivedOrder.Intent` themselves unchanged, so `Appraisal.fs`/`Commitment.fs` needed zero changes and a stored order can never be a `Cancel` by construction. `commandIntake`/`communication` (`Simulation.fs`) extended for append/replace/cancel-and-promote, comms-gated identically to today; `commitmentAndLocalAction` promotes the queue head on `MoveTo` fulfilment, judged the *following* tick (Appraisal already ran this tick) — a documented, deliberate gap distinct from a Cancel-driven promotion, which happens in `communication` before this same tick's Appraisal. New `OrderQueued`/`OrderCancelled` events, new sparse `AgentOrderQueue` diagnostic overlay (all three renderers), a five-keyword `.cwreplay` grammar extension (`queue-move`/`queue-suppress`/`cancel`, the legacy `.cwlog` deliberately untouched per Central decision 4), and a new corpus entry (`order-queue-stacking-and-cancellation`) proving stacking, both cancel outcomes, and the promotion-gap distinction end to end, plus its golden diagnostic pair. `Canonical.FormatVersion` 8 -> 9 (only the new corpus entry carries genuine new state; every other entry, the fixture, and `envelope-full` re-pin byte-layout-only, confirmed via `--regenerate` twice byte-identical and a fresh `--replay-file` checkpoint match). `Simulation.step`'s public signature unchanged (`Cancel` travels through the same `commands` array). Deviation found during implementation: cancelling an active order with an empty queue initially left `Destination` stale, so the agent kept walking toward the cancelled target — fixed in the same `communication` branch, covered by a new dedicated fact. Both Godot scenes' `--selfcheck` hashes re-pinned (`SnapshotDemo` `0xC68F993BC605313C`, `CommandDemo` `0xD27E623504262CE9`) and reconfirmed `MATCH` through the real Godot 4.7.2 editor; `dotnet build` both `.slnx` (Debug/Release) and the Mibo client 0/0; `dotnet test` `306/306`. | `tasks/TASK-044-*.md` created and implemented (`proposed -> review`); `docs/11_BACKLOG.md` B-051 row `proposed -> review`; `content/diagnostics/README.md` new file row + regeneration entry; `docs/12` Pinned facts (`Canonical.FormatVersion`, Green tests) + this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-044-order-queue-stacking-and-cancellation.md) |
| 2026-09-17 | TASK-044 accepted | Recorded acceptance of TASK-044 (realises B-051, "no new UI to review, just accept") — sim-side only, so nothing player-facing changed for Dave to check live, unlike every client-touching task this session; the self-verification (306/306 tests, `--regenerate` twice byte-identical, `--replay-file` OK, both Godot scenes' `--selfcheck` re-pinned and reconfirmed `MATCH`) stood as the acceptance evidence. | `tasks/TASK-044-*.md` Status `review -> done`; `docs/ledger/2026-09-17-TASK-044-*.md` Review `Accepted: yes (2026-09-17)`; `docs/11_BACKLOG.md` B-051 row `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-17) | [detail](ledger/2026-09-17-TASK-044-order-queue-stacking-and-cancellation.md) |
| 2026-09-17 | TASK-045 | Casualties, incapacitation, leadership succession, and squad failure (realises B-031), selected via `AskUserQuestion` over B-030 proper/B-053/B-054, directly after Dave accepted TASK-044 and authorised continuing to the next task the same session. Three central decisions confirmed with Dave via `AskUserQuestion` before drafting: leader identity is a pure derived rule (lowest-id living friendly, no stored field); wound state is wired into `Appraisal` in this task, not deferred; no rescue/stabilize mechanic. New `AgentState.Vitals: VitalStatus` (`Alive of health \| Incapacitated of bleedOutRemaining \| Dead`) and `AgentState.RecentlyWounded: bool`, both genuine canonical state; new `Casualty.fs` leaf (the `Suppression.fs`/`Stress.fs` precedent). `combat` filters shooters/targets to `Alive` only and applies wounds on a hit; `stateConsequences` ticks bleed-out, diffs derived leadership, and latches `SquadFailure` (a signal event only); `appraisal` gains stage-2 `Unable(CriticallyWounded)` and a stage-4 wound resolve penalty plus a new wounded reappraisal trigger; `navigationAndMovement` skips non-`Alive` agents. Four new events (`AgentIncapacitated`/`AgentDied`/`LeadershipTransferred`/`SquadFailure`); two new unconditional diagnostic overlays (`AgentVitals`, `SquadLeadership`); new corpus entry `casualties-succession-and-squad-failure` proving the full lifecycle end to end. `Canonical.FormatVersion` 9 -> 10. **Production bug found and fixed during implementation**: `stateConsequences`'s original leadership/squad-failure diff compared against a snapshot taken at its own phase entry — already-mutated by Combat, which runs earlier the same tick — so it could only ever catch its own bleed-out transitions, never one Combat itself just caused; fixed by adding `StepState.InitialLeader`/`.InitialFriendlyAlive`, the true pre-tick baseline computed once before any phase runs, and covered by a new `SimulationTests` fact that drives real combat rather than injecting state (injection cannot exercise this path — see the ledger detail). Re-pinning after the fix changed four pre-existing entries' event counts a second time (`perception-contact`/`exposed-approach`/`suppress-relieves-exposure`/`canonical-refusal-and-correction`, tick counts unchanged, documented in each entry's `Corpus.fs` `Description`). A `SimulationTests` infinite-loop test hang was also found and fixed (test-only — a perception test's world put the walking friendly within weapon range of a hostile, and the new "an Incapacitated agent never moves" invariant correctly stopped it forever mid-loop). Godot's `RenderShared.fs` needed a `CriticallyWounded` match arm to keep compiling; both scenes' `--selfcheck` hashes re-pinned and reconfirmed `MATCH` through the real Godot 4.7.2 editor. `dotnet build` all `.slnx` (main, Godot Debug/Release, Mibo) 0/0; `dotnet test` `319/319`. | `tasks/TASK-045-*.md` created and implemented (`proposed -> review`); `docs/11_BACKLOG.md` B-031 row `proposed -> review`; `content/diagnostics/README.md` two new file rows + regeneration entry; `docs/12` Pinned facts (`Canonical.FormatVersion`, Green tests) + this row; `src/CommandoWar.Client.Godot/README.md` new section; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-17-TASK-045-casualties-succession-and-squad-failure.md) |
| 2026-09-17 | TASK-045 accepted | Recorded acceptance of TASK-045 (realises B-031, "seems to work") — one follow-up raised, not blocking: without the developer overlay (`F1`) toggled on, there is no player-facing visual indicator that a shot was fired or that an agent is wounded/incapacitated; `CommandDemoScene.DrawList()`'s `fireLines` (and the equivalent for `AgentVitals`) are computed only inside the `devOverlay` branch, confirmed by inspection. Dave also floated needing "better graphics, maybe another free graphics pack" — open, not decided, whether new art or existing primitives suffice. Recorded as new backlog row B-057 (player-facing combat and casualty visual feedback), not implemented — out of this task's own Forbidden scope (Sim-side only). | `tasks/TASK-045-*.md` Status `review -> done`; `docs/ledger/2026-09-17-TASK-045-*.md` Review `Accepted: yes (2026-09-17)`; `docs/11_BACKLOG.md` B-031 row `review -> done`, new B-057 row filed; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-17) | [detail](ledger/2026-09-17-TASK-045-casualties-succession-and-squad-failure.md) |
| 2026-09-18 | TASK-046 | Player-facing combat and casualty visual feedback (realises B-057), selected via `AskUserQuestion` over B-030 proper and the smaller client-polish rows (B-052/053/054/055/056) — a direct, freshly-raised follow-up naming a real gap in TASK-045's own acceptance. Two central decisions confirmed with Dave via `AskUserQuestion` before implementation: (1) reuse existing draw primitives vs. source new sprite art — Dave chose new art; (2) after a live kenney.nl check (downloaded and unzipped both candidate packs, not assumed from memory) found the existing "Isometric Miniature Prototype" pack has no combat-effect sprites and no wound/death character pose, and that Kenney's "Game Icons" packs have only a generic medical cross (no skull, no distinct incapacitated icon) — confirmed **Particle Pack for fire feedback, primitives for vitals** (a mismatched cross badge was explicitly rejected). `FireLine`/`AgentVitals` (TASK-031/045's existing per-tick overlays, already computed regardless of the F1 toggle) promoted from developer-only to always-on. New `DrawItem.Kind = 4` (a one-shot textured point sprite: `TextureId` selects a muzzle flash at the shooter or a hit/miss sprite at the target — a distinct shape per outcome, not a colour-only tint). Vitals rendering mirrors `DiagnosticRender.Svg`'s own vocabulary: a small red wound dot (presence, not tint, is the signal), a darkened figure plus a plain-language "down" text badge for `Incapacitated`, a black cross replacing the figure for `Dead` — satisfies docs/06's "status indicators that do not rely on colour alone" for every state. New `src/CommandoWar.Client.Godot/art/effect_{muzzle_flash,impact_hit,impact_miss}.png` (Kenney "Particle Pack", CC0, resized 512->96px and alpha-boosted for legibility). No `CommandoWar.Sim`/`CommandoWar.Headless` change, no `Canonical.FormatVersion` bump — this is a render-only promotion of overlay data that already existed every tick. `dotnet build` both `.slnx` (main; Godot client Debug/Release) `0/0`; `dotnet test` unaffected `319/319`; both Godot scenes' `--selfcheck` hashes confirmed unchanged. The standard paused `--screenshot` evidence path cannot itself show combat (pauses at tick 0 by design); real evidence of the new rendering was captured via a temporary, fully-reverted local modification to the screenshot priming/capture timing (see the ledger detail for the frame-timing quirk found and worked around) — confirmed present, correctly positioned, and correctly tinted via direct instrumentation, not just by eye. | `tasks/TASK-046-*.md` created and implemented (`proposed -> review`); `docs/11_BACKLOG.md` B-057 row `proposed -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-18-TASK-046-combat-and-casualty-visual-feedback.md) |
| 2026-09-18 | TASK-046 review round 1 | Dave tried it live and raised three points. (1) "Are the agents firing through the scenery?" — confirmed real: `Sight.trace` traced directly against `DemoScenario`'s terrain showed the shooter/target line running through the impassable 2x2 block, authored `Opaque = false` (a demo-content authoring gap, not an engine bug — `Sight.trace`/`Combat.chooseTarget` correctly gate only on `opaque`). Fixed in `DemoScenario.fs` (confirmed with Dave via `AskUserQuestion` to fix now, accepting the wider blast radius): block cells `Opaque = false -> true`; re-pinned `SnapshotDemo`'s `--selfcheck` hash (`0x44B29B73E8F107EF -> 0x0DDADF2AD356E674`, `CommandDemo`'s unaffected) and regenerated two `DiagnosticsTests` goldens (`demo.svg`/`demo.html`, opaque-hatch stroke colour); `dotnet test` back to `319/319`. (2) "It flashes too quick, I didn't really notice any new graphics" — the fire effect was visible for exactly one tick (50ms at 20Hz); fixed with the `orderTextHoldSeconds` precedent: new `heldFireLines`/`fireEffectHoldSeconds` (0.4s, fading) in `CommandDemoScene.fs`, decremented by real wall-clock time independent of tick rate. (3) "I'm guessing movement is performed too fast?" — confirmed (`Terrain.BaseMoveCost = 1` = 20 cells/second at the client's 20Hz), but after trying a `simHz` halving (20 -> 10, a client-only pacing knob) live, Dave explicitly deferred: "let's leave it for later, adding an actual speed to the agent would solve that discrepancy" — reverted `simHz` to `20.0`, filed new backlog row B-058 (agent movement speed), not designed. No `Canonical.FormatVersion` change throughout (an existing field's authored value, not new state). | `tasks/TASK-046-*.md` Review round 1 section added, acceptance-criteria checkboxes annotated; `docs/11_BACKLOG.md` B-057 row updated, new B-058 row filed; `content/diagnostics/demo.{svg,html}` regenerated; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-18-TASK-046-combat-and-casualty-visual-feedback.md) |
| 2026-09-18 | TASK-046 accepted | Recorded acceptance of TASK-046 (realises B-057) after review round 1's three fixes (LOS bug, fire-effect hold, `simHz` revert). All three review points confirmed resolved; no further changes needed to accept. | `tasks/TASK-046-*.md` Status `review -> done`; `docs/ledger/2026-09-18-TASK-046-*.md` Review `Accepted: yes (2026-09-18)`; `docs/11_BACKLOG.md` B-057 row `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-18) | [detail](ledger/2026-09-18-TASK-046-combat-and-casualty-visual-feedback.md) |
| 2026-09-18 | TASK-047 | Assault, Withdraw, Hold order executors and an ammunition/reload/resupply model (realises B-030 proper), selected via `AskUserQuestion` over B-058 (agent movement speed) directly after TASK-046's acceptance. Three further `AskUserQuestion` rounds before drafting: bundle Hold/Assault/Withdraw with the ammo model in one task, not split (Dave's choice); the full staged Assault FSM from `docs/05` section 10's example, not a thin executor (Dave's choice); reload-from-stock ammo (not cooldown-only), applied to all combat fire (not `Suppress` alone), with resupply designed now via a new authored `ResupplyAreas` content type — all Dave's choices over the recommended narrower options. `PlayerIntent` gained `Hold`/`Assault`/`Withdraw` (bare `Cell` targets, the `MoveTo` precedent); `Hold`'s stage 2 redirects through a new `Appraisal.bestCoverNear` (reusing the existing threat-pressure function); `Assault` gets a stricter stage-4 resolve penalty and a staged `Commitment.AssaultStage` finite-state machine (`ApproachingStart`/`AwaitingSupport`/`Advancing`/`ClearingThreat`, a pure per-tick derivation, no stored wait-counter, no timeout — a stalled assault is a deliberate legible outcome); `Withdraw` gets a resolve bonus and its own `Withdrawing` commitment case. New `AgentState.Ammo: AmmoState` (`Ready`/`Reloading`, a new `Ammo.fs` leaf) gates every qualifying shot for every commitment alike; `DecisionReason.InsufficientAmmunition` gates `Suppress`/`Assault` stage 2 only; a new authored `WorldState.ResupplyAreas` (`ScenarioContent.Version` 2 -> 3) triggers instant resupply in State consequences, short-circuiting an in-progress reload. Three new events (`ReloadStarted`/`ReloadCompleted`/`AgentResupplied`), one new sparse overlay (`AgentAmmo`). `Canonical.FormatVersion` 10 -> 11, full re-pin, behaviour-neutral for every pre-existing entry. No new corpus entry — proof via 17 new `SimulationTests` facts directly on the phases and pure functions (the TASK-032/033 precedent), a deliberate, documented scope choice given the mechanism's already-precise unit-level coverage. Two minor deviations found during implementation: `Commitment.ofAgent`'s new context parameters needed a third call site beyond the drafted two (`commitmentAndLocalAction`, for the Assault freeze); a test-only reload-fact off-by-one against the `Casualty.tickBleedOut` precedent it copies (fixed the test, not the leaf). `dotnet build` both `.slnx` (main; Godot client Debug/Release) `0/0`; `dotnet test` `336/336`; `-- corpus` 16/16 (`--regenerate` twice byte-identical); `-- fixture`/`-- replay-file envelope-full` unaffected. No client-facing UI for the three new orders (the TASK-037 precedent); no player-facing ammo readout (F1 dev-overlay detail only, a new `ammo=` HUD field). Godot's `--selfcheck` pinned hashes recomputed via `dotnet fsi` against the built Core assembly (no Godot install in the implementing environment) — not independently re-run through the real Godot editor, flagged for Dave. | `tasks/TASK-047-*.md` created and implemented (`drafted -> review`); `docs/11_BACKLOG.md` B-030 row `proposed -> review`; `docs/04`/`docs/05`/`docs/07` sections updated; `src/CommandoWar.Client.Godot/README.md` new section; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-18-TASK-047-assault-withdraw-hold-and-ammunition.md) |
| 2026-09-18 | TASK-047 accepted | Recorded acceptance of TASK-047 (realises B-030 proper) after Dave asked for the three pinned Godot `--selfcheck` hashes to be independently re-run through the real Godot 4.7.2 editor, rather than trusted from the `dotnet fsi` computation alone, before accepting. Ran `dotnet build CommandoWar.Client.Godot.slnx -c Debug` then all three `--selfcheck` invocations headless: `SnapshotDemo.tscn` `MATCH 0x8E93B48D07AE9CBD` at tick 20; `CommandDemo.tscn` `MATCH 0xE661187DE95E92E6` at tick 20; `AppraisalDemo.tscn` (explicit scene path — `project.godot`'s `run/main_scene` now points at `CommandDemo.tscn`, not `AppraisalDemo.tscn` as the README's older TASK-029 section describes) `MATCH 0x194805888CBE240D` (format 11), agent 0 Refused / agent 1 Accepted. All three exit 0, confirming the byte-layout-only re-pin from `Canonical.FormatVersion` 10 -> 11 held. B-030 now fully done (TASK-037's `Suppress` slice plus this task's `Hold`/`Assault`/`Withdraw` and ammunition model); B-032 (demolition objective/extraction) is now unblocked. | `tasks/TASK-047-*.md` Status `review -> done`, Review section filled in; `src/CommandoWar.Client.Godot/README.md` TASK-047 section caveat updated to record the real-editor confirmation; `docs/ledger/2026-09-18-TASK-047-*.md` Review `Accepted: yes (2026-09-18)`; `docs/11_BACKLOG.md` B-030 row `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-18) | [detail](ledger/2026-09-18-TASK-047-assault-withdraw-hold-and-ammunition.md) |
| 2026-09-18 | TASK-048 | XCOM-style order-mode HUD icons for `Hold`/`Assault`/`Withdraw` (realises new backlog row B-059), chosen directly by Dave (no `AskUserQuestion` needed — he named it explicitly) over B-032/B-058, following up on TASK-047's own flagged client-UI gap. Two `AskUserQuestion` rounds confirmed four central decisions, all Dave's explicit choices over the recommended defaults: (1) XCOM-style HUD icons for order-mode selection, not keyboard chords; (2) `Hold` targets a clicked cell **and shows an outline of the hold area in the UI** (Dave's own addition); (3) real Kenney icon art (a live kenney.nl check), not drawn glyphs; (4) a new `OnOrderModeClick`/`OrderMode` pair on `IClientScene`, with C# owning the fixed icon rects and hit-testing entirely, checked first, falling through to the existing world-cell `OnClick` on a miss. Live-checked kenney.nl (downloaded and unzipped "Board Game Icons" v1.1, CC0, not assumed from memory): `shield.png`/`sword.png`/`arrow_right.png`/`arrow_counterclockwise.png` for Hold/Assault/Move/Withdraw. `CommandDemoScene.fs` gained `orderMode` state (armed by a HUD icon click, consumed on issuing an order — an XCOM ability-consumed-on-use idiom) dispatching `OnClick`'s existing empty-cell branch to `Command.hold`/`.assault`/`.withdraw` (already-existing TASK-047 constructors, zero `CommandoWar.Sim` change) alongside the existing `Command.moveTo`; a new `holdOutlineItems` traces the exact `AppraisalConfig.HoldCoverSearchRadius` (2) Chebyshev square `Appraisal.bestCoverNear` will search, drawn during hover while `Hold` is armed. `committedItems` needed zero change — every one of the four order types already writes `AgentSnapshot.Destination` identically (`Commitment.fs`'s documented precedent). `FSharpSceneHost.cs` gained the icon bar entirely in C# (fixed screen-space rects, hit-testing checked first in `_UnhandledInput`, drawn last in `_Draw` so it always sits on top) — HUD chrome, not world-grid content, per ADR-0004's per-concern table. `CommandDemoDrive.runScriptedSelfCheck` extended to also arm `Hold` via the icon-click path and issue a real `Hold(2,1)` for a second agent, proving the click dispatch (not just the sim side, already proven by TASK-047's `SimulationTests`) — a genuine new tick-by-tick trace, so `CommandDemo.tscn`'s `--selfcheck` hash moved (re-pinned `0x00D3D471EF7354BC`, confirmed `MATCH` through the real Godot 4.7.2 editor); `SnapshotDemo.tscn`/`AppraisalDemo.tscn` reconfirmed unchanged. No `CommandoWar.Sim`/`CommandoWar.Headless` change; `dotnet build` both `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `336/336` (unaffected); `-- corpus` `16/16` (unaffected); committed screenshot `docs/evidence/task-048-order-mode-hud.png` shows the icon bar, `Hold` armed with a gold highlight, and the hover-preview outline together. | `tasks/TASK-048-*.md` created and implemented (`drafted -> review`); `docs/11_BACKLOG.md` new B-059 row (`proposed -> review`); `src/CommandoWar.Client.Godot/README.md` new section; `art/LICENSE-THIRD-PARTY.md` new pack entry; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-18-TASK-048-order-mode-hud-for-hold-assault-withdraw.md) |
| 2026-09-18 | TASK-048 accepted | Recorded acceptance of TASK-048 (realises new backlog B-059) after review — the icon bar, armed-mode highlight, order dispatch, and Hold outline all confirmed working as designed; no further changes needed to accept. | `tasks/TASK-048-*.md` Status `review -> done`; `docs/ledger/2026-09-18-TASK-048-*.md` Review `Accepted: yes (2026-09-18)`; `docs/11_BACKLOG.md` B-059 row `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-18) | [detail](ledger/2026-09-18-TASK-048-order-mode-hud-for-hold-assault-withdraw.md) |
| 2026-09-18 | TASK-049 | Real per-agent movement speed (realises B-058), selected over B-053/B-054 and a not-yet-filed Suppress UI slice since it had been deferred twice, has no dependencies, and affects every scene's pacing. Three `AskUserQuestion` rounds confirmed the design: a per-unit-type stat table (Dave's choice over a bare field), static and non-canonical (the `Discipline`/`CommunicationAvailable` precedent), and a default that reproduces today's pace exactly with only `DemoScenario` authored slower. New `AgentState.MoveSpeed` (static, excluded from `Canonical.encode`), authored via a new `RawScenario.UnitTypes` table (`{ Id; MoveSpeed }`) referenced by `RawDeployment.UnitType` — four new `ScenarioError` cases (blank/duplicate unit-type id, non-positive `MoveSpeed`, an unknown reference), `ScenarioContent.Version` 3 -> 4. `Simulation.navigationAndMovement`'s edge-completion check cross-multiplies `MoveSpeed` against a new `Agent.MoveSpeedDefault` (= 2) rather than scaling the per-tick increment directly — a design correction found mid-implementation: the first attempt scaled the increment itself, which kept tick-completion *counts* identical for a default-speed agent but changed the *stored* intermediate `AgentState.Progress` values, caught by a `slow-terrain` corpus mismatch and two `SimulationTests` failures. The cross-multiplied form keeps the increment universally `Terrain.BaseMoveCost`, so a default-speed agent's comparison is algebraically identical to before (multiplying both sides of an inequality by the same constant), confirmed byte-identical against all 16 corpus/fixture entries (`cwheadless corpus`, `--regenerate`). `DemoScenario` (the scenario both `SnapshotDemo.tscn`/`CommandDemo.tscn` live-step) now authors a `"trooper"` unit type at half speed for all three agents, resolving Dave's "movement feels twice as fast as I thought it would" complaint (raised on TASK-046 review) without the client-only `simHz` halving or shared `Terrain.BaseMoveCost` bump both tried and reverted there. `content/diagnostics/demo.html` regenerated. Five new `ScenarioTests` facts (the four validation errors, one `UnitType -> Deployment.MoveSpeed` bake-through fact) and one new `SimulationTests` fact (a half-speed agent takes exactly twice the ticks of a default-speed one across an ordinary cell). `dotnet build` both `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `342/342` (+6); `-- corpus` 16/16 (`--regenerate` byte-identical); `-- replay-file envelope-full` unaffected; `dotnet list CommandoWar.Sim package` `FSharp.Core` only. `SnapshotDemo.tscn`'s `--selfcheck` hash moves (re-pinned `0xF422ACB8D5A86FF0`, computed via `dotnet fsi` against the built Core assembly — no Godot install in the implementing environment, not independently re-run through the real editor, flagged for Dave); `CommandDemo.tscn`'s hash is unchanged (`0x00D3D471EF7354BC` — its scripted orders complete within a handful of ticks even at half speed); `AppraisalDemoScene` unaffected (steps `exposed-approach`, not `DemoScenario`). | `tasks/TASK-049-*.md` created and implemented (`drafted -> review`); `docs/11_BACKLOG.md` B-058 row `proposed -> review`; `docs/04_SIMULATION_SPEC.md` sections updated; `src/CommandoWar.Client.Godot/README.md` new section; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-18-TASK-049-agent-movement-speed.md) |
| 2026-09-18 | TASK-050 | Disposable turn-resolution harness spike (new backlog B-060), requested directly by Dave, read-only against `Simulation.step`, while TASK-049 (B-058) was still `review` awaiting acceptance -- selected anyway as an explicit override of the project's usual accept-then-select-next ordering, per Dave's own fully-specified instruction (flagged in the task file). New `src/CommandoWar.Headless/TurnDemo.fs` (the `DemoScenario`/`PathDemo`/`LosDemo` hand-built-fixture precedent: five agents, no authored terrain, a hand-typed five-order `MoveTo`/`Suppress` command log across ticks 1/12/25) plus a new `cwheadless turn` subcommand printing every tick's `DiagnosticRender.Ascii` frame via the existing `DiagnosticRender.runFrames` -- the same mechanism `render demo`/`render los`/`render path` already use, just looped over the full 45-tick run instead of one `--tick`. No `CommandoWar.Sim`/`CommandoWar.Client.Godot` change, no ADR, no `Canonical.FormatVersion`/`ScenarioContent.Version` bump. First agent placement (suppressor outside `CombatConfig.WeaponRange`) produced a dead-end static run -- fixed by moving it within range, confirmed by inspecting the actual 46-frame output, not assumed. The corrected run shows the full intended narrative: `Suppress` accepted immediately (target already known), real symmetric engagement from tick 1, two casualties (`Incapacitated`, not dead) by tick 6, a suppression-band-flip reappraisal trigger flipping two `Refused` `MoveTo` orders to `Accepted` with no new order at tick 4, genuine tick-by-tick sub-cell movement to arrival, and a stable legible tick-45 end state. One open question surfaced and recorded, not fixed: an `Incapacitated` (not `Dead`) known contact still fully counts as a live threat for `Appraisal`'s route-exposure model, so agent 0's tick-25 reissued order stays refused for the rest of the run even after the hostile is down and its suppression has decayed. Action-point budgeting and an Overwatch commitment state remain explicitly unscoped follow-on work. `dotnet build` `0/0`; `dotnet test` `342/342` (unaffected); `-- corpus` `16/16` (unaffected); `dotnet list CommandoWar.Sim package` `FSharp.Core` only; `git status --porcelain` matches scope exactly. | `tasks/TASK-050-*.md` created and implemented (`none -> done`, self-verified, no formal review-then-accept cycle since this is a disposable spike, not a feature task); `docs/11_BACKLOG.md` new B-060 row (`done`); `docs/12` this row; `PROJECT_STATE.yaml active_work` updated (TASK-049 untouched, still `review`) | pending (Dave to review the spike's findings; TASK-049 acceptance separately still outstanding) | [detail](ledger/2026-09-18-TASK-050-turn-resolution-spike.md) |
| 2026-09-18 | TASK-049 accepted | Recorded acceptance of TASK-049 (realises B-058), asked for directly after a follow-up question about whether the sim's continuous-tick model could support an XCOM-style AP-budget turn (answered: partly -- per-soldier AP switching is close to free, alternating enemy turns is not, since `Combat`'s automatic engagement is symmetric and continuous regardless of "whose turn" it is). No further changes requested; the Godot `--selfcheck` re-run through the real editor already confirmed `MATCH` before this acceptance. | `tasks/TASK-049-*.md` Status `review -> done`, Review `Accepted: yes (2026-09-18)`; `docs/11_BACKLOG.md` B-058 row `review -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-18) | [detail](ledger/2026-09-18-TASK-049-agent-movement-speed.md) |
| 2026-09-18 | TASK-050 accepted | Recorded acceptance of TASK-050 (the disposable turn-resolution spike, new backlog B-060), in the same batch as TASK-049's acceptance (Dave chose "accept TASK-049/050 first, then pick" over three other next-work options via `AskUserQuestion`). No changes requested; the spike's two observations (resolved-turn feel already exists; an `Incapacitated`-but-not-`Dead` contact still fully counts as a live threat for route exposure) are noted for a later decision, not acted on now. | `tasks/TASK-050-*.md` Review `Accepted: yes (2026-09-18)`; `docs/11_BACKLOG.md` B-060 row annotated accepted; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-18) | [detail](ledger/2026-09-18-TASK-050-turn-resolution-spike.md) |
| 2026-09-18 | TASK-051 | Player-facing fog of war (realises B-055), selected via `AskUserQuestion` over B-053/B-054 and continuing straight into an AP-budget layer, as the largest ready item flagged by Dave multiple times. Three `AskUserQuestion` rounds confirmed the design, all recommended defaults: `CommandDemoScene` only (not the passive `SnapshotDemo.tscn`); a hollow outlined ring plus a text label, not a translucent recolour; opacity following `Contact.Confidence`'s band drop. Investigation before drafting found the task needed **no `CommandoWar.Sim` change at all**: `CommandDemoScene.fs` already builds a `Diagnostics.frameOf` frame every tick regardless of the `F1` toggle (the TASK-046 `AgentVitals` precedent), and that frame's existing `KnownContact` overlay already carries `WorldState.TacticalKnowledge` in full. `agentItems` now branches per hostile: no known-contact entry (never contacted, or expired past `PerceptionConfig.ExpireAfter`) draws nothing; an entry seen this exact tick renders exactly as before; a stale entry draws a new `Kind = 5` hollow ring (`RenderShared.cellRing`, a new `FSharpSceneHost.cs` `_Draw` case) plus a `"?"` label at `Contact.LastKnownCell`, opacity `0.9 * Confidence / ConfidenceFull`. Friendlies are never fogged. A real regression found and fixed during implementation, before it shipped: the first draft applied fog inside the `F1` developer overlay too, breaking TASK-043's own ground-truth-vs-known comparison tool (`docs/06` section 11) — fixed by bypassing fog whenever `devOverlay` is on, via a new shared `renderVitals` helper both branches call. Verified with a temporary `dotnet fsi` scratch probe (the TASK-042 precedent, two files, both removed after use) driving `CommandDemoScene` directly as a plain `IClientScene` — no Godot process needed. Confirmed the exact tick-by-tick sequence: hidden through tick 20 (never contacted) → live at the true position from tick 21 (in range) → a ghost ring at `alpha = 0.90` once retreated out of sight → `alpha = 0.68` exactly `PerceptionConfig.StaleAfter` (20) ticks after last seen → the ghost disappears entirely exactly `PerceptionConfig.ExpireAfter` (60) ticks after last seen, matching every `PerceptionConfig` constant precisely; a second probe confirmed the `F1` bypass (never-contacted hostile still draws live with the overlay on). No `CommandoWar.Sim`/`CommandoWar.Headless` change; `dotnet build` `0/0`; `dotnet test` `342/342` (unaffected); both scenes' `--selfcheck` hashes confirmed `MATCH` through the real Godot 4.7.2 editor (`SnapshotDemo.tscn` `0xF422ACB8D5A86FF0`, `CommandDemo.tscn` `0x00D3D471EF7354BC`, `AppraisalDemo.tscn` `0x194805888CBE240D`, all unaffected — render-only change, as expected); committed screenshot `docs/evidence/task-051-fog-of-war.png` (the same scripted priming sequence as every prior screenshot of this scene, now showing no hostile figure anywhere in frame). | `tasks/TASK-051-*.md` created and implemented (`proposed -> done`, self-verified); `docs/11_BACKLOG.md` B-055 row `proposed -> done`; `src/CommandoWar.Client.Godot/README.md` new section; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-18-TASK-051-player-facing-fog-of-war.md) |
| 2026-09-18 | TASK-051 accepted | Recorded acceptance of TASK-051 (realises B-055). No changes requested. | `tasks/TASK-051-*.md` Review `Accepted: yes (2026-09-18)`; `docs/11_BACKLOG.md` B-055 row annotated accepted; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-18) | [detail](ledger/2026-09-18-TASK-051-player-facing-fog-of-war.md) |
| 2026-09-18 | TASK-052 | Hover highlight and a larger fixed scale (realises B-053 and B-054), named directly by Dave ("pick B-053/B-054") after TASK-051's acceptance -- bundled into one task, the TASK-037 precedent for merging two small rows. One `AskUserQuestion` round resolved B-054's own undecided fork ("camera zoom/pan or a larger fixed scale"): Dave chose the recommended larger fixed scale, not interactive zoom/pan. A real correctness gap found while implementing B-053: `FSharpSceneHost.cs`'s mouse-motion handler called `ScreenToCell` directly, never adopting the click handler's own `TryHitAgentCircle`-first fallback (TASK-040's fix for the identical top-of-circle projection mismatch) -- fixed so hover resolution now matches click resolution exactly, then a new `hoverHighlightItems` (a `Kind = 5` ring, TASK-051's primitive, light-neutral tint) highlights a friendly agent's own cell on hover, never a hostile (fogged or not). For B-054: `TileW`/`TileH` doubled (`44x22 -> 88x44`, play area now ~62%/~50% of the window vs ~34%/~28% before); `Origin` moved `(450,60) -> (552,110)`, clearing the HUD label's worst-case three-line height with real margin -- fixing both halves of Dave's original TASK-043 complaint at once. `DrawTerrainTile` already scales from `TileW`/`TileH`; introduced named `agentRadius` (`10.0f -> 20.0f`) and `haloRadius` (`17.0f -> 34.0f`) constants replacing independent literals, so the agent figure, halo, TASK-051's fog-of-war ghost ring, and the new hover ring all size themselves consistently off one source of truth. Verified with a temporary `dotnet fsi` scratch probe (the TASK-042/051 precedent): the hover ring appears only over a friendly's own cell (never empty terrain, never a hostile); re-ran the TASK-051 fog-of-war probe unchanged and confirmed the identical tick-by-tick sequence, proving the shared-constant refactor altered no behaviour. No `CommandoWar.Sim`/`CommandoWar.Headless` change; `dotnet build` `0/0`; `dotnet test` `342/342` (unaffected); both scenes' `--selfcheck` hashes confirmed `MATCH` through the real Godot 4.7.2 editor (unaffected, as expected for a render-only change); committed screenshot `docs/evidence/task-052-scale-and-hover.png` (captured with `--dev-overlay` on, showing the worst-case HUD height with no overlap against the new scale). | `tasks/TASK-052-*.md` created and implemented (`proposed -> done`, self-verified); `docs/11_BACKLOG.md` B-053 and B-054 rows `proposed -> done`; `src/CommandoWar.Client.Godot/README.md` new section; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-18-TASK-052-hover-highlight-and-scale-polish.md) |
| 2026-09-18 | TASK-052 accepted | Recorded acceptance of TASK-052 (realises B-053 and B-054). No changes requested. A separate nit raised on acceptance -- selection/order-mode UI in `CommandDemoScene.fs` never deactivates when its agent dies ("the cursor which has move active or whatever should be deactivated") -- recorded as new backlog row B-061, not designed or fixed by this task. | `tasks/TASK-052-*.md` Review `Accepted: yes (2026-09-18)`; `docs/11_BACKLOG.md` B-053/B-054 rows annotated accepted, new B-061 row filed; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-18) | [detail](ledger/2026-09-18-TASK-052-hover-highlight-and-scale-polish.md) |
| 2026-09-18 | TASK-053 | Selection/order-mode deactivation on casualty (realises B-061), scoped and drafted directly per Dave's instruction on selecting it. Two `AskUserQuestion` rounds (the second overriding the first's initial "clear `selected`" default) resolved the design after Dave's own free-text framing on round 1's third question ("you should be able to select to view status when that is an option, but all action commands would be disabled"): both `Dead` and `Incapacitated` disable action; `selected` never auto-clears from a vitals change (status-view mode stays available); `orderMode` auto-disarms to `0` the instant its selected agent is not `Alive`; the hover route preview is suppressed while a non-`Alive` agent is selected. `friendlyAt`'s existing vitals-blind selection filter needed no change -- the resolved design confirmed it as the intended status-view mechanism, not the "second gap" the backlog row originally described. New `vitalsOf` helper (`CommandDemoScene.fs`) extracted from `renderVitals`'s existing `AgentVitals`-overlay lookup, shared by the new gating; new `syncOrderModeToSelection`, called from `Update` (post-`stepOnce`) and `OnClick`'s selection branch; `OnClick`'s order-issuing guard and `OnHover`'s `previewPath` both gated on `Casualty.isAlive`. No `CommandoWar.Sim`/`CommandoWar.Headless` change. Verified with two temporary `dotnet fsi` scratch probes (removed after use) driving real, unscripted combat in `DemoScenario` (agent 0 moved into the hostile's weapon range) until it went `Incapacitated` (tick 24) then `Dead` (tick 83, exactly `CasualtyConfig.BleedOutTicks` later): `orderMode` armed before the transition disarmed in the identical tick the agent went `Incapacitated`; a click attempting to issue an order while non-`Alive` left `orderMode` unconsumed (the issuing branch never entered); hover preview absent at both states; `selected` still read `agent 0` at `Dead`; re-selecting `Alive` agent 1 immediately restored normal behaviour. `dotnet build` both `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `342/342` (unaffected); all three scenes' `--selfcheck` hashes confirmed `MATCH` through the real Godot 4.7.2 editor, unchanged from TASK-052's pins (`SnapshotDemo.tscn` `0xF422ACB8D5A86FF0`, `CommandDemo.tscn` `0x00D3D471EF7354BC`, `AppraisalDemo.tscn` `0x194805888CBE240D`). | `tasks/TASK-053-*.md` created and implemented (`proposed -> review`, self-verified); `docs/11_BACKLOG.md` B-061 row `proposed -> done`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-18-TASK-053-selection-deactivation-on-casualty.md) |
| 2026-09-18 | TASK-053 accepted | Recorded acceptance of TASK-053 (realises B-061). No changes requested. | `tasks/TASK-053-*.md` Review `Accepted: yes (2026-09-18)`; `docs/11_BACKLOG.md` B-061 row annotated accepted; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-18) | [detail](ledger/2026-09-18-TASK-053-selection-deactivation-on-casualty.md) |
| 2026-09-19 | TASK-054 | Agent sprite facing/orientation and audio-localised threat indicator (realises B-052 and B-056, bundled per the TASK-037/052 precedent), chosen from the two unblocked backlog paths Dave was asked to pick between via `AskUserQuestion` (B-016b communication mechanics vs. this small-client-polish bundle). Both rows carried Dave's own "do not guess past this" design forks. B-052: `AskUserQuestion` confirmed movement-direction-only facing (no threat-facing); a live re-check of kenney.nl (downloaded and unzipped both "Isometric Miniature Prototype" and "Isometric Miniature Dungeon") found the whole "Isometric Miniature" family shares one rig — a single non-rotating `Idle0` plus a 10-angle `Run0..Run9` cycle, no pack has a rotating idle pose. Dave flagged the first "freeze on a Run frame" compromise as unsatisfying and asked for a further asset search; a web search for a free CC0 pack combining this project's rendered art style with a genuine multi-angle idle found none (the only true 8-directional-idle CC0 packs are pixel art — a style clash against the existing terrain/prop art), so Dave confirmed staying in-style with the original compromise. New `agent_human_facing0..9.png` (`Run0..9` cropped to the union of all 10 frames' own alpha bboxes, `agent_human.png`/`Idle0` retired), a new pure `RenderShared.facingBin`, applied identically in both `DemoRenderScene` and `CommandDemoScene` through the one shared `FSharpSceneHost` draw path (a judgement call, not asked separately, since it follows from one host drawing every scene's agents the same way). B-056: `AskUserQuestion` confirmed the trigger (`FireLine`, shooter `Hostile` with no `KnownContact` at all) and the visual (a screen-edge-ish `Kind = 2`/`3` line-and-label cue, no new `DrawItem.Kind`). **A real correction found during implementation**: the trigger check must read the friendly squad's known-contact set from *before* the tick's own `Simulation.step`, not the post-tick `devFrame` — Perception runs ahead of Combat in phase order, so the post-tick set already reflects any contact that same tick's Perception phase just registered, which would suppress the cue on exactly the "just came into view and fired" tick B-056 exists to cover (`CombatConfig.WeaponRange` (7) sits strictly inside `PerceptionConfig.SightRange` (10), so this is the common case, not an edge case). No `CommandoWar.Sim`/`CommandoWar.Headless` change either feature. Verified with `dotnet fsi` scratch probes (removed after use): `facingBin` checked as a pure function across all 8 primary directions plus the "no destination"/"already arrived" freeze cases; a live scripted `MoveTo(3,0)` confirmed the emitted facing texture tracks the real path; a second probe drove a friendly into a hostile's weapon range and confirmed real combat (`state.Random.Draws` increasing) produced exactly one audio cue, first appearing the same tick as the first shot, unchanged in count through three further ticks of continued fire once the hostile became known, and absent before contact. `dotnet build` both `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `342/342` (unaffected); `-- corpus` `16/16` (unaffected); all three scenes' `--selfcheck` hashes confirmed `MATCH` through the real Godot 4.7.2 editor (unaffected, as expected for a presentation-only change); committed screenshot `docs/evidence/task-054-agent-facing.png` (the standard scripted priming, showing both friendlies now rendering a running-cycle facing frame in place of the old single idle pose — the priming never reaches combat range, so the audio cue's evidence is the scratch-probe trace instead, the TASK-053 precedent for a logic-only feature). | `tasks/TASK-054-*.md` created and implemented (`none -> review`, self-verified); `docs/11_BACKLOG.md` B-052 and B-056 rows `proposed -> review`; `src/CommandoWar.Client.Godot/art/LICENSE-THIRD-PARTY.md` updated; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-19-TASK-054-agent-facing-and-audio-threat-indicator.md) |
| 2026-09-19 | TASK-054 review round 1 | Dave live-tested B-052/B-056 together and found two real facing defects, both fixed; B-056 untouched. Report 1: "facing direction doesnt seem to be any better either" — `RenderShared.facingBin` was binning the raw world-grid angle, but this game's isometric projection is a skew (a world-diagonal move projects to a pure horizontal/vertical screen move), so diagonal movement binned to the wrong on-screen rotation; fixed by projecting the heading through the same skew `CellToScreen` applies before binning. Report 2, after a compass-comparison image built on the fixed math: "looks like part of a running animation, not different angles of ONE frame" — correct: `Human_0_Run0..Run9` (the source this task's design session used) genuinely is a running animation, orientation and gait phase changing together, so no frozen frame could ever read as a clean facing pose regardless of the angle math. Dave then pointed directly at his own local copy of the pack's `Information.png`, which documents the convention neither kenney.nl inspection this session nor TASK-041's original survey caught: `Human_0..7` **are** 8 rotations of one idle pose (`0=N,1=NE,2=E,3=SE,4=S,5=SW,6=W,7=NW`, clockwise, screen-relative) — confirmed by comparing all eight `Human_N_Idle0.png` side by side. Replaced all ten `agent_human_facing*.png` files in place with the eight true `Idle0` rotations (cropped to their own shared alpha-bbox union); `facingBin` reduced to 8 bins/`45°` with its zero-reference realigned so a bin number is directly a `Human_N` index; `FSharpSceneHost.cs`'s `AgentFacingTextures` shrunk to 8 entries; `LICENSE-THIRD-PARTY.md` records both the withdrawn and corrected approach. Re-verified after each fix: `dotnet build` `0/0`; `dotnet test` `342/342`; both scenes' `--selfcheck` reconfirmed `MATCH` through the real Godot 4.7.2 editor. Two comparison images generated directly from the art files via `PIL` (no Godot needed) and shown to Dave inline, not committed separately. | `tasks/TASK-054-*.md` `Outcome` extended with a "Review round 1" section; `docs/11_BACKLOG.md` B-052 row text corrected to describe the final approach; `docs/ledger/2026-09-19-TASK-054-*.md` "Review round 1" section added; `src/CommandoWar.Client.Godot/art/LICENSE-THIRD-PARTY.md` rewritten; `docs/12` this row | pending (facing corrected; B-052/B-056 still awaiting Dave's final acceptance) | [detail](ledger/2026-09-19-TASK-054-agent-facing-and-audio-threat-indicator.md) |
| 2026-09-19 | TASK-055 | Perception stops observing a downed (Incapacitated/Dead) agent (new backlog B-062), diagnosed directly from Dave live-testing TASK-054's audio-cue feature ("after the enemy agent is incapacitated or killed the other agent refuses order due to threat when there is none") -- traced to a gap already surfaced and left unfixed in TASK-050's ledger. `Perception.visibleContactsFor` never checked `Vitals`, and a corpse is never removed from `WorldState.Agents` (TASK-045), so it stayed "seen" at full confidence forever, keeping `Appraisal.cellPressure`/`.routeExposure` treating it as a live threat indefinitely. `AskUserQuestion` confirmed the fix location (`Perception` stops observing a non-`Alive` agent, reusing the existing `StaleAfter`/`ExpireAfter` decay path, over `Appraisal` reading true current `Vitals` directly) and the timing (fix now, since it was blocking further live testing). One added condition (`&& Casualty.isAlive other.Vitals`); symmetric, so a downed friendly also stops being a target for the Hostile side's own picture for free. No `Canonical.FormatVersion` bump, no new field/event. New `SimulationTests` fact proves it directly. 5 of 16 corpus entries legitimately re-pinned (`perception-contact`, `exposed-approach`, `suppress-relieves-exposure`, `canonical-refusal-and-correction`, `casualties-succession-and-squad-failure`); two `DiagnosticsTests` golden pairs regenerated via a temporary `dotnet fsi` script (`cwheadless render` cannot reproduce an arbitrary corpus entry's own scenario, only the shared `Fixture`), every diff inspected line by line and explained (a `seen tick`/stress value freezing, or a contact finally expiring past `ExpireAfter` once every hostile in the scenario is `Dead`, correctly emitting `ContactExpired`); `demo.html` confirmed byte-identical (`DemoScenario` has no combat in 20 ticks). `dotnet build` `0/0`; `dotnet test` `343/343` (+1); `cwheadless corpus` 16/16 after `--regenerate`; both Godot scenes' `--selfcheck` hashes confirmed `MATCH` through the real Godot 4.7.2 editor, unaffected. | `tasks/TASK-055-*.md` created and implemented (`none -> review`, self-verified); `docs/11_BACKLOG.md` new B-062 row; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-19-TASK-055-perception-stops-observing-downed-agents.md) |
| 2026-09-19 | TASK-056 | Corrects backlog B-052 a second time: Dave live-tested TASK-054's facing fix and found it still roughly 30 degrees off, plus no run-cycle animation while moving (a single frozen idle frame the whole transit). Facing: re-examined the Kenney pack's own `Information.png` directly rather than re-deriving on paper again -- its diamond legend labels the tile's four *edges* `N`/`E`/`S`/`W` (not vertices), and the centre-to-edge-midpoint geometry shows the displacement to a tile's `N`-neighbour is exactly a world **pure-axis** move, not a world-diagonal one. `RenderShared.facingBin` had this backwards (confirmed directly from its own prior doc comment), assigning the four Kenney cardinal poses to the four world-diagonal moves instead -- every one of the 8 primary directions landed exactly one 45-degree bin past its correct target under the old uniform-sector rounding, which is why it read as "close but off" rather than random. Rewrote `facingBin` as a nearest-reference-vector match (cosine similarity against the 8 primary directions' own screen-projected vectors, since the 8 target angles are not evenly 45-degrees apart under this anisotropic 2:1 projection) instead of an angle-sector round -- correct by construction for the 8 primary directions and, unlike a fixed-offset patch, also correct for an arbitrary `(dx,dy)` heading (`Destination` is the agent's overall target, not its next path step). Animation: added all 80 `Human_0..7_Run0..9` frames (`agent_human_run{dir}_{frame}.png`, Kenney's genuine run-cycle art, already downloaded); all 88 idle+run textures re-cropped to one shared global alpha-bbox `(66,309)-(190,466)` (found via a `PIL` scan) rather than each pose's own tightest crop, so switching between an idle and a running frame never pops the drawn figure's on-screen size (`DrawAgentFigure` derives height from each texture's own aspect ratio at a fixed on-screen width). A new `RenderShared.runFrameIndex`/`RunFrameSeconds` plus a per-scene wall-clock `runClock` (gated to real ticks advancing -- `not paused` in `CommandDemoScene`, so the run cycle never animates while the sim itself is frozen) drive a new `DrawItem.Cx2` run-frame index (a `-1` sentinel freezes on the idle pose) while `Destination <> Some Position`, applied identically in `DemoRenderScene` and `CommandDemoScene`; `Incapacitated` always passes `-1` regardless of any stale `Destination` left over from before going down. **Verified live this round, not just on paper**: a temporary command-line screenshot probe (`--facing-probe-dir`, `FSharpSceneHost.cs`, fully removed before finishing) drove `CommandDemoScene` through the real Godot 4.7.2 editor (windowed, not headless -- screenshot capture needs a real framebuffer) for all 8 primary directions from an open DemoScenario cell; each capture compared directly against `Information.png`'s own back-view/front-view pattern (N/W/NW cluster reads as back-view, E/S/SE cluster as front-view, matching the diamond legend's upper-edge/lower-edge split exactly) -- confirmed consistent, not just numerically self-consistent. A second probe variant (also removed) drove a long, obstacle-clear traversal and captured two mid-transit screenshots showing genuinely different run-cycle limb positions, plus a later screenshot showing the frozen idle pose unchanged across three separate post-arrival ticks. One test-setup gotcha found and worked around, not a product bug: the first traversal path ran straight through agent 1's permanently stationary cell, and the two agents' route-contention logic stalled agent 0 in place indefinitely -- switched the probe to an unobstructed row. No `CommandoWar.Sim`/`CommandoWar.Headless` change. `dotnet build` both `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `343/343` (unaffected); all three scenes' `--selfcheck` hashes confirmed `MATCH` through the real Godot 4.7.2 editor (unaffected, as expected for a presentation-only change); `git status --porcelain` matches this task's allowed scope exactly, no probe code left behind. B-052 stays `review`, not `done` -- awaiting Dave's live confirmation of both fixes. | `tasks/TASK-056-*.md` created and implemented (`none -> in progress`, self-verified); `docs/11_BACKLOG.md` B-052 row extended with the second correction round; `docs/evidence/task-056-facing-compass-check.png`, `docs/evidence/task-056-run-animation-check.png`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-19-TASK-056-agent-facing-correction-and-run-animation.md) |
| 2026-09-19 | TASK-056 review round 1 | Dave live-tested the corrected facing and new run animation: "its better but i dont think it always stay the correct facing. also theres some sort of jerkyness to the movement, it also also moving really fast too." Both traced to one shared root cause. `AgentState.Position` only changes once a whole grid edge completes (`Progress` accumulates across several mid-edge ticks first, TASK-018), but rendering lerped screen position between the *previous* and *current tick's* discrete `Position` alone -- identical on every mid-edge tick, so the figure sat frozen for most of an edge then snapped across the whole cell inside one tick's 50ms window: jerky, and reading as fast since the entire displacement compresses into that short burst. The same crow-flies-to-final-`Destination` assumption (not the immediate next path step) explained the facing complaint: correct on a straight leg, visibly wrong the instant a route bends or the player redirects mid-route. Fixed by computing each moving agent's true next path cell once per tick (`Pathfinding.find` from `Position` toward `Destination`, the `committedItems` route-preview precedent, cached per tick rather than recomputed every render frame) and feeding it into `facingBin` in place of the raw `Destination`, and using it as the render lerp *target* in place of `Position`; the lerp *fraction* comes from the already-canonical, exact `AgentSnapshot.Progress` normalised against a per-agent `edgeTickEstimate` *learned* from the most recently completed edge (the exact tick-count an edge takes depends on `Terrain.moveCost` and the agent's own non-exposed `MoveSpeed`, neither fully available client-side without a `CommandoWar.Sim` change, deliberately not made) -- self-corrects every edge regardless of a wrong guess, since `Position` itself always snaps to the true cell the instant an edge genuinely completes. Applied identically to `CommandDemoScene` and `DemoRenderScene`. Verified live via a temporary `--motion-probe` burst-screenshot hook (removed before finishing): tracked the moving agent's on-screen centroid across a 24-frame, 0.12s-interval burst (Python/`PIL`, inverting `FSharpSceneHost.cs`'s own `CellToScreen`) and confirmed continuously, monotonically increasing fractional cell coordinates -- no flat-then-jump pattern, the opposite of the old hold-then-snap; the same burst drove a real mid-route direction change (south then, after 1s, redirected east) and confirmed the rendered facing pose visibly switches to match the new heading. One test-setup gotcha repeated and fixed, not a product bug: the first probe route again passed close enough to `DemoScenario`'s hostile to trigger real combat mid-test (the identical mistake made once already earlier this same task) -- moved the route outside `CombatConfig.WeaponRange` entirely. No `CommandoWar.Sim`/`CommandoWar.Headless` change. `dotnet build` both `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `343/343` (unaffected); all three scenes' `--selfcheck` hashes reconfirmed `MATCH` through the real Godot 4.7.2 editor, unaffected. Flagged for Dave, not changed: whether the underlying grid movement *pace* (`DemoScenario`'s Trooper half-speed, TASK-049) is still too fast once the jerkiness is gone -- that constant lives in `CommandoWar.Headless` (content authoring, not rendering), outside this task's declared forbidden scope. | `tasks/TASK-056-*.md` "Review round 1" section added; `docs/ledger/2026-09-19-TASK-056-*.md` "Review round 1" section added; `docs/11_BACKLOG.md` B-052 row extended; `docs/evidence/task-056-round1-facing-on-turn.png`, `docs/evidence/task-056-round1-smoothness-sequence.png`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending (both fixes plus this round's two further fixes still awaiting Dave's final acceptance) | [detail](ledger/2026-09-19-TASK-056-agent-facing-correction-and-run-animation.md) |
| 2026-09-19 | TASK-056 review round 2 | Dave tried again: "i just tried again and it seemed the same, did you not fix this?" -- plus two new, specific reports: the selection halo "jumps between cells", and the run animation "runs continously if the agents get stuck on the same cell". Ruled out a stale build first (rebuilt both `.slnx` clean). Root cause: round 1's fix only updated the real figure's own render position -- `haloItems` (the selection halo) still read the raw, discrete `Position` directly via `agentPosition`, so it kept snapping a whole cell at a time on every edge completion even though the figure under it had already smoothed out; since the halo is the single largest, most attention-grabbing element while watching a selected agent move, this alone made the whole fix read as unchanged. Separately, `isMoving` was still just "has an unmet `Destination`", true forever for an agent stalled by a route-reservation contest (`Simulation.navigationAndMovement` freezes such an agent's `Progress`, never increments it, while `Destination` stays set) -- so the run-cycle animation played forever on a figure that was not actually advancing, and the render-time `alpha` ramp kept getting credited to an edge that would never complete, producing a small creep-then-snap-back sawtooth on top. Fixed with one new `stalled: Map<int, bool>` (recomputed fresh each tick: compares an agent's `Progress` entering the tick against its value after -- unchanged means no real progress happened) and one new shared `renderPos` helper (`CommandDemoScene.fs`) that the real figure and the selection halo both now call, so they cannot disagree; while `stalled`, the render position freezes exactly (no `alpha` credit, `Progress / estTicks` only) and `isMoving` (hence the run cycle) goes false. Also fixed a one-tick blind spot in `stalled` itself: `prevProgress` now seeds from each agent's real starting `Progress` in `Ready()` (previously `Map.empty`, defaulting to a `-1` sentinel that could misread an order that stalls immediately, exactly this round's own verification scenario, as "made progress" purely from the sentinel). Applied identically to `DemoRenderScene.fs` (no halo there, but the identical `stalled` freeze). Verified live through the real Godot 4.7.2 editor with a temporary, fully-removed `--stall-probe` burst-screenshot hook and a purpose-built scenario (not a repeat of round 1's probe): agent 0 issued a real, unblocked `MoveTo(5,0)` first (confirming the halo tracks correctly during genuine motion), then redirected into agent 1's own permanently-occupied cell `(0,1)`, forcing a genuine stall. A pixel-diff across 16 consecutive captures spanning 2.4 real seconds of the stalled state (excluding the HUD text strip, which legitimately changes every tick) shows zero changed pixels -- the figure, halo, and run-cycle frame all fully static; the pre-fix code would necessarily have shown dozens of run-cycle texture swaps and continuous sawtooth drift across that same span (`docs/evidence/task-056-round2-halo-and-stall-freeze.png`). One unexplained, single-frame, transient muted-colour render of the moving figure was observed around the redirect tick and investigated directly, not waved off: ruled out a vitals/combat misread (`state.Random.Draws` stayed `0` throughout; `Diagnostics.agentVitalsOverlays` confirmed unconditional, one entry per agent every tick, never sparse) and confirmed the `R`/`G`/`B` fields feeding that draw call are a compile-time constant untouched by this round's diff; self-corrected within one frame and never recurred through the full 2.4s frozen window that follows. Flagged for awareness, not resolved, does not affect either reported defect. No `CommandoWar.Sim`/`CommandoWar.Headless` change. `dotnet build` both `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `343/343` (unaffected); all three scenes' `--selfcheck` hashes reconfirmed `MATCH` through the real Godot 4.7.2 editor, unaffected; `git status --porcelain` confirms the temporary probe hook is not present in the final diff. | `tasks/TASK-056-*.md` "Review round 2" section added; `docs/ledger/2026-09-19-TASK-056-*.md` "Review round 2" section added; `docs/11_BACKLOG.md` B-052 row extended a third time; `docs/evidence/task-056-round2-halo-and-stall-freeze.png`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending (all three rounds' fixes still awaiting Dave's live acceptance) | [detail](ledger/2026-09-19-TASK-056-agent-facing-correction-and-run-animation.md) |
| 2026-09-19 | TASK-056 accepted | Dave live-tested review round 2's halo/stall-freeze fix and confirmed: "ok that looks vetter". B-052 now `done` across all three correction rounds this session (TASK-054's original facing rewrite, TASK-056's facing/run-animation redo, and TASK-056 review rounds 1 and 2's smoothing and freeze fixes). No further changes requested. | `tasks/TASK-056-*.md` Status -> `done`, Review section closed; `docs/11_BACKLOG.md` B-052 row -> `done`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | accepted | [detail](ledger/2026-09-19-TASK-056-agent-facing-correction-and-run-animation.md) |
| 2026-09-19 | TASK-054 accepted | Dave tried B-056 (the audio-localised threat cue) live in the running game and confirmed it, alongside the already-corrected B-052 facing (accepted separately via TASK-056 above). No changes requested; B-056 was never touched by any of TASK-056's three correction rounds. | `tasks/TASK-054-*.md` Status -> `done`, Review section closed; `docs/11_BACKLOG.md` B-056 row -> `done`; `docs/ledger/2026-09-19-TASK-054-*.md` Review section closed; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-19) | [detail](ledger/2026-09-19-TASK-054-agent-facing-and-audio-threat-indicator.md) |
| 2026-09-19 | TASK-055 accepted | Dave tried the B-062 fix (Perception stops observing a downed agent) live in the running game and confirmed it — the gap he had hit blocking B-056's own live test earlier in the session is resolved. No changes requested. | `tasks/TASK-055-*.md` Status -> `done`, Review section closed; `docs/11_BACKLOG.md` B-062 row -> `done`; `docs/ledger/2026-09-19-TASK-055-*.md` Review section closed; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-19) | [detail](ledger/2026-09-19-TASK-055-perception-stops-observing-downed-agents.md) |
| 2026-09-19 | TASK-057 | Divergence-visualisation renderer (realises B-050), selected via `AskUserQuestion` over B-016b and B-043 -- Dave asked for B-050 then B-016b, both this same session. Design was already fully specified by `docs/notes/2026-09-06-tooling-and-debug-display.md` section 2, so no further `AskUserQuestion` rounds were needed; three judgement calls made directly (two frames only, not a multi-tick diff -- the note's own minimal ask and `Divergence.diagnose`'s existing "first divergence only" scope both point there; a `section`-text prefix distinguishes reference/candidate rather than a `DiagnosticFrame` schema change; SVG writes two files since one `<svg>` document cannot hold two root elements). New `Overlay.Divergence of section: string * agents: AgentId[]` (`Diagnostics.fs`), caller-supplied only, the `SightRay`/`PlannedPath` precedent. `Divergence.fs` gained `diagnoseDetailed` (extracted `runBoth` from `diagnose`'s existing two `Replay.run` calls; `diagnose` itself unchanged) returning both full `ReplayOutcome`s so a caller can index `TickStates` at the divergent tick. New `cwheadless render-divergence <log-a> <log-b> [--ticks N] [--format ascii\|svg\|html] [--out PATH]`: on `Match`/`TruncatedRun` prints the same text `compare` already does and exits without rendering; on `Diverged` builds both frames via `Diagnostics.frame`, attaches a `Divergence` overlay to each (`section` prefixed `reference:`/`candidate:`, `agents` parsed from a `Canonical.firstDifferingSection` `Agent[N]` label only). `Svg` draws a thick solid magenta square around the named agent's cell (deliberately distinct from every existing overlay glyph) plus a footer line; `Html`'s `annotations` panel gets an optional banner. A full-tree grep before drafting (not left to the compiler) found every other exhaustive `Overlay`-match site: `AppraisalDemo.fs`'s `unhandled` tracker and four `DiagnosticsTests.fs` sparse-filter blocks needed one new arm each; the Godot client's `RenderShared.fs` needed none (its only match is already a wildcard). No `Canonical.FormatVersion` bump, no existing hash re-pinned -- a pure observer addition. New `DiagnosticsTests` fact reuses `ReplayTests.fs`'s own proven tick-1 `Agent[0]` divergence fixture and asserts the render path end to end in all three formats; a manual CLI smoke test on two hand-built `.cwlog` files (removed after use) confirmed `MATCH` (exit 0, no render) and `Diverged` (exit 3, correct section/agent text) in `ascii`/`svg`/`html`. `dotnet build` all three `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `344/344` (+1); `cwheadless corpus` `16/16` (unaffected). | `tasks/TASK-057-*.md` created and implemented (`none -> review`, self-verified); `docs/11_BACKLOG.md` B-050 row `proposed -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-19-TASK-057-divergence-visualisation-renderer.md) |
| 2026-09-19 | TASK-058 | Communication range, delivery delay, dynamic jamming, and radio-destroyed (realises B-016b), selected via `AskUserQuestion` after TASK-057, per Dave's own instruction to do both this session. Five `AskUserQuestion` rounds confirmed the design (full bundle; a new authored HQ entity for range; a flat delivery-delay constant; a new authored Jammer entity; radio-destroyed as a new independent field set by a combat-hit chance); a sixth round confirmed gating the whole feature set behind a scenario authoring an HQ, after investigating that making it unconditional would shift every existing corpus entry's RNG stream and delivery timing -- a real behaviour change, not a byte-layout one. New leaf `Communication.fs` (`CommsConfig`, `.available`/`.jammed`/`.reason`); new `Scenario.Headquarters`/`.Jammers` (`ScenarioContent.Version` 4 -> 5) with matching validation; new `AgentState.RadioDestroyed`/`.PendingDelivery`, both genuinely canonical (`Canonical.FormatVersion` 11 -> 12). `Simulation.communication` rewritten: availability now goes through `Communication.available` (static blackout **and**, only under an authored HQ, range/jamming/radio-destroyed); an in-range order under an HQ is stashed in `PendingDelivery` (due tick = accept + `DeliveryDelayTicks`) instead of applied immediately, with a new second pass delivering or dropping every due one, re-checked at delivery time; a superseding command silently drops an in-flight one. `Simulation.combat` gained an independent radio-destroy roll per qualifying hit, gated on the HQ opt-in so no existing entry's RNG stream shifts. New sparse `Overlay.AgentRadioLost`/`.AgentPendingDelivery` (named distinctly from the `EventBody.AgentRadioDestroyed` event, the `OrderAppraisal`/`OrderDisposition` precedent) satisfy the diagnostics-extension rule for the two new per-tick canonical fields; `Headquarters`/`Jammers` themselves get no overlay (static authored geometry, the `ResupplyAreas` precedent -- confirmed by inspection that TASK-047 never gave `ResupplyAreas` one either). **A real bug found and fixed during implementation**: `commandIntake`'s `holdsCommand` checked only `Order`/`OrderQueue`, so a `Cancel` targeting an in-flight `PendingDelivery` order was rejected `UnknownTargetCommand` before `communication` ever ran -- caught immediately by the new in-flight-cancellation test, not discovered later. A second deviation: the first `applyDelivered` refactor silently dropped the pre-existing `OrderQueued` event for an `Append` landing behind an active order, caught by `cwheadless corpus`'s own event-count check on the pre-existing `order-queue-stacking-and-cancellation` entry. Every one of the 16 existing corpus entries, `Fixture`, and `envelope-full` re-pinned byte-layout-only (verified diff by diff, not assumed) since none of them author a `Headquarters`. No new corpus entry -- a deliberate deviation from the task's own original plan, given the fragility of authoring one scenario to cleanly exercise all four mechanics with correct tick timing; full coverage instead via 10 new `SimulationTests` facts and 8 new `ScenarioTests` validation facts (the TASK-032/033 direct-phase-test precedent), plus one `DiagnosticsTests` fact proving both new overlays render. `dotnet build` all three `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `362/362` (+18); `cwheadless corpus` `16/16` (unaffected); `dotnet list CommandoWar.Sim package` `FSharp.Core` only. | `tasks/TASK-058-*.md` created and implemented (`none -> review`, self-verified); `docs/11_BACKLOG.md` B-016b row `proposed -> review`; `docs/12` this row + Pinned facts (`Canonical.FormatVersion`, `ScenarioContent.Version`, green-test count) refreshed; `PROJECT_STATE.yaml active_work` updated | pending | [detail](ledger/2026-09-19-TASK-058-communication-range-delay-jamming-and-radio-destroyed.md) |
| 2026-09-19 | TASK-057 accepted | Dave accepted TASK-057 (realises B-050) on the self-verification evidence -- no client UI in this task, a headless CLI tool. No changes requested. | `tasks/TASK-057-*.md` Status -> `done`, Review section closed; `docs/11_BACKLOG.md` B-050 row -> `done`; `docs/ledger/2026-09-19-TASK-057-*.md` Review section closed; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-19) | [detail](ledger/2026-09-19-TASK-057-divergence-visualisation-renderer.md) |
| 2026-09-19 | TASK-058 accepted | Dave accepted TASK-058 (realises B-016b) on the self-verification evidence -- no client UI in this task, Sim-side only. No changes requested. Both tasks Dave asked for this session (B-050 then B-016b) are now done. | `tasks/TASK-058-*.md` Status -> `done`, Review section closed; `docs/11_BACKLOG.md` B-016b row -> `done`; `docs/ledger/2026-09-19-TASK-058-*.md` Review section closed; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-19) | [detail](ledger/2026-09-19-TASK-058-communication-range-delay-jamming-and-radio-destroyed.md) |
| 2026-09-19 | TASK-059 | Formation slots (realises B-011d), selected via `AskUserQuestion` over B-043 (Mibo 5.x reconsideration spike, still blocked on Dave's own interactive ADR-0001 review-trigger-1 measurement). Three further `AskUserQuestion` rounds confirmed the design before drafting: no new order type or multi-agent dispatch -- a formation changes only how a formationed agent's own accepted `MoveTo` target resolves (anchor = that agent's own ordered cell, plus its authored slot offset); a blocked or occupied slot cell falls back to the nearest passable/free cell in a bounded radius (`Appraisal.bestCoverNear`'s own precedent), never a hard order failure; both `Friendly` and `Hostile` agents may be authored into a formation (Dave's own choice, overriding the friendly-only recommended default); formations are authored per-scenario (`RawScenario.Formations`, the TASK-058 `Jammers`-table precedent), not hardcoded shapes; scoped to `MoveTo` only, `Hold`/`Assault`/`Withdraw`/`Suppress` keep their existing target semantics for a formationed agent. New pure `Appraisal.resolveFormationTarget` (the `bestCoverNear` shape, scored by occupancy instead of threat pressure) threaded into `Appraisal.appraise`'s `MoveTo` case and into `Simulation.appraisal`'s `Destination`-write / `fulfilled` check identically; new static, non-canonical `AgentState.FormationOffset` (the `MoveSpeed` precedent) resolved from a new `RawScenario.Formations`/`RawDeployment.FormationId`/`.SlotIndex` (`ScenarioContent.Version` 5 -> 6, no silent default on a non-blank reference). New sparse `Overlay.AgentFormationSlot` satisfies the diagnostics-extension rule; new `content/replays/formation-slots` corpus entry (two agents in a two-slot "wedge" formation both ordered to the identical nominal cell `(5,5)`, landing on `(4,5)`/`(6,5)` instead of colliding) plus its `content/diagnostics/formation-slots-tick-001.*` golden, generated via a temporary `dotnet fsi` scratch script (removed after use, the TASK-018/042/051 precedent) since corpus-derived goldens are not reachable through the `render` CLI verb. Adding `FormationId`/`SlotIndex` to `RawDeployment` required the identical mechanical literal-update sweep TASK-049's `UnitType` field did: 30 pre-existing `RawDeployment` construction sites across `Scenario.fs`/`Corpus.fs`/`DemoScenario.fs`/`TurnDemo.fs`/`PathDemo.fs`/`LosDemo.fs`/`ScenarioTests.fs`. No `Canonical.FormatVersion` bump (the offset is static, excluded, the `MoveSpeed`/`Headquarters`/`Jammers` precedent). `dotnet build` all three `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `377/377` (+15); `cwheadless corpus` `17/17` (`--regenerate` added only the new entry's two files, every one of the 16 pre-existing entries confirmed byte-identical by `git status`); `cwheadless fixture` format 12 unaffected; `dotnet list CommandoWar.Sim package` `FSharp.Core` only. No Godot client change (confirmed by a clean `CommandoWar.Client.Godot.slnx` Debug build) -- its `--selfcheck` scripted sequences author no formation in any of the three scenes, so their pinned hashes are expected unaffected, but were not independently re-run through the real Godot editor in this session (no Godot install in the implementing environment, the TASK-047-original-submission precedent) -- flagged for Dave to confirm, or accept on the self-verification evidence as several recent Sim-only tasks have been. | `tasks/TASK-059-*.md` created and implemented (`proposed -> review`, self-verified); `docs/11_BACKLOG.md` B-011d row `proposed -> done`; `docs/04_SIMULATION_SPEC.md` section 12.5 gained the formation-slot-resolution paragraph; `src/CommandoWar.Sim/Perception.fs` and `Simulation.fs` stale "B-011d unstarted" comments corrected; `docs/12` this row + Pinned facts (`ScenarioContent.Version`, green-test count) refreshed; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-19) | [detail](ledger/2026-09-19-TASK-059-formation-slots.md) |
| 2026-09-19 | TASK-059 accepted | Dave accepted TASK-059 (realises B-011d) on the self-verification evidence -- confirmed no client-scenario change (no formation authored in any of the three scripted Godot demos), going as expected; the Godot editor's `--selfcheck` hashes were not independently re-run live, but Dave accepted without requiring that re-check for this task. No changes requested. | `tasks/TASK-059-*.md` Status -> `done`; `docs/11_BACKLOG.md` B-011d row acceptance note appended; `docs/ledger/2026-09-19-TASK-059-*.md` Review section closed; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-19) | [detail](ledger/2026-09-19-TASK-059-formation-slots.md) |
| 2026-09-19 | TASK-060 | Scenario content format (`.cwscenario`), `cwheadless import`, and a Godot terrain-authoring `TileSet`/Custom-Data pipeline (realises B-024; three live review rounds found and fixed a tile-shape checkerboard bug, a Y-sort/texture-anchor overlap-and-offset bug, and a map-sizing/negative-coordinate gap; Dave's own live verdict closed B-043 / ADR-0001 review trigger 1) | `proposed -> done` | yes (2026-09-19) | [detail](ledger/2026-09-19-TASK-060-scenario-content-format-and-godot-terrain-authoring.md) |
| 2026-09-19 | TASK-061 | Bridgehead greybox map (realises B-025): hand-authored `content/scenarios/bridgehead.cwscenario` (18x12; one rail-bridge chokepoint over an impassable river, a two-building depot with crate cover; 6 friendly incl. two TASK-059 fireteams of 3, 5 enemy incl. one MG-team agent + 4 riflemen; `ObjectiveAreas`/`StaticTargets`/`ExtractionAreas` landmarks only, no authored mission-sequence content per the scope split against B-032), valid under `cwheadless import` (exit 0, no faults). New minimal terrain-layer importer, the `ExportTerrainScript.cs`/`exportScenario` reverse: `Core/TerrainAuthoring.importTerrainLayer`, `tools/ImportTerrainScript.cs` (`[Tool] EditorScript`, not run through the interactive editor in this environment -- no display), and an additive `TerrainTileSet.Resolve` lookup (the `Sources` table itself untouched). Proven for real headless through the installed Godot 4.7.2 editor by `tools/BridgeheadTerrainImportProof.cs` (the `TerrainRoundTripProof.cs` reverse): all 43 authored terrain cells resolved, painted, and re-exported to an exact, order-independent match against the original. One deviation found and resolved during implementation: `Scenario.validate` unconditionally rejects an empty `Objectives[]` array (`MissingRequiredMarker "Objective"`), directly conflicting with the task's own literal "Objectives = [||]" instruction and its "no `Objectives[]` entries" acceptance criterion -- resolved by authoring exactly one placeholder `IsOptional=true` "reach" objective (not real mission content), the exact precedent `TerrainAuthoring.exportScenario`'s own skeleton and `godot-painted-test.cwscenario` already set; that one acceptance criterion is marked unmet, not silently checked off. A second gap: `cwheadless render` has no `.cwscenario` target (confirmed by inspecting `Program.fs`), so the "visual sanity check independent of Godot" step used an ad hoc, not-committed console harness instead of a permanent CLI verb (out of allowed scope to add). `dotnet build` all three `.slnx` (main Release; Godot client Debug; Mibo Debug) `0/0`; `dotnet test` `395/395` (unaffected); `-- corpus` `17/17` (unaffected); `dotnet list CommandoWar.Sim package --include-transitive` = `FSharp.Core` only. A regenerated `art/terrain.tres` (an unrelated, pre-existing drift between the committed generated resource and its current generator, surfaced as a side effect of running the Godot proofs) was reverted, not committed. | `tasks/TASK-061-*.md` created and implemented (`proposed -> review`, self-verified); `docs/11_BACKLOG.md` B-025 row `proposed -> review`; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | see next row | [detail](ledger/2026-09-19-TASK-061-bridgehead-greybox-map.md) |
| 2026-09-19 | TASK-061 accepted | Dave accepted TASK-061 (realises B-025) after a live review round in the real Godot 4.7.2 editor. Real bug found and fixed: `ImportTerrainScript` never cleared the `TileMapLayer` before painting, so it silently overlaid Bridgehead's 43 cells onto a large leftover hand-painted test area still baked into the committed `TerrainAuthoring.tscn` from TASK-060's own review rounds -- fixed with one `layerNode.Clear()` call. A confusing intermediate "map looks mostly empty" screenshot symptom was run to ground via a temporary headless diagnostic reading Dave's own saved scene file back through Godot's own API, confirming all 43 cells were present and correct throughout (a viewport/timing artefact, not a real defect). `TerrainAuthoring.tscn` reset back to its documented empty starter-scene state afterward, per Dave's explicit choice. No further changes requested; verdict "this looks ok". Not committed to git; left in the working tree for Dave. | `tasks/TASK-061-*.md` Status -> `done`; `docs/11_BACKLOG.md` B-025 row -> `done`; `docs/ledger/2026-09-19-TASK-061-*.md` Review section closed; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-19) | [detail](ledger/2026-09-19-TASK-061-bridgehead-greybox-map.md) |
| 2026-09-19 | TASK-062 | Demolition objective, extraction, and mission success/failure (realises B-032). Turns `Simulation.fs`'s `Mission` phase, an explicit no-op reserved for exactly this since `Phases.fs`'s original phase-order design, into real behaviour, and wires up `Scenario.fs`'s fully-authored-but-never-evaluated `Objective`/`ScenarioRules` algebra. Selected and scoped via two `AskUserQuestion` rounds (whether to scope B-032 now, hand-tweak the Bridgehead map, or push unpushed commits; then four design-fork questions, all answered with the recommended option): charge-planting is occupancy-based (any `Alive` friendly agent on the target's own cell for a configured tick count, no new order type, reusing `MoveTo`/`Hold` exactly as authored today -- `Objective.DestroyTarget`'s own doc comment had already named this as the planned "fixed-duration plant" shape); failure is exactly the existing, never-consumed `ScenarioRules.FailOnFriendlyForceEliminated` wired up, no new timeout mechanism; extraction is sticky (once reached, an agent stays counted even after walking off); Bridgehead's TASK-061 placeholder single optional `reach` objective is replaced with real content (a non-optional `destroy` against the already-authored `bridge-charge` target, 10-tick plant, and a non-optional `extract` against the already-authored `extraction` area). Generalised beyond the four confirmed answers, documented as my own implementation-level decisions in the task file: sticky one-shot completion applies uniformly to every objective kind (`WorldState.CompletedObjectives`), not just extraction; `ObjectiveId`/`AreaId`/`TargetId`/`Area`/`StaticTarget`/`AgentSelection`/`Objective`/`ScenarioRules` moved `Scenario.fs -> Domain.fs` (the `Jammer`/`QueueMode` precedent -- `WorldState` needs to reference them and `Domain.fs` compiles first); a real bug found and fixed before it ever reached a test: the naive success gate (`Array.forall` over an empty `Objectives` array) is vacuously `true`, which would have made every `World.create`/`Setup.sixAgentWorld` world -- every pre-existing test fixture -- instantly "succeed" on tick 1; guarded with an explicit `Objectives.Length > 0` check. New canonical `AgentState.Extracted`, `WorldState.MissionOutcome`/`.CompletedObjectives`/`.ObjectiveProgress` (`Canonical.FormatVersion` 12 -> 13); new events `AgentExtracted`/`ObjectiveCompleted`/`MissionSucceeded`/`MissionFailed`; new sparse `Overlay.MissionStatus` (the `AgentSuppression`/`Divergence` precedent, satisfying the diagnostics-extension rule); `ScenarioContent.Version` 6 -> 7 (a validation-rule change reusing the existing `HoldTicks` grammar column for `"destroy"`, not a new field). A second real find while re-pinning: an existing `ScenarioTests` fixture (`fixtureScenario`, a `Scenario`-validated expression of the shared six-agent fixture) already authored a real `reach observation` objective at agent 3's own `MoveTo` target, purely to satisfy `Scenario.validate`'s required-marker check, on the explicit assumption "`World.ofScenario` reads neither [objective nor extraction area], so they cannot affect the hash" -- now false; that fixture genuinely reaches `MissionOutcome = Succeeded` at tick 31, a real behaviour change confirmed by rerunning it, not a byte-layout-only re-pin like every other affected fixture. `content/scenarios/bridgehead.cwscenario` now carries the real three-objective content and re-validates clean. New `demolition-success` corpus entry (hand-built `RawScenario`, the `LosDemo`/`PathDemo` bespoke-scenario precedent, since the shared `ScenarioSpec` builder always authors exactly one non-optional `reach` objective) proves `MissionOutcome -> Succeeded` end to end, including a genuine mid-plant `ObjectiveProgress` frame; `casualties-succession-and-squad-failure` (pre-existing) now also demonstrates `MissionOutcome -> Failed` for free, since it already authors `fail-on-friendly-eliminated true` and its scripted combat already wipes out the friendly side. `dotnet build` all three `.slnx` (main Release; Godot client Debug; Mibo Debug) `0/0`; `dotnet test` `408/408` (+13); `-- corpus` `18/18` (`--regenerate` byte-identical on a second run); `-- import content/scenarios/bridgehead.cwscenario` exit 0, 3 objectives; `dotnet list CommandoWar.Sim package --include-transitive` = `FSharp.Core` only; source scan clean. | `tasks/TASK-062-*.md` created and implemented (`drafted -> review`, self-verified); `docs/11_BACKLOG.md` B-032 row `selected -> done`; `docs/04_SIMULATION_SPEC.md` section 12.10 (realised design) and section 21 (Mission-phase consumption) updated; `docs/12` this row + Pinned facts (`Canonical.FormatVersion`, `ScenarioContent.Version`, green-test count) refreshed; `PROJECT_STATE.yaml active_work` updated | see next row | [detail](ledger/2026-09-19-TASK-062-demolition-objective-extraction-and-mission-outcome.md) |
| 2026-09-20 | TASK-062 accepted | Dave accepted TASK-062 (realises B-032) on the self-verification evidence: "ok if you are happy then i am". No changes requested. | `tasks/TASK-062-*.md` Status -> `done`; `docs/11_BACKLOG.md` B-032 row acceptance note appended; `docs/ledger/2026-09-19-TASK-062-*.md` Review section closed; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-20) | [detail](ledger/2026-09-19-TASK-062-demolition-objective-extraction-and-mission-outcome.md) |
| 2026-09-20 | TASK-063 | Mission summary panel in `CommandDemoScene` (realises narrowed B-033, split from "replay playback and mission summary" via `AskUserQuestion`; replay playback deferred to new B-064). New `IClientScene.MissionSummaryLines`, shown as a fixed-screen-space panel (`FSharpSceneHost.DrawMissionSummaryPanel`) the instant `WorldState.MissionOutcome` leaves `InProgress`: outcome headline, completed objectives, in-progress objective status, and extracted agents (`RenderShared.missionSummaryLines`, reading `WorldState` directly -- no `CommandoWar.Sim` change). The scene auto-pauses and refuses further order-issuing once the mission ends. `CommandDemoDrive.runScriptedSelfCheck` extended (12 + 28 = 40 ticks, was 20) to send agent 0 on to `DemoScenario`'s own sole authored objective at `(4,4)` after its existing `MoveTo(3,0)`/`Hold(2,1)` sequence, reaching a real `MissionOutcome = Succeeded`; re-pinned `CommandDemo.tscn`'s `--selfcheck` hash. Two real, pre-existing bugs found and fixed during verification, both predating this task: `SnapshotDemo.tscn`/`AppraisalDemo.tscn`'s own `--selfcheck` pins had been silently stale since TASK-062's `Canonical.FormatVersion` 12 -> 13 bump (which TASK-062 never re-verified against the Godot client) -- confirmed by reproducing the identical mismatch on committed `main` before any TASK-063 change, then re-pinned; and the panel's own first draft anchored its text box to the screen centre instead of the panel's own left edge, rendering every line shifted a half-panel-width right of the panel (found from the first evidence screenshot, fixed, re-captured). `dotnet build` both `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `408/408` (unaffected); `-- corpus` `18/18` (unaffected); all three scenes' `--selfcheck` `MATCH` through the real Godot 4.7.2 editor. Not exercised live (flagged): `HoldArea`/`DestroyTarget`/`ExtractAgents`/`AllOf`/`Optional` objective-label formatting and the extracted-agent line, since `DemoScenario` authors only a `ReachArea` objective. | `proposed -> review` | see next row | [detail](ledger/2026-09-20-TASK-063-mission-summary-screen.md) |
| 2026-09-20 | TASK-063 review round 1 | Dave tried it live and could not trigger the panel (tick 1141, `order=no order`, still `running`) -- a real, blocking gap: `WorldState.ObjectiveAreas`/`.ExtractionAreas` were never rendered anywhere in `CommandDemoScene` (confirmed by inspection: the only references to either field in the whole client were in the terrain-authoring export/import tool, not any play scene), so the objective cell looked like ordinary terrain with nothing to click toward precisely. Confirmed via `AskUserQuestion` to fix in this task rather than defer. Added an always-on marker (a `Kind = 5` ring plus its `AreaId` label, `RenderShared.cellRing`/`.cellLabel`) for each objective/extraction area cell, built once in `Ready` from already-held `WorldState` fields -- no `IClientScene`/interaction change, purely additive rendering. The first colour choice (gold) was visually identical to the selection halo's own ring exactly where it mattered most (a selected agent standing on the objective); changed to violet, extraction stays a distinct cyan. All three scenes' `--selfcheck` hashes reconfirmed `MATCH` unchanged (render-only); screenshot recaptured. Accepted by Dave immediately after: "ok it works". | none (fix folded into the same task, not a status change of its own) | yes (2026-09-20) | [detail](ledger/2026-09-20-TASK-063-mission-summary-screen.md) |
| 2026-09-20 | TASK-063 accepted | Dave accepted TASK-063 (realises narrowed B-033) after one live review round: "ok it works". No further changes requested. | `tasks/TASK-063-*.md` Status -> `done`; `docs/11_BACKLOG.md` B-033 row -> `done`; `docs/ledger/2026-09-20-TASK-063-*.md` Review section closed; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-20) | [detail](ledger/2026-09-20-TASK-063-mission-summary-screen.md) |
| 2026-09-20 | TASK-064 | Integrate and verify the Bridgehead vertical slice (realises B-035, unblocked for the first time now B-025 through B-034 are all `done`). `CommandDemoScene.fs`'s `Ready()` now loads `content/scenarios/bridgehead.cwscenario` for real (`ScenarioFile.parse` -> `Scenario.validate` -> `World.ofScenario`) instead of the small hand-authored `DemoScenario` fixture -- the first task to run the actual docs/07 mission content in the actual play scene. `IClientScene.Ready` gained a `scenarioContentPath` parameter; `FSharpSceneHost.cs` resolves it via `ProjectSettings.GlobalizePath` (the `AppraisalDemoScene.cs` precedent). A real, material gap was found and fixed, confirmed with Dave via `AskUserQuestion`: the order-mode HUD had no way to issue `Suppress` at all (only indices 0-3 existed, TASK-048) -- one of docs/07's five required commands. Added index 4 (a procedural crosshair icon, no new art asset) and a new `enemyAt` helper resolving the clicked cell's occupant as the `Command.suppress` target. `CommandDemoDrive.runScriptedSelfCheck` rewritten entirely against real Bridgehead coordinates (all six agents, exercising all six formation-slot resolutions against real terrain for the first time, TASK-059): sending every agent at once gives the machine-gun team more simultaneous targets than a lone agent, so real automatic engagement brings it down to `Dead` while every friendly agent stays `Alive` -- a reproducible, casualty-free neutralisation of the scenario's central threat through the real click path (`0xB99E7F74EA1C3CDE` at tick 90). Despite extensive investigation (a series of temporary `dotnet fsi` probes against the built client assembly, removed after use), a full `Succeeded` run was not reduced to a reliable scripted sequence: the `bridge-charge` target cell (9,5) itself appears covered by a threat beyond the machine gun (most likely a depot rifleman), and a friendly agent's own corpse permanently blocks its cell (never vacated, and still `friendlyAt`-selectable, so a click there re-selects it for status view rather than ever issuing a new order elsewhere). A full `Failed` run is also geometrically capped well short of six friendly agents by the bridge's own two-lane chokepoint. Two of docs/07 section 9's 12 functional acceptance criteria (3: canonical refusal; 7: mission succeeds/fails without developer intervention) are therefore not closed on Bridgehead -- flagged for Dave's own decision, not silently resolved; the other ten are met. `dotnet build` both `.slnx` `0/0`; `dotnet test` `408/408` (unaffected); `-- corpus` `18/18` (unaffected); `-- import bridgehead.cwscenario` exit 0 (unaffected); `CommandDemo.tscn --selfcheck` re-pinned `MATCH 0xB99E7F74EA1C3CDE` through the real Godot 4.7.2 editor; `SnapshotDemo.tscn`/`AppraisalDemo.tscn` reconfirmed unaffected. Screenshot: `docs/evidence/task-064-bridgehead-integration.png`. | `proposed -> review` | no (awaiting Dave's decision on the criterion-7 finding) | [detail](ledger/2026-09-20-TASK-064-integrate-and-verify-vertical-slice.md) |
| 2026-09-20 | TASK-064 review round 1 | Dave tried the real editor and reported it felt static and out of control -- no enemy movement, no fire lines, no idea what was happening -- despite orders visibly registering and him trying to move across the bridge. Root cause, confirmed by a fresh `dotnet fsi` probe: a `MoveTo` order for any agent other than a fireteam leader (a non-zero `AgentState.FormationOffset`) does not target the literal clicked cell -- `Appraisal.resolveFormationTarget`'s own bounded-radius fallback can land the real destination well short of the click (confirmed exactly: agent 2 ordered to `(8,6)` actually resolves to `(7,6)`, outside the machine gun's engagement range) with `Disposition = Accepted` throughout and zero client-side indication this happened. Fixed, client-side only: `OnHover`'s route preview now resolves through `Appraisal.resolveFormationTarget` (the identical inputs `Simulation.fs`'s own appraisal phase uses) whenever `MoveTo` is armed, so the preview shows the real destination before the click; the order-status HUD text now appends the agent's real `Destination` (`"accepted -> (7,6)"`) instead of bare `"accepted"`. `dotnet build`/`test`/`corpus` unaffected (`0/0`/`408/408`/`18/18`); `CommandDemo.tscn --selfcheck` reconfirmed `MATCH 0xB99E7F74EA1C3CDE` unchanged (presentational only); `SnapshotDemo.tscn`/`AppraisalDemo.tscn` unaffected. One unrelated editor-triggered reformat (`ExportTerrainScript.cs`/`project.godot`) caught and reverted, not committed. | none (fix folded into the same task, not a status change of its own) | not yet (awaiting Dave's next live try, and separately his decision on the criterion-7 finding) | [detail](ledger/2026-09-20-TASK-064-integrate-and-verify-vertical-slice.md) |
| 2026-09-20 | TASK-064 review round 2 | After round 1's fix, Dave confirmed it worked, then raised four further points: "you cant visually tell who is a leader, still no sense of a fight, just blindly moving men to die, no reaction under fire, no cover, no enemy response either." Confirmed via `AskUserQuestion` to fix three now (cheap, real, client-only) and leave the fourth as the already-existing, explicitly-descoped backlog row B-022 (enemy doctrine, deferred since TASK-037). Fixed: a leader marker (`Casualty.currentLeader state.Agents`, a pure function over already-held state -- a green ring plus a "LEADER" label, updating automatically on succession); an under-fire hit-flash (the target's figure blends toward white for 0.3s on a landed `FireLine`, fading back, reusing the `heldFireLines` timer pattern); and a cover indicator (`Terrain.Cover`, never rendered anywhere since found unpaintable at TASK-060 -- a short cyan spoke per covered cell/direction, thicker for a higher level). All render-only or a local non-authoritative timer, no `CommandoWar.Sim` change. A suspected fourth issue (a "grey" figure in the recaptured screenshot) was investigated with a temporary `dotnet fsi` probe and found to be the pre-existing, correctly-functioning selection halo, not a defect. `dotnet build`/`test`/`corpus` unaffected; all three scenes' `--selfcheck` reconfirmed `MATCH` unchanged. Screenshot recaptured: `docs/evidence/task-064-bridgehead-integration.png`. | none (fix folded into the same task) | not yet (awaiting Dave's next live try, and separately his decision on criterion 7 and B-022) | [detail](ledger/2026-09-20-TASK-064-integrate-and-verify-vertical-slice.md) |
| 2026-09-20 | TASK-064 accepted | Dave's own further live playtest of Bridgehead confirmed round 2's three fixes (no further complaint about leader marker/hit-flash/cover) and separately surfaced a new, genuine sim-side movement deadlock -- scoped as B-065/`tasks/TASK-065-STALLED-ORDER-VISIBLE-FAILURE.md`, not a defect in this task. Via `AskUserQuestion`, Dave then accepted the criterion-3/7 finding as a known, tracked gap (no Bridgehead map rebalance, no corpse-occupancy fix), kept B-022 (enemy doctrine) descoped as-is, and selected TASK-065 to implement next. | `tasks/TASK-064-*.md` Status -> `done`; `docs/11_BACKLOG.md` B-035 row -> `done`; `docs/07_VERTICAL_SLICE.md` section 9 criteria 3/7 annotated "accepted known gap"; `docs/ledger/2026-09-20-TASK-064-*.md` Review section closed; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> TASK-065` | yes (2026-09-20) | [detail](ledger/2026-09-20-TASK-064-integrate-and-verify-vertical-slice.md) |
| 2026-09-20 | TASK-065 | Turn a permanently stalled movement order into a visible failure (realises B-065). A new `AgentState.StalledTicks` counter (`Canonical.FormatVersion` 13 -> 14) tracks consecutive same-tick-reservation-loss/stationary-occupant freezes against the same route in `Simulation.navigationAndMovement`; once it would reach `Simulation.StallAbandonTicks` (40, first-cut/not playtest-derived), the order is abandoned outright -- `Destination`/`Route` clear, the counter resets, and a new `MovementAbandoned` event fires in place of that tick's usual `MovementYielded`/`MovementObstructed` -- instead of freezing silently forever. No `Pathfinding`/`Appraisal` change, per Dave's own chosen direction (a visible-failure fix, not occupancy-aware routing). New `Diagnostics.Overlay.Abandoned` (ASCII/SVG rendered, the `Reserved`/`Obstructed` precedent) and a new `stalled-order-abandoned` corpus entry prove it end to end with a committed golden. Client-side: `CommandDemoScene`'s order-status text now reads `"accepted -> abandoned (route blocked)"` for a few seconds after the event (an abandoned order clears `Destination` exactly like a fulfilled one, so the existing text alone would misread as "arrived"), and the `F1` developer overlay gets a matching marker -- no new UI system, one more case each. Re-run against the exact original repro (a temporary `dotnet fsi` probe against real `bridgehead.cwscenario`, removed after use): five of six agents now reach `MovementAbandoned` by tick 42, all settled, instead of freezing past tick 500. Two new `SimulationTests` facts prove the threshold behaviour directly (a brief resolved yield never aborts; a permanent block aborts at the threshold). `Canonical.FormatVersion`'s bump re-pinned every one of the 18 pre-existing corpus/fixture/diagnostics goldens and all three Godot `--selfcheck` hashes, confirmed byte-layout-only diff by diff. A pre-existing, unrelated staleness was found and fixed while re-pinning: `content/fixtures/SPIKE-FIXTURE.md` (and this ledger's own "Shared fixture" Pinned-facts row) had been silently stuck at format 4 since TASK-028 -- nine intervening bumps never touched them, unlike `FixtureTests.fs`'s own hashes, which stayed current -- corrected straight to format 14, flagged for Dave rather than backfilled through every intervening version. `dotnet build` `0/0`; `dotnet test` `412/412` (+4); `-- corpus` `19/19` (+1); all three Godot `--selfcheck` hashes re-pinned and reconfirmed `MATCH` through the real Godot 4.7.2 editor. | `tasks/TASK-065-*.md` created and implemented (`proposed -> review`, self-verified); `docs/11_BACKLOG.md` B-065 row `proposed -> review`; `docs/04_SIMULATION_SPEC.md` step 6 gained the give-up-on-a-permanent-freeze paragraph; `content/replays/CORPUS.md` new entry row; `docs/12` this row + Pinned facts (`Canonical.FormatVersion`, green-test count, Shared-fixture hashes/event-count) refreshed and de-staled; `PROJECT_STATE.yaml active_work` updated; `src/CommandoWar.Client.Godot/README.md` new section | not yet (awaiting Dave's live playtest of the on-screen text) | [detail](ledger/2026-09-20-TASK-065-stalled-order-visible-failure.md) |
| 2026-09-20 | TASK-066 | A dead agent's corpse stops blocking movement (realises B-066). Raised live: after TASK-065 shipped, Dave asked to actually watch a scripted playthrough rather than read a headless hash, so a new `--screenshot-squad <frameCount> <path>` capture mode was added to `FSharpSceneHost.cs` (the real six-agent Bridgehead advance through the actual click path, stays unpaused, captures at any requested tick). The resulting filmstrip showed most of the squad permanently jamming at the bridge chokepoint; Dave's diagnosis -- "they would move over a dead body, or just around it if there is space" -- is exactly TASK-064's own already-flagged, already-accepted-as-a-known-gap finding, now selected to fix. Root cause: `Simulation.navigationAndMovement`'s `occupantOf` map included every agent regardless of `Vitals`, so a corpse blocked forever exactly like a live occupant; `Pathfinding.fs` itself has no occupancy concept at all, so the fix needed none. `occupantOf` now excludes any non-`Alive` agent (`Casualty.isAlive`) -- a corpse's cell reads as free, a live agent walks through/onto it. Two new `SimulationTests` facts prove it directly (`Dead` and `Incapacitated` both covered). No existing corpus entry or Godot self-check hash moved -- none of the 19 committed entries combines a death with a subsequent move through that exact cell, confirmed by a clean `cwheadless corpus` pass with no `--regenerate`. **Honestly flagged, not smoothed over:** re-running `--screenshot-squad` against the same real Bridgehead scenario is byte-identical to the pre-fix filmstrip, because the specific jam Dave watched never involves a death at all -- it is five of six agents blocking each other while still `Alive`, pure formation-offset contention at the chokepoint, a different mechanism entirely (parked separately as B-067: multi-select + joint squad orders, formation no longer auto-applying to a lone individual order). `dotnet build` `0/0`; `dotnet test` `414/414` (+2); `-- corpus` `19/19` (unchanged, no re-pin needed). | `tasks/TASK-066-*.md` created and implemented (`proposed -> review`, self-verified); `docs/11_BACKLOG.md` B-066 row `proposed -> review`, B-067 added (proposed, parked); `docs/12` this row; `PROJECT_STATE.yaml active_work` updated; `src/CommandoWar.Client.Godot/README.md` new `--screenshot-squad` section | not yet (correct for its own scope; does not fix the squad-jam Dave actually watched -- see B-067) | [detail](ledger/2026-09-20-TASK-066-corpses-stop-blocking-movement.md) |
| 2026-09-20 | TASK-065 and TASK-066 accepted | Via `AskUserQuestion`, Dave confirmed all three open items in one round: commit the combined TASK-065 + TASK-066 working tree now (rather than review the diff first); accept both tasks on the self-verification evidence alone, explicitly declining to live-test TASK-065's on-screen "abandoned" text or TASK-066's corpse walkthrough first; and hold B-067 (multi-select + joint squad orders) as a proposed, unscoped backlog row rather than start design work on it now. TASK-066's own known limitation (does not fix the live-agent chokepoint jam Dave actually watched via `--screenshot-squad`) is accepted as-is, not resolved by this acceptance -- B-067 remains the real fix, still unscoped. | `tasks/TASK-065-*.md`/`tasks/TASK-066-*.md` Status -> `done`; `docs/11_BACKLOG.md` B-065 row -> `done`, B-066 row -> `done`; `docs/ledger/2026-09-20-TASK-065-*.md`/`docs/ledger/2026-09-20-TASK-066-*.md` Review sections closed; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-20) | [detail: TASK-065](ledger/2026-09-20-TASK-065-stalled-order-visible-failure.md), [detail: TASK-066](ledger/2026-09-20-TASK-066-corpses-stop-blocking-movement.md) |
| 2026-09-20 | TASK-067 | Formation redirect applies only to group orders, not solo clicks (realises the sim-side half of B-067). Scoped via two parallel research passes (the client click/order pipeline and every `Appraisal.resolveFormationTarget` call site; the TASK-047/048 command-addition pattern, confirming `PlayerCommand.Recipients`/`Command.moveToMany` already address multiple agents with one command since TASK-020) plus `AskUserQuestion`: formation for a group order uses each agent's own authored `FormationOffset` only (no ad-hoc formation for an arbitrary selection); the eventual multi-select gesture will be both drag-select and shift-click, deferred to an unscoped follow-up. New `ReceivedOrder.AsGroup: bool` (`Canonical.FormatVersion` 14 -> 15), set once at `Simulation.commandIntake` from the originating command's own `Recipients.Length > 1` -- no new `Command`/`PlayerIntent` shape needed. Gates `Appraisal.appraise`'s `MoveTo` branch, `Simulation.fs`'s two mirrored `resolveFormationTarget` calls, and `Diagnostics.formationSlotOverlays`; `CommandDemoScene.fs`'s `OnHover` preview no longer calls `resolveFormationTarget` at all (every order it issues is single-recipient, so it always resolves literally now), a net simplification. Two real, genuine behaviour changes found and handled while re-verifying, not smoothed over: (1) the `formation-slots` corpus entry issued two single-recipient orders sharing a tick/target -- re-authored to one real `Command.moveToMany` (`Corpus.fs`'s new `MoveOrderGroup`), its own outcome unchanged; (2) `CommandDemoDrive.runScriptedSelfCheck`'s six-agent Bridgehead sequence relied on the old unconditional redirect to keep agents with coincidentally-shared literal targets apart -- re-running it unchanged produced a genuine friendly casualty, confirmed via a temporary `dotnet fsi` probe (removed after use); fixed by aiming each order at its own pre-TASK-067 resolved cell directly, a second probe run confirming this reproduces the real `--selfcheck` hash exactly (no friendly casualties, machine gun neutralised, unchanged). A third, unrelated staleness found and fixed: `content/fixtures/SPIKE-FIXTURE.md` was still pinned at format 4 despite a prior TASK-065 ledger note claiming it had already been corrected to format 14 -- that claim did not match the committed file (`git log` confirms its last real edit was TASK-028) -- corrected for real this time, straight to format 15. `dotnet build` `0/0`; `dotnet test` `415/415` (+1); `-- corpus` `19/19`; `envelope-full` replay checkpoints OK after re-pinning; all three Godot `--selfcheck` hashes re-pinned and reconfirmed `MATCH` through the real Godot 4.7.2 editor. | `tasks/TASK-067-*.md` created and implemented (`proposed -> review`, self-verified); `docs/11_BACKLOG.md` B-067 row updated; `docs/04_SIMULATION_SPEC.md` formation-resolution paragraph extended; `content/replays/CORPUS.md` `formation-slots` row updated; `docs/12` this row + Pinned facts (`Canonical.FormatVersion`, green-test count, Shared-fixture hashes) refreshed; `PROJECT_STATE.yaml active_work` updated; `src/CommandoWar.Client.Godot/README.md` new section | not yet (awaiting Dave's acceptance) | [detail](ledger/2026-09-20-TASK-067-formation-applies-only-to-group-orders.md) |
| 2026-09-20 | TASK-067 accepted | Dave accepted TASK-067 (realises the sim-side half of B-067) on the self-verification evidence: "if it looks good then accept." No further changes requested. The multi-select UI half of B-067 remains unscoped. | `tasks/TASK-067-*.md` Status -> `done`; `docs/11_BACKLOG.md` B-067 row -> `in progress` (sim-side half done, UI half unscoped); `docs/ledger/2026-09-20-TASK-067-*.md` Review section closed; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-20) | [detail](ledger/2026-09-20-TASK-067-formation-applies-only-to-group-orders.md) |
| 2026-09-21 | TASK-068 | Multi-select UI and joint order dispatch (realises the client-side half of B-067). Scoped via two parallel research passes (every `CommandDemoScene.fs` site touching `selected: AgentId option`; `FSharpSceneHost.cs`'s mouse-input pipeline, which had no button-up/`Pressed: false` handling anywhere) plus one `AskUserQuestion` round resolving four product-facing forks: right-click clears the entire selection regardless of size; the hover/route preview shows every selected agent's own route, formation-aware (`Appraisal.resolveFormationTarget`) once more than one is selected and `MoveTo` is armed; the HUD selection line shows a count above one agent, not a list; a Dead/Incapacitated/already-on-target selected agent is silently dropped from a dispatched order, the rest of the (eligible) selection still receives it. `selected` becomes `Set<AgentId>`; drag rubber-band-select and shift-click (both already confirmed in the TASK-067 session) join the existing plain click, which still replaces the whole selection with one agent, unchanged. A `MoveTo` order to more than one selected agent dispatches one real `Command.moveToMany` -- `ReceivedOrder.AsGroup` (TASK-067) is `true` for a client-issued order for the first time, so formation redirect applies to a real group order, not just a unit test; `Hold`/`Assault`/`Withdraw`/`Suppress` (no `*Many` builder, none reads formation, confirmed in `Appraisal.appraise`'s own intent match) dispatch as N existing single-recipient commands instead. A real, pre-existing bug found and fixed while generalising dispatch, not introduced by it: the `pending`-order dedup keyed on `PlayerCommand.Agent` (`List.head Recipients`) alone, so a stale pending order for a non-head recipient of a new multi-recipient command would survive the check and land in the same tick's batch alongside it -- fixed to match on `Recipients` membership against every agent about to be addressed. No `CommandoWar.Sim`/`CommandoWar.Headless` change at all (`Command.moveToMany` already existed, tested, and accepted since TASK-020/067) -- `dotnet build`/`test`/`-- corpus`/replay-checkpoints all unaffected, and `CommandDemo.tscn --selfcheck` reconfirmed `MATCH 0x2629A1FE165F94BB` unchanged (the scripted sequence is single-agent, no-shift, so it dispatches byte-identically to before this task); `SnapshotDemo.tscn`/`AppraisalDemo.tscn` also unaffected. Verified with a temporary `dotnet fsi` probe (removed after use) driving the real `IClientScene` methods directly against `bridgehead.cwscenario`: drag-select/shift-click toggling confirmed via `HudText()`'s selection count and `DrawList()`'s halo-item count; a plain click on a third, unselected agent collapses an existing multi-selection to size 1; and, the central proof, two selected agents with distinct authored `FormationOffset`s (`(0,0)`/`(1,0)`) ordered jointly to one shared literal cell resolve to two distinct cells matching an independently-computed `Appraisal.resolveFormationTarget` prediction against the same scenario/seed exactly (the preview-dot route endpoints match the prediction). **Honestly flagged, not smoothed over:** stepping that order to settlement (180 ticks) shows the two agents end one cell apart, not each at its own full resolved slot -- their overlapping routes contend for the same cells, and the trailing agent's order stalls against the leading, still-`Alive` agent and is abandoned (`Simulation.StallAbandonTicks`, TASK-065's own mechanism, working as designed) one cell short, permanently. This is the same live-agent chokepoint contention TASK-066/067 already found and left as B-067's own accepted, unresolved gap -- reproduced here for the first time through a real client-issued group order rather than six independent solo ones, not a new defect and not this task's scope to fix. A separate, pre-existing behaviour clarified (not a defect): the "already on target cell" half of the partial-dispatch filter is unreachable via the real click path both before and after this task, since `friendlyAt` always resolves a click on any friendly-occupied cell (dead or alive) to a selection click first -- preserved for parity with the pre-existing single-agent guard, not removed as unrelated cleanup. `dotnet build` both `.slnx` `0/0`; `dotnet test` `415/415` (unaffected); `-- corpus` `19/19` (unaffected); replay checkpoints OK (unaffected); all three Godot `--selfcheck` hashes reconfirmed `MATCH` unchanged through the real Godot 4.7.2 editor. Screenshot evidence: `docs/evidence/task-068-multiselect.png` (`--screenshot-multiselect`, a new scripted priming mode, the `--screenshot-squad` precedent). **Not verified, flagged for Dave:** the raw mouse press/motion/release sequence, `ShiftPressed`, and `Rect2` marquee geometry added to `FSharpSceneHost.cs` compile clean against the real `GodotSharp` 4.7.2 assembly but were only exercised through the F# `IClientScene` surface directly (the probe), not an actual click-and-drag in a live windowed session -- needs Dave's own interactive pass, the TASK-060/061/064 precedent for anything this environment cannot provide. | `tasks/TASK-068-*.md` created and implemented (`in progress -> review`, self-verified); `docs/11_BACKLOG.md` B-067 row updated (client-side half `review`, not yet `done`); `docs/12` this row; `PROJECT_STATE.yaml active_work` updated; `src/CommandoWar.Client.Godot/README.md` new section | not yet (awaiting Dave's acceptance and live editor pass) | [detail](ledger/2026-09-21-TASK-068-multi-select-and-joint-order-dispatch.md) |
| 2026-09-21 | TASK-069 | Bridgehead trooper movement was twice as fast as intended (realises new B-068). Raised live by Dave immediately after reviewing TASK-068: "the game does run really quickly though, its probably at least twice as fast as it should be, the simulation should be running at 20hz? but the game should be running smoothly, currently its fast and choppy like it running fast forward." Investigated directly, not assumed: a temporary diagnostic (`FSharpSceneHost.cs`, real wall-clock time logged against the scene's own tick once per second, removed after use) confirmed 20 ticks/second exactly, stable 60fps -- ruling out a frame-pacing/engine bug (`real=1.00s tick=20` through `real=8.12s tick=162`, linear throughout). Root cause: `Terrain.BaseMoveCost = 1` and Trooper's authored `MoveSpeed = 2` (`Domain.fs`'s `Agent.MoveSpeedDefault`, confirmed by its own doc comment to reproduce the sim's pre-unit-type pace exactly, TASK-049) satisfy the edge-completion inequality at `startProgress = 0` for any open cell, so a Trooper crosses a whole grid cell in exactly one 20Hz tick (50ms) -- true since before unit types existed, not a recent regression, and not touched by TASK-068. This explains both "twice as fast" and "choppy": 50ms/cell is genuinely very fast, and it also starves the render-interpolation (`renderPos`/`alpha`) of any real window (`edgeTickEstimate` learns down to 1, leaving ~3 render frames per cell). Confirmed via `AskUserQuestion`: fix by halving Trooper's authored `MoveSpeed` in content, not by scaling `simHz` (confirmed should stay at its documented 20Hz target). `content/scenarios/bridgehead.cwscenario`'s sole `unit-type trooper 2` line -> `1` -- every friendly and hostile agent references this one unit type, so this halves movement uniformly for the whole squad and both sides; no other content touched (`DemoScenario.fs`/`Corpus.fs`/test fixtures/etc. author their own unit types independently and are never loaded by the actual played scene, confirmed by grep). No `CommandoWar.Sim`/`CommandoWar.Headless` code change -- content only. `CommandDemo.tscn --selfcheck` re-pinned (`0x2629A1FE165F94BB -> 0x84A25E3559111E9B` at tick 90) after confirming via a second temporary `dotnet fsi` probe (dev overlay enabled, ground truth ignoring fog of war) that the demonstrated outcome survives at half speed unchanged: by tick 90 the machine-gun team (cell `(11,5)`) is `Dead`, exactly one agent total has died, and all six friendly agents remain `Alive` -- "no friendly casualties, machine gun neutralised" reached the same way, just twice as slowly. `SnapshotDemo.tscn`/`AppraisalDemo.tscn` reconfirmed unaffected (neither loads Bridgehead); `dotnet test` `415/415`, `-- corpus` `19/19`, `envelope-full` replay checkpoints all unaffected. | `tasks/TASK-069-*.md` created and implemented (`none -> review`, self-verified); `docs/11_BACKLOG.md` new B-068 row; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated; `src/CommandoWar.Client.Godot/README.md` pinned-hash note | not yet (awaiting Dave's acceptance) | [detail](ledger/2026-09-21-TASK-069-bridgehead-trooper-move-speed.md) |
| 2026-09-21 | TASK-068 and TASK-069 accepted | Dave accepted both on the self-verification evidence: "commit and lets move on." No live editor test of TASK-068's drag/shift-click gesture. For TASK-069, Dave's own further live playtest reported no perceptible speed change ("seems just as fast, no matter") -- accepted anyway, not pursued further; the mechanism itself (ticks-per-cell doubled) is independently verified correct, honestly recorded as not resolving the felt complaint, not smoothed over. B-067 (both halves) and B-068 now `done`. | `tasks/TASK-068-*.md`/`tasks/TASK-069-*.md` Status -> `done`; `docs/11_BACKLOG.md` B-067 row -> `done`, B-068 row -> `done`; `docs/ledger/2026-09-21-TASK-068-*.md`/`docs/ledger/2026-09-21-TASK-069-*.md` Review sections closed; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-21) | [detail: TASK-068](ledger/2026-09-21-TASK-068-multi-select-and-joint-order-dispatch.md), [detail: TASK-069](ledger/2026-09-21-TASK-069-bridgehead-trooper-move-speed.md) |
| 2026-09-21 | TASK-070 | Route around a permanently parked agent instead of stalling and abandoning (realises new B-069) -- the live-agent chokepoint jam TASK-066/067/068 each independently found and left open, most recently TASK-068's own client-issued-group-order repro. Scoped via two parallel research agents (one tracing `Simulation.navigationAndMovement`/`Pathfinding.fs`/`Appraisal.resolveFormationTarget`/the TASK-065 stall mechanism end to end; one researching multi-agent route-coordination techniques for fixed-tick deterministic simulation) plus `AskUserQuestion`. Key finding that reframed the original two-way fork Dave was first asked to choose between: staggered/ordered departure alone does not fix this bug, since the blocking agent is permanently parked at its own resolved destination, not transiting through -- "wait your turn" waits forever. Dave confirmed the direction the code trace surfaced instead of either original option: `Simulation.navigationAndMovement`'s `obstructedBy` freeze branch, when the blocker itself holds no active order (`Destination = None`, pre-tick, read from `s.Agents` before Pass 3 mutation), now tries one `Pathfinding.findWithin` query -- its own public contract completely unmodified -- against a locally patched, throwaway `Terrain` (new additive `Terrain.withImpassable` helper) marking every currently-parked agent's cell impassable; a genuine alternate route is adopted (new event `MovementRerouted`, `StalledTicks` reset to 0), or the existing TASK-065 freeze-then-abandon path applies exactly as before. No `Pathfinding.fs`/`Appraisal.fs` change of any kind, and no `Canonical.FormatVersion` bump (`AgentState.Route` stays the pre-existing non-canonical derived cache, the TASK-066 behaviour-only precedent). New `Diagnostics.Overlay.Rerouted` (derivation function + every exclusion-list site + ascii/svg rendering, the `Abandoned` precedent) and a new `chokepoint-detour` corpus entry with committed goldens satisfy `AGENTS.md`'s diagnostics mandate. Verified directly against the exact TASK-068 repro shape as a new `SimulationTests` fact: two formationed agents jointly ordered through a shared chokepoint now both reach their own full resolved formation slots, zero `MovementAbandoned`, where before this task the trailing agent stalled and gave up one cell short. Three pre-existing `SimulationTests` facts genuinely changed and were found and fixed, not smoothed over: on `occWorld`'s open 8x8 terrain a detour around one parked blocker now always exists, so two facts specifically proving "no possible detour, must give up" needed a new walled-corridor fixture (`corridorWorld`) to keep demonstrating that path for real; a third ("two agents converging on a stationary third") still passed unmodified but for a materially different reason, recorded honestly rather than left stale -- **known limitation, not resolved further:** two agents independently detouring around the same single parked blocker can converge on the same alternate cell and obstruct *each other* instead (the `swap-standoff` shape), since neither blocker is itself parked at that point; the fact's doc comment and assertions were updated to assert this real mechanism directly. `stalled-order-abandoned` re-authored onto the same walled corridor -- hashes byte-identical (`Terrain` is excluded from `Canonical.encode`), only description text and the walled row's ascii/svg glyphs changed; `formation-slots` confirmed unaffected by inspection (its 12-tick window never reaches this contention). `dotnet build` both `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `422/422` (+7); `-- corpus` `20/20` (+1); `envelope-full` replay checkpoints OK, unaffected. All three Godot `--selfcheck` hashes reconfirmed `MATCH` **unchanged** through the real Godot 4.7.2 editor -- `CommandDemoDrive.runScriptedSelfCheck` is single-agent/no-shift (TASK-068's own finding), so it never exercises the joint-order contention this task fixes; no client-side change was needed or made. `docs/10_RISK_REGISTER.md` R-010 deliberately left `open`, not silently reassessed, since the independent-detour swap-standoff limitation above is a real, live residual case -- flagged for Dave's own decision at review time. | `tasks/TASK-070-*.md` created and implemented (`proposed -> review`, self-verified); `docs/11_BACKLOG.md` new B-069 row (`proposed -> done` on self-verification evidence); `docs/04_SIMULATION_SPEC.md` step 6 gained the detour-mechanism paragraph and its known swap-standoff limitation; `content/replays/CORPUS.md` `stalled-order-abandoned` row updated, `chokepoint-detour` row added; `docs/12` this row + Pinned facts (green-test count) refreshed; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-21) | [detail](ledger/2026-09-21-TASK-070-chokepoint-detour-around-parked-agents.md) |
| 2026-09-21 | TASK-070 accepted | Dave accepted on the self-verification evidence: "accept TASK-070, commit and lets move on." No separate live editor pass. `docs/10_RISK_REGISTER.md` R-010 stays `open`, not resolved by this acceptance -- the swap-standoff limitation is a real, live residual gap. B-069 now `done`. | `tasks/TASK-070-*.md` Status -> `done`; `docs/11_BACKLOG.md` B-069 row -> `done`; `docs/ledger/2026-09-21-TASK-070-*.md` Review section closed; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-21) | [detail](ledger/2026-09-21-TASK-070-chokepoint-detour-around-parked-agents.md) |
| 2026-09-21 | TASK-071 drafted and selected | Replay playback scrubber in the Godot client (realises B-064), selected via `AskUserQuestion` as the one remaining actionable G4-scope backlog row -- B-063 (scenario-authoring tooling) is the only other `proposed` item at this gate and is explicitly marked not blocking anything, while B-064 is a named "required presentation" gap (`docs/07_VERTICAL_SLICE.md` section 6) still entirely unbuilt inside the client. Scoped via two parallel research passes (replay/serialisation mechanism; Godot client scene architecture) plus `AskUserQuestion` resolving all three forks B-064's own row named: build the in-engine scrubber now (not close via the existing `demo.html` static scrubber); a new dedicated scene (`ReplayDemoScene`), not a mode bolted onto `CommandDemoScene`; reuse `Replay.run`'s existing full `TickStates` as-is, not the checkpoint-cadence mechanism its own doc comment flags as a future scale replacement. Not yet implemented -- no code changed this session beyond the task file, the backlog row, and this note. | `tasks/TASK-071-*.md` created (`none -> proposed`); `docs/11_BACKLOG.md` B-064 row updated with the scoping rationale; `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | not applicable (not yet implemented) | [task file](../tasks/TASK-071-REPLAY-PLAYBACK-SCRUBBER.md) |
| 2026-09-21 | TASK-071 | Replay playback scrubber in the Godot client (realises B-064) implemented and self-verified. `CwClientCore.ReplayDemoScene`/`scenes/ReplayDemo.tscn` loads `content/replays/chokepoint-detour.cwreplay` through the exact `cwheadless replay-file` pipeline (`ReplaySerialisation.parse` -> `Corpus.all` resolve -> `toReplayRecord` -> `Replay.run`), holding `TickStates` in memory for the scene's lifetime (central decision 3, no checkpoint-cadence mechanism). `IClientScene` gained `TickCount`/`CurrentTick`/`SetTick`/`CurrentHash`, no-op'd mechanically in `CommandDemoScene`/`DemoRenderScene`. `FSharpSceneHost.cs` gained a scrub bar (drag/click/step-key/`Space`-play-pause), gated on `_scene.TickCount() > 0`. New `--selfcheck` case scrubs tick 0-10 through the scene's own real `SetTick`/`CurrentHash` path, reproducing `cwheadless replay-file`'s ground-truth hash chain exactly (`MATCH 0x5876C1280DDAE2CB` at tick 10, re-derived directly, not guessed); `CommandDemoScene`/`DemoRenderScene` reconfirmed `MATCH` unchanged. No `CommandoWar.Sim`/`CommandoWar.Headless` change; `dotnet test` `422/422`, `-- corpus` `20/20`, both unaffected. Wrong assumption found and corrected: `AppraisalDemoScene` is not an `IClientScene` implementer (a standalone pre-ADR-0004 script), contrary to this task's own drafting -- corrected, not carried through. Honest caveat: the scrub bar's raw drag gesture verified structurally (self-check exercises the identical `SetTick` call; a windowed screenshot confirms rendering/positioning) but not through an actual live drag in this environment -- flagged for Dave, the TASK-068 precedent. | `src/CommandoWar.Client.Godot/Core/ReplayDemoScene.fs` (new), `IClientScene.fs`, `CommandDemoScene.fs`/`DemoRenderScene.fs` (no-op additions only), `RenderShared.fs` (new `deadCross`), `.fsproj`; `src/CommandoWar.Client.Godot/src/FSharpSceneHost.cs`; `scenes/ReplayDemo.tscn` (new); `tasks/TASK-071-*.md` (`proposed -> review`); `docs/11_BACKLOG.md` B-064 row; `docs/evidence/task-071-replay-scrubber.png` (new); `docs/12` this row; `PROJECT_STATE.yaml active_work` updated | yes (2026-09-21) | [detail](ledger/2026-09-21-TASK-071-replay-playback-scrubber.md) |
| 2026-09-21 | TASK-071 accepted | Dave accepted on the self-verification evidence alone. The scrub bar's raw mouse drag gesture (flagged as not independently exercised live in this environment) was not separately re-tested before acceptance. B-064 now `done`. | `tasks/TASK-071-*.md` Status -> `done`; `docs/11_BACKLOG.md` B-064 row -> `done`; `docs/ledger/2026-09-21-TASK-071-*.md` Review section closed; `docs/12` this row; `PROJECT_STATE.yaml active_work.selected_task -> none` | yes (2026-09-21) | [detail](ledger/2026-09-21-TASK-071-replay-playback-scrubber.md) |
