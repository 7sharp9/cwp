# Controlled Backlog

Status: authoritative work tracker  
Rule: only one task may be `active`

## 1. Status definitions

- `proposed`: idea recorded but not yet bounded for implementation.
- `ready`: dependencies satisfied and a complete task file exists.
- `active`: the single task currently selected in `PROJECT_STATE.yaml`.
- `blocked`: cannot proceed; blocker is named.
- `review`: implementation complete but evidence not yet accepted by Dave.
- `done`: acceptance criteria and evidence accepted.
- `rejected`: deliberately not proceeding.
- `parked`: outside the current gate or scope.

A backlog row is not sufficient authority for an agent to implement work. A `ready` or `active` item must have a task file under `tasks/`.

## 2. Current work

| ID | Phase | Task | Size | Dependencies | Status | Task file |
|---|---|---|---|---|---|---|
| TASK-001 | P0 | Establish repository and build baseline | S | project pack | done | `tasks/TASK-001-REPOSITORY-BASELINE.md` |
| TASK-002 | P1 | Create framework-neutral simulation skeleton | S | TASK-001 | done | `tasks/TASK-002-SIMULATION-SKELETON.md` |
| TASK-003 | P1/P2 | Establish deterministic random, hash, and replay harness | M | TASK-002 | done | `tasks/TASK-003-DETERMINISM-HARNESS.md` |
| TASK-004 | P1 | Implement disposable Godot .NET framework spike | M | TASK-003 | done | `tasks/TASK-004-GODOT-SPIKE.md` |
| TASK-005 | P1 | Implement disposable Mibo plus raylib framework spike (trimmed; pinned to Mibo 4.1.0 per ADR-0003 2026-09-02 amendment) | S | TASK-004, ADR-0003 amendment | done | `tasks/TASK-005-MIBO-SPIKE.md` |
| TASK-006 | P1 | Evaluate spikes and accept framework ADR | S | TASK-004, TASK-005 | done | `tasks/TASK-006-FRAMEWORK-DECISION.md` |
| TASK-007 | P2 | Design the low-impedance Godot C#/F# client boundary | S | TASK-006 | done | `tasks/TASK-007-GODOT-FSHARP-BOUNDARY.md` |
| TASK-008 | P2 | Define and validate the initial scenario DTO and content version | S | TASK-007 | done | `tasks/TASK-008-SCENARIO-DTO-AND-CONTENT-VERSION.md` |
| TASK-009 | P2 | Restructure the progress ledger into a summary index plus per-entry detail files | S | TASK-008 | done | `tasks/TASK-009-PROGRESS-LEDGER-RESTRUCTURE.md` |
| TASK-010 | P2 | Implement the authoritative terrain grid: elevation, passability, movement cost, directional cover | M | TASK-008 | done | `tasks/TASK-010-TERRAIN-GRID.md` |
| TASK-011 | P2 | Headless diagnostic visualisation and the framework-neutral diagnostic frame | M | TASK-010 | done | `tasks/TASK-011-DIAGNOSTIC-VISUALISATION.md` |
| TASK-012 | P2 | Deterministic line of sight and opacity | M | TASK-010 | done | `tasks/TASK-012-LINE-OF-SIGHT.md` |
| TASK-013 | P2 | Deterministic grid pathfinding with stable tie-breaking | M | TASK-010 | done | `tasks/TASK-013-PATHFINDING.md` |
| TASK-014 | P2 | Headless performance and allocation benchmark harness | S | TASK-010 | done | `tasks/TASK-014-BENCHMARK-HARNESS.md` |
| TASK-015 | P2 | Navigation and movement phase: `Pathfinding`-driven single-agent executor (scoped; formation slots and multi-agent reservation deferred to B-011b) | M | TASK-013 | done | `tasks/TASK-015-MOVEMENT-EXECUTOR.md` |
| TASK-016 | P2 | Divergence diagnostics and replay corpus infrastructure (committed multi-entry corpus, in-suite determinism check, `cwheadless corpus` verb; generative property tests and component subhashes deferred to B-012b) | M | TASK-015 | done | `tasks/TASK-016-REPLAY-CORPUS.md` |
| TASK-017 | P2 | Same-tick cell reservation and deadlock avoidance in the Navigation and movement phase (priority by remaining route length, ties broken by agent id; a new `MovementYielded` event and `Reserved` diagnostic overlay; formation slots and sub-cell movement progress deferred to B-011c) | M | TASK-015 | done | `tasks/TASK-017-CELL-RESERVATION.md` |
| TASK-018 | P2 | Sub-cell movement progress within an edge (`AgentState.Progress`, threshold = `Terrain.moveCost`, increment = `Terrain.BaseMoveCost`; `Canonical.FormatVersion` bumped to 2; reservation generalised to "would complete this tick"; new `slow-terrain` corpus entry; formation slots deferred to B-011d) | M | TASK-017 | done | `tasks/TASK-018-SUBCELL-MOVEMENT-PROGRESS.md` |
| TASK-019 | P2 | Generative determinism property tests (`FsCheck`/`FsCheck.Xunit` 3.3.4, test project only): determinism under random commands, no agent on an invalid cell after movement, pathfinding endpoints/cost on random terrain; component-level subhashes deferred, staying in B-012b | M | TASK-016, TASK-018 | done | `tasks/TASK-019-DETERMINISM-PROPERTY-TESTS.md` |
| TASK-020 | P3 | Command validation and recipient selection — **partial** section 13 envelope (`PlayerCommand.Recipients: AgentId list` generalised from a single agent, back-compatible `Command.moveTo`; `Urgency`/`RiskTolerance` envelope fields, inert until B-017; new `CommandRejection` cases `EmptyRecipients`, `DuplicateRecipient`, `UnauthorisedRecipient`, batch-order-independent `DuplicateCommandId` that rejects every command in a duplicated-id group; `commandIntake` copies the agent array once per phase invocation not per command, emit-before-apply; no `Canonical.FormatVersion` or `CommandLogFile.Version` change, no hash re-pin). Issuer identity and issue-tick eligibility stay out; the latter and the replay-command serialisation get mandatory follow-ups B-044 and B-045. Implemented and accepted 2026-09-06: `178 -> 184` green, `-- corpus` / `-- fixture` byte-identical, a back-compatible `PlayerCommand.Agent` read accessor kept so pre-existing `CorpusTests` compile unchanged. | S | B-011 | done | `tasks/TASK-020-COMMAND-VALIDATION-AND-RECIPIENTS.md` |
| TASK-021 | P3 | Minimum passable movement cost and overflow-safe pathfinding cost (realises B-046). Passable `MoveCost` must lie in `[Terrain.BaseMoveCost, Terrain.MaxMoveCost]` (new `MaxMoveCost` literal, new `MoveCostOutOfRange` `ScenarioError`), restoring the A* heuristic's admissibility/consistency precondition and keeping `moveCost` for a passable cell strictly below the `BlockedCost` sentinel and cost accumulation inside `int32`. Adds a Dijkstra-reference optimality property. No A* algorithm change, no `Canonical.FormatVersion` bump, no hash re-pin. | S | none | done | `tasks/TASK-021-MOVEMENT-COST-BOUNDS.md` |
| TASK-022 | P3 | Runtime cell-occupancy correctness in the Navigation and movement phase (realises B-047). `Simulation.navigationAndMovement` assigns `Position = next` for a mover with no same-tick rival without checking whether `next` is held by a stationary agent; TASK-017 reservation only arbitrates rival claimants for the same cell. Defines the pre-combat occupancy policy — an occupied cell blocks entry; two-agent swaps and n-agent rotation cycles are blocked; a follow chain advances this tick only when it clears to a free cell — via a deterministic vacation-chain resolution stage added to Pass 2 (after rival arbitration, before apply), a new `MovementObstructed` event, an `Obstructed` diagnostic overlay, and a unique-live-agent-cell-occupancy property plus corpus entries. No cooperative pathfinder, no `Canonical.FormatVersion` bump, no new `AgentState` field, no hash re-pin. Implemented 2026-09-06: stage-2b additive fixpoint over the completing non-yielded movers, new `MovementObstructed` event, `Overlay.Obstructed`, `WorldError.AgentsShareCell` base-case guard, a fifth FsCheck property (distinct live agents hold distinct cells, 200 cases), `follow-chain` + `swap-standoff` corpus entries, one golden pair. `184 -> 194` green; `-- corpus` / `-- fixture` byte-identical; `Canonical.FormatVersion` stays 2. One pre-existing `SimulationTests` fact had its wall repositioned (its detour ran through parked agents — the fixed defect). Accepted by Dave 2026-09-06. | M | none | done | `tasks/TASK-022-CELL-OCCUPANCY.md` |
| TASK-023 | P3 | Continuous integration for the framework-neutral solution (realises B-048). A GitHub Actions workflow (`.github/workflows/ci.yml`, `windows-latest`, on push and PR) that restores/builds/tests `CommandoWar.slnx` with the `global.json`-pinned SDK, then runs `cwheadless corpus` (fails the job on any divergence from the committed `content/replays/*.md` tables) and `cwheadless fixture` (CLI smoke). Makes the build, the `171`-test suite, and the committed replay/fixture hashes reproducible off the developer's machine (R-022, R-009, R-016; `docs/09` section 6). Selected-client compile, content validation, and package smoke deferred to the first P4 client task. Lockfile / `--locked-mode` restore considered and deferred (source change; revisit if R-016 materialises). One-line Windows-x64 / .NET `10.0.303` environment annotation added to the corpus/fixture docs. No source, no `.fsproj`, no test, no hash, no `Canonical.FormatVersion` change; no ADR. Implemented 2026-09-06: `ci.yml` written (checkout / setup-dotnet SHA-pinned, `global-json-file`, restore/build/test/`-- corpus`/`-- fixture`/clean-tree); environment annotations + `docs/09` section 6 realised-note landed. Verified locally: restore/build (0/0)/test (`Passed: 194`)/`-- corpus` (7× PASS)/`-- fixture` all exit 0, tree clean after the verbs; negative test fails `dotnet test` and `-- corpus` as required, reverted clean. Branch `task-023-simulation-ci` pushed and merged to `main` via PR #1; the first CI run on a real `windows-latest` runner was green. Accepted by Dave 2026-09-06. | S | none | done | `tasks/TASK-023-SIMULATION-CI.md` |
| TASK-024 | P3 | Issue-tick / submission-tick semantics (realises B-044) — **mandatory before G3**. Decision-plus-minimal-implementation: settle whether `PlayerCommand.IssueTick` is authored / submission / acceptance time (recommended: issue-time provenance + future appraisal input, renamed `IssuedAtTick`, distinct from a `SubmitAtTick` delivery tick); whether future-dated commands reject (recommended: `IssuedInFuture`) or queue; whether stale commands reject (recommended: no — appraisal's concern, no give-up horizon at intake); whether replay tick and issue tick must match (recommended: independent); whether command scheduling / identity is authoritative state (recommended: no — `CommandId` stays a batch-local dedup key plus a replay-log uniqueness invariant `DuplicateCommandIdInLog`, no `WorldState` field, so `Canonical.FormatVersion` does not move). The legacy `.cwlog` collapses both ticks to its one field. No hash re-pin, no ADR (settles a gate-required TASK-020 deferral, enforces `docs/04` section 2). Implemented 2026-09-07 on the recommended resolutions: `IssueTick -> IssuedAtTick` (only that field renamed; `RecordedCommand.Tick` kept with a clarifying doc comment), one `IssueTickOutOfRange` rejection case, `Replay.validate` gains `DuplicateCommandIdInLog`; `194 -> 199` green, `-- corpus` / `-- fixture` byte-identical, `Canonical.FormatVersion` stays 2. Accepted by Dave 2026-09-07. | S–M | B-014 | done | `tasks/TASK-024-ISSUE-TICK-SEMANTICS.md` |
| TASK-025 | P3 | Production replay-command serialisation (realises B-045) — **mandatory before G3**. Depends on TASK-024. After TASK-020 the `.cwlog` grammar (frozen at v1, legacy fixture-script) cannot express multi-recipient addressing, `Urgency`, `RiskTolerance`, or a distinct issue tick. Adds a new versioned, lossless, hand-rolled replay-command format in `CommandoWar.Sim` (recommended: deterministic line-based text; no new package — hand-rolled like `CommandLogFile.parse`), a `parse`/`serialise` round-trip property, one committed new-format replay fixture carrying a full envelope, and a `cwheadless replay` verb. `.cwlog`, `CommandLogFile.Version`, `Replay.FormatVersion`, `CommandLog.Version`, `Canonical.FormatVersion` (2) all unchanged; the seven existing corpus entries and the spike fixture untouched (corpus migration + the shared F# builder are B-049). No existing-hash re-pin, no ADR (realises `docs/04` section 16). Implemented 2026-09-07 on the confirmed resolutions (deterministic line-based text; header + command log, initial state stays a scenario reference): `src/CommandoWar.Sim/ReplaySerialisation.fs` (replay-command file format v1, `FSharp.Core` only), a round-trip property (`MaxTest = 200`) + full-field-matrix fact, unknown-version / canonical-mismatch / out-of-order rejection facts, `content/replays/envelope-full.{cwreplay,md}` (3-recipient `MoveTo`, `Immediate`/`Aggressive`, issued a tick before delivery), and a `cwheadless replay-file` verb (exit 2 parse/validate, 3 checkpoint divergence). `199 -> 206` green; `-- corpus` / `-- fixture` byte-identical; `Canonical.FormatVersion` / `CommandLogFile.Version` / `Replay.FormatVersion` / `CommandLog.Version` all unchanged. | M | B-014, B-044 | done | `tasks/TASK-025-REPLAY-COMMAND-SERIALISATION.md` |
| TASK-027 | P3 | Communication constraints and order delivery (realises the communication half of B-016; splits range/delay/jamming/aging to B-016b). Turns the `docs/04` section 12.2 no-op into a real Communication phase. `commandIntake` stops writing `AgentState.Destination` — it records an accepted `(command, recipient, target)` as a `StepState`-local **pending order**; the Communication phase (slot 2, immediately after intake) delivers it (`Destination` written, zero delay) to a recipient with `AgentState.CommunicationAvailable = true`, or emits **`OrderUndelivered of command * recipient * reason`** (`DeliveryFailure = UnableToCommunicate`) and drops it for one with `false`. No `OrderDelivered` success event (deferred to B-016b — with zero delay it duplicates `CommandAccepted`). `CommunicationAvailable` is **static** authored data (`RawDeployment` / `Deployment.CommunicationAvailable`, default `true`; a `commsBlackout` list on the corpus builder), **excluded from `Canonical.encode`** exactly as `Terrain` is (ADR-0002 amendment) — **`Canonical.FormatVersion` stays 3, zero hash re-pin, the nine existing corpus/fixture entries byte-identical**. No PRNG draw. New `Overlay.UndeliveredOrder` + `AgentMarker.CommunicationAvailable` (rendered only when `false`), one committed golden (`lost-comms-tick-001.*`), new `lost-comms` corpus entry (the "Lost communication" scenario, `docs/05` section 16). `216 → 228` green (+5 `SimulationTests`, +1 `DeterminismPropertyTests` property 7 at 200 cases, +1 `CanonicalHashTests`, +3 `DiagnosticsTests`, +1 `ScenarioTests`, +1 `CorpusTests` theory case). Radio range / delay / jamming / radio-destroyed / report aging → B-016b. No ADR (ADR-0002 amendment governs the static-data exclusion). Central decisions A–E confirmed with Dave 2026-09-07 before the phase bodies. Accepted by Dave and merged to `main` (`--no-ff`, branch deleted; not pushed) 2026-09-08. | S–M | B-015 | done | `tasks/TASK-027-COMMUNICATION-AND-ORDER-DELIVERY.md` |
| TASK-028 | P3 | Staged order appraisal and typed reasons (realises B-017; the G3 keystone). Turns the `docs/04` section 12.5 no-op into a real Appraisal phase over a new `Appraisal` leaf module. The Communication phase now writes a new **canonical** `AgentState.Order: ReceivedOrder option` (and resets `AgentState.Disposition`) instead of `Destination`; the Appraisal phase (slot 5) reads `Order` + `WorldState.TacticalKnowledge` + terrain + a new static `AgentState.Discipline`, runs stages 1–4 (stage 1 guaranteed upstream; stage 2 `Pathfinding` feasibility; stage 3 route exposure to *known* threats only — engagement range, `Sight.visible` from the last-known cell, directional `Terrain.cover`; stage 4 exposure vs a threshold from `Discipline` + the order's `RiskTolerance` / `Urgency`), and produces an **`OrderDisposition`** (`Accepted | Refused of reasons | Unable of reasons` — `Adapted` / `Delayed` deferred). **`DecisionReason`** subset `NoKnownRoute | RouteTooExposed of threat: AgentId option`. On `Accepted` the phase writes `Destination`. New **`OrderAppraised of agent * command * disposition`** event (every appraisal, including `Accepted`); new **`Overlay.OrderAppraisal`** (derived in `frame` and `frameOf`, shows the disposition + exposed route cells). `PlayerIntent` / `Urgency` / `RiskTolerance` moved `Commands.fs -> Domain.fs`. `AppraisalConfig` = one module of `[<Literal>]` integers (the `PerceptionConfig` precedent); **no PRNG draw**. `Order` + `Disposition` are genuine per-tick canonical memory, so **`Canonical.FormatVersion` bumps 3 -> 4** with a full re-pin — behaviour-neutral for movement (tick counts unchanged on all ten entries; event counts move by exactly one `OrderAppraised` per order), except `blocked-goal` whose unreachable-target order is now `Unable(NoKnownRoute)` at appraisal instead of `MovementBlocked` at navigation (same tick count, same event count). `Discipline` is static authored data (`Deployment.Discipline`), **excluded** from the canonical image (the `CommunicationAvailable` precedent). New `exposed-approach` corpus entry — two friendlies of different `Discipline` ordered along the same exposed approach past a known hostile, one `Refused` and one `Accepted` (the `docs/07` section 9 criterion 2 G3 evidence). Reappraisal trigger in scope: "a new order is received" only; hysteresis, suppression, stress, trust, and the other triggers are B-021; commitments / the finite executor are B-018; stage-5 safer adaptation is B-018. No ADR (ADR-0002 amendment governs the bump). Central decisions A–H confirmed with Dave 2026-09-08 before the phase bodies. `228 -> 242` green; `dotnet build` `0/0`; `-- corpus` 10/10; `-- fixture` format 4 / 34 events; `-- replay-file envelope-full` OK at canonical 4; `corpus --regenerate` a byte-identical zero diff. Accepted by Dave and merged to `main` (`--no-ff`, branch deleted; not pushed) 2026-09-08. | M | B-014, B-016 | done | `tasks/TASK-028-ORDER-APPRAISAL-AND-TYPED-REASONS.md` |
| TASK-029 | P3 | Godot appraisal-divergence demo (a read-only, corpus-scoped slice of backlog B-029 pulled forward as P3 decision-support; B-029 proper stays P4). A runnable Godot scene (`src/CommandoWar.Client.Godot/scenes/AppraisalDemo.tscn` + `src/AppraisalDemoScene.cs`, `run/main_scene`) that loads the committed `exposed-approach` corpus entry, builds its `DiagnosticFrame` sequence via `DiagnosticRender.runFrames`, and renders the authoritative state + the appraisal overlay with a 13-frame tick slider: agents by side, the `KnownContact` ring for contact 2, each agent's `OrderDisposition` (agent 0 `Refused RouteTooExposed threat-agent-2` / agent 1 `Accepted`) as readable text, and the exposed route cells tinted — the visual proof of `docs/07` section 9 criteria 2 and 11, and the refuse-near-threats feel check before B-019. All non-trivial logic (corpus load, `runFrames`, disposition → text / tone, the flat `[<CLIMutable>]` view model) is in a new framework-neutral F# helper `src/CommandoWar.Headless/AppraisalDemo.fs`; the C# script is a thin renderer. **Scoped deviation** from ADR-0004's "F# client-core / no logic in C#" rule for this disposable P3 demo — recorded in the task file + ledger, **not** a precedent for the P4 client tasks (B-026 / B-027 / B-029). No `CommandoWar.Sim` change, no new event / `Overlay` case / `Canonical.FormatVersion` bump / golden re-pin, no new NuGet / Godot dependency. `CommandoWar.Client.Godot` gains one `ProjectReference` to `CommandoWar.Headless`; still out of `CommandoWar.slnx`. `242 -> 244` green (+2 `DiagnosticsTests`); `dotnet build CommandoWar.slnx` + the client `.slnx` both `0/0`; `-- corpus` 10/10 and `-- fixture` byte-identical; headless Godot `--selfcheck` smoke MATCH; committed screenshot `docs/evidence/task-029-appraisal-demo.png`. No ADR (Decision F). Central decisions A–F confirmed with Dave 2026-09-08 before the phase bodies. Accepted by Dave and merged to `main` (`--no-ff`, branch deleted; not pushed) 2026-09-12. | S–M | B-017 (TASK-028), TASK-004 | done | `tasks/TASK-029-GODOT-APPRAISAL-DEMO.md` |
| TASK-031 | P3 | Basic hitscan combat and directional cover effects (realises B-019; unblocked by TASK-026's `AgentState.VisibleContacts`). Turns the `docs/04` section 12.8 no-op into a real Combat phase: automatic symmetric engagement (candidates from a shooter's own `VisibleContacts`, line of fire re-verified fresh at combat time, nearest qualifying target wins, ties by ascending `AgentId`); a deterministic `0..1000`-scale hit chance (range + directional `Terrain.cover` mitigation, reusing a newly-public `Appraisal.attackDirection`); one `RandomStream.next` draw per engaging agent — **the simulation's first real gameplay PRNG consumption**. New leaf `src/CommandoWar.Sim/Combat.fs` (`CombatConfig`, `Combat.hitChance`, `Combat.chooseTarget`); new `ShotFired of shooter * target * hit` event; new `Overlay.FireLine` (`frameOf`-only). **No ammunition, weapon readiness, cooldown, or reload** (a future task, not scoped); **no wound, death, or suppression consequence** (B-020/B-031). New `open-engagement` corpus entry proves the phase wiring end-to-end; cover/range monotonicity and target selection are proved directly on the pure `Combat.hitChance`/`Combat.chooseTarget` functions. **`Canonical.FormatVersion` unchanged** (`WorldState.Random` was already canonical) — but `perception-contact` and `exposed-approach` (TASK-026/028's enemy-bearing entries) legitimately re-pin from the tick an agent first comes into weapon range and line of sight of the opposing side (tick counts unchanged on both; `exposed-approach` tick 1 unaffected); `demo.html` similarly gains combat from tick 8. `254 -> 265` green (+1 `CorpusTests` theory case, +8 `SimulationTests`, +1 `DeterminismPropertyTests` property 10 at 200 cases, +1 `DiagnosticsTests` golden fact). `dotnet build` 0/0; `-- corpus` 12/12; `-- fixture` byte-identical; `-- replay-file envelope-full` unchanged. No ADR. Central decisions A–I confirmed with Dave 2026-09-13 before the phase bodies. Accepted by Dave and merged to `main` (`--no-ff`, branch deleted; not pushed) 2026-09-13. | M | B-009, B-011 (TASK-026) | done | `tasks/TASK-031-HITSCAN-COMBAT.md` |
| TASK-032 | P3 | Suppression and exposure model (realises B-020; unblocked by TASK-031's Combat phase). Turns the `docs/04` section 12.8 "create suppression independent of a hit" bullet and section 12.9 (State consequences, previously fully no-op) into real behaviour: Combat now also raises the target's `AgentState.Suppression` on every qualifying shot (`Suppression.gain`, independent of a hit, mitigated by the same directional `Terrain.cover` geometry as hit chance — "the exposure model"), and the State consequences phase decays every agent's `Suppression` unconditionally each tick, floored at 0 (`Suppression.decay`). New leaf `src/CommandoWar.Sim/Suppression.fs` (`SuppressionConfig`, `Suppression.gain`/`.raise`/`.decay`, pure); `Combat.fs` itself unchanged. No new event — a rise is a deterministic function of the already-emitted `ShotFired`, decay is silent (the `Progress` precedent); new sparse `Overlay.AgentSuppression` (one per agent with `Suppression > 0`, the `UndeliveredOrder`/`KnownContact` precedent). **Deliberately mechanic only**: no wiring into Appraisal's resolve threshold, no reappraisal trigger, no movement/executor effect — all B-021. `AgentState.Suppression` is genuine canonical state: `Canonical.FormatVersion` **4 -> 5**, full re-pin of the fixture + all twelve `content/replays/*.md` + `envelope-full.{cwreplay,md}` + every `content/diagnostics` golden — **tick counts and event counts unchanged everywhere** (no new event type); `open-engagement`, `perception-contact`, and `exposed-approach` additionally carry genuine new non-zero `Suppression` values from the tick combat first fires (TASK-031's pinned ticks); the TASK-029 Godot demo's `--selfcheck` pinned hash moved too and was updated. No new corpus entry — decay and clamping proved directly on the pure `Suppression.decay`/`.raise` functions. `265 -> 274` green (+8 `SimulationTests`, +1 `DeterminismPropertyTests` property 11 at 200 cases; the `open-engagement` `DiagnosticsTests` fact extended in place). `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` idempotent); `-- fixture` format 5, 36 events unchanged; `-- replay-file envelope-full` OK at canonical 5, 78 events unchanged. No ADR. Central decisions A–H confirmed with Dave 2026-09-14 before the phase bodies. Accepted by Dave and merged to `main` (`--no-ff`, branch deleted; not pushed) 2026-09-14. | M | B-019 (TASK-031) | done | `tasks/TASK-032-SUPPRESSION-AND-EXPOSURE.md` |
| TASK-033 | P3 | Stress and bounded reappraisal (realises B-021, partial; unblocked by TASK-032's `AgentState.Suppression`). Turns the `docs/04` section 12.9 "update stress from recent events" bullet into real behaviour and adds two of `docs/05` section 14's six reappraisal triggers on top of "a new order is received". New leaf `src/CommandoWar.Sim/Stress.fs` (`StressConfig`, `Stress.gain`/`.raise`/`.decay`, pure); `AgentState.Stress` (raised while `VisibleContacts` is non-empty — the one `docs/05` section 8 stress source with a system behind it — and decayed every tick, both in State consequences) and `AgentState.SuppressionBand` (a hysteresis latch over `Suppression`, `Enter = 500` / `Exit = 300`, computed inside the Appraisal phase) are new canonical fields. `Appraisal.resolveThreshold`/`.appraise` gain `stress`/`suppressed` parameters — a continuous drag (`StressDivisor = 25`) and a discrete penalty (`SuppressionBandPenalty = 30`), the whole threshold floored at 0 so a fully unexposed route is never refused for stress/suppression alone. Two new reappraisal triggers live inside `Simulation.appraisal` (no new `Phases.order` slot): **knowledge-change** (any `ContactObserved`/`ContactExpired` this tick, global) and **suppression-band** (the latch flipping, either direction) — both reset an already-appraised, non-fulfilled order's `Disposition` to `None`, and both explicitly exclude a fulfilled order (found necessary during implementation: resetting one would defeat `commitmentAndLocalAction`'s completion clear). `AgentState.Discipline` deliberately stays static and non-canonical — corrects B-021's literal backlog wording, since it never mutates in this cut. **Deliberately narrower than B-021's full backlog text**: exposure-band, wounded, support, and leadership triggers, and dynamic trust, stay out — the first three need systems that don't exist (B-030/B-031), exposure-band needs per-tick route-exposure tracking (a materially larger cut). No new event type — a trigger-driven reappraisal still emits the existing `OrderAppraised`; new sparse `Overlay.AgentStress` (the `AgentSuppression` precedent). `AgentState.Stress`/`.SuppressionBand` are genuine canonical state: `Canonical.FormatVersion` **5 -> 6**, full re-pin — **tick counts and event counts unchanged everywhere** (both triggers only ever flip an outcome, never add/remove a tick or event); `exposed-approach`, `open-engagement`, and `perception-contact` additionally carry genuine new non-zero `Stress` values from the tick each pair first sees each other; the TASK-029 Godot demo's `--selfcheck` pinned hash moved a third time and was updated. No new corpus entry — proof is via `SimulationTests` facts directly on the phase (a `Refused` order becomes `Accepted` once its only known threat's contact expires; the suppression-band latch reappraises an `Accepted` order on either flip direction) plus pure-function facts. `274 -> 285` green (+10 `SimulationTests`, +1 `DeterminismPropertyTests` property 12 at 200 cases; the combat-determinism fact extended in place). `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` idempotent, confirmed via `md5sum`); `-- fixture` format 6, 36 events unchanged; `-- replay-file envelope-full` OK at canonical 6, 78 events unchanged. No ADR. Central decisions A–I confirmed with Dave 2026-09-14 before the phase bodies. Accepted by Dave and merged to `main` (`--no-ff`, branch `task-033-stress-and-bounded-reappraisal` deleted; not pushed) 2026-09-15. | M | B-020 (TASK-032) | done | `tasks/TASK-033-STRESS-AND-BOUNDED-REAPPRAISAL.md` |
| TASK-034 | P3 | Hostile tactical picture and observed-only targeting proof (realises B-022, partial; unblocked by TASK-026's `Perception.mergeKnowledge` and TASK-031's `Simulation.combat`). New `WorldState.HostileTacticalKnowledge: Contact[]`, symmetric to the friendly `TacticalKnowledge` (TASK-026) and built by calling the already side-agnostic `Perception.mergeKnowledge` a second time, filtered to `Side = Hostile`, inside the existing `tacticalKnowledge` phase slot — no change to `Perception.fs`. New `Overlay.HostileKnownContact` diagnostic case (the `AgentStress`/`AgentSuppression` precedent — a distinct case, not a reuse of `Overlay.KnownContact`). Also proves, rather than builds, `docs/09` section 8's "enemy does not target an unobserved player position" criterion: `Simulation.combat` already draws shooter candidates only from same-tick `VisibleContacts` (TASK-031 Decision A), strictly tighter than any tactical-knowledge store, so one new test fact plus a `docs/09` wording correction closes it out. `Canonical.FormatVersion` **6 -> 7**, full re-pin — **not** behaviour-neutral this time: `open-engagement`, `perception-contact`, and `exposed-approach` (the same three entries TASK-031/032/033 kept re-pinning) legitimately gain new non-zero `HostileTacticalKnowledge` values from the tick a hostile first sees a friendly. No new corpus entry, no new event type, no ADR. **Deliberately narrower than B-022's full text**: suppress-likely-routes (needs a `Suppress` order, B-030), seek-adjacent-cover-under-pressure (needs the first autonomous-movement decision for an unordered agent — no design exists), and scripted fall-back (needs authored conditions + `Withdraw` semantics) all stay out — considered and rejected as a materially larger cut (see the task file's "Considered and rejected" section). B-022 therefore stays `review`, not `done`, after this task — the B-021/TASK-033 precedent. Drafted 2026-09-15: central decisions A–F confirmed with Dave (Decision A directly via `AskUserQuestion` before drafting; B–F in the task file). Implemented 2026-09-15: `285 -> 287` green (+1 `CanonicalHashTests` fact for the new section / `firstDifferingSection` label, +1 `SimulationTests` fact for the Decision E proof; `DeterminismPropertyTests` property 6 extended in place to also ground `HostileTacticalKnowledge`; the perception-contact `DiagnosticsTests` fact and the exposed-approach `AppraisalDemo` unhandled-overlay fact extended in place for the new symmetric state). `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` twice byte-identical); `-- fixture` format 7, 36 events unchanged; `-- replay-file envelope-full` OK at canonical 7, 78 events unchanged; `dotnet list` FSharp.Core only; source scan clean. Accepted by Dave and merged to `main` (`--no-ff`, branch `task-034-hostile-tactical-picture` deleted; not pushed) 2026-09-15. | M | B-015 (TASK-026), B-019 (TASK-031) | done | `tasks/TASK-034-HOSTILE-TACTICAL-PICTURE.md` |
| TASK-030 | P3 | Commitment and finite move/hold executor (realises B-018; unblocked by TASK-028's Appraisal phase). Turns the `docs/04` section 12.6 no-op into a real phase: a `Commitment` DU (`Holding \| Moving of MoveCommitment`, the only two cases current systems support), realised as its own `commitmentAndLocalAction` phase in the existing `Phases.order` slot (between Appraisal and Navigation), and the one interrupt priority current systems can source (`docs/05` section 11 priority 6, "new higher-priority command" — a superseding order). **`Commitment` is a pure derived value over `Order`/`Disposition`/`Destination` (the `AgentState.Route` precedent), not new canonical state — no `Canonical.FormatVersion` bump, no hash re-pin.** New leaf `src/CommandoWar.Sim/Commitment.fs`; `appraisal`'s fulfilled-order housekeeping branch relocated (unchanged) into the new phase; new `CommitmentEstablished` / `CommitmentCompleted` events; new `Overlay.AgentCommitment` (derived in `frame` and `frameOf`); new `reissued-order` corpus entry proving supersession end-to-end (`docs/07` section 8 step 7). Stage-5 safer adaptation (`Adapted`, an exposure-aware reroute) and interrupt priorities 1–5 (need combat/suppression/a stall counter) stay out. Every existing corpus entry, the fixture, and `envelope-full` gain `CommitmentEstablished`/`CommitmentCompleted` events in their trace with zero state-hash change. `244 -> 254` green (+8 `SimulationTests`, +1 `DeterminismPropertyTests` property 9 at 200 cases, +1 `DiagnosticsTests` golden fact). `dotnet build` 0/0; `-- corpus` 11/11; `-- fixture` format 4, hashes unchanged, 34 -> 36 events; `-- replay-file envelope-full` OK at canonical 4, hashes unchanged, 75 -> 78 events. No ADR. Central decisions A–H confirmed with Dave 2026-09-13 before the phase bodies. Accepted by Dave and merged to `main` (`--no-ff`, branch deleted; not pushed) 2026-09-13. | M | B-017 (TASK-028) | done | `tasks/TASK-030-COMMITMENT-AND-FINITE-EXECUTOR.md` |
| TASK-036 | P3 | Shared F# fixture builder for the test corpus (realises B-049; unblocked by B-045/TASK-025). One builder in `Corpus.fs` authors scenario geometry (grid, friendly/enemy deployments, terrain runs, cover, objective/extraction) and a per-agent command schedule as a single value, replacing the eleven hand-written `…World ()` functions and their hand-authored `.cwlog` command files with a generator and a regenerated, committed `.cwreplay` per entry (`ReplaySerialisation`, B-045's production format — the frozen `.cwlog` v1 grammar cannot express multi-recipient commands, `Urgency`, or `RiskTolerance`). The builder's authored value is the sole runtime source of truth; `.cwreplay` is a generated, diff-checked artefact, not read back (the `<name>.md` precedent), eliminating the `Corpus.fs` ↔ `.cwlog` drift the backlog item names. `spike-fixture` and `tests/CommandoWar.Sim.Tests/SimulationTests.fs` (~280 existing facts, unrelated-refactor scope) stay untouched. No `CommandoWar.Sim` change, no `Canonical.FormatVersion` change, no ADR — a pure tooling refactor. Drafted 2026-09-15: two central decisions (`.cwreplay` over `.cwlog`; migrate all eleven entries) confirmed with Dave via `AskUserQuestion`. Implemented 2026-09-16: `Entry` gains `Commands: RecordedCommand[] option`; `CommandId`/`Sequence` numbering deliberately mirrors `CommandLogFile.parse`'s order-of-appearance convention (canonical-hash-affecting). Deviation found during implementation: `tests/CommandoWar.Sim.Tests/DiagnosticsTests.fs` (nine call sites) and `src/CommandoWar.Headless/AppraisalDemo.fs` also called `Corpus.loadLog` directly on a migrated entry and needed the same swap to `Corpus.commandsOf`, beyond the drafted "Allowed scope" — both mechanical, no behaviour change. Every migrated `.md` byte-identical except its "Command log" row (corrected `.cwlog` -> `.cwreplay`, invisible to `Corpus.parseTable`). `287 -> 287` green; `dotnet build` 0/0; `-- corpus` 12/12 (`--regenerate` twice byte-identical); `-- fixture` format 7, 36 events unchanged; `-- replay-file envelope-full` unchanged. Implemented directly on `main`, no branch (the DIAG-001/TASK-035 precedent). Accepted by Dave 2026-09-16. | M | B-045 (TASK-025) | done | `tasks/TASK-036-SHARED-FIXTURE-BUILDER.md` |
| TASK-037 | P3 | Minimal Suppress order and B-021/B-022 dependency closeout (realises a thin slice of B-030, pulled forward as P3 decision-support the TASK-029/034 precedent; explicitly descopes B-021 and B-022 to `done`). New `PlayerIntent.Suppress of target: AgentId` (`Domain.fs`, targeting a known contact, never a bare `Cell` — no suspected-threat model exists) and `DecisionReason.TargetNotKnown` (the pre-planned `docs/05` vocabulary case). `Appraisal.appraise` gains a `Suppress` branch: `Accepted` if the named contact is in the issuing agent's own tactical knowledge, else `Unable(TargetNotKnown)` — no route, so stages 3/4 never run for it. New `Commitment.Suppressing of SuppressCommitment` (`docs/05` section 9's own pre-named shape): the executor holds position and, in Combat, pins the named contact as the shooter's only candidate (bypassing nearest-candidate selection) — `Combat.fs`/`Combat.chooseTarget` themselves unchanged. `Appraisal.routeExposure`/`cellPressure` gain a `suppressedThreats: AgentId[]` parameter and zero a threat's pressure contribution while its `AgentState.SuppressionBand` is latched (reusing the already-canonical, symmetric TASK-033/034 hysteresis latch, not new "who is suppressing whom" bookkeeping). A third reappraisal trigger — any agent's `SuppressionBand` flipping this tick, global scope, the knowledge-change precedent — resets a *different* agent's non-fulfilled `Refused`/`Unable` order, realising `docs/07` section 8 step 6. New `suppress-relieves-exposure` corpus entry proves the mechanism end to end: a low-`Discipline` friendly `Refused RouteTooExposed` against a known hostile; a second friendly's `Suppress` order against that hostile drives its `Suppression` into `SuppressionBand` within a worst-case 5 ticks (`SuppressionConfig.GainOnMiss` / `DecayPerTick`, deterministic regardless of hit/miss RNG); the first friendly's order resets and reappraises `Accepted`. `Canonical.FormatVersion` **7 -> 8** (the new `PlayerIntent`/`DecisionReason` cases sit inside the already-canonical `Order`/`Disposition` sections — a byte-layout change, not a new section); full re-pin, behaviour-neutral except the new corpus entry. `ReplaySerialisation`'s grammar already had room for a `suppress <targetAgentId>` command keyword without its own version bump (`docs/04`'s own comment anticipated this). No new event type (`CommandAccepted`/`OrderAppraised`/`ShotFired` already cover every state change; a `Suppress` command's `CommandAccepted` reports the recipient's own position, since it has no `Cell` target of its own). `293` green (+6 `SimulationTests` facts; `DeterminismPropertyTests` properties extended in place for exhaustiveness); `dotnet build` 0/0; `-- corpus` 13/13 (`--regenerate` twice byte-identical); `-- fixture` format 8, 36 events unchanged; `-- replay-file envelope-full` OK at canonical 8, 78 events unchanged; `dotnet list` `FSharp.Core` only; source scan clean. Godot's `--selfcheck` pinned hash updated for the moved `exposed-approach` tick-1 hash but not independently re-run through Godot in this session (no Godot install here) — flagged for Dave to re-check before accepting. Eleven central decisions (A-K) confirmed with Dave via `AskUserQuestion` before the phase bodies. Accepted by Dave and merged to `main` (`--no-ff`, branch `task-037-suppress-order-and-dependency-closeout` deleted; not pushed) 2026-09-16. | L | B-018 (TASK-030), B-020 (TASK-032) | done | `tasks/TASK-037-SUPPRESS-ORDER-AND-DEPENDENCY-CLOSEOUT.md` |
| TASK-026 | P3 | Observations and shared squad tactical knowledge (realises B-015). First phase consumer of the TASK-012 `Sight` module. Perception (phase 12.3) populates a per-agent `AgentState.VisibleContacts` **derived cache** (excluded from `Canonical.encode`, the `Route` precedent) from `Sight.visible` over `Terrain` within a `SightRange` cap, for both sides, emitting `ContactObserved` on a new sighting. Tactical knowledge (phase 12.4) merges friendly observations into a new **canonical** `WorldState.TacticalKnowledge` (contact id, last-known cell, `LastSeenTick`, decaying confidence; instant single-squad sharing; `ContactExpired` on decay-out). `Canonical.FormatVersion` bumps **2 -> 3** with a full **behaviour-neutral** hash re-pin — every committed scenario is enemy-free so the store is empty at every checkpoint (the TASK-018 `Progress` precedent); tick counts and event counts must be unchanged for all seven entries. New `KnownContact` diagnostic overlay + committed golden, the first enemy-bearing corpus entry, one tuning structure (`SightRange` / confidence bands / `StaleAfter` / `ExpireAfter`), a tactical-knowledge property. Symmetric raw visibility, friendly-squad-only shared knowledge. Communication / report aging (B-016), appraisal (B-017), combat (B-019), enemy doctrine and "enemy does not target an unobserved position" (B-022), private persistent beliefs (`docs/05` section 17) all explicitly out. No ADR (the ADR-0002 amendment governs the format-version bump). Independent of TASK-024 / TASK-025. **Implemented 2026-09-07:** decisions confirmed (full decaying store; `SightRange = 10` Chebyshev, `ConfidenceFull = 1000`, `ConfidenceBandDrop = 250`, `StaleAfter = 20`, `ExpireAfter = 60`; symmetric visibility, friendly-only knowledge). `Canonical.FormatVersion` `2 -> 3`, behaviour-neutral re-pin of the fixture + all seven `.cwlog` + `envelope-full.{cwreplay,md}` (tick/event counts unchanged); `DemoScenario`'s hostile means `demo.html` legitimately gains the `KnownContact` overlay. New leaf `Perception.fs`; new `perception-contact` corpus entry + golden; `206 -> 216` green. Accepted by Dave and merged to `main` 2026-09-07. | M | B-009 | done | `tasks/TASK-026-PERCEPTION-AND-TACTICAL-KNOWLEDGE.md` |

## 3. Planned simulation work

These items are not implementation-ready. Create and review a task file before selecting one.

| ID | Phase | Work item | Size | Dependencies | Gate | Status |
|---|---|---|---|---|---|---|
| B-007 | P2 | Define and validate initial scenario DTO and content version | S | TASK-006 | G2 | done (`tasks/TASK-008-SCENARIO-DTO-AND-CONTENT-VERSION.md`) |
| B-008 | P2 | Implement terrain grid, elevation, passability, and directional cover | M | B-007 | G2 | done (`tasks/TASK-010-TERRAIN-GRID.md`) |
| B-012a | P2 | Headless diagnostic visualisation and the framework-neutral diagnostic frame | M | B-008 | G2 | done (`tasks/TASK-011-DIAGNOSTIC-VISUALISATION.md`) |
| B-009 | P2 | Implement deterministic line of sight and opacity | M | B-008 | G2 | done (`tasks/TASK-012-LINE-OF-SIGHT.md`) |
| B-010 | P2 | Implement deterministic grid pathfinding with stable tie-breaking | M | B-008 | G2 | done (`tasks/TASK-013-PATHFINDING.md`) |
| B-011 | P2 | Implement movement, formation slots, and short-horizon cell reservation. First phase consumer of the TASK-013 `Pathfinding` module: the Navigation and movement phase (12.7) replaces `PlaceholderMovement` with a `Pathfinding`-driven executor. | L | B-010 | G2 | done (`tasks/TASK-015-MOVEMENT-EXECUTOR.md`; single-agent executor only) |
| B-011b | P2 | Multi-agent movement: short-horizon cell reservation and deadlock avoidance. Split from B-011 by TASK-015 (single-agent executor only); narrowed to reservation alone by TASK-017, which split formation slots and sub-cell movement progress out to B-011c. | M | B-011 | G2 | done (`tasks/TASK-017-CELL-RESERVATION.md`) |
| B-011c | P2 | Sub-cell movement progress within an edge (`AgentState.Progress`; threshold `Terrain.moveCost`, increment `Terrain.BaseMoveCost`). New per-tick authoritative state with no derivation path from `Position` alone. Narrowed from "formation slots and sub-cell progress" to progress alone by TASK-018, which split formation slots out to B-011d. | M | B-011b | G2 | done (`tasks/TASK-018-SUBCELL-MOVEMENT-PROGRESS.md`) |
| B-011d | P2 | Formation slots: a squad/formation grouping and per-agent slot assignment do not exist anywhere yet (closer to `docs/05_COMMAND_AND_AGENT_AI.md` than to movement mechanics). Split from B-011c by TASK-018, which landed sub-cell movement progress only. Realised by TASK-059 (`tasks/TASK-059-FORMATION-SLOTS.md`): no new order type or multi-agent dispatch -- a formationed agent's own accepted `MoveTo` order resolves its real target as the order's own literal cell (the formation anchor) plus the agent's authored slot offset (`Appraisal.resolveFormationTarget`), redirected to the nearest passable, unoccupied cell in `AppraisalConfig.FormationSlotSearchRadius` (2) Chebyshev cells if the exact offset is blocked or occupied (the `bestCoverNear` precedent), falling back to the literal anchor if nothing in radius qualifies. New `RawScenario.Formations` table (`ScenarioContent.Version` 5 -> 6) referenced by an optional `RawDeployment.FormationId`/`.SlotIndex`, resolved onto a new static, non-canonical `AgentState.FormationOffset` (the `MoveSpeed` precedent) -- both `Friendly` and `Hostile` agents may be authored into a formation. Scoped to `MoveTo` only; `Hold`/`Assault`/`Withdraw`/`Suppress` keep their existing target semantics for a formationed agent, composing formation offsets with those flagged as a separate future question, not built here. New `Overlay.AgentFormationSlot` satisfies the diagnostics-extension rule; new `content/replays/formation-slots` corpus entry (two agents in a "wedge" formation both ordered to one nominal cell, landing on distinct offset destinations) and its `content/diagnostics/formation-slots-tick-001.*` golden. No `Canonical.FormatVersion` bump (the offset is static, excluded). `dotnet build` all three `.slnx` (main Release; Godot client Debug) 0/0; `dotnet test` 377/377 (+15); `cwheadless corpus` 17/17 (`--regenerate` added only the one new entry, every pre-existing entry byte-identical); `cwheadless fixture` format 12 unaffected. | M | B-011c | G2 | done (`tasks/TASK-059-FORMATION-SLOTS.md`; accepted by Dave 2026-09-19, no client-scenario change flagged, no changes requested) |
| B-012 | P2 | Add divergence diagnostics and replay corpus infrastructure: a committed multi-entry replay corpus, an in-suite determinism check, and a `cwheadless corpus` localisation verb. | M | B-008 | G2 | done (`tasks/TASK-016-REPLAY-CORPUS.md`) |
| B-012b | P2 | Component-level per-agent subhashes in the divergence report. Split from B-012 by TASK-016; narrowed by TASK-019, which landed the generative/FsCheck determinism property tests alone — `Canonical.firstDifferingSection` already reports the first differing `Agent[id]` by direct comparison, and the stated justification for subhashes (desirable "once the world grows") has no evidence yet (nothing has run past ~50 agents, `content/benchmarks/BASELINE.md`). Reopen only if a future benchmark or property-test counterexample shows the existing per-agent diff is inadequate. | S | B-012 | G2 | proposed |
| B-013 | P2 | Add headless performance and allocation benchmark harness | S | B-008 | G2 | done (`tasks/TASK-014-BENCHMARK-HARNESS.md`) |
| B-014 | P3 | Define command validation and recipient selection | S | B-011 | G3 | done (`tasks/TASK-020-COMMAND-VALIDATION-AND-RECIPIENTS.md`; 2026-09-06, partial section 13 envelope; B-044 / B-045 still mandatory before G3) |
| B-044 | P3 | Resolve issue-tick / submission-tick semantics — **mandatory before G3**. `PlayerCommand.IssueTick` and `RecordedCommand.Tick` are two tick concepts that `CommandLogFile.parse` currently forces equal. Define whether `IssueTick` is authored / submission / acceptance time; whether future-dated commands queue or reject; whether stale commands are rejected; whether replay tick and issue tick must match; whether command scheduling/identity is authoritative state (this also bounds cross-tick duplicate-`CommandId` handling). Likely renames the fields (`IssuedAtTick` / `SubmitAtTick`). | S–M | B-014 | G3 | done (`tasks/TASK-024-ISSUE-TICK-SEMANTICS.md`; only `PlayerCommand.IssueTick -> IssuedAtTick` renamed, `RecordedCommand.Tick` kept; `IssueTickOutOfRange` + `DuplicateCommandIdInLog`; no `Canonical.FormatVersion` bump; accepted 2026-09-07) |
| B-045 | P3 | Decide the production replay-command serialisation — **mandatory before G3**. After B-014, `.cwlog` is a legacy fixture-script format that cannot express multi-recipient addressing, `Urgency`, or `RiskTolerance`. Choose: version `.cwlog` to carry the whole accepted envelope, or add a separate versioned replay-command format. G3 evidence requires replay to reproduce the complete decision trace. After TASK-024 the envelope also carries an `IssuedAtTick` distinct from the delivery tick that `.cwlog` cannot express. | M | B-014, B-044 | G3 | done (`tasks/TASK-025-REPLAY-COMMAND-SERIALISATION.md`; 2026-09-07. Both selection decisions confirmed with Dave: deterministic line-based text; header + `RecordedCommand[]`, initial state stays a scenario reference. `ReplaySerialisation` v1, `cwheadless replay-file`, `content/replays/envelope-full.cwreplay`. No existing-hash re-pin; no ADR. Accepted by Dave and merged to `main` 2026-09-07) |
| B-046 | P3 | Correctness fix to the G2 terrain/pathfinding deliverables (TASK-010 / TASK-013), found post-gate by the standing review: a passable cell with `MoveCost` below `Terrain.BaseMoveCost` is valid authored content and breaks the A* heuristic's admissibility/consistency precondition (`Pathfinding.find` can return a non-lowest-cost path); `Pathfinding` accumulates `g + moveCost` in unchecked `int` with no authored upper bound (overflow; passable cost can equal the `BlockedCost` sentinel). Bound passable `MoveCost` both sides, add a Dijkstra-reference optimality property. Run before B-015 / B-019. | S | none | G3 | done (`tasks/TASK-021-MOVEMENT-COST-BOUNDS.md`) |
| B-047 | P3 | Correctness fix to the G2 movement deliverables (TASK-015 / TASK-017), found post-gate by the standing review: runtime cell occupancy is unprotected — `Simulation.navigationAndMovement` lets a mover with no same-tick rival assign `Position = next` without checking whether `next` is already held by a stationary (idle, blocked, arrived, or mid-edge) agent; TASK-017 reservation only arbitrates rival claimants for the same next cell, and the TASK-017 ledger flagged both the stationary-occupant and rotation-deadlock gaps for a later task that never picked them up. Define the occupancy policy before perception/combat: a stationary-occupied cell blocks entry; two-agent swaps and n-agent rotation cycles are blocked (no first mover); a follow chain advances same-tick only when it terminates in a free cell. Add a deterministic vacation-chain resolution stage to Pass 2 (not a cooperative pathfinder), a `MovementObstructed` event and `Obstructed` overlay, and a unique-live-agent-cell-occupancy property. Run before B-015 / B-019. | M | none | G3 | done (`tasks/TASK-022-CELL-OCCUPANCY.md`; accepted 2026-09-06) |
| B-048 | P3 | Continuous integration for the framework-neutral solution, from the standing review's recommendation and `docs/ledger/2026-09-06-control-plane-reconciliation.md` "Deviations" ("no CI workflow; the '171 passing tests' figure is locally recorded, not independently reproducible ... no B-item yet"). A `.github/workflows/ci.yml` on `windows-latest` (matches the environment the committed hashes were pinned in — `docs/09` section 3 makes the determinism contract per-environment), on push and PR: restore + Release build of `CommandoWar.slnx` (the slnx *is* the framework-neutral boundary; `bench/` builds with it, benchmarks are not run) with the `global.json` SDK pin, `dotnet test` (fixture + corpus theory + the three FsCheck properties at 200 cases), then `cwheadless corpus` (no `--regenerate`; exits non-zero and fails the job on any divergence from the committed `content/replays/*.md`) and `cwheadless fixture` (CLI smoke — it prints, does not self-check). Realises `docs/09` section 6's framework-neutral matrix items; mitigates R-022 (continuity / rehydration), R-009 (determinism verified per push), R-016 (cold-cache pinned restore). **Deferred to P4 / the first client task:** selected-client (Godot) compile, content validation, package smoke — client work is P4, gated on G3. **Considered and deferred:** `packages.lock.json` + `--locked-mode` restore (a source change; the existing exact version pins + `global.json` satisfy "pinned dependencies"; revisit as a separate S task if R-016 materialises). Adds a one-line Windows-x64 / .NET `10.0.303` environment annotation to `content/replays/CORPUS.md`, `content/fixtures/SPIKE-FIXTURE.md`, and the `docs/12` "Shared fixture" block. No source / `.fsproj` / test / hash / `Canonical.FormatVersion` change; no ADR (verifies existing contracts, decides no new one; the CI requirement already lives in `docs/09` section 6, so no `docs/08` gate change). | S | none | G3 | done (`tasks/TASK-023-SIMULATION-CI.md`; branch `task-023-simulation-ci` merged to `main` via PR #1, first `windows-latest` run green; accepted 2026-09-06) |
| B-049 | P3 | Shared F# fixture builder for the test corpus and cross-module scenario tests. Generalise `src/CommandoWar.Headless/Corpus.fs`'s `rawScenario` / `worldOf` / `wall` / `costly` into one builder that authors a scenario (grid, friendly + enemy deployments, terrain runs, cover, objective/extraction) and a per-agent command schedule as a single value, and emits both the corpus `WorldState` + command stream and the equivalent `SimulationTests` world — removing the `Corpus.fs` ↔ `.cwlog` drift and the `Corpus.fs` ↔ `SimulationTests.fs` duplication. Not a text / on-disk scenario format (stays B-024); not authoritative content. From `docs/notes/2026-09-06-tooling-and-debug-display.md` section 1. | S–M | B-045; after TASK-022 | G3 | done (`tasks/TASK-036-SHARED-FIXTURE-BUILDER.md`; implemented and accepted 2026-09-16 — scoped to the corpus/`.cwlog` half only, no `SimulationTests.fs` retrofit, see the task file Decision D) |
| B-050 | P3 | Divergence-visualisation renderer. Realised by TASK-057 (`tasks/TASK-057-DIVERGENCE-VISUALISATION-RENDERER.md`): a new `cwheadless render-divergence <log-a> <log-b> [--ticks N] [--format ascii\|svg\|html] [--out PATH]` verb rendering the first differing tick of two replayed command logs — the reference and candidate `DiagnosticFrame` at the divergent tick, in ASCII, SVG, or the existing HTML scrubber — via a new `Overlay.Divergence of section: string * agents: AgentId[]` case, caller-supplied only (`SightRay`/`PlannedPath` precedent; `Diagnostics.frame`/`.frameOf` never emit one). Uses `Divergence.diagnoseDetailed` (new, alongside the unchanged `diagnose`) to get both full `ReplayOutcome`s; `section`/`agents` come straight from `Canonical.firstDifferingSection`'s existing "at most one label" scope, no new determinism machinery. On `Match`/`TruncatedRun` (nothing to render) it reports the same text `compare` already does and exits without rendering. `Svg` draws a thick solid magenta square around the named agent's cell — deliberately distinct from every existing overlay glyph — plus a footer line naming the section; `Html`'s annotation panel gets an optional banner. Five exhaustive `Overlay`-match sites elsewhere (`AppraisalDemo.fs`, four blocks in `DiagnosticsTests.fs`) needed one new arm each, found by a full-tree grep before drafting; the Godot client's `RenderShared.fs` needed none (its only `Overlay` match is already a wildcard). No `Canonical.FormatVersion` bump, no existing hash re-pinned — a pure observer/tooling addition. New `DiagnosticsTests` fact drives the render path end to end (reusing `ReplayTests.fs`'s own proven tick-1 `Agent[0]` divergence fixture) and asserts the `DIVERGED`/agent-id text in every format; a manual CLI smoke test on two hand-built `.cwlog` files confirmed `MATCH`/`Diverged` exit codes and the rendered output in all three formats. `dotnet build` all three `.slnx` `0/0`; `dotnet test` `344/344` (+1); `cwheadless corpus` `16/16` (unaffected). From `docs/notes/2026-09-06-tooling-and-debug-display.md` section 2. | S | B-012 | G3 | done (`tasks/TASK-057-DIVERGENCE-VISUALISATION-RENDERER.md`; accepted by Dave 2026-09-19) |
| B-015 | P3 | Implement observations and shared squad tactical knowledge (first phase consumer of the TASK-012 `Sight` module: Perception, phase 12.3, reads `Sight.trace` / `Sight.visible`; Tactical knowledge, phase 12.4, adds a canonical `WorldState.TacticalKnowledge` store and bumps `Canonical.FormatVersion` 2 -> 3 with a behaviour-neutral re-pin) | M | B-009 | G3 | done (`tasks/TASK-026-PERCEPTION-AND-TACTICAL-KNOWLEDGE.md`; accepted by Dave and merged to `main` 2026-09-07) |
| B-016 | P3 | Implement communication constraints and order delivery (the communication-failure half). Communication phase (12.2) becomes real: command intake records an accepted order as a pending order; the Communication phase delivers it (writes `Destination`) to a recipient with static `AgentState.CommunicationAvailable = true`, or emits `OrderUndelivered` (`UnableToCommunicate`) and drops it. Static authored blackout only, no PRNG, `CommunicationAvailable` excluded from `Canonical.encode` (the `Terrain` precedent — `Canonical.FormatVersion` stays 3, no hash re-pin). Radio range / delay / jamming / radio-destroyed / report aging split to B-016b. | S–M | B-015 | G3 | done (`tasks/TASK-027-COMMUNICATION-AND-ORDER-DELIVERY.md`; accepted by Dave and merged to `main` 2026-09-08) |
| B-016b | P3 | Communication range, non-zero delivery delay, dynamic jamming, radio-destroyed, and report aging beyond the `StaleAfter` / `ExpireAfter` bands. Split from B-016 by TASK-027, which landed the static-blackout communication-failure check only. Realised by TASK-058 (`tasks/TASK-058-COMMUNICATION-RANGE-DELAY-JAMMING-AND-RADIO-DESTROYED.md`): a new authored `Scenario.Headquarters: Cell option` (range = Chebyshev distance) and `Scenario.Jammers: Jammer[]` (position/radius/authored active-tick window), a new `AgentState.RadioDestroyed: bool` (a `CommsConfig.RadioDestroyChanceOnHit` chance on a qualifying combat hit, alongside the existing wound roll) and `AgentState.PendingDelivery` (an in-flight order, due `CommsConfig.DeliveryDelayTicks` ticks after acceptance, re-checked against `Communication.available` at delivery time), a new `OrderDelivered` success event, and `DeliveryFailure` cases `OutOfRange` / `Jammed` / `RadioDestroyed`. "Report aging" needed no new mechanic: an unreachable agent simply cannot receive an order, and the squad's shared knowledge of what it has and has not seen already ages via the pre-existing `StaleAfter`/`ExpireAfter` bands regardless of comms state. **The whole feature set is gated behind a scenario authoring a `Headquarters`** (a central decision confirmed directly with Dave after investigating the alternative's cost): every one of the 16 pre-existing corpus entries authors none, so `Communication.available` reduces to exactly the pre-existing static-blackout check for them, delivery stays same-tick, and Combat draws no extra radio-destroy roll — re-pinning them is a pure `Canonical.FormatVersion` 11 → 12 byte-layout change (`AgentState.RadioDestroyed`/`.PendingDelivery` added to `writeAgent`; `Headquarters`/`Jammers` themselves stay excluded, the `ResupplyAreas`/`Terrain` precedent — confirmed by grep that TASK-047's `ResupplyAreas` never got a diagnostic overlay either, so neither do these), never a behaviour change. Two new sparse `Overlay` cases (`AgentRadioLost`/`AgentPendingDelivery`, named distinctly from the `EventBody.AgentRadioDestroyed` transition event, the `OrderAppraisal`/`OrderDisposition` case-name precedent) satisfy the diagnostics-extension rule for the two genuinely new per-tick canonical fields. No new corpus entry (a deliberate deviation from the task's own original plan, given authoring one scenario that cleanly exercises all four mechanics with correct tick timing would have been fragile) — full coverage instead via 10 new `SimulationTests` facts and 8 new `ScenarioTests` validation facts, the TASK-032/033 direct-phase-test precedent. A real bug found and fixed during implementation: `commandIntake`'s `holdsCommand` checked only `Order`/`OrderQueue`, so a `Cancel` targeting an in-flight `PendingDelivery` order was rejected `UnknownTargetCommand` before `communication` ever ran — caught immediately by the new in-flight-cancellation test, not discovered later. `dotnet build` all three `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `362/362` (+18); `cwheadless corpus` `16/16` (unaffected). | M | B-016 | G3 | done (`tasks/TASK-058-COMMUNICATION-RANGE-DELAY-JAMMING-AND-RADIO-DESTROYED.md`; accepted by Dave 2026-09-19) |
| B-017 | P3 | Implement staged order appraisal and typed reasons | M | B-014, B-016 | G3 | done (`tasks/TASK-028-ORDER-APPRAISAL-AND-TYPED-REASONS.md`; implemented, accepted by Dave, and merged to `main` (`--no-ff`, branch deleted; not pushed) 2026-09-08 — real Appraisal phase (12.5), `AgentState.Order` / `.Disposition` canonical (`Canonical.FormatVersion` 3 -> 4), static `AgentState.Discipline`, `OrderDisposition` / `DecisionReason` subset, `AppraisalConfig`, `OrderAppraised` event, `Overlay.OrderAppraisal`, `exposed-approach` corpus entry) |
| B-018 | P3 | Implement commitments and finite movement/hold executors (unblocked by TASK-028: the Appraisal phase produces an `Accepted` / `Adapted` `OrderDisposition`; this builds the commitment store, the finite action executor, the interrupt table, and stage-5 safer adaptation on top — until then Appraisal writes `AgentState.Destination` directly) | M | B-011, B-017 | G3 | done (`tasks/TASK-030-COMMITMENT-AND-FINITE-EXECUTOR.md`; accepted by Dave and merged to `main` 2026-09-13. `Commitment` (`Holding \| Moving`) realised as a derived value — not a commitment *store* — over a real `commitmentAndLocalAction` phase, plus the one interrupt priority current systems can source; stage-5 safer adaptation stays out, named for a follow-up) |
| B-019 | P3 | Implement basic hitscan combat and directional cover effects (unblocked by TASK-026: validates line of fire against `AgentState.VisibleContacts`, both sides) | M | B-009, B-011 | G3 | done (`tasks/TASK-031-HITSCAN-COMBAT.md`; accepted by Dave and merged to `main` 2026-09-13. A real Combat phase — deterministic hit chance, directional cover mitigation, the stream's first real PRNG draw — with no ammunition/weapon-readiness/cooldown and no wound/death/suppression consequence, both deliberately deferred) |
| B-020 | P3 | Implement suppression and exposure model | M | B-019 | G3 | done (`tasks/TASK-032-SUPPRESSION-AND-EXPOSURE.md`; accepted by Dave and merged to `main` 2026-09-14. Combat now raises the target's `AgentState.Suppression` on every qualifying shot, cover-mitigated; State consequences decays it every tick. Mechanic only — no wiring into Appraisal's resolve threshold or a reappraisal trigger, both B-021) |
| B-021 | P3 | Implement stress, discipline, trust, and bounded reappraisal | M | B-017, B-020 | G3 | done (`tasks/TASK-033-STRESS-AND-BOUNDED-REAPPRAISAL.md`; accepted by Dave and merged to `main` 2026-09-15, partial realisation. `AgentState.Stress` (from `VisibleContacts` only) and a `SuppressionBand` hysteresis latch now feed the stage-4 resolve threshold, plus a knowledge-change and a suppression-band reappraisal trigger. `Discipline` deliberately stays static and non-canonical — corrects the row's original wording. **Explicitly descoped by TASK-037 (2026-09-16)**: exposure-band, wounded, support, and leadership triggers, and dynamic trust, stay unbuilt — the first three need systems that still do not exist (B-030's remaining Assault/Withdraw executors, B-031), exposure-band needs per-tick route-exposure tracking for an idle agent (a materially larger cut). None are touched by TASK-037's Suppress-order slice. Moved `review` -> `done` per this row's own "until those land or are explicitly descoped" wording; the follow-ups above remain unassigned future work) |
| B-022 | P3 | Implement simple enemy hold-and-defend doctrine (unblocked by TASK-026, which deliberately left it the whole hostile side: a hostile squad tactical picture, enemy doctrine reacting to observed contacts, and "the enemy does not target an unobserved player position") | M | B-015, B-019 | G3 | done (`tasks/TASK-034-HOSTILE-TACTICAL-PICTURE.md`; accepted by Dave and merged to `main` 2026-09-15, partial realisation. Realises two of six doctrine bullets: `WorldState.HostileTacticalKnowledge` ("observe and report") and the observed-only-targeting proof for "engage visible targets". "Hold assigned area" was already true by omission. **Explicitly descoped by TASK-037 (2026-09-16)**: suppress-likely-routes, seek-cover-under-pressure, and scripted fall-back stay unbuilt — all three are **Hostile-side** doctrine (the enemy proactively choosing to suppress or fall back without a player order), not the **player-issued** `Suppress` order TASK-037 built; none are touched by it. Moved `review` -> `done` per this row's own "until those land or are explicitly descoped" wording; the follow-ups above need either a Suppress-equivalent for Hostile AI or the first autonomous-movement decision in the codebase, and remain unassigned future work) |
| B-023 | P3 | Implement canonical refusal and correction scenario. Realised end to end by TASK-038 as the `canonical-refusal-and-correction` corpus entry (the `suppress-relieves-exposure` geometry plus a step 7 reissue of the identical intent on tick 8): steps 1-3 `Refused RouteTooExposed`, step 4 a structured named reason (never a raw score — no new mechanism needed, `OrderAppraised` never carried one), steps 5-6 the `Suppress`-driven automatic reappraisal to `Accepted`, steps 7-8 the reissue's own `Accepted` + fresh `CommitmentEstablished`, consistent through arrival at tick 13. Closed on the accepts-only reading of step 8 (Dave's explicit decision, AskUserQuestion, 2026-09-16): no `Adapted` `OrderDisposition` case exists (stage-5 adaptation, B-018 follow-up) — that half is a new, not-yet-filed follow-up, not built here. | M | B-018, B-020, B-021, B-022 | G3 | done |

## 4. Planned client and mission work

| ID | Phase | Work item | Size | Dependencies | Gate | Status |
|---|---|---|---|---|---|---|
| B-043 | P4 | Mibo 5.x production reconsideration spike (ADR-0001 decision review). Re-score the framework comparison for the Mibo classic-MVU route now that Mibo 5.0.0 re-separates `Mibo.Mvu` from `Mibo.Adaptive.Mibo` (ADR-0003 2026-09-06 amendment) and the packaging blocker is gone. Gated on the ADR-0001 review-trigger-1 editor edit-to-visible-result measurement. Outcome: an ADR-0001 amendment keeping Godot, or a new ADR selecting Mibo. Resolved 2026-09-19 (`decisions/ADR-0001-FRAMEWORK-SELECTION.md` 2026-09-19 note): TASK-060's terrain-painting tool gave Dave a real editor edit->visible-result task to measure against hand-typing `RawTerrainCell` records; his own live verdict, unprompted -- "its definitly easer to paint wi the mouse rather than typinf cells" -- answers review trigger 1 directly. Qualified, not a blanket "always paint from scratch": his intended workflow is generate a draft then hand-tweak it with this tool, not hand-author an entire map from an empty canvas -- later authoring tasks (B-025 onward) should scope around that shape. Godot decision stands; no new ADR. | S | ADR-0003 2026-09-06 amendment; ADR-0001 review trigger 1 | G4 | done (`decisions/ADR-0001-FRAMEWORK-SELECTION.md` 2026-09-19 note; measured via `tasks/TASK-060-SCENARIO-CONTENT-FORMAT-AND-GODOT-TERRAIN-AUTHORING.md`) |
| B-024 | P4 | Implement selected-framework content importer and validation command. Realised by TASK-060 (`tasks/TASK-060-SCENARIO-CONTENT-FORMAT-AND-GODOT-TERRAIN-AUTHORING.md`): a new deterministic, line-based `.cwscenario` content format for `RawScenario` (`src/CommandoWar.Sim/ScenarioFile.fs`, the `ReplaySerialisation.fs` style -- not JSON, no serialisation library) plus a new `cwheadless import <path>` verb (parses, runs `Scenario.validate`, reports a summary or every fault); and a Godot-editor terrain-authoring surface (`art/terrain.tres`: a `TileSet` on the three TASK-041 placeholder sprites with Custom Data Layers for `Class`/`MoveCost`/`Opaque`/`Elevation`, `scenes/TerrainAuthoring.tscn`, `tools/ExportTerrainScript.cs`) proven end to end via `tools/TerrainRoundTripProof.cs`, run headless through the real Godot 4.7.2 editor: a painted `TileMapLayer` exported to `content/scenarios/godot-painted-test.cwscenario`, accepted by `cwheadless import` with the resulting `RawTerrainLayer` matching what was painted cell by cell. Original dependency ordering (`B-007, TASK-006, B-043`) was circular against ADR-0001 review trigger 1's own text (which expects a Bridgehead-authoring task to *generate* the B-043 evidence, not wait behind it) -- corrected; this task does not depend on B-043. Terrain-layer authoring only (deployments/objectives/etc. still hand-authored in the text format); `Elevation` via tile alternates, `Cover` not yet paintable (a single tile has one Custom Data set, cover is up to four independent per-direction values per cell -- a real modelling mismatch, left for a later task). Generated the raw material for B-043's review-trigger-1 verdict; Dave then used the tool live, found and confirmed two real bugs fixed same-session (tile shape; Y-sort/texture-anchor), and gave the verdict himself -- B-043 closed 2026-09-19 (`decisions/ADR-0001-FRAMEWORK-SELECTION.md` note; B-043's own row). | M | none | G4 | done (`tasks/TASK-060-SCENARIO-CONTENT-FORMAT-AND-GODOT-TERRAIN-AUTHORING.md`) |
| B-025 | P4 | Build Bridgehead greybox map. Scoped into `TASK-061-BRIDGEHEAD-GREYBOX-MAP.md` (2026-09-19), selected as the natural next item once B-024 and B-043 both closed. Three `AskUserQuestion` rounds resolved: the generate-then-hand-tweak workflow B-043's closing note asked for means hand-authoring the `.cwscenario` text directly from `docs/07` section 3's description (not a procedural/scripted generator — `docs/07` section 7 excludes procedural maps outright, and Bridgehead is one fixed bespoke layout anyway); the scope split against B-032 (terrain + deployments only, no authored `Objectives[]` — confirmed by inspection that no objective-evaluation logic exists anywhere in `Simulation.fs` yet, so that's B-032's job, which already depends on this task); and a real tooling gap found while scoping — TASK-060's terrain tool only exports, never imports, so a hand-authored draft could not actually be hand-tweaked visually as intended — closed by adding a minimal reverse `ImportTerrainScript.cs`, in scope for this task. Automating the description-to-draft step itself (a helper LLM or tooling) is Dave's own explicitly deferred idea, filed as new row B-063, not built now. Implemented by TASK-061: `content/scenarios/bridgehead.cwscenario` (18x12; a single two-cell rail-bridge crossing over an impassable river, a two-building depot with crate cover on the far bank; 6 friendly incl. two TASK-059 fireteams of 3, 5 enemy incl. one MG-team agent controlling the bridge exit lane plus 4 riflemen in prepared positions with directional cover; `ObjectiveAreas`/`StaticTargets`/`ExtractionAreas` landmarks only), validated by `cwheadless import` (exit 0, no faults) and visually sanity-checked headlessly via `DiagnosticRender.Ascii` (an ad hoc, not-committed preview harness — `cwheadless render` has no `.cwscenario` target, confirmed by inspection, and adding one was out of this task's allowed scope). One deviation from the task file, found during implementation and not resolvable without violating either the task's own headline "valid under `cwheadless import`, exit 0, no faults" requirement or `Scenario.validate`'s unconditional `MissingRequiredMarker "Objective"` check on an empty `Objectives[]` array: the scenario carries exactly one placeholder `IsOptional=true` "reach" objective (not the docs/07 mission sequence, still B-032's job) rather than a literal `Objectives = [||]`, following the exact precedent `TerrainAuthoring.exportScenario`'s own skeleton and `godot-painted-test.cwscenario` already set. Also built: `Core/TerrainAuthoring.importTerrainLayer` + `tools/ImportTerrainScript.cs` (the `ExportTerrainScript.cs` reverse; `[Tool] EditorScript`, not run through the interactive editor in this environment, no display) and `TerrainTileSet.Resolve` (a new, additive lookup beside the untouched `Sources` table); proven for real headless through the installed Godot 4.7.2 editor by `tools/BridgeheadTerrainImportProof.cs` (the `TerrainRoundTripProof.cs` reverse): all 43 authored terrain cells resolved and painted, re-export matched the original set exactly (order-independent). `dotnet build` all three `.slnx` (main + Godot client Debug + Mibo Debug) 0/0; `dotnet test` 395/395 (unaffected); `-- corpus` 17/17 (unaffected); `dotnet list CommandoWar.Sim package --include-transitive` = `FSharp.Core` only. See `docs/ledger/2026-09-19-TASK-061-bridgehead-greybox-map.md` for full evidence. **Review round 1 (2026-09-19, live):** Dave ran `ImportTerrainScript.cs` for real in the interactive Godot 4.7.2 editor. A real bug found and fixed same-session: the script never cleared the `TileMapLayer` before painting, so it silently overlaid Bridgehead's 43 cells onto a large leftover hand-painted test area still baked into the committed `TerrainAuthoring.tscn` from TASK-060's own review rounds -- fixed with one `layerNode.Clear()` call, so an import now always replaces rather than overlays. A confusing intermediate symptom (screenshots appearing to show only a small fraction of the map even after zooming out) was run to ground rather than left unresolved: a temporary headless diagnostic loaded Dave's own *saved* `TerrainAuthoring.tscn` from disk and read it back through Godot's own `GetUsedCells()`/`GetCellTileData()` API, confirming all 43 cells were present and correct throughout -- a viewport/timing artefact, not a real defect. `TerrainAuthoring.tscn` was reset back to its documented empty starter-scene state afterward (Dave's explicit choice -- a shared scratch canvas, not a per-map artifact). Accepted by Dave: "this looks ok". | M-L | B-024 | G4 | done (`tasks/TASK-061-BRIDGEHEAD-GREYBOX-MAP.md`) |
| B-026 | P4 | Implement selection, input mapping, tactical pause, and command preview. Realised by TASK-040: a new `scenes/CommandDemo.tscn` (`CwClientCore.CommandDemoScene`) where the player, not a canned command log, drives `Simulation.step` for the first time — left-click selects a live friendly agent, left-click elsewhere issues a real `Command.moveTo` queued for next-tick delivery; hovering previews the actual `Pathfinding.find` route; `Space` toggles tactical pause, and an order can be composed and issued while paused, delivered on resume. `IClientScene` gains `OnClick`/`OnHover`/`OnTogglePause` (primitives only, ADR-0004's interop idiom); terrain/depth-sort helpers extracted from `DemoRenderScene.fs` into `Core/RenderShared.fs` so the two scenes share them (`SnapshotDemo.tscn`'s own `--selfcheck` hash confirmed unchanged after the extraction). Single-unit selection only (fireteam/multi-select deferred); no accepted/refused reason UI (B-028's scope). No `CommandoWar.Sim`/`CommandoWar.Headless` change. `dotnet build` 0/0; `dotnet test` 297/297 (unaffected); `CommandDemo.tscn --selfcheck` MATCH `0x649FA4D08E2931CA` (tick 20), cross-checked against an independent scratch probe; committed screenshot `docs/evidence/task-040-selection-and-preview.png`. Four central decisions confirmed with Dave via `AskUserQuestion` before drafting. Review round 1 (2026-09-17): Dave found selection fiddly (a real click-projection bug — an agent's circle draws `TileH/2` above where `ScreenToCell`'s diamond-centre inverse assumed) and no visual difference between a hover preview and a set order, especially while paused; both fixed (a real `TryHitAgentCircle` hit-test; three distinct route colours — hover/pending/committed). Multi-waypoint stacking + cancellation raised and deliberately deferred to new row B-051. | M | B-014, TASK-006 | G4 | done (`tasks/TASK-040-SELECTION-INPUT-AND-TACTICAL-PAUSE.md`; accepted by Dave 2026-09-17, "seems right") |
| B-027 | P4 | Implement snapshot rendering and isometric depth ordering. Realised by TASK-039: the first production Godot scene (`scenes/SnapshotDemo.tscn`), live-stepping `DemoScenario` (the terrain-demo's ridge/block/movement-cost-patch/wall/cover, confirmed with Dave via `AskUserQuestion` as the render source ahead of the real Bridgehead map) via `Simulation.step`/`RenderSnapshot`, and standing up ADR-0004's F# client-core architecture for real for the first time (`Core/CommandoWar.Client.Godot.Core.fsproj`, `CwClientCore.IClientScene`, the generic `FSharpSceneHost.cs`). Terrain and agents draw from one depth-sorted list (`(Cx + Cy)` ascending, terrain before an agent on the same cell) instead of "all terrain then all agents" (the disposable TASK-004 spike's actual gap). No `CommandoWar.Sim`/`CommandoWar.Headless` change. `--selfcheck` MATCH `0x11B06E6EDE0C52E3` (DemoScenario tick 20), run for real through Godot 4.7.2 in this environment. Sized L in practice (the backlog's M undercounts the one-time client-core scaffold cost); every later P4 client task reuses it. | M | B-011, TASK-006 | G4 | done |
| B-028 | P4 | Implement player-facing reason and disposition UI. Realised by TASK-042: docs/06 section 8/11's "order acknowledgement and disposition" / "concise explanations for refusal" requirements, closed for the first time. `AgentSnapshot` gains a values-only `Disposition: OrderDisposition option` field (a copy of the already-canonical `AgentState.Disposition`, the `Destination`/`Progress` precedent) — `Simulation.output` and both Godot scenes' initial-snapshot construction updated. New `RenderShared.dispositionText`, a player-facing structured-value-to-text mapping deliberately separate from `DiagnosticRender`'s private developer-facing one (docs/06 draws that distinction explicitly). `CommandDemoScene.HudText()` (chosen over a new floating label via `AskUserQuestion` — smaller change, reuses the established `selected=agent N` pattern) appends `order=<disposition text>` for the selected agent only. No `CommandoWar.Sim` behaviour change (field addition only); no `FSharpSceneHost.cs`/`_Draw` change. Both scenes' `--selfcheck` hashes confirmed unchanged; a headless `dotnet fsi` scratch probe drove the real scene and printed `HudText()` at each tick, proving `no order -> accepted -> no order` (cleared on arrival) against the exact pinned hash sequence, plus direct checks of the `Refused`/`Unable` text branches. `dotnet build` both `.slnx` (Debug and Release) 0/0; `dotnet test` 297/297; committed screenshot `docs/evidence/task-042-reason-and-disposition.png`. Review round 1 (2026-09-17): Dave found the `order=` text flashed by too fast to read at the default 20 Hz sim rate (`DemoScenario`'s short route completes in ~150ms wall-clock); fixed with a client-side, presentation-only hold timer (`orderTextHoldSeconds = 1.5`) that keeps a meaningful message visible for at least 1.5s past when it would otherwise clear, verified with a new headless probe driving `Update(dt)` directly (unlike `--selfcheck`, which never exercises the hold timer). No `CommandoWar.Sim`/hash change; both scenes' `--selfcheck` reconfirmed unchanged. | M | B-017, B-026 | G4 | done (`tasks/TASK-042-REASON-AND-DISPOSITION-UI.md`; accepted by Dave 2026-09-17, "thats readable now") |
| B-051 | P4 | Multi-waypoint order stacking and cancellation. Raised by Dave during TASK-040 review (2026-09-17): being able to queue several waypoints for one agent so orders can be "really stacked", with a way to cancel a queued or in-progress order rather than only replacing it by issuing a new one. Central design fork — **resolved 2026-09-17 via `AskUserQuestion`: an authoritative sim-side order queue**, not client-side sequencing. `WorldState`/`AgentState` gains real canonical queue state, touching `Canonical.FormatVersion` and the `.cwreplay` replay format — the larger option, chosen to stay faithful to "recorded commands and replay evidence" as a fixed project constraint, since a client-only plan is not itself recordable/replayable. Scoped into `TASK-044-ORDER-QUEUE-STACKING-AND-CANCELLATION.md` (2026-09-17, later session), selected via `AskUserQuestion` over B-030 proper/B-031/B-053; four further central decisions (queue scope, replace-stays-default, cancel-by-`CommandId`, `.cwlog` left alone) confirmed the same way before drafting. `Appraisal.fs`/`Commitment.fs` turned out to need no change (both already operate on one `ReceivedOrder` at a time); `Simulation.step`'s public signature is unchanged. TASK-040 (B-026) deliberately shipped single-order-only (a new click replaces, not stacks). Implemented and self-verified 2026-09-17 (`dotnet test` 306/306; `--corpus`/`--regenerate` twice byte-identical; `--replay-file` OK; both Godot scenes' `--selfcheck` re-pinned and confirmed `MATCH`). Accepted by Dave 2026-09-17 ("no new UI to review, just accept") — sim-side only, nothing player-facing changed. | M–L | B-026 | G4 | done (`tasks/TASK-044-ORDER-QUEUE-STACKING-AND-CANCELLATION.md`) |
| B-052 | P4 | Agent sprite facing/orientation. Raised by Dave on accepting TASK-041 (2026-09-17): the Kenney placeholder human figure (`art/agent_human.png`, a single idle pose) doesn't turn to face the direction an agent last moved, so it can read as facing the wrong way after a route. Realised by TASK-054 (`tasks/TASK-054-AGENT-FACING-AND-AUDIO-THREAT-INDICATOR.md`, bundled with B-056), corrected through one live review round with Dave. **First cut (superseded)**: live-checked kenney.nl (downloaded and unzipped both "Isometric Miniature Prototype" and "Isometric Miniature Dungeon") and misread `Human_0..7` as 8 colour/character variants each with a single non-rotating `Idle0` plus a `Run0..Run9` animation set — concluding no rotating idle pose existed, and repurposing `Human_0`'s `Run0..Run9` as 10 rotation bins instead. Dave tried this live: "doesn't seem any better" — a real math bug (binning the raw world-grid angle instead of the actual screen-projected one, since this game's isometric projection is a skew, not a rotation). Fixing the math didn't help either: Dave correctly read a follow-up compass image as "part of a running animation, not different angles of one frame" — because `Human_0_Run0..Run9` genuinely is a running *animation* (orientation and gait phase change together), so no frozen frame could ever read as a clean facing pose. **Corrected (current)**: Dave pointed directly at his own local copy of the pack's `Information.png`, which documents the real convention: `Human_0..7` **are** 8 rotations of one idle pose (`0=N,1=NE,2=E,3=SE,4=S,5=SW,6=W,7=NW`, clockwise, screen-relative) — confirmed by comparing all eight `Human_N_Idle0.png` side by side, a clean rotating figure with none of the animation confound. `agent_human_facing0..7.png` now crop `Human_N_Idle0.png` (8 files, not 10); `RenderShared.facingBin` reduced to 8 bins/`45°` with its zero-reference aligned directly to the pack's own `N` (a bin number *is* a `Human_N` index). Movement-direction-only facing (no threat-facing — Dave's confirmed choice). Applied identically in both `DemoRenderScene` and `CommandDemoScene` through the one shared `FSharpSceneHost` draw path. No `CommandoWar.Sim` change; both scenes' `--selfcheck` hashes confirmed `MATCH` through the real Godot 4.7.2 editor throughout, unaffected as expected for a presentation-only change. Verified with `dotnet fsi` scratch probes at each step, the final one confirming all 4 world-diagonal moves land exactly on the 4 screen-cardinal bins the isometric geometry predicts.

**Second correction round (TASK-056, `tasks/TASK-056-AGENT-FACING-CORRECTION-AND-RUN-ANIMATION.md`)**: Dave live-tested the above and found it still roughly 30 degrees off, and a moving agent still froze on one static idle frame instead of animating. Facing: re-examined `Information.png`'s own diamond legend directly (it labels the tile's four *edges* N/E/S/W, not vertices) and found `facingBin` had the mapping backwards — it assigned the four Kenney cardinal poses to the four world-*diagonal* moves instead of the four world pure-axis moves the geometry actually requires; every one of the 8 primary directions landed exactly one 45° bin past its correct target under the old uniform-sector rounding. Rewrote `facingBin` as a nearest-reference-vector match (cosine similarity against the 8 primary directions' own projected screen vectors) instead of an angle-sector round — correct by construction for the 8 primary directions and, unlike a fixed-offset patch, also correct for an arbitrary `(dx,dy)` heading (`Destination` is the agent's overall target, not its next path step). Animation: added all 80 `Human_0..7_Run0..9` frames (`agent_human_run{dir}_{frame}.png`), all 88 idle+run textures re-cropped to one shared global bbox so switching between them never pops the figure's on-screen size; a wall-clock `runClock` (gated to real ticks advancing, so it never animates while paused) drives a `DrawItem.Cx2` run-frame index while `Destination <> Some Position`, `-1` (frozen idle) otherwise. Verified against the real Godot 4.7.2 editor this round, not just pure-function probes: a temporary screenshot-probe hook (removed before finishing) captured all 8 primary directions and compared them directly against `Information.png`'s own back-view/front-view pattern (confirmed consistent), plus mid-transit frames proving the run cycle visibly advances and freezes correctly on arrival (`docs/evidence/task-056-facing-compass-check.png`, `docs/evidence/task-056-run-animation-check.png`). Still `review`, not `done` — awaiting Dave's live confirmation.

**Review round 1 on TASK-056 (2026-09-19)**: Dave live-tested and reported "its better but i dont think it always stay the correct facing. also theres some sort of jerkyness to the movement, it also also moving really fast too." Both traced to one root cause: `AgentSnapshot.Position` only changes once a whole grid edge completes (`Progress` accumulates across several mid-edge ticks first, TASK-018), but rendering lerped screen position between the *previous* and *current tick's* discrete `Position` alone — identical on every mid-edge tick, so the figure sat frozen then snapped across a whole cell inside one tick's 50ms window (jerky, and reading as fast since the whole displacement compresses into that short burst). The same root cause explained the facing complaint: `facingBin` was fed the crow-flies bearing to the agent's *overall final* `Destination`, correct on a straight leg but wrong the instant a route bends or the player redirects mid-route. Fixed by computing each moving agent's true immediate next path cell once per tick (`Pathfinding.find`, the `committedItems` route-preview precedent) and using it both as `facingBin`'s target and as the render lerp target, with the lerp fraction driven by the already-canonical `AgentSnapshot.Progress` normalised against a per-agent tick-count estimate learned from the most recently completed edge (self-correcting every edge; no `CommandoWar.Sim` change, since the agent's own `MoveSpeed` is deliberately not exposed to the client). Verified live via a temporary burst-screenshot probe (removed after use): tracked on-screen centroid across a 24-frame, 0.12s-interval burst and confirmed continuous fractional cell progression (no flat-then-jump pattern), plus a real mid-route direction change (south to east) showing the facing pose genuinely switch to match (`docs/evidence/task-056-round1-facing-on-turn.png`, `docs/evidence/task-056-round1-smoothness-sequence.png`). `dotnet test` 343/343 unaffected; all three scenes' `--selfcheck` hashes unaffected. Whether the underlying grid movement *pace* itself (`DemoScenario`'s Trooper half-speed, TASK-049) is still too fast once the jerkiness is gone is flagged for Dave, not changed — that's content authoring in `CommandoWar.Headless`, outside this task's presentation-only scope. Still `review`, not `done`.

**Review round 2 on TASK-056 (2026-09-19)**: Dave tried again: "i just tried again and it seemed the same, did you not fix this?" — plus two new, specific reports round 1's own probe never checked: the selection halo "jumps between cells", and the run animation "runs continously if the agents get stuck on the same cell". Ruled out a stale build first (rebuilt both `.slnx` clean). Root cause: round 1's fix only ever updated the real figure's own render position — the selection halo (`haloItems`) still read the raw, discrete `Position` directly, so it kept snapping a whole cell at a time on every edge completion even though the figure under it had already smoothed out; since the halo is the single largest, most attention-grabbing element while watching a selected agent move, this alone made the whole fix read as "no different". Separately, `isMoving` was still just "has an unmet `Destination`", true forever for an agent stalled by a route-reservation contest (`Simulation.navigationAndMovement` freezes such an agent's `Progress`, never increments it, while `Destination` stays set) — so the run-cycle animation played forever on a figure that was not actually advancing, and the render-time `alpha` ramp kept getting credited to an edge that would never complete, producing a small creep-then-snap-back sawtooth on top. Fixed with one new `stalled` map (compares an agent's `Progress` entering a tick against its value after — unchanged means no real progress happened) and one new shared `renderPos` helper that the real figure and the selection halo both now call, so they cannot disagree; while `stalled`, the render position freezes exactly (no `alpha` credit) and the run cycle stops. Verified live through the real Godot 4.7.2 editor with a purpose-built scenario (not a repeat of round 1's probe): an agent redirected into a second, stationary agent's own occupied cell, forcing a genuine stall. A pixel-diff across 16 consecutive captures spanning 2.4 real seconds of the stalled state shows zero changed pixels — the figure, halo, and run-cycle frame all fully static, proof against both the run-forever bug and the sawtooth (`docs/evidence/task-056-round2-halo-and-stall-freeze.png`). One unexplained, single-frame, transient colour glitch was observed and investigated but not resolved — ruled out as a vitals/combat misread (`state.Random.Draws` stayed 0 throughout; `AgentVitals` confirmed emitted unconditionally every tick) and as anything this round's own diff (which never touches the R/G/B fields involved) could explain; flagged for awareness, does not recur and does not affect either reported defect. `dotnet test` 343/343 unaffected; all three scenes' `--selfcheck` hashes unaffected. Dave live-tested this round's fix and confirmed: "ok that looks vetter" -- accepted. | S–M | B-034 (TASK-041) | G4 | done (`tasks/TASK-054-AGENT-FACING-AND-AUDIO-THREAT-INDICATOR.md`, `tasks/TASK-056-AGENT-FACING-CORRECTION-AND-RUN-ANIMATION.md`; accepted by Dave 2026-09-19 after three live-review correction rounds) |
| B-053 | P4 | Hover highlight for a selectable agent. Raised by Dave on accepting TASK-041 (2026-09-17), hedged as a suggestion ("possibly"): selection still feels fiddly even after TASK-040 review's `TryHitAgentCircle` hit-test fix; hovering a friendly agent (not yet clicking) could highlight it so the player knows a click will select it, mirroring the hover-preview pattern `CommandDemoScene.OnHover` already uses for routes. Realised by TASK-052 (`tasks/TASK-052-HOVER-HIGHLIGHT-AND-SCALE-POLISH.md`, bundled with B-054 per Dave's own instruction): a new `hoverHighlightItems` reuses TASK-051's `Kind = 5` ring primitive with a light neutral tint, gated to a friendly agent only (never a hostile, fogged or not). A real correctness gap found during implementation: `FSharpSceneHost.cs`'s mouse-motion handler called `ScreenToCell` directly, never adopting the click handler's own `TryHitAgentCircle`-first fallback (TASK-040's fix for the identical top-of-circle projection mismatch) — fixed so hover resolution now matches click resolution exactly. Verified with a temporary `dotnet fsi` scratch probe: the ring appears only over a friendly's own cell, never empty terrain or a hostile. No `CommandoWar.Sim`/`CommandoWar.Headless` change; both scenes' `--selfcheck` hashes unaffected, confirmed `MATCH` through the real Godot 4.7.2 editor. | S | B-026 | G4 | done (`tasks/TASK-052-HOVER-HIGHLIGHT-AND-SCALE-POLISH.md`; accepted by Dave 2026-09-18) |
| B-054 | P4 | Godot client viewport/camera scale and HUD layout polish. Raised by Dave during TASK-043 review (2026-09-17): the isometric play area (`FSharpSceneHost.cs`'s hardcoded `TileW=44`/`TileH=22`/`Origin=(450,60)`) occupies a small fraction of the window, and the corner HUD `Label` overlaps the tick counter and agent sprites — general "developer experience needs a bit of love", not a single specific fix. Realised by TASK-052 (bundled with B-053): the undecided fork this row itself named ("camera zoom/pan or a larger fixed scale") resolved via `AskUserQuestion` — a larger fixed scale, not interactive zoom/pan (a materially bigger feature, deliberately not added). `TileW`/`TileH` doubled (44x22 -> 88x44, play area now ~62%/~50% of the 1280x800 window's width/height, up from ~34%/~28%); `Origin` moved (450,60) -> (552,110), recentred for the new scale and raised on `Y` past the HUD label's worst-case three-line height with real margin, fixing the overlap directly rather than moving the HUD out of the grid's way. `DrawTerrainTile` already scales from `TileW`/`TileH`; `CommandDemoScene.fs` gained named `agentRadius` (10.0f -> 20.0f) and `haloRadius` (17.0f -> 34.0f) constants replacing independent literals, so the agent figure, halo, the TASK-051 fog-of-war ghost ring, and B-053's new hover ring all size themselves consistently. Verified: the TASK-051 fog-of-war probe re-run unchanged, confirming the shared-constant refactor altered no behaviour; both scenes' `--selfcheck` hashes unaffected, confirmed `MATCH` through the real Godot 4.7.2 editor; committed screenshot `docs/evidence/task-052-scale-and-hover.png` (captured with `--dev-overlay` on, showing the worst-case HUD height with no overlap). | S–M | B-027 (TASK-039) | G4 | done (`tasks/TASK-052-HOVER-HIGHLIGHT-AND-SCALE-POLISH.md`; accepted by Dave 2026-09-18) |
| B-055 | P4 | Player-facing fog of war. Raised by Dave during TASK-043 review (2026-09-17): today `CommandDemoScene` draws every agent, hostile included, at its true position unconditionally — there is no fog-of-war in the player's own view (TASK-043's dev-overlay `KnownContact` ghost-marker-vs-real-agent comparison is developer-only by design, docs/06 section 11). Realised by TASK-051 (`tasks/TASK-051-PLAYER-FACING-FOG-OF-WAR.md`): smaller than expected — **no `CommandoWar.Sim` change was needed**, since `devFrame`'s existing `KnownContact` overlay (built every tick regardless of the `F1` toggle, the TASK-046 `AgentVitals` precedent) already carries `WorldState.TacticalKnowledge` in full. A hostile never contacted (or fully expired, `PerceptionConfig.ExpireAfter`) is hidden entirely; one seen this tick renders exactly as before; one known but not currently visible draws a new `Kind = 5` hollow ring plus a `"?"` label at its last-known cell, not its true position, opacity following `Contact.Confidence`'s band drop. Three central decisions confirmed with Dave via `AskUserQuestion`, all the recommended default: `CommandDemoScene` only (not the passive `SnapshotDemo.tscn`); a distinct outlined-ring shape, not a translucent recolour (docs/06 "status indicators that do not rely on colour alone"); confidence-scaled opacity. A real regression found and fixed during implementation: the first draft applied fog inside the `F1` developer overlay too, breaking TASK-043's own ground-truth-vs-known comparison tool — fixed by bypassing fog whenever the dev overlay is on. Verified with a temporary `dotnet fsi` scratch probe (the TASK-042 precedent) driving `CommandDemoScene` directly, confirming the exact hidden -> live -> ghost (alpha 0.90) -> faded (alpha 0.68 after `PerceptionConfig.StaleAfter`) -> expired (after `PerceptionConfig.ExpireAfter`) sequence, plus the `F1` bypass. Both scenes' `--selfcheck` hashes unaffected (render-only), confirmed `MATCH` through the real Godot 4.7.2 editor; `dotnet build`/`dotnet test` unaffected (`342/342`); committed screenshot `docs/evidence/task-051-fog-of-war.png`. | M–L | B-015 (TASK-026), B-029 (TASK-043) | G4 | done (`tasks/TASK-051-PLAYER-FACING-FOG-OF-WAR.md`; accepted by Dave 2026-09-18) |
| B-056 | P4 | Audio-localised threat indicator. Raised by Dave during TASK-043 review (2026-09-17), an early unscoped idea: a shot fired (or similar loud event) could be "audio-localised" by the player even without line of sight to the shooter, shown as a general-direction visual symbol (a rough bearing, not the shooter's exact position). Realised by TASK-054 (`tasks/TASK-054-AGENT-FACING-AND-AUDIO-THREAT-INDICATOR.md`, bundled with B-052): triggers on `FireLine` (`ShotFired`) only when the shooter is `Hostile` and had **no** `KnownContact` entry at all for the friendly side as of *before* that tick's own Perception phase (not the post-tick `devFrame` — Perception runs ahead of Combat in phase order, so checking the post-tick set would suppress the cue on exactly the "just came into view and fired" tick this row exists to cover; found and corrected during implementation). Visual: a `Kind = 2` line plus a `Kind = 3` `"!"` label at a boundary anchor point just outside `state.Bounds`, one of 8 coarse `45°` sectors from the friendly squad's living-agent centroid to the shooter's true cell — no new `DrawItem.Kind`, the `fireEffectHoldSeconds` fade-timer precedent (`audioCueHoldSeconds`). Always-on regardless of the `F1` dev overlay (the `fireEffects` precedent). No `CommandoWar.Sim` change; both scenes' `--selfcheck` hashes confirmed unaffected. Verified with a `dotnet fsi` scratch probe driving a friendly agent into a hostile's weapon range: exactly one cue appears the first tick a shot lands from the still-unknown hostile, persists (not re-added) through three further ticks of continued mutual fire once the hostile becomes known, and never appears before contact. | S–M | B-019 (TASK-031) | G4 | done (`tasks/TASK-054-AGENT-FACING-AND-AUDIO-THREAT-INDICATOR.md`; accepted by Dave 2026-09-19, confirmed live in the running game) |
| B-057 | P4 | Player-facing combat and casualty visual feedback. Raised by Dave on accepting TASK-045 (2026-09-17): "seems to work but there is no visual indicator of fire without the overlay". Related to but distinct from B-056 (audio-localised, no-line-of-sight indicator) — this is the baseline "something visible happens when a shot lands or a wound occurs, even with line of sight" gap. Realised by TASK-046 (`tasks/TASK-046-COMBAT-AND-CASUALTY-VISUAL-FEEDBACK.md`): two design forks resolved via `AskUserQuestion` — new art vs. reused primitives (Dave chose new art), then, after a live kenney.nl check found the existing Isometric Miniature Prototype pack has no combat-effect sprites and no wound/death pose, which pack to add (Kenney's CC0 "Particle Pack" for fire feedback; primitives — a wound dot, a "down" text badge, a dead cross, mirroring `DiagnosticRender.Svg`'s own vocabulary — for vitals, since no matching wound/death icon exists in any Kenney pack checked). `FireLine`/`AgentVitals` (already computed every tick) promoted from developer-only to always-on; new `DrawItem.Kind = 4` (a textured point sprite). No `CommandoWar.Sim` engine change. **Review round 1** (Dave live): confirmed a real LOS bug — agents could fire through the demo's impassable block since its cells were authored `Opaque = false` — fixed in `DemoScenario.fs` (`CommandoWar.Headless` content, confirmed with Dave via `AskUserQuestion`; re-pinned `SnapshotDemo`'s `--selfcheck` hash and two `DiagnosticsTests` goldens); the fire effect's one-tick (50ms) visibility was fixed with a held/fading buffer (`fireEffectHoldSeconds`, the `orderTextHoldSeconds` precedent); a client-only `simHz` halving was tried live for the "movement feels too fast" complaint (`Terrain.BaseMoveCost = 1` = 20 cells/sec at 20Hz) then reverted — Dave deferred the actual fix to a proper agent movement-speed value, filed as B-058. | S–M | B-019 (TASK-031), B-031 (TASK-045) | G4 | done (`tasks/TASK-046-COMBAT-AND-CASUALTY-VISUAL-FEEDBACK.md`; accepted by Dave 2026-09-18) |
| B-058 | P4 | Agent movement speed. Raised by Dave during TASK-046 review (2026-09-18): on open ground `Terrain.BaseMoveCost = 1`, so an agent enters a new cell every tick — 20 cells/second at the client's 20Hz rate, which read as "twice as fast as I thought it would" run. A client-only `simHz` halving was tried live (slows the whole scene's real-time pacing uniformly — movement, combat, suppression/stress decay, bleed-out — together) and works, but Dave explicitly deferred it: "let's leave it for later, adding an actual speed to the agent would solve that discrepancy" — wants a real per-agent movement-speed value instead of either the client-pacing knob or a blanket `Terrain.BaseMoveCost` change (which would decouple walking from every other tick-denominated system and, since `MoveCost = 1` is the default for every scenario in the project, ripple through most corpus entries and re-pin many hashes). Realised by TASK-049 (`tasks/TASK-049-AGENT-MOVEMENT-SPEED.md`): new `AgentState.MoveSpeed` (static, non-canonical, the `Discipline`/`CommunicationAvailable` precedent), authored via a new per-unit-type `RawScenario.UnitTypes` table (Dave's chosen location over a bare field) referenced by each `RawDeployment.UnitType` — no silent default, an unresolved reference is a validation error (`ScenarioContent.Version` 3 -> 4). `Simulation.navigationAndMovement`'s edge-completion check cross-multiplies `MoveSpeed` against a new `Agent.MoveSpeedDefault` (= 2) rather than scaling the per-tick increment directly, so a default-speed agent's `AgentState.Progress` trajectory is byte-for-byte identical to before at every tick (not just at completion) — confirmed against all 16 corpus/fixture entries (`cwheadless corpus` 16/16, `--regenerate` byte-identical). `content/diagnostics/demo.html`'s `DemoScenario` now authors a `"trooper"` unit type at half speed for its three agents, resolving the original complaint with no change to `Terrain.BaseMoveCost` or any other content. `dotnet build` both `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `342/342` (+6); `-- corpus`/`-- replay-file` unaffected. `SnapshotDemo.tscn`'s `--selfcheck` hash re-pinned (`0xF422ACB8D5A86FF0`), confirmed `MATCH` through the real Godot 4.7.2 editor; `CommandDemo.tscn`/`AppraisalDemo.tscn` unaffected, both reconfirmed `MATCH`. Three central decisions confirmed with Dave via `AskUserQuestion` before drafting. | S–M | none | G4 | done (`tasks/TASK-049-AGENT-MOVEMENT-SPEED.md`; accepted by Dave 2026-09-18) |
| B-059 | P4 | Client UI for issuing `Hold`/`Assault`/`Withdraw` orders. Raised by Dave on accepting TASK-047 (2026-09-18): B-030 proper landed sim-side only, with "no client-facing UI for issuing the three new orders" flagged as "the natural next client task if Dave wants it" — Dave chose it directly over B-032/B-058. Realised by TASK-048 (`tasks/TASK-048-ORDER-MODE-HUD-FOR-HOLD-ASSAULT-WITHDRAW.md`): an XCOM-style HUD order-mode icon bar in `CommandDemoScene` (Dave's explicit design direction over the recommended keyboard-armed-mode default) — four fixed screen-space icons (move/hold/assault/withdraw), owned entirely in C# (`FSharpSceneHost.cs`: `OrderModeTextures`, `OrderModeIconRect`, `TryHitOrderModeIcon`, `DrawOrderModeBar`) as HUD chrome, not world-grid content. Clicking an icon arms that order type (`IClientScene.OnOrderModeClick`/`OrderMode`, primitives only); the next click on a target cell with an agent selected issues the corresponding real `Command.hold`/`.assault`/`.withdraw` (already-existing TASK-047 constructors, zero `CommandoWar.Sim` change) and the armed mode resets to `MoveTo` (an ability-consumed-on-use idiom). While `Hold` is armed and hovering, an outline of the exact `AppraisalConfig.HoldCoverSearchRadius` (2) Chebyshev square `Appraisal.bestCoverNear` will search is drawn — Dave's own addition beyond the original click-a-cell question ("an outline of the hold area is shown in the UI"). Icon art: Kenney "Board Game Icons" (CC0), live-checked against kenney.nl — downloaded and unzipped to inspect actual files, not assumed from memory (Dave's explicit choice over reusing existing draw primitives) — neither prior pack (Isometric Miniature Prototype, Particle Pack) has command-vocabulary icons. `CommandDemoDrive.runScriptedSelfCheck` extended to also arm `Hold` via the icon-click path and issue `Hold(2,1)` for a second agent — proving the click dispatch reaches a real command, not just a direct sim-side call; re-pinned `CommandDemo.tscn --selfcheck` (`MATCH 0x00D3D471EF7354BC`), confirmed through the real Godot 4.7.2 editor; `SnapshotDemo.tscn`/`AppraisalDemo.tscn` reconfirmed unchanged. No UI added for `Suppress` (already sim-side complete since TASK-037; not part of Dave's task selection). `dotnet build` both `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `336/336` (unaffected); `-- corpus` `16/16` (unaffected); committed screenshot `docs/evidence/task-048-order-mode-hud.png`. Two central decisions confirmed with Dave via `AskUserQuestion` before drafting. | M | B-030 | G4 | done (`tasks/TASK-048-ORDER-MODE-HUD-FOR-HOLD-ASSAULT-WITHDRAW.md`; accepted by Dave 2026-09-18) |
| B-029 | P4 | Render the DiagnosticFrame in Godot (developer perception, appraisal, reservation, and hash overlays) | M | B-012a, B-012, B-017, B-027 | G4 | done (`tasks/TASK-043-DEVELOPER-OVERLAY-RENDERING.md`; accepted by Dave 2026-09-17, "i confirm the features work"). A read-only, corpus-scoped slice landed early as P3 decision-support via TASK-029 (`AppraisalDemo.tscn`); B-029 proper realised by TASK-043: `CommandDemoScene` (the only live-input scene) toggles a developer overlay (`F1`) that renders `Diagnostics.frame`/`.frameOf`'s own `Overlay[]` directly — coordinate labels, `Reserved`/`Obstructed` cell markers, `KnownContact` ghost markers against the always-rendered real agent, the selected agent's `OrderAppraisal.exposedCells` and a `[dev]` commitment/suppression/stress/reason HUD line, a hover-driven `Sight.trace` line-of-sight ray with its occluder, tick/hash/random-draw counter, and (round 2) a `[legend]` line mapping every overlay colour to its meaning. `DrawItem` gained `Kind = 2` (line segment) and `Kind = 3` (text label); `IClientScene` gained `OnToggleDevOverlay`. Discipline and trust deliberately excluded (trust has no backing state anywhere; discipline has no `Diagnostics.Overlay` case, and adding one would ripple into `CommandoWar.Sim`/`Headless` and a golden regen — left as a follow-up). Round 1 review fixed two rendering bugs (dev overlay z-order, an off-grid LOS ray chasing the cursor); round 2 review corrected a misdiagnosis (the reported "flashing green line" is the this-tick-only `FireLine` overlay, not the LOS ray — working as designed) and added the legend. No `CommandoWar.Sim`/`CommandoWar.Headless` change; `dotnet build` both `.slnx` 0/0 (Debug and Release); `dotnet test` 297/297 (unaffected); committed screenshot `docs/evidence/task-043-developer-overlay.png`. Both scenes' `--selfcheck` hashes were confirmed unchanged (`0x11B06E6EDE0C52E3`, `0x649FA4D08E2931CA`) through round 1; round 2's string-literal-only diff was not independently re-run against `--selfcheck` (headless Godot launches hung on this machine for unrelated reasons — see the task file's round 2 section), but is unreachable from the self-check driver. |
| B-030 | P4 | Implement suppress and assault command executors | L | B-018, B-020 | G4 | done (a minimal `Suppress`-order slice landed early as P3 decision-support via TASK-037: `PlayerIntent.Suppress of target: AgentId`, a `Suppressing` commitment that holds position and pins the named contact as Combat's chosen candidate, and the stage-3 exposure-zeroing this unblocks for B-023 step 5-6. B-030 proper realised by TASK-047 (`tasks/TASK-047-ASSAULT-WITHDRAW-HOLD-AND-AMMUNITION.md`): `Hold`/`Assault`/`Withdraw` `PlayerIntent` cases, each with real appraisal (Hold redirects to nearby cover via `Appraisal.bestCoverNear`; Assault gets a stricter resolve penalty; Withdraw gets a resolve bonus), a commitment (`Withdrawing`; `Assaulting` with a staged `AssaultStage` finite-state machine — `ApproachingStart` / `AwaitingSupport` / `Advancing` / `ClearingThreat`, no stored wait-counter, no timeout), and an executor (`commitmentAndLocalAction` freezes/unfreezes `Destination` for the `AwaitingSupport` stage; `Combat`'s existing automatic engagement handles "cross danger area"/"clear immediate threat" unmodified). A finite per-agent ammunition model closes the second half: `AgentState.Ammo` (magazine + reserve, automatic reload once empty, instant resupply on a new authored `WorldState.ResupplyAreas` cell — `ScenarioContent.Version` 2 -> 3) gates every qualifying shot for every commitment alike; `DecisionReason.InsufficientAmmunition` gates `Suppress`/`Assault` orders (not `MoveTo`/`Hold`/`Withdraw`) at stage 2. `Canonical.FormatVersion` 10 -> 11, full re-pin (behaviour-neutral for every pre-existing corpus entry). No new corpus entry — proof via 17 new `SimulationTests` facts directly on the phases and pure functions (the TASK-032/033 precedent), covering the full `AssaultStage` derivation, the Withdraw/Assault threshold contrast against the identical MoveTo geometry, Hold's cover redirect, and the ammo fire/reload/resupply/gate cycle. `dotnet build` both `.slnx` (main; Godot client Debug/Release) `0/0`; `dotnet test` `336/336`; `-- corpus` 16/16 (`--regenerate` twice byte-identical); `-- fixture`/`-- replay-file envelope-full` unaffected. No client-facing UI for issuing the three new orders (the TASK-037 precedent — sim-side proof only); no player-facing ammo readout (F1 dev-overlay detail only). Godot's `--selfcheck` pinned hashes originally computed via `dotnet fsi` against the built `Core.dll`, then re-run through the real Godot 4.7.2 editor and confirmed `MATCH` on acceptance (SnapshotDemo/CommandDemo/AppraisalDemo exposed-approach, all exit 0). Accepted by Dave 2026-09-18) |
| B-031 | P4 | Implement casualties, leadership succession, and squad failure | M | B-019, B-021 | G4 | done (`tasks/TASK-045-CASUALTIES-SUCCESSION-AND-SQUAD-FAILURE.md`; accepted by Dave 2026-09-17, "seems to work" — a follow-up raised, not blocking, see B-057) |
| B-032 | P4 | Implement demolition objective, extraction, success, and failure. Realised by TASK-062 (`tasks/TASK-062-DEMOLITION-OBJECTIVE-EXTRACTION-AND-MISSION-OUTCOME.md`): turns `Simulation.fs`'s `Mission` phase, an explicit no-op reserved for exactly this, and `Scenario.fs`'s fully-authored-but-never-evaluated `Objective`/`ScenarioRules` algebra, into real behaviour. One `AskUserQuestion` round (four questions) resolved: charge-planting is occupancy-based (any `Alive` friendly agent on the target's own cell for a configured tick count, no new order type, reusing `MoveTo`/`Hold` as authored today); failure is exactly the existing `ScenarioRules.FailOnFriendlyForceEliminated` wired up, no new timeout mechanism; extraction is sticky (once reached, stays counted even after the agent walks off); Bridgehead's TASK-061 placeholder optional `reach` objective is joined by a non-optional `destroy` (10-tick plant against the already-authored `bridge-charge` target) and a non-optional `extract` (against the already-authored `extraction` area). Generalised beyond the four confirmed answers (documented in the task file, not separately asked): sticky one-shot completion applies uniformly to every objective kind (`WorldState.CompletedObjectives`), not just extraction; `ObjectiveId`/`AreaId`/`TargetId`/`Area`/`StaticTarget`/`AgentSelection`/`Objective`/`ScenarioRules` moved `Scenario.fs -> Domain.fs` (the `Jammer` precedent, since `WorldState` needs to reference them and `Domain.fs` compiles first). A real vacuous-success bug found and fixed before it reached a test: a naive `Array.forall` success gate is trivially true over an empty `Objectives` array, which would have made every pre-existing test fixture (none of which authors objectives) instantly "succeed" on tick 1 -- guarded with an explicit non-empty check. New canonical `AgentState.Extracted` / `WorldState.MissionOutcome`/`.CompletedObjectives`/`.ObjectiveProgress` (`Canonical.FormatVersion` 12 -> 13); new events `AgentExtracted`/`ObjectiveCompleted`/`MissionSucceeded`/`MissionFailed`; new sparse `Overlay.MissionStatus`; `ScenarioContent.Version` 6 -> 7 (`"destroy"` now requires positive `HoldTicks`, reusing the existing grammar column). New `demolition-success` corpus entry proves `MissionOutcome -> Succeeded` end to end; the pre-existing `casualties-succession-and-squad-failure` entry now also demonstrates `MissionOutcome -> Failed` for free (it already authors `fail-on-friendly-eliminated true` and already scripts a full friendly wipeout). `dotnet build` all three `.slnx` `0/0`; `dotnet test` `408/408` (+13); `-- corpus` `18/18` (`--regenerate` idempotent); `content/scenarios/bridgehead.cwscenario` re-validates with its real 3-objective content. Accepted by Dave 2026-09-20 on the self-verification evidence ("ok if you are happy then i am"). | M | B-025, B-030, B-031 | G4 | done (`tasks/TASK-062-DEMOLITION-OBJECTIVE-EXTRACTION-AND-MISSION-OUTCOME.md`) |
| B-033 | P4 | Implement mission summary panel. Narrowed from "replay playback and mission summary" (2026-09-20, `AskUserQuestion`, four rounds): B-032's acceptance unblocked B-033 for the first time, but research before drafting found replay playback and mission summary share no design or implementation surface -- replay data is commands-only (`.cwreplay`; full per-tick state is only ever reconstructed by `Replay.run`, not stored long-term) with no client playback UI anywhere (the only existing "scrubber" is `content/diagnostics/demo.html`'s static generated-HTML slider, not reusable inside a live Godot scene), while mission summary has complete sim-side data (`WorldState.MissionOutcome`/`.CompletedObjectives`/`.ObjectiveProgress`, `AgentState.Extracted`, all done since TASK-062) but zero UI infrastructure of its own. Split into two backlog rows; mission summary built first (scoped into `tasks/TASK-063-MISSION-SUMMARY-SCREEN.md`), replay playback deferred to new row B-064. Scope: a panel in `CommandDemoScene`, auto-shown the tick `MissionOutcome` first leaves `InProgress`, showing outcome/completed objectives/in-progress objective status/extracted agents and blocking further order-issuing -- real Godot client UI (the first task to build any post-mission presentation), not a headless-only proof; no new dedicated results scene, no restart/play-again control. Implemented by TASK-063: new `IClientScene.MissionSummaryLines`/`RenderShared.missionSummaryLines`, a panel drawn last in `FSharpSceneHost._Draw`, `CommandDemoScene` auto-pausing and refusing further orders once `MissionOutcome <> InProgress`. Two real, pre-existing bugs found and fixed during verification: `SnapshotDemo.tscn`/`AppraisalDemo.tscn`'s own `--selfcheck` pins had gone stale since TASK-062's `Canonical.FormatVersion` 12 -> 13 bump (TASK-062 never re-ran the Godot client's self-checks); and the panel's own first draft mis-anchored its text box a half-panel-width right of the panel (found from the first evidence screenshot, fixed). `dotnet build` both `.slnx` `0/0`; `dotnet test` `408/408` (unaffected); `-- corpus` `18/18` (unaffected); all three scenes' `--selfcheck` `MATCH` through the real Godot 4.7.2 editor. `HoldArea`/`DestroyTarget`/`ExtractAgents`/`AllOf`/`Optional` label formatting and the extracted-agent line are inspection-only, not exercised live (`DemoScenario` authors only a `ReachArea` objective). **Review round 1 (2026-09-20, live):** Dave tried it in the real editor and could not trigger the panel -- traced to a real gap: `WorldState.ObjectiveAreas`/`.ExtractionAreas` were never rendered anywhere in `CommandDemoScene`, so the objective cell looked like ordinary terrain and Dave had no way to click it precisely. Confirmed via `AskUserQuestion` to fix in this task rather than defer: added an always-on marker (a `Kind = 5` ring plus its `AreaId` label) for each objective/extraction area, built once from already-held `WorldState` fields, no interface or interaction change. First colour choice (gold) was visually identical to the selection halo where it mattered most; changed to violet, extraction stays cyan. All three scenes' `--selfcheck` hashes reconfirmed `MATCH` unchanged (render-only). Accepted by Dave 2026-09-20 ("ok it works") after round 1's objective-marker fix. | S-M | B-032 | G4 | done (`tasks/TASK-063-MISSION-SUMMARY-SCREEN.md`) |
| B-064 | P4 | Implement replay playback in the Godot client: a scrubber/viewer over a recorded `.cwreplay`. Split from B-033 (2026-09-20, see B-033's own row) -- no client-side playback UI exists today; `content/diagnostics/demo.html`'s static generated-HTML slider is the only precedent, and it bakes every tick's rendered frame into one file at generation time rather than stepping a live scene, so this needs real design work before drafting (reuse the generate-a-static-file approach vs. a live in-engine scrubber against `Replay.run`'s recomputed `TickStates`, and whether storing/replaying full per-tick state is acceptable at scale or needs the checkpoint-cadence alternative already flagged in `ReplaySerialisation.fs`). Explicitly deferred -- not designed, not built, not blocking B-033/B-035. | M | B-012 | G4 | proposed |
| B-034 | P4 | Produce minimal coherent placeholder or test art set. Realised by TASK-041: Kenney's "Isometric Miniature Prototype" pack (v2.3, CC0, live-checked at kenney.nl, not assumed from memory — the only pack found covering both terrain blocks and a human figure in one coherent family, so no second pack was needed). New `src/CommandoWar.Client.Godot/art/` (`terrain_floor.png`, `terrain_block.png`, `terrain_crate.png`, `agent_human.png`, `LICENSE-THIRD-PARTY.md`). `DrawItem` gains one new primitive field, `TextureId`, selecting a terrain texture by passable/impassable/opaque category (shape, not colour alone, now carries that distinction); a real full-opacity agent draws the Kenney human figure, cropped and tinted per side; a translucent overlay marker (halo, route-preview dot) still draws a plain circle. `FSharpSceneHost.cs` `_Draw` only (`DrawTextureRect`/`DrawTextureRectRegion`, textures loaded once via static fields) — no `CommandoWar.Sim`/`CommandoWar.Headless` change. Both scenes' `--selfcheck` hashes confirmed unchanged (`0x11B06E6EDE0C52E3`, `0x649FA4D08E2931CA`) — a genuinely render-only reskin. `dotnet build` both `.slnx` 0/0 (Debug and Release); `dotnet test` 297/297 (unaffected); committed screenshots `docs/evidence/task-041-placeholder-art-snapshot.png`, `docs/evidence/task-041-placeholder-art-command.png`. | M | B-027 | G4 | done (`tasks/TASK-041-PLACEHOLDER-ART-SET.md`; accepted by Dave 2026-09-17, "seems to work" — two follow-ups raised, not blocking, see B-052 and B-053) |
| B-060 | P4 | Disposable turn-resolution harness spike. Requested directly by Dave (2026-09-18), read-only against `Simulation.step`: whether queuing several `MoveTo`/`Suppress` orders up front and stepping the simulation 40-50 ticks already reads as a resolved "turn," XCOM-style, before any action-point budgeting or Overwatch commitment state is designed. Realised by TASK-050 (`tasks/TASK-050-TURN-RESOLUTION-SPIKE.md`): new `src/CommandoWar.Headless/TurnDemo.fs` (the `DemoScenario`/`PathDemo`/`LosDemo` hand-built-fixture precedent) plus a new `cwheadless turn` subcommand that prints every tick's `DiagnosticRender.Ascii` frame in sequence, reusing the existing `DiagnosticRender.runFrames`/`render demo`/`render los`/`render path` mechanism verbatim. No `CommandoWar.Sim`/`CommandoWar.Client.Godot` change, no ADR, no `Canonical.FormatVersion`/`ScenarioContent.Version` bump. The run confirms the resolved-turn feel already exists end to end (refusal, engagement, suppression, casualties, reappraisal, incremental sub-cell movement, all from one batch of orders) and surfaces one open question worth a deliberate decision before any Overwatch work: an `Incapacitated`-but-not-`Dead` known contact still fully counts as a live threat for route-exposure appraisal, so a route near it can stay refused long after the fight is effectively over. Action-point budgeting and an Overwatch commitment state remain explicitly unscoped follow-on work, only worth pursuing if Dave wants to act on this spike. `dotnet build` `0/0`; `dotnet test` `342/342` (unaffected); `-- corpus` `16/16` (unaffected); `dotnet list CommandoWar.Sim package` `FSharp.Core` only. | S | none | G4 | done (`tasks/TASK-050-TURN-RESOLUTION-SPIKE.md`; accepted by Dave 2026-09-18, findings noted for a later decision, not acted on) |
| B-061 | P4 | Selection/order-mode UI does not deactivate when its agent dies. Raised by Dave on accepting TASK-052 (2026-09-18), a nit: when an agent is removed due to death (or incapacitation?), "the cursor which has move active or whatever should be deactivated" -- today `CommandDemoScene.fs`'s `selected: AgentId option` and `orderMode: int` are never cleared by a vitals change, so the selection halo, hover-preview machinery, and an armed Hold/Assault/Withdraw icon can all keep pointing at a corpse; a follow-up click in that state would issue an order to a dead recipient that the sim silently no-ops (`navigationAndMovement`/`Combat` already skip non-`Alive` agents, so nothing breaks, but nothing visibly tells the player their selection is now meaningless). Realised by TASK-053 (`tasks/TASK-053-SELECTION-DEACTIVATION-ON-CASUALTY.md`): two `AskUserQuestion` rounds resolved the open questions, the second round overriding the first's initial "clear `selected` on Dead/Incapacitated" default -- Dave's own framing: "you should be able to select to view status when that is an option, but all action commands would be disabled, e.g. you could only view status." Final design: both `Dead` and `Incapacitated` disable action (not `Dead` only); `selected` itself never auto-clears from a vitals change, so `friendlyAt`'s pre-existing behaviour of allowing a click to select a non-`Alive` friendly's own cell turned out to be the *intended* mechanism for status-view mode, not the second gap the row originally named it as -- left unchanged; `orderMode` auto-disarms to `0` the instant its selected agent stops being `Alive` (the TASK-048 "ability consumed" idiom, applied to "no longer a valid actor"); the hover route preview is suppressed while a non-`Alive` agent is selected; `OnClick`'s order-issuing branch is gated on `Casualty.isAlive`. A new `vitalsOf` helper (extracted from `renderVitals`'s existing `AgentVitals`-overlay lookup) is the single source both the gating and the vitals rendering read. No `CommandoWar.Sim`/`CommandoWar.Headless` change. Verified with a `dotnet fsi` scratch probe (the TASK-042/051/052/053 precedent, removed after use) driving real combat in `DemoScenario` until the selected agent went `Incapacitated` then `Dead`: `orderMode` auto-disarmed at incapacitation; a click attempting to issue an order while non-`Alive` left `orderMode` unconsumed (never entered the issuing branch); the hover preview stayed absent through both states; `selected` still read `agent 0` at `Dead`; re-selecting a different, `Alive` agent immediately restored normal order-issuing and hover-preview behaviour. `dotnet build` both `.slnx` (main Release; Godot client Debug) `0/0`; `dotnet test` `342/342` (unaffected). All three scenes' `--selfcheck` hashes reconfirmed `MATCH` through the real Godot 4.7.2 editor, unaffected as expected for a presentation-only change: `SnapshotDemo.tscn` `0xF422ACB8D5A86FF0`, `CommandDemo.tscn` `0x00D3D471EF7354BC`, `AppraisalDemo.tscn` `0x194805888CBE240D`. | S | B-031 (TASK-045), B-026 (TASK-040) | G4 | done (`tasks/TASK-053-SELECTION-DEACTIVATION-ON-CASUALTY.md`; accepted by Dave 2026-09-18) |
| B-062 | P4 | Perception stops observing a downed (Incapacitated/Dead) agent. Raised by Dave live-testing TASK-054's audio-cue feature (2026-09-19): "after the enemy agent is incapacitated or killed the other agent refuses order due to threat when there is none." Traced to a gap already surfaced and left unfixed in TASK-050's ledger ("Appraisal does not currently distinguish `Incapacitated` from `Alive` for exposure purposes"): `Perception.visibleContactsFor` never checked `Vitals`, and a corpse is never removed from `WorldState.Agents` (TASK-045), so it stayed "seen" at full confidence forever, keeping `Appraisal.cellPressure`/`routeExposure` treating it as a live threat indefinitely. Realised by TASK-055 (`tasks/TASK-055-PERCEPTION-STOPS-OBSERVING-DOWNED-AGENTS.md`): `AskUserQuestion` confirmed the fix location (`Perception` stops observing a non-`Alive` agent, reusing the existing `StaleAfter`/`ExpireAfter` decay path, rather than `Appraisal` reading true current `Vitals` directly) and the timing (fix now, since it was blocking further live testing). One added condition (`&& Casualty.isAlive other.Vitals`) in `visibleContactsFor`; symmetric, so a downed friendly also stops being a target for the Hostile side's own picture, for free. No `Canonical.FormatVersion` bump, no new field/event -- a behavioural fix to an existing pure function over already-canonical `Vitals`. New `SimulationTests` fact proves it directly (an `Alive` hostile observed at baseline stops appearing in `VisibleContacts` once forced `Incapacitated`/`Dead` at the same cell, still in range and LOS; its existing `Contact.LastSeenTick` stays pinned rather than refreshing). 5 of 16 corpus entries legitimately re-pinned (only ones with combat reaching a non-`Alive` transition within their tick window); two `DiagnosticsTests` goldens regenerated, each diff inspected and explained (a `seen tick`/stress value freezing, or a contact finally expiring past `ExpireAfter` once every hostile in the scenario is `Dead`, correctly emitting `ContactExpired`). `demo.html` confirmed byte-identical (`DemoScenario` has no combat in 20 ticks). `dotnet build` `0/0`; `dotnet test` `343/343` (+1); `cwheadless corpus` 16/16 after `--regenerate`; both Godot scenes' `--selfcheck` hashes confirmed `MATCH` through the real Godot 4.7.2 editor, unaffected. | S | B-031 (TASK-045), B-017 (TASK-028) | G4 | done (`tasks/TASK-055-PERCEPTION-STOPS-OBSERVING-DOWNED-AGENTS.md`; accepted by Dave 2026-09-19, confirmed live in the running game) |
| B-035 | P4 | Integrate and verify complete vertical slice. Unblocked for the first time 2026-09-20 (B-025 through B-034 all `done`). Realised by TASK-064 (`tasks/TASK-064-INTEGRATE-AND-VERIFY-VERTICAL-SLICE.md`): the real integration gap found by inspection was that `CommandDemoScene.fs` had only ever loaded the small hand-authored `DemoScenario` fixture, never `content/scenarios/bridgehead.cwscenario` -- every mechanism (demolition, extraction, mission outcome, mission summary, formation slots, order-mode HUD, developer overlay, placeholder art, the Bridgehead map itself) was `done` in isolation but none had run together against the real mission content in the real play scene. `CommandDemoScene` now loads Bridgehead for real (`ScenarioFile.parse` -> `Scenario.validate` -> `World.ofScenario`, the `DemoScenario.fs` pipeline shape); a real, previously-flagged gap was found and fixed in the same task (confirmed with Dave via `AskUserQuestion`) -- the client had no UI at all for issuing `Suppress`, one of docs/07's five required commands -- adding a fifth, procedural order-mode icon. The rewritten scripted self-check (all six agents, real formation-slot resolution against real terrain) reaches a genuine, reproducible, casualty-free neutralisation of the machine-gun team through the real click path. Extensive investigation (temporary `dotnet fsi` probes, removed after use) did **not** close two of docs/07 section 9's 12 acceptance criteria on Bridgehead itself: criterion 3 (canonical refusal -- every Bridgehead `MoveTo` order tested was `Accepted` outright, since contact with the machine gun only exists once already inside its engagement range) and criterion 7 (mission succeeds/fails without developer intervention -- a full `Succeeded` run was blocked by an apparent additional threat covering the `bridge-charge` target cell itself, and a full `Failed` run is geometrically capped well short of six friendly agents by the bridge's own two-lane chokepoint; a friendly agent's own corpse was also found to permanently block a cell). Both are recorded as open findings for Dave's own decision, not silently resolved. `dotnet build` `0/0`; `dotnet test` `408/408` (unaffected); `-- corpus` `18/18` (unaffected); `CommandDemo.tscn --selfcheck` re-pinned `MATCH 0xB99E7F74EA1C3CDE`, confirmed through the real Godot 4.7.2 editor; `SnapshotDemo.tscn`/`AppraisalDemo.tscn` reconfirmed unaffected. **Review round 1 (2026-09-20, live):** Dave reported the game felt static and out of control despite issuing orders and trying to cross the bridge -- root cause confirmed as a real bug, not just the fog-of-war design: a `MoveTo` order for any non-leader agent (a formationed slot with a non-zero offset) silently resolves to a cell well short of the click (`Appraisal.resolveFormationTarget`'s own fallback), with `Disposition = Accepted` and zero indication anything was redirected. Fixed client-side: the hover preview now shows the real resolved destination, and the order-status text appends it (`"accepted -> (7,6)"`). No `CommandoWar.Sim` change; all self-checks reconfirmed unchanged. **Review round 2 (2026-09-20, live):** Dave confirmed round 1's fix worked, then raised four further points ("cant tell who is a leader... no sense of a fight... no reaction under fire, no cover, no enemy response"). Fixed three, client-side only: a leader marker (`Casualty.currentLeader`, a pure function over already-held state), an under-fire hit-flash (target figure blends toward white briefly on a landed shot), and a cover indicator (`Terrain.Cover` had never been rendered anywhere, since found unpaintable at TASK-060). Left enemy doctrine (B-022, "no enemy response") as its own already-descoped row, not touched here. Round 2's fixes were confirmed by Dave's own further live playtest, which separately surfaced a genuine sim-side movement deadlock, scoped as new row B-065. Accepted by Dave 2026-09-20 via `AskUserQuestion`: the criterion-3/7 finding (canonical refusal and full mission succeed/fail not demonstrated on Bridgehead) is accepted as a known, tracked gap, not pursued further (no map rebalance, no corpse-occupancy fix); B-022 stays descoped as-is. | L | B-025 through B-034 | G4 | done (`tasks/TASK-064-INTEGRATE-AND-VERIFY-VERTICAL-SLICE.md`; accepted by Dave 2026-09-20) |
| B-063 | P4/P5 | Automate or assist the "verbal description -> draft `.cwscenario`" step (a helper LLM or other tooling bridging natural-language level description to authored content), so it does not stay a fully manual agent-authoring pass forever. Raised by Dave while scoping TASK-061 (2026-09-19): "we can refine this part later so the tooling is automated or has a helper LLM that bridges this aspect." Explicitly deferred — not designed, not built, not blocking B-025 or anything else. | S-M | none | G4 | proposed |
| B-065 | P4 | A friendly agent's route can become permanently blocked by another agent that has already arrived and parked on one of its path cells, with zero player-visible feedback -- the order silently freezes forever instead of failing. Raised by Dave live-playtesting TASK-064/B-035's real Bridgehead integration (2026-09-20): "i expected the ai to be more autonomous, they all seem to just get stuck... you should ply test it." Confirmed by directly driving the real click path with a `dotnet fsi` probe: an ordinary six-agent squad-movement order (not even toward the bridge) left two agents permanently stalled from tick ~30 through tick 500 with no recovery. Root cause, already documented and predicted: `docs/04_SIMULATION_SPEC.md`'s own movement spec names "replan when the next path cell becomes invalid" as step 6, but `Simulation.fs`'s Navigation and movement phase only triggers a replan when the cached next cell becomes `Terrain`-impassable -- never when it is merely occupied by another (now-stationary) agent (TASK-015's own ledger: "TASK-017's reservation does not force a replan on a yield ... so multi-agent contention does not exercise it either"). This is exactly the risk the project's own pre-mortem named: `docs/01_ADVERSARIAL_REVIEW.md` -- "reservation deadlocks can dominate development"; `docs/10_RISK_REGISTER.md` R-010, impact/probability 4/4, status `open`. `Pathfinding.fs` itself is terrain-only (no agent-occupancy awareness at all), so this was never exercised by any prior task's small synthetic fixtures -- Bridgehead's real six-agent squad moving through genuinely shared, narrow terrain is the first content to trigger it in practice. Scoped via `AskUserQuestion` (2026-09-20) to the smaller, safer of two fix directions: turn a too-long-stalled order into a visible failure (clear `Destination`, emit a new event) rather than attempting real dynamic route-avoidance around parked agents (bigger, `Pathfinding`-contract-changing, corpus/determinism-risking -- left as a further, larger follow-up if Dave wants full "deadlock avoidance", B-011b's own original, never-fully-delivered promise). Scoped into `tasks/TASK-065-STALLED-ORDER-VISIBLE-FAILURE.md`. Implemented and self-verified 2026-09-20: a new `AgentState.StalledTicks` counter (`Canonical.FormatVersion` 13 -> 14) tracks consecutive same-tick-reservation-loss/stationary-occupant freezes against the same route; once it would reach `Simulation.StallAbandonTicks` (40, first-cut/not playtest-derived), `navigationAndMovement` abandons the order outright (`Destination`/`Route` cleared, counter reset) and emits a new `MovementAbandoned` event instead of freezing again. No `Pathfinding`/`Appraisal` change, per Dave's own chosen direction. Client-side: `CommandDemoScene`'s order-status text now reads `"accepted -> abandoned (route blocked)"` for a few seconds after the event, and the `F1` developer overlay gets a distinct marker; a new `stalled-order-abandoned` corpus entry and diagnostics golden prove the mechanism end to end. Re-run against the exact original repro (a temporary `dotnet fsi` probe against real `bridgehead.cwscenario`): five of six agents now reach `MovementAbandoned` by tick 42, all settled, instead of freezing past tick 500. `dotnet test` `412/412` (+4); `-- corpus` `19/19` (+1); all three Godot `--selfcheck` hashes re-pinned and reconfirmed through the real editor. One pre-existing, unrelated staleness found and fixed: `content/fixtures/SPIKE-FIXTURE.md` had been silently stuck at format 4 since TASK-028. Accepted by Dave 2026-09-20 on the self-verification evidence alone, his explicit choice not to live-test the on-screen text first. | M | B-011b (TASK-017), B-025 (Bridgehead map) | G4 | done (`tasks/TASK-065-STALLED-ORDER-VISIBLE-FAILURE.md`; accepted by Dave 2026-09-20) |
| B-066 | P4 | A dead agent's corpse permanently blocks movement into its own cell -- `Simulation.navigationAndMovement`'s `occupantOf` map is built from every agent's `Position` with no `Vitals` check, so a corpse behaves exactly like a live occupant, forever. Raised by Dave live-playtesting the real Bridgehead squad advance (2026-09-20), watched visually via a new `--screenshot-squad` capture tool built this session (real click-path priming, stays unpaused, captures at any requested tick -- `src/CommandoWar.Client.Godot/src/FSharpSceneHost.cs`): most of the squad jams at the bridge chokepoint once casualties occur and never moves again -- "the agents should work like real ones, they would move over a dead body, or just around it if there is space." This is exactly the finding TASK-064's own investigation already surfaced and flagged as a known, accepted gap ("a friendly agent's own corpse permanently blocks its cell ... a scout who dies exactly on an objective/target cell can make that objective permanently unreachable"), now selected to fix. `Pathfinding.fs` itself has no occupancy concept at all (confirmed by inspection) -- the fix is scoped to `occupantOf` alone: exclude a non-`Alive` agent, so its cell reads as free and a live agent walks through/onto it exactly as if empty. No `Pathfinding` change, no real dynamic avoidance around a still-*live* obstacle (that stays the separate, larger, explicitly deferred direction from TASK-065). Scoped into `tasks/TASK-066-CORPSES-STOP-BLOCKING-MOVEMENT.md`, confirmed via direct conversation with Dave (2026-09-20): do the corpse fix now, park the multi-select/joint-squad-order idea (B-067) on the backlog. Implemented and self-verified 2026-09-20: `occupantOf` now excludes any non-`Alive` agent, proven by two new `SimulationTests` facts; zero blast radius on committed content (`cwheadless corpus` 19/19, no `--regenerate`). Does **not** resolve the specific live-agent chokepoint jam Dave watched (that is B-067's job, unrelated mechanism) -- accepted by Dave 2026-09-20 on the self-verification evidence as a known, tracked gap. | S | B-011b (TASK-017), B-022 (TASK-034, vacation-chain precedent) | G4 | done (`tasks/TASK-066-CORPSES-STOP-BLOCKING-MOVEMENT.md`; accepted by Dave 2026-09-20) |
| B-067 | P4/P5 | Multi-select and joint squad orders: select N agents, issue one order, and have them move together with formation applied to the group order specifically -- not, as today, automatically redirecting *any* individual agent's own solo `MoveTo` via its static `AgentState.FormationOffset` regardless of whether the player meant to move it alone. Raised by Dave in the same conversation as B-066 (2026-09-20): "the formation movement is weird, it just seem like you cant move where you want to, it doesnt seem like formation at the time. when moving a single agent you just want them to go where you click. move the whole squad as a unit, that would have intelligent formation, we dont have a control scheme for that though. like selecting n agents then doing a joint order." Scoped 2026-09-20 via two parallel research passes plus `AskUserQuestion`, then split into two tasks (the B-030/B-059 sim-then-client precedent): the mechanism already half-exists -- `PlayerCommand.Recipients`/`Command.moveToMany` (TASK-020) address several agents with one command, but every recipient is appraised independently and `Appraisal.appraise` reads each one's static `FormationOffset` unconditionally, so a "squad move" today is just N independently-redirected solo orders. Half 1, `tasks/TASK-067-FORMATION-APPLIES-ONLY-TO-GROUP-ORDERS.md` (selected): key the redirect off the order's own recipient count (`ReceivedOrder.AsGroup`, derived from `Recipients.Length > 1` at intake) instead of the agent's static field -- no new UI needed, ships the "solo click always goes where clicked" fix alone. Half 2 (not yet drafted): the multi-select UI itself -- confirmed via `AskUserQuestion`: formation for a group order uses each agent's own authored `FormationOffset` only (no ad-hoc/computed formation for an arbitrary selection), and the select gesture is both drag rubber-band-select and shift-click add/remove. Half 1 implemented and self-verified 2026-09-20: `ReceivedOrder.AsGroup` (`Canonical.FormatVersion` 14 -> 15) gates every `resolveFormationTarget` call site; `CommandDemoScene.fs`'s hover preview no longer needs the call at all, since every order it issues today is single-recipient. Two real, genuine (not byte-layout-only) consequences found and fixed, not smoothed over: the `formation-slots` corpus entry had to be re-authored to a genuine `Command.moveToMany` (it previously issued two coincidentally-same-tick solo orders, exactly the pattern this task stops redirecting); the `CommandDemo.tscn` scripted self-check produced a real friendly casualty once redirect stopped compensating for its shared literal target cells, fixed by aiming each order at its own pre-TASK-067 resolved cell directly (confirmed via a temporary probe to reproduce the identical prior outcome byte-for-byte). `dotnet test` `415/415` (+1); `-- corpus` `19/19`; all three Godot `--selfcheck` hashes re-pinned and reconfirmed through the real editor. Accepted by Dave 2026-09-20 on the self-verification evidence. | M-L | B-011d (TASK-059, the formation-offset mechanism this narrows the scope of) | G4 | in progress (`tasks/TASK-067-FORMATION-APPLIES-ONLY-TO-GROUP-ORDERS.md` done -- sim-side half accepted 2026-09-20; multi-select UI half still not drafted) |

## 5. Planned playtest and decision work

| ID | Phase | Work item | Size | Dependencies | Gate | Status |
|---|---|---|---|---|---|---|
| B-036 | P5 | Prepare clean build, neutral playtest script, and issue form | S | B-035 | G5 | proposed |
| B-037 | P5 | Run first external comprehension and control playtest | M | B-036 | G5 | proposed |
| B-038 | P5 | Analyse logs, observations, and recurring failures | S | B-037 | G5 | proposed |
| B-039 | P5 | Implement first focused interaction revision | M | B-038 | G5 | proposed |
| B-040 | P5 | Run second test only if first-gate failures justify it | M | B-039 | G5 | proposed |
| B-041 | P6 | Evaluate product, technical, content, and research evidence | M | G5 result | G6 | proposed |
| B-042 | P6 | Record proceed, re-scope, research-fork, or stop decision | S | B-041 | G6 | proposed |

## 6. Parked ideas

These are intentionally unavailable before G6 unless the charter and gates are changed through an ADR.

| Idea | Reason parked |
|---|---|
| vehicles | multiplies movement, occupancy, animation, combat, and AI states |
| multiplayer | requires a stronger determinism and networking contract |
| multiple eras or Action Concept-style packs | repeats the original project's platform-before-game mistake |
| campaign progression | cannot be designed responsibly before one mission is enjoyable |
| pairwise relationships | adds state and explanations without proving the core loop |
| procedural maps | conflicts with controlled tactical and appraisal testing |
| general mod scripting | weakens validation and expands compatibility obligations |
| general HTN or GOAP | unnecessary search and debugging surface for five intents |
| Mibo.Adaptive | experimental complexity not needed for the slice |
| runtime LLM agents | nondeterministic, difficult to test, and irrelevant to the first product hypothesis |
| formal human-subject research instrumentation | requires a stable game and separate ethics/method plan |

## 7. Selecting the next task

Dave should select the next task only when:

1. all dependencies are `done`;
2. a complete task file exists;
3. acceptance criteria are objective;
4. prohibited scope is explicit;
5. the required evidence can be produced locally;
6. `PROJECT_STATE.yaml` is updated to name that task and no other.
