project:
  working_name: Commando War spiritual successor
  public_title: null
  status: discovery
  current_phase: P3_command_loop_headless
  current_gate: G3_command_loop_proven_headless
  framework_decision: godot_dotnet_fsharp_sim (ADR-0001, accepted 2026-09-03)
  updated: 2026-09-15 (TASK-034 accepted and merged to main; no task selected)

active_work:
  selected_task: none
  task_file: null
  owner: Dave
  blocked: false
  blockers: []
  updated: 2026-09-16
  note: >-
    2026-09-16: TASK-036 (Shared F# fixture builder for the test corpus,
    backlog B-049) accepted by Dave; status `done` -- never occupied the
    selected-task slot for long (the DIAG-001/TASK-035 precedent). One
    ScenarioSpec value in Corpus.fs now authors
    geometry and a per-agent command schedule together; all eleven
    non-fixture corpus entries migrated onto it, replacing rawScenario and
    the eleven ...World() functions. Entry gains Commands:
    RecordedCommand[] option (Some = the sole runtime source of truth for
    a migrated entry, None for spike-fixture, unchanged). regenerateEntry
    now writes each migrated entry's content/replays/<name>.cwreplay
    (ReplaySerialisation) instead of a hand-authored .cwlog; CommandId /
    Sequence numbering deliberately mirrors CommandLogFile.parse's
    order-of-appearance convention exactly, since CommandId is part of
    Canonical.encode. Deviation found during implementation, beyond the
    drafted Allowed scope: DiagnosticsTests.fs (nine call sites) and
    AppraisalDemo.fs (the TASK-029 Godot demo helper) also called
    Corpus.loadLog directly on a migrated entry and needed the same swap
    to Corpus.commandsOf -- both mechanical, no behaviour change. Every
    migrated .md byte-identical except its "Command log" row (corrected
    .cwlog -> .cwreplay, invisible to Corpus.parseTable). No
    CommandoWar.Sim change, no Canonical.FormatVersion change, no ADR.
    287 -> 287 green; dotnet build 0/0; -- corpus 12/12 (--regenerate
    twice byte-identical); -- fixture format 7, 36 events unchanged; --
    replay-file envelope-full unchanged. Implemented directly on main, no
    branch -- the DIAG-001/TASK-035 precedent, justified by this task's
    low-risk, fully-verified refactor profile (a departure from the
    TASK-030-034 M-sized-task branch precedent, flagged to Dave). Both
    deviations found during implementation and the "Command log" row
    correction accepted as-is. Re-verified before accepting: dotnet build
    0/0, dotnet test 287/287, -- corpus 12/12, -- fixture format 7 / 36
    events unchanged, git status matched expected scope.
    (docs/ledger/2026-09-16-TASK-036-shared-fixture-builder.md)

    Prior: 2026-09-15: TASK-036 drafted and selected. Of the backlog items
    ready per docs/11 section 7 after TASK-035 (B-016b, B-049, B-050,
    B-011d), none sit on G3's critical path -- that is blocked on a slice
    of B-030 for B-023 step 5, a separate larger discussion flagged to
    Dave, not resolved here. B-049 picked as the one paying down
    compounding risk (Corpus.fs / .cwlog / SimulationTests.fs duplication
    and drift). Two central decisions confirmed with Dave via
    AskUserQuestion: .cwreplay over the frozen .cwlog v1 grammar; migrate
    all eleven existing entries. (docs/ledger/2026-09-15-TASK-036-drafted.md)

    Prior: 2026-09-15: TASK-035 (Event agent attribution for diagnostic narration,
    presentation-only, no backlog item) accepted by Dave ("looks good");
    status `done` -- never occupied the selected-task slot (the DIAG-001
    precedent for a short single-session task, implemented and self-verified
    directly on main, no branch). Re-verified before accepting: dotnet build
    0/0, dotnet test 287/287, -- corpus 12/12, -- fixture format 7 / 36
    events unchanged, git status matched expected scope. Closes the gap
    DIAG-001 flagged: `Diagnostics.EventMarker` gains
    `Agents: AgentId[]` (the `Cells` shape precedent), populated from fields
    each `DomainEvent` already carries; `DiagnosticRender.eventNarration`
    now names the agent(s) in every sentence (e.g. "Shot fired: hit." ->
    "Agent 0 fired at agent 5: hit."). `Ascii`'s terse events line
    deliberately left unchanged (Decision B, flagged for Dave's review) --
    redundant with the fuller `overlays:` section already one line above it.
    No `Canonical.FormatVersion` change (`Diagnostics` is not referenced by
    `Canonical.fs`); only `content/diagnostics/demo.html` moves. 287 -> 287
    green (two existing `DiagnosticsTests` facts extended in place, no new
    facts). `dotnet build` 0/0; `-- corpus` 12/12; `-- fixture` format 7, 36
    events unchanged. No ADR. (docs/ledger/2026-09-15-TASK-035-event-agent-attribution.md)

    Prior: 2026-09-15: TASK-034 (Hostile tactical picture and observed-only
    targeting proof, backlog B-022, partial) accepted by Dave and merged to
    main (--no-ff, branch task-034-hostile-tactical-picture deleted; not
    pushed). New WorldState.HostileTacticalKnowledge: Contact[], symmetric to the
    friendly TacticalKnowledge (TASK-026), built by the tacticalKnowledge
    phase calling the already side-agnostic Perception.mergeKnowledge a
    second time filtered to Side = Hostile -- no change to Perception.fs.
    New Overlay.HostileKnownContact diagnostic case (distinct from
    KnownContact, the AgentStress/AgentSuppression precedent). Also proves,
    rather than builds, docs/09 section 8's "enemy does not target an
    unobserved player position": Simulation.combat already draws shooter
    candidates only from same-tick VisibleContacts, strictly tighter than
    any tactical-knowledge store, so no Combat.fs change -- one new
    SimulationTests fact pins it and docs/09 is corrected from "partially
    realised" to fully realised for the targeting half. Canonical.FormatVersion
    6 -> 7, full re-pin -- NOT behaviour-neutral this time: open-engagement,
    perception-contact, and exposed-approach each gain a genuine new
    non-zero HostileTacticalKnowledge entry from the tick a hostile first
    sees a friendly (tick counts and event counts unchanged everywhere). No
    new corpus entry, no new event type, no ADR. Deliberately narrower than
    B-022's full text: suppress-likely-routes, seek-adjacent-cover-under-pressure,
    and scripted fall-back all stay open (need B-030's order vocabulary or a
    new autonomous-movement design), so B-022 stays review, not done. 285 ->
    287 green; dotnet build 0/0; -- corpus 12/12 (--regenerate twice
    byte-identical); -- fixture format 7, 36 events unchanged; --
    replay-file envelope-full OK at canonical 7, 78 events unchanged. No
    ADR. Godot's --selfcheck pinned hash updated for the moved
    exposed-approach tick-1 hash but not re-run through Godot this session
    (no Godot install here) -- Dave to confirm on his machine when
    convenient.
    (docs/ledger/2026-09-15-TASK-034-hostile-tactical-picture.md)

    Newly unblocked (from TASK-034, B-022 still open): none new -- B-023
    still additionally needs B-021's remaining triggers and all of B-022,
    not just this task's slice.

    Prior: TASK-033 (Stress and bounded reappraisal, backlog B-021,
    partial) accepted by Dave and merged to main (--no-ff; the two commits
    were originally directly on main, unpushed -- moved onto branch
    task-033-stress-and-bounded-reappraisal before acceptance for
    consistency with the established per-task branch workflow; branch
    deleted after merge, not pushed). New Stress.fs leaf; AgentState.Stress
    (raised while VisibleContacts is non-empty, decayed every tick, both in
    State consequences) and AgentState.SuppressionBand (a hysteresis latch
    over Suppression, computed in Appraisal) are new canonical fields.
    resolveThreshold gains a continuous Stress drag and a discrete
    SuppressionBand penalty, the whole threshold floored at 0 so a fully
    safe route is never refused for stress/suppression alone. Two new
    reappraisal triggers inside Simulation.appraisal: knowledge-change (any
    ContactObserved/ContactExpired this tick, global) and suppression-band
    (the latch flipping, either direction) -- both exclude a fulfilled
    order (found necessary during implementation: it would otherwise defeat
    commitmentAndLocalAction's completion clear). Discipline deliberately
    stays static and non-canonical -- corrects B-021's literal backlog
    wording. Deliberately narrower than B-021's full text: exposure-band,
    wounded, support, and leadership triggers, and dynamic trust, stay
    deferred (need systems that don't exist, or a materially larger cut) --
    B-021 stays "review", not "done", until those land or are explicitly
    descoped. No new event type; new sparse Overlay.AgentStress.
    Canonical.FormatVersion 5 -> 6, full re-pin (tick counts and event
    counts unchanged everywhere). 274 -> 285 green; dotnet build 0/0; --
    corpus 12/12; -- fixture format 6, 36 events unchanged; -- replay-file
    envelope-full OK at canonical 6, 78 events unchanged. No ADR. No task
    selected. (docs/ledger/2026-09-14-TASK-033-stress-and-bounded-reappraisal.md)

    Newly unblocked (from TASK-033, B-021 still open): none new -- B-022
    (enemy doctrine) and B-023 (canonical refusal sequence) were already
    unblocked-or-nearly-unblocked before this task; B-023 still additionally
    needs B-021's remaining triggers and B-022.

    Prior: TASK-032 (Suppression and exposure model, backlog B-020)
    accepted by Dave and merged to main (--no-ff, branch
    task-032-suppression-and-exposure deleted; not pushed) 2026-09-14.
    Combat raises AgentState.Suppression on every qualifying shot,
    State consequences decays it every tick. Canonical.FormatVersion 4 ->
    5, full re-pin. Mechanic only at the time: no wiring into Appraisal,
    deliberately deferred to B-021 (this task).
    (docs/ledger/2026-09-14-TASK-032-suppression-and-exposure.md)

    Newly unblocked (from TASK-032, still open): B-021 (this task) now has
    every dependency done (B-020 was the last one).

    Prior: TASK-031 (Basic hitscan combat and directional cover
    effects, backlog B-019) accepted by Dave and merged to main (--no-ff,
    branch task-031-hitscan-combat deleted; not pushed) 2026-09-13. A real Combat phase
    (docs/04 section 12.8): automatic symmetric engagement from a shooter's
    own VisibleContacts, a deterministic 0..1000-scale hit chance (range +
    directional Terrain.cover), the simulation's first real gameplay PRNG
    draw -- no Canonical.FormatVersion bump, since WorldState.Random was
    already canonical. No ammunition/weapon-readiness and no
    wound/death/suppression consequence, both deliberately deferred. New
    Combat.fs leaf, new ShotFired event, new Overlay.FireLine, new
    open-engagement corpus entry. Correction found during implementation:
    perception-contact and exposed-approach (TASK-026/028's enemy-bearing
    entries) legitimately re-pin once an agent comes into weapon range and
    line of sight of the opposing side (tick counts unchanged on both);
    demo.html similarly gains combat from tick 8. 254 -> 265 green; dotnet
    build 0/0; -- corpus 12/12; -- fixture byte-identical; -- replay-file
    envelope-full unchanged. No ADR. No task selected.
    (docs/ledger/2026-09-13-TASK-031-hitscan-combat.md)

    Newly unblocked (from TASK-031, still open): B-020 (suppression and
    exposure model) and B-022 (simple enemy hold-and-defend doctrine) both
    now have every dependency done (B-019 was the last one for each).

    Prior: TASK-030 (Commitment and finite move/hold executor, backlog
    B-018) accepted by Dave and merged to main (--no-ff, branch
    task-030-commitment-and-finite-executor deleted; not pushed) 2026-09-13.
    A real commitmentAndLocalAction phase (docs/04 section 12.6) realising a
    Commitment DU (Holding | Moving) as a pure derived value over the
    already-canonical Order/Disposition/Destination -- no
    Canonical.FormatVersion bump, no hash re-pin anywhere. New
    CommitmentEstablished / CommitmentCompleted events, new
    Overlay.AgentCommitment, new reissued-order corpus entry. 244 -> 254
    green before TASK-031 branched.
    (docs/ledger/2026-09-13-TASK-030-commitment-and-finite-executor.md)

    Prior: TASK-029 (Godot appraisal-divergence demo -- a read-only,
    corpus-scoped slice of backlog B-029 pulled forward as P3 decision-support;
    B-029 proper stays P4) accepted by Dave and merged to main (--no-ff, branch
    task-029-godot-appraisal-demo deleted; not pushed) 2026-09-12. Dave
    launched the scene interactively and confirmed the divergence reads
    right. main build 0/0, 244 green, -- corpus 10/10, -- fixture format 4 /
    34 events before TASK-030 branched.

    TASK-029 summary: a runnable Godot scene
    (src/CommandoWar.Client.Godot/scenes/AppraisalDemo.tscn +
    src/AppraisalDemoScene.cs, now run/main_scene; the greybox spike files
    untouched) loads the committed exposed-approach corpus entry, builds its
    DiagnosticFrame sequence via DiagnosticRender.runFrames, and renders the
    KnownContact / PlannedPath / OrderAppraisal overlays with a 13-frame tick
    slider and a tick/hash/format/draws HUD -- the visual proof of docs/07
    section 9 criteria 2 and 11. All non-trivial logic (corpus load,
    runFrames, disposition -> text/tone, the flat [<CLIMutable>] view model)
    lives in a new framework-neutral F# helper
    src/CommandoWar.Headless/AppraisalDemo.fs; the C# scene is a thin renderer
    (a scoped deviation from ADR-0004's "F# client-core" rule for this
    disposable demo, not a precedent for the P4 client tasks). No
    CommandoWar.Sim change; no new event / Overlay case /
    Canonical.FormatVersion bump / golden re-pin; no new NuGet/Godot
    dependency. 242 -> 244 green (+2 DiagnosticsTests). No ADR.
    (docs/ledger/2026-09-08-TASK-029-godot-appraisal-demo.md)

    Prior: TASK-028 (backlog B-017, staged order appraisal and typed reasons)
    accepted by Dave and merged to main (--no-ff, branch deleted; not pushed)
    2026-09-08. Realised the Appraisal phase (12.5): Communication writes a
    new canonical AgentState.Order (resets Disposition); Appraisal reads Order
    + TacticalKnowledge + terrain + a static AgentState.Discipline, runs
    stages 1-4, produces an OrderDisposition, emits OrderAppraised, and on
    Accepted writes Destination. Canonical.FormatVersion 3 -> 4 (full
    behaviour-neutral re-pin). New exposed-approach corpus entry (the G3
    divergence TASK-029 now visualises: agent 0 Refused RouteTooExposed
    threat-agent-2, agent 1 Accepted). 228 -> 242 green.
    (docs/ledger/2026-09-08-TASK-028-order-appraisal-and-typed-reasons.md)

    Newly unblocked (from TASK-028, still open): B-018 (commitments / finite
    executors -- the Appraisal phase now produces an Accepted disposition to
    build on), B-021 (stress / discipline / trust dynamics + the remaining
    reappraisal triggers + hysteresis; makes Discipline canonical), B-023
    (canonical refusal sequence -- steps 1-3 landed as exposed-approach).

sources_of_truth:
  product: docs/00_PROJECT_CHARTER.md
  decisions: decisions/
  backlog: docs/11_BACKLOG.md
  simulation: docs/04_SIMULATION_SPEC.md
  agent_ai: docs/05_COMMAND_AND_AGENT_AI.md
  progress: docs/12_PROGRESS_LEDGER.md
  risks: docs/10_RISK_REGISTER.md

gates:
  G0_project_control_ready: passed
  G1_framework_selected: passed
  G2_deterministic_core_proven: passed
  G3_command_loop_proven_headless: pending
  G4_vertical_slice_feature_complete: pending
  G5_external_playtest_passed: pending
  G6_production_go_no_go: pending

fixed_constraints:
  - Desktop-first
  - Original title and assets
  - Framework-independent F# authoritative simulation
  - Integer fixed-tick authoritative time
  - Recorded commands, deterministic random source, and replay evidence
  - One mission before campaign, vehicles, multiplayer, modding, or research expansion
  - Coding agents work from bounded task files

candidate_stack_as_researched_2026_09_02:
  dotnet: verify_installed_SDK_before_scaffolding
  godot_dotnet: 4.7.2
  monogame: 3.8.5.1
  raylib: 6.0
  mibo: 4.5.3
  tiled: 1.12.2

prohibited_before_G5:
  - Mibo.Adaptive
  - Runtime LLMs
  - Multiplayer
  - Vehicles
  - General-purpose mod scripting
  - Procedural campaign generation
  - Pairwise social relationship simulation
  - General HTN or GOAP planner
  - Full ECS migration
  - Research study instrumentation beyond ordinary telemetry and replay
