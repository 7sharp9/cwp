project:
  working_name: Commando War spiritual successor
  public_title: null
  status: discovery
  current_phase: P3_command_loop_headless
  current_gate: G3_command_loop_proven_headless
  framework_decision: godot_dotnet_fsharp_sim (ADR-0001, accepted 2026-09-03)
  updated: 2026-09-06 (TASK-023 implemented, pending acceptance)

active_work:
  selected_task: TASK-023
  task_file: tasks/TASK-023-SIMULATION-CI.md
  owner: Dave
  blocked: false
  blockers: []
  note: >-
    2026-09-06: TASK-023 (backlog B-048) selected — continuous integration for
    the framework-neutral solution. Adds .github/workflows/ci.yml (one job,
    windows-latest, on push and pull_request -> main) that restores, Release-
    builds, and tests CommandoWar.slnx under the global.json SDK pin, then runs
    cwheadless corpus (fails the job on any divergence from the committed
    content/replays/*.md tables) and cwheadless fixture (CLI smoke), and checks
    the working tree is unchanged by the verbs. One-line Windows-x64 / .NET
    10.0.303 environment annotation added to content/replays/CORPUS.md,
    content/fixtures/SPIKE-FIXTURE.md, and the docs/12 "Shared fixture" block;
    docs/09 section 6 marks its framework-neutral matrix items realised, with
    client-compile / content-validation / package-smoke still P4. No source /
    .fsproj / test / hash / Canonical.FormatVersion change; no ADR. Verified
    locally from the repo root: restore / build (0/0) / test (Passed: 194) /
    -- corpus (7x PASS, exit 0) / -- fixture (exit 0) all green; the working
    tree stays clean after the verbs; the negative test (perturb one committed
    content/replays row) fails both dotnet test and -- corpus with the first
    bad tick and expected/actual hash, then reverts clean. This session cannot
    push, so the green-run and red-run acceptance criteria on a real Actions
    runner are left for Dave to confirm by pushing branch
    task-023-simulation-ci. Pending Dave's acceptance.
    (docs/ledger/2026-09-06-TASK-023-simulation-ci.md)

    G2 passed 2026-09-05 (docs/ledger/2026-09-05-G2-gate-decision.md);
    phase and gate advanced to P3/G3. TASK-021 (backlog B-046, minimum
    passable movement cost + overflow-safe pathfinding cost) implemented
    2026-09-06 and marked done in tasks/ and docs/11_BACKLOG.md, still
    pending Dave's formal acceptance
    (docs/ledger/2026-09-06-TASK-021-movement-cost-bounds.md) — that
    acceptance state is Dave's to set and is untouched here. 2026-09-06:
    TASK-020 (backlog B-014) selected and implemented in a single headless
    session and accepted by Dave the same day — PlayerCommand.Recipients:
    AgentId list generalised from the single Agent field (back-compatible
    Command.moveTo, new Command.moveToMany), inert Urgency/RiskTolerance
    envelope fields, four new CommandRejection cases (EmptyRecipients,
    DuplicateRecipient, UnauthorisedRecipient, batch-order-independent
    DuplicateCommandId), commandIntake copies the agent array once per phase
    invocation, emit-before-apply; a partial section 13 envelope only (issuer
    identity and issue-tick eligibility stay out, with mandatory follow-ups
    B-044 and B-045 before G3). No Canonical.FormatVersion or
    CommandLogFile.Version change, no hash re-pin; 178 -> 184 green
    (docs/ledger/2026-09-06-TASK-020-command-validation-and-recipients.md).
    Mibo
    5.0.0 recorded as a framework reconsideration option (ADR-0003 amendment,
    backlog B-043); framework decision unchanged. 2026-09-06: TASK-022 (backlog
    B-047) selected — runtime cell-occupancy correctness in the Navigation and
    movement phase, a post-gate correctness fix to the G2 movement deliverables
    (TASK-015 / TASK-017). Adds a deterministic vacation-chain resolution stage
    to Pass 2 (after rival arbitration, before apply), a new MovementObstructed
    event and Obstructed diagnostic overlay, and a unique-live-agent-cell
    occupancy property. Implemented the same day: stage-2b additive fixpoint,
    MovementObstructed, Overlay.Obstructed, WorldError.AgentsShareCell
    base-case guard, follow-chain + swap-standoff corpus entries, one golden
    pair; 184 -> 194 green; -- corpus / -- fixture byte-identical;
    Canonical.FormatVersion stays 2, no hash re-pin
    (docs/ledger/2026-09-06-TASK-022-cell-occupancy.md). Pending Dave's
    acceptance. TASK-021 acceptance state untouched (Dave's to set).

sources_of_truth:
  product: docs/00_PROJECT_CHARTER.md
  decisions: decisions/
  backlog: docs/11_BACKLOG.md
  simulation: docs/04_SIMULATION_SPEC.md
  agent_ai: docs/05_COMMAND_AND_AGENT_AI.md
  progress: docs/12_PROGRESS_LEDGER.md
  risks: docs/10_RISK_REGISTER.md

gates:
  G0_project_control_ready: passed
  G1_framework_selected: passed
  G2_deterministic_core_proven: passed
  G3_command_loop_proven_headless: pending
  G4_vertical_slice_feature_complete: pending
  G5_external_playtest_passed: pending
  G6_production_go_no_go: pending

fixed_constraints:
  - Desktop-first
  - Original title and assets
  - Framework-independent F# authoritative simulation
  - Integer fixed-tick authoritative time
  - Recorded commands, deterministic random source, and replay evidence
  - One mission before campaign, vehicles, multiplayer, modding, or research expansion
  - Coding agents work from bounded task files

candidate_stack_as_researched_2026_09_02:
  dotnet: verify_installed_SDK_before_scaffolding
  godot_dotnet: 4.7.2
  monogame: 3.8.5.1
  raylib: 6.0
  mibo: 4.5.3
  tiled: 1.12.2

prohibited_before_G5:
  - Mibo.Adaptive
  - Runtime LLMs
  - Multiplayer
  - Vehicles
  - General-purpose mod scripting
  - Procedural campaign generation
  - Pairwise social relationship simulation
  - General HTN or GOAP planner
  - Full ECS migration
  - Research study instrumentation beyond ordinary telemetry and replay
