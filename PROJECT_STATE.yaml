project:
  working_name: Commando War spiritual successor
  public_title: null
  status: discovery
  current_phase: P3_command_loop_headless
  current_gate: G3_command_loop_proven_headless
  framework_decision: godot_dotnet_fsharp_sim (ADR-0001, accepted 2026-09-03)
  updated: 2026-09-07 (TASK-026 implemented on branch, in review)

active_work:
  selected_task: TASK-026
  task_file: tasks/TASK-026-PERCEPTION-AND-TACTICAL-KNOWLEDGE.md
  owner: Dave
  blocked: false
  blockers: []
  updated: 2026-09-07
  note: >-
    2026-09-07: TASK-026 (backlog B-015, observations and shared squad
    tactical knowledge — first phase consumer of the Sight module) implemented
    on branch task-026-perception-and-tactical-knowledge off main. Committed
    locally, NOT pushed; awaiting Dave's review (task file `review`).

    Three central decisions confirmed with Dave before the canonical encoder
    / phase bodies were written: full retained decaying tactical-knowledge
    store (not the narrowed cut); PerceptionConfig module literals
    SightRange = 10 (Chebyshev), ConfidenceFull = 1000,
    ConfidenceBandDrop = 250, StaleAfter = 20, ExpireAfter = 60; symmetric
    raw visibility (both sides get AgentState.VisibleContacts) with
    friendly-squad-only WorldState.TacticalKnowledge (hostile picture + enemy
    doctrine stay B-022).

    New leaf src/CommandoWar.Sim/Perception.fs (PerceptionConfig,
    visibleContactsFor / sweep / mergeKnowledge; pure, total, integer-only).
    Real Perception (12.3) and TacticalKnowledge (12.4) phases in
    Simulation.fs. AgentState.VisibleContacts is a non-canonical derived cache
    (excluded from Canonical.encode, the Route precedent);
    WorldState.TacticalKnowledge is genuine canonical state, so
    Canonical.FormatVersion bumped 2 -> 3 with a full BEHAVIOUR-NEUTRAL re-pin
    (fixture + all seven .cwlog corpus entries + envelope-full.{cwreplay,md} —
    tick counts and event counts byte-identical; every committed scenario is
    enemy-free). DemoScenario has one hostile, so demo.html legitimately gains
    the new KnownContact overlay + contact-observed markers (it is not one of
    the seven). New ContactObserved / ContactExpired events; new
    Overlay.KnownContact in both Diagnostics.frame and frameOf. New
    enemy-bearing corpus entry perception-contact (14 ticks, 12 events) +
    golden perception-contact-tick-005.*.

    206 -> 216 green (+5 SimulationTests, +1 DeterminismPropertyTests property
    at 200 cases, +1 CanonicalHashTests, +2 DiagnosticsTests, +1 CorpusTests
    theory case); dotnet build 0/0; -- corpus 8/8 PASS; -- fixture format 3,
    33 events unchanged; -- replay-file envelope-full checkpoints OK;
    src/CommandoWar.Sim packages FSharp.Core only. No ADR (ADR-0002 amendment
    governs the format bump).
    (docs/ledger/2026-09-07-TASK-026-perception-and-tactical-knowledge.md)

    B-016 / B-017 / B-019 / B-022 are now unblocked (they consume
    WorldState.TacticalKnowledge / AgentState.VisibleContacts). No drafted
    TASK-027.

    main is ahead of origin/main by the earlier TASK-025 commits plus the
    new task-026 branch — all left for Dave to push.

    Earlier this session (2026-09-06): TASK-021 (B-046), TASK-022 (B-047),
    TASK-023 (B-048) recorded as accepted by Dave; TASK-023's
    .github/workflows/ci.yml is merged to main (PR #1) and green on
    windows-latest.
    (docs/ledger/2026-09-06-reconcile-021-022-023-acceptances.md)

    G2 passed 2026-09-05 (docs/ledger/2026-09-05-G2-gate-decision.md); phase
    P3, gate G3 (pending). ADR-0001 (Godot) stands; ADR-0003's 2026-09-06
    amendment records Mibo 5.x classic MVU as a live reconsideration option
    (backlog B-043); the framework decision is unchanged.

sources_of_truth:
  product: docs/00_PROJECT_CHARTER.md
  decisions: decisions/
  backlog: docs/11_BACKLOG.md
  simulation: docs/04_SIMULATION_SPEC.md
  agent_ai: docs/05_COMMAND_AND_AGENT_AI.md
  progress: docs/12_PROGRESS_LEDGER.md
  risks: docs/10_RISK_REGISTER.md

gates:
  G0_project_control_ready: passed
  G1_framework_selected: passed
  G2_deterministic_core_proven: passed
  G3_command_loop_proven_headless: pending
  G4_vertical_slice_feature_complete: pending
  G5_external_playtest_passed: pending
  G6_production_go_no_go: pending

fixed_constraints:
  - Desktop-first
  - Original title and assets
  - Framework-independent F# authoritative simulation
  - Integer fixed-tick authoritative time
  - Recorded commands, deterministic random source, and replay evidence
  - One mission before campaign, vehicles, multiplayer, modding, or research expansion
  - Coding agents work from bounded task files

candidate_stack_as_researched_2026_09_02:
  dotnet: verify_installed_SDK_before_scaffolding
  godot_dotnet: 4.7.2
  monogame: 3.8.5.1
  raylib: 6.0
  mibo: 4.5.3
  tiled: 1.12.2

prohibited_before_G5:
  - Mibo.Adaptive
  - Runtime LLMs
  - Multiplayer
  - Vehicles
  - General-purpose mod scripting
  - Procedural campaign generation
  - Pairwise social relationship simulation
  - General HTN or GOAP planner
  - Full ECS migration
  - Research study instrumentation beyond ordinary telemetry and replay
